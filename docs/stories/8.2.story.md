# Story 8.2: Fundação `bank_accounts` + saldo derivado + Regra dos Planos como teste estrutural

## Status
InReview

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["ruff (apps/api)", "pytest apps/api/tests -m 'not rls_e2e' (test_bank_accounts.py + test_money_planes.py + suíte completa)", "pytest -m rls_e2e (test_bank_rls.py — testcontainers, Postgres real, papel e1p_app não-superusuário)", "alembic upgrade head + downgrade da 0058 num Postgres real", "bash scripts/check.sh", "os 3 agentes de QA (regression-tester, bug-hunter, dedup-checker)"]
```
> [AUTO-DECISION] Módulo de backend novo com migration + RLS → `executor="@dev"`. `quality_gate="@architect"` (convenção histórica do projeto). @data-engineer **não** é executor: a DDL aqui é uma tabela simples que copia literalmente o padrão de `0048_cost_centers.py`/`0049_investments.py`, sem query a otimizar nem política de RLS nova a desenhar.

## Story
**As a** dono da empresa de 1 pessoa que tem **mais de uma conta PJ** (corrente + poupança + aplicação, possivelmente em bancos diferentes),
**I want** cadastrar cada uma dessas contas no e1p, com o saldo que ela tinha numa data de partida, e ver o saldo que o sistema calcula para ela,
**so that** o e1p passe a conhecer o **plano 3 do dinheiro** (o meu dinheiro, no meu banco) como entidade de primeira classe — e nunca mais confunda isso com o saldo da Carteira da plataforma.

## Acceptance Criteria

1. **Tabela `bank_accounts` com RLS `FORCE`, criada por migration aditiva.** Colunas conforme design §2.1: `id` `VARCHAR(36)` PK, `tenant_id`, `name` `VARCHAR(120)` NOT NULL, `kind` `VARCHAR(16)` NOT NULL, `institution`/`institution_code`/`branch`/`number`/`holder_document`/`pix_key` (todos NOT NULL com default `''`), `opening_balance_cents` `BIGINT` NOT NULL default 0, `opening_date` `DATE` NOT NULL, `is_primary` `BOOLEAN` NOT NULL default `FALSE`, `archived_at` `TIMESTAMPTZ` NULL, `created_at`/`updated_at`. Índice `ix_bank_accounts_tenant_id`; índice **único parcial** `uq_bank_accounts_tenant_ident` em `(tenant_id, institution_code, branch, number)` `WHERE number <> ''`. `ENABLE` + `FORCE ROW LEVEL SECURITY` + policy `tenant_isolation` com `USING` **e** `WITH CHECK`, copiando `_enable_rls` de `0049_investments.py`. → design §2.1; REQ-1
2. **N contas por tenant, arquivar em vez de deletar.** O CRUD aceita quantas contas o tenant quiser (`checking`/`savings`/`investment`/`cash`); `DELETE` **não existe** — a remoção é lógica (`archived_at`, mesmo padrão de `chart_accounts.archive_account`), porque conta encerrada não pode levar o histórico de movimentos junto. `list_accounts` esconde arquivadas por default e as inclui sob flag explícita. → design §2.1; **decisão do fundador F3 (2026-07-29)**
3. **Identidade bancária única, sem vazar existência entre tenants.** Cadastrar duas contas com a mesma `(institution_code, branch, number)` no mesmo tenant → **409** com mensagem acionável. Conta sem `number` (ex.: "Caixinha") **nunca** colide, quantas houver. O `tenant_id` é a **primeira** coluna da constraint (índice único é global e não respeita RLS — sem isso o tenant B receberia um 409 inexplicável por causa de um dado do tenant A, que é bug **e** vazamento de existência). → design §2.1
4. **`kind` é validado, ao contrário dos outros `kind` do projeto.** Aceita apenas `checking|savings|investment|cash` → 422 fora disso. `platform_wallet` existe no vocabulário do design mas é **explicitamente rejeitado** pela API (422) — modelar a Carteira como conta bancária violaria a Regra dos Planos §1.3a, e o valor fica só reservado. A coluna permanece `VARCHAR(16)` (nenhuma migration futura necessária se o vocabulário crescer). → design §2.1 (`kind='platform_wallet'`: *"até lá, ninguém escreve nele"*); decisão pendente **D6** do design (default adotado: não criar)
5. **Saldo derivado, nunca materializado.** Existe `service.derived_balance(db, *, bank_account_id, until=None) -> int` implementando `opening_balance_cents + SUM(movimentos)` (design §3.1). **Nesta story não há movimentos** (`bank_transactions` é da Story 8.3), então o resultado é `opening_balance_cents` — e a função nasce com o ponto de extensão isolado numa função interna (`_movements_sum`) que a 8.3 preenche **sem alterar a assinatura pública**. Não existe coluna de saldo em `bank_accounts`, em nenhuma hipótese. → design §3.1; REQ-2
6. **Todo saldo exposto declara a origem.** `BankAccountOut` traz `saldo_derivado_cents` **e** `saldo_derivado_origem` fixo em `ORIGEM_BANCO` (`app.core.money_planes`, criado pela Story 8.1). Nenhum campo de saldo trafega sem procedência. → design §1.3c; epic §4.1c
7. **Uma conta primária por tenant, no máximo.** `is_primary` é mantido consistente pelo service: a primeira conta criada nasce primária; marcar outra como primária desmarca a anterior **na mesma transação**; arquivar a primária deixa o tenant sem primária (não elege sucessora em silêncio). `service.primary_account(db) -> BankAccount | None` existe para a Onda 6 consumir. → design §2.1 (*"conta padrão de débito/crédito"*); §6.6
8. **A Regra dos Planos vira teste executável, não convenção.** `apps/api/tests/test_money_planes.py` (arquivo novo, no estilo estrutural de `tests/test_tenancy_guard.py`) contém, no mínimo: `test_wallet_nao_importa_bank` (varre `apps/api/app/modules/wallet/**/*.py` procurando `from app.modules.bank` / `import app.modules.bank` e **falha** se achar — o equivalente executável do `grep -r "from app.modules.bank" apps/api/app/modules/wallet/` do epic §5 Onda 6 AC2); `test_bank_balance_ignora_wallet` (criar `Transaction` na Carteira não muda `derived_balance` em 1 centavo); `test_wallet_summary_ignora_bank` (criar conta bancária com saldo de abertura não muda `wallet_summary` em 1 centavo). → design §1.3; epic §4.1
9. **Isolamento validado no Postgres real.** `apps/api/tests/test_bank_rls.py` (marcado `pytest.mark.rls_e2e`, testcontainers, `alembic upgrade head` rodado como o papel **não-superusuário** `e1p_app`): tenant A não vê/edita conta do tenant B; escrita com `tenant_id` de outro tenant é barrada pelo `WITH CHECK`; sem a GUC `app.current_tenant_id` a leitura é fail-closed. → epic §4.7; job `cross-tenant-rls` do CI

### Integration Verification
- **IV1 — DRE e Lucratividade intactas:** `dre.py` agrega exatamente `charges` + `payables` + `transactions` (`dre.py:51,135-156`); `bank_accounts` **não é nenhuma delas e nunca será adicionada**. Teste de snapshot: `dre_report` idêntico campo a campo antes e depois de cadastrar uma conta com saldo de abertura de valor arbitrário. `profitability.py` deriva da DRE → coberto pelo mesmo teste. → design §3.5, §6.4
- **IV2 — Cockpit intacto:** `cockpit/service.py:130` continua calculando `net_revenue = available + pending + withdrawn` como **faturamento líquido** (uso correto — é faturamento, não saldo) e não é tocado. O card "Em conta" é escopo da Story 8.7, não desta.
- **IV3 — Carteira intacta:** `wallet_summary` inalterado; nenhum arquivo de `app/modules/wallet/` é editado por esta story (o AC8 transforma isso em teste permanente).
- **IV4 — Fila de Pagamentos e Contas a Pagar/Receber intactas:** `payables`, `receivables` e `payables/receipts.py` não são tocados; nenhuma coluna nova em `payables` (o `payables.bank_account_id` opcional é registrado no design §6.7 como **fora** das ondas 0–6).
- **IV5 — Projeção de Caixa inalterada nesta story:** a projeção continua com `saldo_inicial_origem="plataforma"` (Story 8.1). Passar a usar o saldo bancário é a **Story 8.8** — cadastrar uma conta aqui **não** pode mudar a resposta de `GET /financial-intelligence/projection`. Teste explícito (é o erro mais provável de acoplamento acidental).
- **IV6 — Exclusão de conta (LGPD) cobre a tabela nova:** `platform.delete_account` purga dinamicamente as subclasses de `TenantMixin`, então `bank_accounts` é coberta **automaticamente** — mas isso precisa ser **verificado por teste** (tenant com conta bancária → `delete_account` → zero linha remanescente), não assumido. → epic §4; design §7.4
- **IV7 — Zero regressão:** a suíte inteira segue verde; `alembic upgrade head` e o `downgrade` da 0058 aplicam limpo num Postgres real.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false`, mesma decisão já documentada nas Stories 5.7, 6.1, 6.2 e 7.5.
> Quality validation will use manual review process only (ver `quality_gate_tools` acima e `scripts/check.sh`).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [x] **Task 1 — Confirmar o head das migrations ANTES de escrever a migration (AC: 1)**
  - [x] Rodar `ls apps/api/migrations/versions/ | sort | tail -5` e/ou `alembic heads` e confirmar qual é o head real **no momento da implementação**. O design §2 e o epic §"Contexto" ancoram em **0057** (`device_tokens`) e prescrevem **0058** para a Onda 1 — mas esse número é a expectativa da data do design, não uma garantia.
  - [x] **Não fixar `down_revision` cegamente.** Se o head não for `0057`, encadear **linearmente após o head real** e documentar o desvio na docstring da migration — exatamente como `0049_investments.py` documenta (encadear num revision antigo cria **múltiplos heads** e `alembic upgrade head` falha com *"multiple heads"*).
  - [x] Registrar o head confirmado no `Dev Agent Record`.

- [x] **Task 2 — Migration `00NN_bank_accounts.py` (AC: 1, 3)**
  - [x] Copiar o esqueleto de `0049_investments.py`: `_enable_rls(table)` com `ENABLE` + `FORCE ROW LEVEL SECURITY` + `CREATE POLICY tenant_isolation … USING (…) WITH CHECK (…)`.
  - [x] `op.create_table("bank_accounts", …)` com as colunas do AC1. `op.create_index("ix_bank_accounts_tenant_id", …)`.
  - [x] Índice único **parcial**: `op.create_index("uq_bank_accounts_tenant_ident", "bank_accounts", ["tenant_id","institution_code","branch","number"], unique=True, postgresql_where=sa.text("number <> ''"))`.
  - [x] `downgrade()` derruba os dois índices e a tabela.
  - [x] **Estritamente aditiva, zero backfill.** A armadilha do `UPDATE` filtrado a zero linhas sob `FORCE RLS` (documentada em `0046_ledger_classification.py`, e que o SQLite dos testes **não** pega) **não se aplica** aqui — e a docstring da migration deve dizer isso explicitamente, para que a próxima migration do módulo (que **vai** ter backfill, na Onda 2) não copie um modelo sem a disciplina.

- [x] **Task 3 — Módulo `app/modules/bank/` — modelo (AC: 1, 4, 6)**
  - [x] Criar `apps/api/app/modules/bank/{__init__.py,models.py,schemas.py,service.py,router.py}`.
  - [x] `models.py`: `class BankAccount(Base, TenantMixin, TimestampMixin)`, `__tablename__ = "bank_accounts"`, `id` com `default=_uuid` (`app/db/base.py`), dinheiro em `BigInteger`, `opening_date` como `Date`, `archived_at` como `DateTime(timezone=True)` nullable.
  - [x] Constantes: `KIND_CHECKING/KIND_SAVINGS/KIND_INVESTMENT/KIND_CASH` + `KINDS: tuple[str, ...]` (os 4 aceitos) + `KIND_PLATFORM_WALLET = "platform_wallet"` **separado dos aceitos**, com comentário citando design §2.1 (reservado, ninguém escreve).
  - [x] Declarar o índice único parcial **também no modelo** (`__table_args__` com `Index(..., unique=True, postgresql_where=text("number <> ''"), sqlite_where=text("number <> ''"))`). ⚠️ **`sqlite_where` é obrigatório**: os testes unitários criam o schema com `Base.metadata.create_all` em **SQLite** (`tests/conftest.py`) — sem o `sqlite_where`, o índice nasce total no SQLite e o teste do AC3 ("conta sem número não colide") **passaria por engano em Postgres e falharia no SQLite**, ou vice-versa.
  - [x] Docstring do modelo, obrigatória, com três avisos: (a) este é o **plano 3** do dinheiro — nunca somar com `transactions`/`wallet` sem rótulo (Regra dos Planos §1.3a); (b) o saldo é **derivado**, não existe coluna de saldo, e materializá-lo está proibido pelo design §3.1; (c) `kind` aqui é **validado** (ao contrário de `investment_accounts.kind` e `cost_centers.kind`, que são texto livre) porque tem **comportamento** associado — a Story 8.8 exclui `investment` do saldo de caixa e `platform_wallet` é proibido. Sem esse aviso, uma story futura "harmoniza" o campo e quebra as duas regras.
  - [x] Registrar o modelo em `apps/api/app/db/registry.py` (sem isso, `create_all` dos testes não cria a tabela e o Alembic autogenerate não a conhece).

- [x] **Task 4 — Service: CRUD + saldo derivado + primária (AC: 2, 3, 4, 5, 7)**
  - [x] `class BankError(Exception)` com `status_code` (padrão de `InvestmentError`/`ChartAccountError`).
  - [x] `get_account(db, account_id) -> BankAccount` — `db.get`; `None` → `BankError(..., 404)`. Cross-tenant cai aqui pela RLS (fail-closed), sem filtro manual de `tenant_id`.
  - [x] `list_accounts(db, *, include_archived: bool = False) -> list[BankAccount]` — ordena por `name`; default esconde `archived_at IS NOT NULL` (padrão `chart_of_accounts.list_accounts`).
  - [x] `create_account(db, *, tenant_id, actor, data: BankAccountCreate) -> BankAccount` — valida `kind` ∈ `KINDS` (422; `platform_wallet` cai aqui com mensagem própria), `opening_date` não futura (422), `name` não vazio; `opening_balance_cents` **pode ser negativo** (conta no limite/cheque especial) e isso precisa de teste; `IntegrityError` → `BankError(..., 409)` com `db.rollback()` (padrão `chart_of_accounts`); `audit.record(action="bank.account.create")`; primeira conta do tenant nasce `is_primary=True`.
  - [x] `update_account(db, *, account_id, tenant_id, actor, data: BankAccountUpdate) -> BankAccount` — edita `name`, `kind`, `institution*`, `branch`, `number`, `holder_document`, `pix_key`, `opening_balance_cents`, `opening_date`, `is_primary`. Mesmo tratamento de `IntegrityError` → 409. **Não** permite editar `archived_at` por aqui.
  - [x] `set_primary(db, *, account_id, tenant_id, actor)` (ou a lógica embutida no `update_account`): desmarca as demais e marca esta, **num commit só**; conta arquivada não pode ser primária (422). `primary_account(db) -> BankAccount | None` retorna a primária ativa.
  - [x] `archive_account(db, *, account_id, tenant_id, actor) -> BankAccount` — seta `archived_at` se ainda `None` (idempotente); se era primária, limpa `is_primary` sem eleger sucessora; `audit.record`.
  - [x] `derived_balance(db, *, bank_account_id: str, until: date | None = None) -> int` — `opening_balance_cents + _movements_sum(db, bank_account_id=…, until=…)`. `_movements_sum` **retorna 0 nesta story** (não existe `bank_transactions`), com docstring dizendo em uma linha que a Story 8.3 substitui o corpo por `SUM(amount_cents)` sob as condições do design §3.1 (`posted_at > opening_date`, `posted_at <= until`, `status <> 'ignored'`) e que **a assinatura pública não muda**.
  - [x] **`derived_balances_as_of(db, *, as_of: date | None = None, include_archived: bool = False) -> dict[str, int]`** — saldo de **todas** as contas numa **data comum**, em **uma** passada. ⚠️ **[RENOMEADA por @po, 2026-07-29 — assinatura canônica ratificada pela @architect, design §3.1.1.]** Chamava-se `derived_balances`; o rename **não é cosmético**: `derived_balance` e `derived_balances` diferiam por **um `s` final** e são, respectivamente, a função certa e a errada para o mesmo trabalho — e o sintoma de escolher errado é uma divergência **falsa, silenciosa e plausível** (o relatório não quebra; ele mente um número). `as_of` no nome **e** no parâmetro declara que há **uma** data para todas as contas, e o nome deixa de ser um autocomplete de distância 1 da função correta.
  - [x] A docstring de `derived_balances_as_of` **deve** dizer: *"**PROIBIDA na conferência (design §5.1 / Story 8.5)** — lá cada conta tem a sua própria data de referência (o `reference_date` do seu checkpoint), e um `as_of` comum compararia o saldo do banco de uma data com o saldo do sistema de outra, que é o erro clássico que o service deve **recusar**. Use para **tela de lista** (Story 8.7) e como base do `active_balance_total`."*
  - [x] `active_balance_total(db, *, until: date | None = None, exclude_kinds: tuple[str, ...] = (KIND_INVESTMENT,)) -> int` — Σ dos saldos das contas ativas, excluindo `investment` por default (é a parcela "no banco" que a Story 8.8 soma).
  - [x] Docstring do módulo: RLS é a única garantia de isolamento (nenhuma query filtra tenant à mão — Regra de Ouro nº 1); **referências soltas, sem FK dura** (padrão do projeto — `charges.client_id`, `payables.cost_center_id`), integridade validada no service.

- [x] **Task 5 — Schemas + router (AC: 2, 4, 6, 7)**
  - [x] `schemas.py`: `BankAccountCreate`, `BankAccountUpdate` (todos os campos opcionais), `BankAccountOut` com `id, name, kind, institution, institution_code, branch, number, holder_document, pix_key, opening_balance_cents, opening_date, is_primary, archived_at, saldo_derivado_cents, saldo_derivado_origem, created_at`. `saldo_derivado_origem` **importa `ORIGEM_BANCO` de `app.core.money_planes`** (criado pela Story 8.1) — nunca uma string literal solta.
  - [x] `holder_document`: validar **só tamanho/normalização de dígitos**, sem dígito verificador — é a dívida global já registrada no `CLAUDE.md` §6.1 ("Validação de CPF/CNPJ"), e resolvê-la aqui criaria inconsistência com o resto do produto. Documentar a escolha.
  - [x] `router.py`: `APIRouter(prefix="/bank", tags=["bank"])`, `_guard = require_module("bank")`, `db = Depends(get_tenant_db)` em **todas** as rotas (nunca `get_db` — `tests/test_tenancy_guard.py` falha se este módulo aparecer fora da allowlist, e ele **não deve** entrar na allowlist).
  - [x] Rotas: `GET /bank/accounts` (query `include_archived: bool = False`), `POST /bank/accounts` (201), `GET /bank/accounts/{id}`, `PATCH /bank/accounts/{id}`, `POST /bank/accounts/{id}/archive`, `GET /bank/accounts/{id}/balance` (`{saldo_derivado_cents, saldo_derivado_origem, until}`). **Nenhum `DELETE`** (AC2).
  - [x] Registrar `bank_router` em `apps/api/app/modules/__init__.py` (`ALL_ROUTERS`).

- [x] **Task 6 — Testes estruturais da Regra dos Planos (AC: 8)**
  - [x] `apps/api/tests/test_money_planes.py`, com docstring no estilo de `test_tenancy_guard.py` explicando **por que** o teste existe (design §1.3: *"sem (1) o resto degrada por acidente: basta uma story futura importar o módulo errado para recriar o bug numa forma nova"*).
  - [x] `test_wallet_nao_importa_bank` — varre recursivamente `apps/api/app/modules/wallet/**/*.py` e falha se encontrar `from app.modules.bank` ou `import app.modules.bank`. Sem allowlist: o ponto de contato da Onda 6 vive do lado `bank` e usa `core/events`, então **não existe** exceção legítima.
  - [x] `test_bank_balance_ignora_wallet` — cria `Transaction`(s) na Carteira (inclusive `status='available'`) e assere que `derived_balance` da conta não muda em 1 centavo.
  - [x] `test_wallet_summary_ignora_bank` — o recíproco: cria conta bancária com `opening_balance_cents` grande e assere que `wallet_summary(db)` é idêntico campo a campo.
  - [x] `test_bank_nao_referencia_transaction` **[SUPOSIÇÃO DO @SM]** — varredura estática de `apps/api/app/modules/bank/**/*.py` procurando `Transaction`/`wallet.models`; hoje deve ser vazio. Docstring: a Regra dos Planos §1.3b **permite** `bank → wallet`, então este teste é mais estrito que o design de propósito — o ponto de contato da Onda 6 é desenhado via `core/events` (design §6.6) e não precisa do modelo. Se uma story futura precisar legitimamente do símbolo, ela **atualiza este teste com justificativa escrita** (padrão de allowlist do `test_tenancy_guard.py`), nunca o apaga.
  - [x] **Não** duplicar aqui `test_projecao_declara_origem_do_saldo_inicial` (design §1.3 item 4): ele é implementado pela **Story 8.1** em `test_financial_intelligence_projection.py`. Referenciar por comentário para o leitor achar os 4 testes da regra. **[AUTO-DECISION]** — evita dois testes com o mesmo nome em arquivos diferentes.

- [x] **Task 7 — Testes de unidade do módulo (AC: 1–7; IV1–IV6)**
  - [x] `apps/api/tests/test_bank_accounts.py`: criar conta (201, saldo derivado == `opening_balance_cents`); **N contas** no mesmo tenant, inclusive duas do mesmo `kind` e em bancos diferentes (AC2 / decisão F3); `kind` inválido → 422; `kind='platform_wallet'` → 422; `opening_date` futura → 422; `opening_balance_cents` negativo aceito; duplicidade de `(institution_code, branch, number)` → 409; **duas contas sem `number` convivem** (índice parcial); arquivar (idempotente, sai da listagem default, aparece com `include_archived=True`); `is_primary` único (marcar B desmarca A; arquivar a primária deixa o tenant sem primária); `derived_balances_as_of`/`active_balance_total` (a última exclui `kind='investment'`); `GET /bank/accounts/{id}` de id inexistente → 404.
  - [x] `saldo_derivado_origem == "banco"` em toda superfície que devolve saldo (AC6) — asserção sobre a constante importada, não sobre a string.
  - [x] **IV1:** `test_conta_bancaria_nao_altera_dre` — snapshot de `dre_report` antes/depois, igualdade campo a campo.
  - [x] **IV5:** `test_conta_bancaria_nao_altera_projecao` — `cash_projection` idêntica (inclusive `saldo_inicial_cents` e `saldo_inicial_origem == "plataforma"`) depois de cadastrar conta com saldo de abertura. É o acoplamento acidental mais provável desta onda.
  - [x] **IV6:** estender `apps/api/tests/test_platform.py` (ou arquivo próprio) — tenant com `bank_accounts` → `delete_account` → zero linha remanescente.

- [x] **Task 8 — RLS e2e no Postgres real (AC: 9)**
  - [x] `apps/api/tests/test_bank_rls.py`, `pytest.mark.rls_e2e`, seguindo `test_receipts_rls.py`/`test_investments_rls.py`: sobe Postgres via testcontainers, roda `alembic upgrade head` como o papel **não-superusuário** `e1p_app` (isso exercita de fato a migration nova), cria conta em A e em B, e assere: A não lista/lê/edita/arquiva a de B; `INSERT` com `tenant_id` alheio é barrado pelo `WITH CHECK`; sem a GUC `app.current_tenant_id` a leitura devolve zero linhas (fail-closed).
  - [x] **Este arquivo é o ponto de extensão das Stories 8.3 e 8.4** — elas acrescentam casos aqui em vez de criar dois novos arquivos de testcontainer (cada boot custa minutos de CI). **[AUTO-DECISION]**, registrada também nas Dev Notes daquelas stories.

- [x] **Task 9 — Fechamento (AC: todas; IV7)**
  - [x] `bash scripts/check.sh` verde; suíte completa sem regressão; os 3 agentes de QA passam.
  - [x] `alembic upgrade head` **e** `alembic downgrade -1` aplicam limpo num Postgres real (não só SQLite).

## Dev Notes

### Decisão do fundador que define o formato desta story (2026-07-29)

**VÁRIAS CONTAS PJ.** A topologia real do fundador é **corrente + poupança + aplicação, possivelmente em bancos diferentes** — não é uma conta só, e **não** é conta PF misturada com PJ (o que tornaria a divergência ruído crônico). Consequências diretas aqui:

- `bank_accounts` suporta **N contas desde esta story** — não há caminho "uma conta fixa" a ser generalizado depois (epic §9 F3; design D2/D3).
- A conferência da Story 8.5 é **por conta**, nunca só consolidada: três contas divergindo +R$ 1.200, −R$ 900 e +R$ 40 dão um consolidado de +R$ 340 que **parece saudável e esconde dois problemas** (epic §3.2). Esta story é a que torna isso possível, expondo `derived_balance` por conta (uma conta, uma data).
- Como não há mistura PF/PJ, a divergência é sinal **relativamente limpo** — o que é justamente o que faz a Onda 1 valer como experimento de medição (epic §3.1).

### A Regra dos Planos — o que esta story trava, e por quê é teste e não convenção

| Plano | O que é | Tabelas | Dono do dinheiro |
|---|---|---|---|
| 1 — Plataforma | trilho e1p, split 40/30/20 | `transactions`, `platform_earnings` (global) | está **na e1p** |
| 2 — Negócio | direitos e obrigações (competência **e** caixa) | `charges`, `payables`, `chart_accounts`, `cost_centers` | ninguém — é promessa |
| 3 — Bancário | o extrato real da conta do usuário | **`bank_accounts` (esta story)** + `bank_transactions` (8.3) | está **no banco do usuário** |

**Regra dos Planos** (design §1.3): **(a)** nenhum cálculo de saldo bancário lê `transactions`, nenhum cálculo de saldo de carteira lê `bank_transactions`, e as duas somas **nunca ocupam o mesmo campo numérico**; **(b)** `app.modules.bank` **pode** importar `app.modules.wallet`, `app.modules.wallet` **nunca** importa `app.modules.bank` — o ponto de contato vive no lado `bank`; **(c)** todo campo de API que carrega saldo declara a procedência em `*_origem`.

O **único** ponto de contato legítimo entre plano 1 e plano 3 é o **payout da Carteira** (design §1.2): `request_payout` deixa o dinheiro de estar "na plataforma" e passa a estar "no banco". Isso é a **Onda 6 — não liberada** (epic §5), e quando vier será via `core/events` (design §6.6), preservando (b).

O bug do §1.1 do design (a projeção usando `available_cents` como saldo bancário) é **exatamente** a violação de (a). Sem o teste estrutural do AC8, basta uma story futura importar o módulo errado para recriá-lo numa forma nova — por isso o design o classifica como **gate**, não como convenção, e por isso ele é AC e não "nice to have".

### O saldo é derivado — e materializá-lo está proibido

```
saldo(conta, até_data) = opening_balance_cents
                       + SUM(bank_transactions.amount_cents
                             WHERE bank_account_id = conta
                               AND posted_at > opening_date
                               AND posted_at <= até_data
                               AND status <> 'ignored')
```

O design §3.1 rejeita explicitamente a materialização: derivado é impossível de divergir dos próprios movimentos, a auditoria "de onde vem esse número" **é a própria lista**, e corrigir um movimento corrige o saldo sem recálculo. O argumento de performance não se aplica: ~40 movimentos/mês `[ESTIMATIVA do design §3.1]` = ~5.000 em dez anos por conta, sob índice — `SUM` sobre isso é ruído. **Materializar aqui seria pagar com a única propriedade que o produto está vendendo: que o número é conferível.** Se um dia doer, a resposta é mover o `opening_date` para frente com um checkpoint de corte (auditável), **não** um saldo materializado.

`opening_balance_cents` + `opening_date` são o coração da usabilidade: bancos brasileiros entregam tipicamente só os **últimos 60 dias** de extrato (pesquisa §2.2, design §2.1 `[CONFIRMADO 2026-07-29]`), então sem saldo de abertura o derivado começaria errado para sempre. Com ele, o usuário **confirma um número** que está a 5 segundos do polegar dele (app do banco) — dentro do teto de simplicidade do design §0 (*"o e1p pode pedir ao usuário que ele CONFIRME um número. Não pode pedir que ele CONSTRUA um número"*).

### Padrão de RLS e de migration — copiar, não reinventar

- **Padrão exato a copiar:** `apps/api/migrations/versions/0049_investments.py` (função `_enable_rls`) e `0048_cost_centers.py`. Toda tabela de negócio: `tenant_id` + `ENABLE` + `FORCE ROW LEVEL SECURITY` + policy `tenant_isolation` com `USING` **e** `WITH CHECK`.
- **Mixins:** `app/db/base.py` — `Base`, `TenantMixin` (a coluna `tenant_id` indexada que a RLS usa), `TimestampMixin`, `_uuid` (default do `id`).
- **A app conecta como `e1p_app` (non-superuser)** — superusuário faz bypass de RLS **mesmo com FORCE** (`CLAUDE.md` Regra de Ouro nº 1). Por isso o `rls_e2e` roda as migrations como esse papel.
- **Sem FK dura entre entidades financeiras** — padrão do projeto (`charges.client_id`, `payables.contract_id`/`cost_center_id`): integridade por RLS + validação no service. **Siga isso**: nem `bank_accounts` nem as tabelas das Stories 8.3/8.4 declaram `ForeignKey`.
- **Armadilha do backfill sob FORCE RLS** (`0046_ledger_classification.py`): `UPDATE` numa migration sem a GUC `app.current_tenant_id` é filtrado a **zero linhas em silêncio**, e o SQLite dos testes não pega. **Esta story não faz backfill** — mas a docstring da migration deve registrar a armadilha, porque a Onda 2 **vai** fazer (design §6.2) e vai copiar este arquivo como modelo.

### Numeração de migration — confirmar, não fixar

O design §2/§8 e o epic ancoram a Onda 1 em **0058**, supondo head `0057_device_tokens.py`. Verificado nesta sessão: o head **é** `0057`. Ainda assim, a Task 1 manda confirmar no momento da implementação — e há uma razão concreta: **as Stories 8.2, 8.3 e 8.4 criam uma tabela cada**, então a Onda 1 consome **três** revisions (previsivelmente 0058/0059/0060), não uma. Isso **contradiz** o "uma migration por onda" do design §2 e o mapa `Onda 2 → 0059` / `Onda 3 → 0060` do epic §5.

**✅ RESOLVIDO pela @architect (ratificação D-6; design §2 e §8 já corrigidos):** o design **parou de fixar número de revision**, e a granularidade correta passou a ser **"uma migration por story que cria tabela"** — não uma por onda. Portanto a Onda 1 consumir três revisions **não é desvio**, é o comportamento certo; forçar uma migration só para "cumprir o mapa" acoplaria três stories a um arquivo, com três `@dev` disputando o mesmo diff. O que é lei continua sendo **encadear linearmente após o head real** (a lição da docstring da `0049`), e a **Task 1 desta story fica como está — é a prática correta, e deve ser copiada por toda story deste epic que crie tabela**. O número nominal do epic §5 vale como **ordem**, não como identificador (ajuste do epic pendente com o @pm; não bloqueia nada).

### Contrato público que esta story cria (consumido pelas Stories 8.3–8.8)

| Símbolo | Assinatura | Quem consome |
|---|---|---|
| `app.modules.bank.models.BankAccount` | modelo SQLAlchemy | 8.3 (`bank_account_id`), 8.4, 8.5, 8.7 |
| `...models.KIND_CHECKING/SAVINGS/INVESTMENT/CASH`, `KINDS`, `KIND_PLATFORM_WALLET` | constantes | 8.7 (vocabulário na UI), 8.8 (exclui `investment`) |
| `...service.BankError` | `Exception` com `.status_code` | 8.3, 8.4, 8.5 (mesma família de erro → mesmo `_err()` no router) |
| `...service.get_account(db, account_id) -> BankAccount` | 404 fail-closed | 8.3 (valida a conta do movimento), 8.4 (valida a conta do checkpoint) |
| `...service.list_accounts(db, *, include_archived=False) -> list[BankAccount]` | — | 8.5 (itera contas para a conferência **por conta**), 8.7 |
| `...service.derived_balance(db, *, bank_account_id, until=None) -> int` | centavos | **8.3** (preenche `_movements_sum`), **8.5** (`saldo_sistema_cents` na data de referência), 8.7 |
| `...service.derived_balances_as_of(db, *, as_of=None, include_archived=False) -> dict[str, int]` | `{account_id: centavos}` | **8.7** (saldo de todas as contas na mesma data, sem N+1), base de `active_balance_total` (8.8). ⚠️ **PROIBIDA na conferência (8.5)** — design §3.1.1; ver nota 2 abaixo e 8.5 AC4b |
| `...service.active_balance_total(db, *, until=None, exclude_kinds=(KIND_INVESTMENT,)) -> int` | centavos | **8.8** (a parcela "no banco" do `origem="misto"`) |
| `...service.primary_account(db) -> BankAccount \| None` | — | Onda 6 (payout), 8.7 |
| `...service.create_account/update_account/archive_account/set_primary` | ver Task 4 | 8.7 (tela) |
| `BankAccountOut.saldo_derivado_cents` + `saldo_derivado_origem` | `ORIGEM_BANCO` | 8.7 |
| rota `GET /bank/accounts/{id}/balance` | `{saldo_derivado_cents, saldo_derivado_origem, until}` | 8.7 |

**Para o @sm/@dev da metade 8.5–8.8, três pontos de alinhamento de assinatura:**
1. `until` é sempre um `date` (nunca `datetime`) e é **inclusivo** — a conferência da 8.5 compara na **mesma data de referência** do checkpoint, e comparar datas diferentes é o erro clássico que o design §5.1 manda o service **recusar**.
2. **[REVOGADO por @po, 2026-07-29 — a recomendação original desta story estava ERRADA e induziria o @dev a um bug. Confirmado pela @architect: ratificação D-4.]** O texto anterior dizia que a 8.5 deveria *"preferir `derived_balances` (uma passada) a um loop de `derived_balance`"*. **Não serve, e a 8.5 tem razão em recusar:** aquela função recebe **uma data comum**, mas a conferência tem **uma data de referência por conta** (o `reference_date` do checkpoint mais recente de *cada* conta). Uma data comum reintroduziria exatamente o erro que o design §5.1 manda o service **recusar**.
   **Contrato que vale (design §3.1.1):** a conferência usa **laço de `derived_balance` com o `until` de cada conta**; `derived_balances_as_of` é para **tela de lista**. Escala: empresa de 1 pessoa com um punhado de contas — não há performance a otimizar aqui.
   **Por que NÃO mudar a assinatura para um mapa `{conta: data}`:** o ganho da versão em lote é fazer **uma** passada; com uma data por conta isso degenera em N queries — ou num `CASE` por conta menos legível que o laço. O mapa custaria complexidade para entregar zero benefício e, pior, convidaria o chamador a construí-lo a partir de uma data única, que é o bug de volta por outro caminho.
   Em qualquer caso, o `saldo_sistema` da conferência é **por conta**; consolidado sem decomposição é **proibido** (epic §3.2).
3. A 8.8 deve usar `active_balance_total` (que já exclui `kind='investment'`, conforme design §6.1) e **somar** com `available_cents` da Carteira sob `origem="misto"`, com as **duas parcelas rotuladas** na UI. Somar sim; esconder a composição, nunca.

### File Locations (novos/alterados)

- **NOVO:** `apps/api/migrations/versions/00NN_bank_accounts.py` (NN = head real + 1, ver Task 1)
- **NOVO:** `apps/api/app/modules/bank/__init__.py`, `models.py`, `schemas.py`, `service.py`, `router.py`
- **NOVO:** `apps/api/tests/test_bank_accounts.py`, `apps/api/tests/test_money_planes.py`, `apps/api/tests/test_bank_rls.py`
- **ALTERAR:** `apps/api/app/db/registry.py` (importar `BankAccount`)
- **ALTERAR:** `apps/api/app/modules/__init__.py` (`bank_router` em `ALL_ROUTERS`)
- **ALTERAR:** `apps/api/tests/test_platform.py` (IV6 — purga de `bank_accounts` no `delete_account`)
- **Nenhum arquivo de `app/modules/wallet/`, `payables/`, `receivables/`, `cockpit/`, `financial_intelligence/` é tocado.** Nenhum arquivo em `apps/web/` (a tela é a Story 8.7).

### Technical Constraints

- **Dinheiro em centavos `BigInteger`** — nunca float (`CLAUDE.md` Regra de Ouro; design §0).
- **RLS é a única garantia de isolamento** — nenhum filtro manual de `tenant_id` (decisão de arquitetura registrada no `CLAUDE.md`: defesa-em-profundidade foi **considerada e rejeitada** para não criar o padrão "algumas queries filtram, outras não").
- **Sem FK dura** entre tabelas de negócio.
- **`archived_at`, nunca `DELETE`** (design §2.1).
- **Saldo derivado, nunca materializado** (design §3.1) — se aparecer coluna de saldo, o gate reprova.
- **Módulo gated por `require_module("bank")`** e sempre `get_tenant_db` (o `test_tenancy_guard.py` é o guarda automático disso).
- **Zero custo recorrente novo, zero chamada de rede** (Regra de Ouro nº 4; epic §4.6).
- **Nada de UI nesta story** (epic §6 Story 8.2: *"Não inclui: movimentos, checkpoints, conferência, tela"*).

### Testing

- `pytest` (SQLite, `-m "not rls_e2e"`) para o CRUD e os testes estruturais. ⚠️ **A RLS não é exercida em SQLite** (`tests/conftest.py` diz isso explicitamente) — o isolamento **só** é provado pelo `rls_e2e`.
- `pytest -m rls_e2e` (testcontainers, Postgres real, papel `e1p_app`) — obrigatório, é o gate de RLS de **toda** story deste epic (epic §4.7).
- O teste de maior valor desta story é `test_wallet_nao_importa_bank`: ele é a única defesa **permanente** contra a reintrodução do bug original numa forma nova, e custa ~20 linhas.
- O segundo é `test_conta_bancaria_nao_altera_projecao` (IV5): garante que a fundação do plano 3 não vaza para a projeção antes da Story 8.8 decidir como.
- `bash scripts/check.sh` + os 3 agentes de QA (`CLAUDE.md` §5).

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-29 | 0.1 | Story criada a partir do Epic 8 (Controle Bancário e Conferência), Onda 1 — River (@sm) | River (@sm) |
| 2026-07-29 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready. **Correção de defeito de costura entre as duas metades do épico:** a Dev Note "ponto de alinhamento 2" instruía a Story 8.5 a usar `derived_balances` (um `until` só) na conferência — o que reintroduziria o erro de comparar saldos em datas diferentes que o design §5.1 manda **recusar**. A 8.5 (AC4b) recusou corretamente; a nota foi reescrita em favor do laço de `derived_balance` por conta, e a linha de contrato de `derived_balances` foi reapontada para os consumidores reais (8.7, base do `active_balance_total` da 8.8). Escopo, ACs e tasks inalterados. | @po |
| 2026-07-29 | 0.1.2 | Reconciliada com a **ratificação da @architect** (D-4 e D-6). Segue **GO (9/10), Ready**. (a) **D-4** — a @architect confirmou que *"a 8.5 está certa"* e **renomeou** a função em lote: `derived_balances` → **`derived_balances_as_of(db, *, as_of=None, include_archived=False)`**, com a **proibição de uso na conferência escrita na própria docstring**. O rename não é cosmético: os dois nomes diferiam por **um `s`** e eram a função certa e a errada para o mesmo trabalho, com sintoma **silencioso e plausível**. Assinaturas canônicas gravadas no design §3.1.1. (b) **D-6 RATIFICADO** — o design **parou de fixar revision** e adotou *"uma migration por story que cria tabela"*; a nota "Numeração de migration" passa de "contradição não resolvida" a **resolvida**, e a Task 1 fica como está (é a prática correta, a ser copiada por toda story do epic que crie tabela). | @po |
| 2026-07-29 | 0.2 | **Implementada.** Status: Ready → InProgress → InReview. Migration **0058** encadeada no head real **0057** (confirmado programaticamente). Módulo `app/modules/bank/` (models/schemas/service/router) + `test_bank_accounts.py`, `test_money_planes.py`, `test_bank_rls.py`. Backend 887 passed / 1 skipped (+52); `rls_e2e` 15/15 no Postgres real; frontend 197/45 intacto (nada de `apps/web/` tocado). Um desvio (validação de `holder_document` com dígito verificador, seguindo o padrão real do projeto em vez da premissa desatualizada da Task 5) e três achados registrados no Dev Agent Record. | Dex (@dev) |
| 2026-07-30 | 0.2.1 | **QA Gate da onda (8.1–8.8): FAIL — Status: InReview → InProgress.** Esta é a única das oito stories que muda de status; as demais seguem em `InReview` aguardando o gate da onda, porque nenhuma tem correção a fazer. **Motivo:** 🔴 **BANK-001 (HIGH)** — `update_account` aceita mover `opening_date` para frente sem guarda, e como `_movements_sums` só soma `posted_at > opening_date`, os movimentos anteriores param de contar no saldo derivado **continuando visíveis na lista**; a conferência então **relata um furo que não existe** (reproduzido: divergência 0 → −R$ 800 fora da banda, após um `PATCH` que responde 200). É a falha nº 1 que este épico existe para impedir, em código novo, alcançável pela UI. A guarda equivalente já existe no caminho de criação (`_validate_posted_at`) — falta espelhá-la no `update`, com 2 testes. **Também registrado:** ⚠️ TEST-001 (MEDIUM) — `_imported_modules` de `test_money_planes.py` não apensava o alias do `ImportFrom`, e `from app.modules import bank` dentro de `wallet/` **passava nos dois testes** (AST e texto cru); o gate que a própria docstring chama de *"o teste de maior valor da Story 8.2"* tinha evasão trivial. **Corrigido pelo @qa** (alteração só de teste, verificada por mutação). Detalhes, evidência e teste de mutação em `docs/qa/epic-8-onda-0-1-gate-2026-07-30.md`. | Quinn (@qa) |
| 2026-07-30 | 0.2.2 | **BANK-001 corrigido. Status: InProgress → InReview.** `_validate_opening_date_move` recusa com 422 mover `opening_date` para frente quando existe movimento na janela `(abertura atual, nova]` — a guarda irmã de `_validate_posted_at`, com a razão escrita no código; a mensagem diz **quantos** movimentos ficariam de fora, **qual o mais antigo** e três caminhos de saída reais (não sugere apagar movimento, que não existe neste módulo). **Bordas decididas e registradas:** mover para trás continua livre (só acrescenta movimento ao saldo, e é o caminho de reparo de órfão pré-guarda); `posted_at == nova_data` é **recusado** (o saldo soma `>`, estritamente — a mesma assimetria que a 8.4 fixou); movimento `ignored` **conta** (senão `unignore` deixaria de devolvê-lo ao saldo, em silêncio). **5 testes novos**, com 3 mutantes aplicados e mortos. Backend **1050 passed, 1 skipped** (baseline 1045, +5, zero regressão); `rls_e2e` **22 passed**; frontend **288/50** intacto; `ruff`/`tsc`/`eslint --max-warnings 0` limpos. Nada de `apps/web/` foi tocado. **REL-001 avaliado e não corrigido** (outra story, decisão devolvida ao solicitante). | Dex (@dev) |
| 2026-07-30 | 0.2.2 | **RE-GATE: BANK-001 FECHADO.** Reproduzi o cenário original do gate (o `PATCH opening_date -> 15/06` que devolvia `200` e `divergencia=-80000`): agora responde **422** e a divergência permanece `0`. As três decisões de borda do @dev foram julgadas e **as três estão certas** — mover para trás livre (só acrescenta ao saldo, e é o único caminho de reparo do dado torto anterior à guarda), `posted_at == nova_data` recusado (a soma usa `>` estrito, então ficaria órfão igual — mesma assimetria que a 8.4 fixou em `_validate_reference_date`) e movimento `ignored` contando (o argumento é sobre o futuro: `unignore` promete devolver ao saldo e não cumpriria depois). Os 3 mutantes dele conferem; apliquei um **4º que ele não fez** e ele **sobreviveu**: removendo o filtro `bank_account_id == account.id` da guarda, a suíte ficava verde — o filtro mais importante dela não era exercitado por nada. A ausência dele seria o espelho do BANK-001: em vez de divergência fantasma, um **bloqueio fantasma** (422 numa edição legítima, nomeando um movimento que está em OUTRA conta). Acrescentei `test_patch_opening_date_ignora_movimento_de_OUTRA_conta_do_mesmo_tenant` (RG-1) — código de produção não tocado, está correto. 🟢 RG-2 (LOW, aberto): concordância singular/plural quebrada na mensagem do caso `ignored`, no sufixo compartilhado. Detalhes na seção RE-GATE de `docs/qa/epic-8-onda-0-1-gate-2026-07-30.md`. | Quinn (@qa) |

## Dev Agent Record

### Agent Model Used
Dex (@dev) — claude-opus-5 (1M), sessão autônoma de 2026-07-29.

### Debug Log References

Tudo rodado em **foreground**, saída real:

| Gate | Comando | Resultado |
|---|---|---|
| Head da migration | `ScriptDirectory.get_heads()` | `['0057']` — head único, confirmado ANTES de escrever a migration (Task 1) |
| Lint backend | `ruff check .` (apps/api) | `All checks passed!` |
| Suíte backend | `pytest -q -m "not rls_e2e"` | **887 passed, 1 skipped**, 15 deselected, 313s (baseline pós-8.1: 835 passed, 1 skipped → **+52 testes, zero regressão**) |
| RLS e2e | `pytest -q -m rls_e2e` | **15 passed**, 208s (baseline 14 → +1, o `test_bank_rls.py`). Postgres real via testcontainers, migrations aplicadas como `e1p_app` (não-superusuário) |
| Migration up/down | script ad-hoc com testcontainers | `upgrade head` OK → `relrowsecurity=True, relforcerowsecurity=True`; policy `tenant_isolation` com `USING` **e** `WITH CHECK`; `CREATE UNIQUE INDEX … WHERE ((number)::text <> ''::text)`; `downgrade -1` remove só `bank_accounts` (44 tabelas restantes intactas); `upgrade head` de novo OK |
| Typecheck frontend | `pnpm --filter @e1p/web typecheck` | `tsc --noEmit` sem erro |
| Testes frontend | `pnpm --filter @e1p/web test` | **197 passed / 45 arquivos** (baseline 195/45; os +2 vêm da emenda paralela da 8.1 em `ProjecaoCaixaPage`, **não desta story** — nenhum arquivo de `apps/web/` foi tocado aqui) |

⚠️ **Flake de infraestrutura registrado:** numa rodada intermediária, dois testes `rls_e2e`
(`test_bank_rls` e `test_chart_of_accounts_rls`) falharam com
`docker.errors.DockerException: Error while fetching server API version: 500 Server Error` — o
daemon do Docker Desktop degradou (havia 29 containers no host, vários órfãos de 26–28h de outras
sessões). **Não é falha de código:** os mesmos testes passaram antes e depois, e a suíte
`rls_e2e` completa fechou 15/15. Registrado porque vai reaparecer no CI se o host acumular
containers.

### Completion Notes List

**Decisões**

1. **Migration `0058`, encadeada em `0057`** — head confirmado programaticamente no momento da
   implementação (`get_heads() == ['0057']`), não copiado do design. Único head antes e depois.
2. **`_movements_sum` / `_movements_sums` como ponto de extensão da 8.3** — ambas privadas, ambas
   devolvendo o elemento neutro hoje (0 e `{}`). A assinatura **pública** (`derived_balance`,
   `derived_balances_as_of`, `active_balance_total`) já é a canônica do design §3.1.1 e não muda
   na 8.3. A versão em lote existe porque, com movimentos, o laço de `derived_balance` viraria um
   `SUM` por conta (N+1) — hoje o ganho é só a segunda query economizada.
3. **`derived_balances_as_of` com a proibição na própria docstring** (ratificação D-4), apontando
   a 8.5 para o laço de `derived_balance` e nomeando os consumidores legítimos (8.7 / base do
   `active_balance_total` da 8.8). Nenhum resíduo do nome revogado `derived_balances` no código —
   a única ocorrência é a frase da docstring que **explica** o rename.
4. **`is_primary` mantido pelo service, sem constraint parcial no banco** — uma
   `UNIQUE (tenant_id) WHERE is_primary` obrigaria a ordenar os `UPDATE`s para não colidir no meio
   da troca, e o campo só é escrito pelo service. A troca acontece num commit só (`set_primary` e
   `update_account`); arquivar a primária **não** elege sucessora.
5. **`platform_wallet` recusado com mensagem própria no service, não pelo Pydantic** — o usuário
   precisa ler *por que* a Carteira não é uma conta bancária, e o 422 genérico de enum não diz isso.
6. **Auditoria com `db.flush()` antes do `audit.record`** — ver "Achados" nº 2.

**Desvios da story (1)**

- **Task 5, `holder_document`.** A story mandava validar *"só tamanho/normalização de dígitos, sem
  dígito verificador"*, justificando com a dívida global do `CLAUDE.md` §6.1 e o risco de *"criar
  inconsistência com o resto do produto"*. **A premissa está desatualizada:**
  `app/core/validators.py` já implementa CPF/CNPJ **com** dígito verificador e é usado por `auth`,
  `crm`, `contracts` e `platform`. Seguir a letra produziria exatamente a inconsistência que a
  instrução queria evitar. **Implementado no padrão real do projeto** (opcional; quando informado,
  normaliza e valida — mesmo formato de `crm.schemas.ClientCreate.document`), com a justificativa
  escrita na docstring de `bank/schemas.py::_digits_or_empty`. Corrigir a nota velha do
  `CLAUDE.md` §6.1 é fora do escopo desta story.

**Achados registrados (não corrigidos — fora do escopo)**

1. **O 5º teste da Regra dos Planos não existe.** A ratificação (linha 234) atribuiu
   `test_todo_saldo_declara_origem` (varredura de contrato sobre os schemas de saída, exigindo o
   irmão `*_origem` para todo campo de saldo) à **Story 8.1**, dizendo que *"a 8.2 pode
   estendê-lo"*. A 8.1 não o criou — `test_financial_intelligence_projection.py` tem
   `test_projecao_declara_origem_do_saldo_inicial` (caso específico), não a varredura genérica.
   Não há o que estender. A parte (c) da regra está coberta **por instância** nesta story
   (`test_todas_as_superficies_de_saldo_declaram_a_origem`), não por contrato global.
   **Encaminhar ao @po/@architect: o gate genérico continua em aberto.**
2. **`chart_of_accounts.create_account` grava auditoria com `target` vazio.** `audit.record` é
   chamado logo após `db.add(acc)`, mas `id` tem default Python-side (`_uuid`), aplicado só no
   INSERT — então `acc.id` ainda é `None`. O rastro de criação de categoria não aponta para nada.
   O módulo `bank` faz `db.flush()` antes do `record` (e `test_operacoes_deixam_rastro_de_auditoria`
   cobre isso — foi ele que revelou o problema); a correção em `chart_of_accounts` é de outra story.
   **Provável que o mesmo padrão exista em outros módulos** — vale um sweep.
3. **`CLAUDE.md` §6.1** afirma que a validação de CPF/CNPJ *"só valida tamanho; falta dígito
   verificador"*. Está velho — ver o desvio acima.

**Fora de escopo, confirmado:** nenhum arquivo de `wallet/`, `payables/`, `receivables/`,
`cockpit/`, `financial_intelligence/` ou `apps/web/` foi tocado. A emenda paralela da Story 8.1 em
`ProjecaoCaixaPage.tsx` não foi encostada.

---

### Correção do gate — 🔴 BANK-001 (2026-07-30, Dex @dev)

**A guarda.** `_validate_opening_date_move(db, *, account, nova)` em
`apps/api/app/modules/bank/service.py`, chamada por `update_account` junto com a guarda de data
futura que já existia. Recusa com **422** mover `opening_date` **para frente** quando existe
movimento na janela `(opening_date atual, nova]` — os que deixariam de somar. A razão está escrita
na docstring, no estilo do módulo, e é literalmente a de `_validate_posted_at` vista do outro lado
da relação: lá o movimento é recusado porque *"a linha existiria, o saldo não mudaria, e ninguém
entenderia por quê"*; aqui o mesmo estado era alcançável por trás, movendo a data de corte por
baixo do movimento — com o agravante de que a conferência da 8.5 passa a relatar uma divergência
**inventada**.

**A mensagem** diz **quantos** movimentos ficariam de fora, **qual o mais antigo**, o que
aconteceria com o saldo e com a lista, e **três caminhos** de saída: corrigir a data dos movimentos,
arquivar a conta e cadastrá-la de novo com o saldo de abertura do dia (o idioma que o módulo já usa
para conta encerrada), ou alterar só o saldo de abertura sem mexer na data. **Não** sugere "apagar
os movimentos": `DELETE` de movimento não existe neste módulo por decisão da 8.3 (AC6), e oferecer
uma saída inexistente seria inventar requisito (Artigo IV).

**Decisões de borda (as três que o gate pediu para registrar e não inventar):**

| Caso | Decisão | Razão |
|---|---|---|
| Mover a data **para trás** | **Permitido**, sem guarda nova | O filtro é `posted_at > opening_date`: recuar só pode **acrescentar** movimento ao saldo, nunca tirar — não há órfão a criar. É também o **caminho de reparo** de uma conta que já tenha órfão de dado anterior à guarda: recuar devolve o movimento ao saldo. Coberto por teste, incluindo o cenário de reparo |
| `posted_at == nova_opening_date` | **Recusado** (a guarda usa `<=`) | `_movements_sums` soma `posted_at > opening_date`, **estritamente** — o movimento exatamente na nova data de abertura ficaria órfão igual. Consistente com a assimetria que a 8.4 documentou em `_validate_reference_date`: movimento exige `>`, checkpoint aceita `>=` |
| Movimento `status='ignored'` | **Conta** para a guarda | Hoje ele já está fora do saldo derivado e mover a data não mudaria número nenhum. Mas `unignore_transaction` promete *"devolve o movimento ao saldo"* e, com a data movida por cima dele, não teria como cumprir: o status voltaria a `unmatched` e o saldo não se mexeria, **em silêncio**. É o mesmo modo de falha do BANK-001 com o gatilho adiado para um clique futuro. A mensagem de 422 explicita esse motivo quando há ignorado na janela |

**Testes (5 novos, `apps/api/tests/test_bank_accounts.py`, seção BANK-001):**

1. `test_patch_opening_date_para_frente_por_cima_de_movimento_422` — **o cenário exato do gate**
   (abertura 01/06 R$ 1.000, débito R$ 800 em 10/06, saldo declarado R$ 200 em 20/06, batendo
   exatamente). Afere o 422, o conteúdo da mensagem (contagem + data do mais antigo) **e** que a
   conferência continua com `divergencia_cents == 0` depois da tentativa — a divergência inventada
   nunca aparece, e a conta não ficou alterada pela metade;
2. `test_patch_opening_date_na_data_exata_do_movimento_422` — a borda `==`;
3. `test_patch_opening_date_para_frente_conta_movimento_ignorado` — o ignorado bloqueia, com o
   motivo na mensagem;
4. `test_patch_opening_date_para_frente_sem_movimento_no_caminho_continua_permitido` — **a operação
   legítima segue passando**: corrigir a abertura para frente quando não há movimento no caminho é
   200;
5. `test_patch_opening_date_para_tras_continua_permitido_e_pode_reparar` — recuar é 200 e ressuscita
   o órfão fabricado por escrita direta na coluna (o estado que só existe em dado pré-guarda).

**Prova de que o teste não é vácuo — 3 mutantes, 3 mortos** (cada um aplicado, executado e
revertido; a árvore foi conferida limpa depois):

| # | Mutante | Resultado |
|---|---|---|
| M1 | `update_account` volta a chamar só `_validate_opening_date` (a guarda some) | ✅ **3 failed** — `..._por_cima_de_movimento_422`, `..._na_data_exata_do_movimento_422`, `..._conta_movimento_ignorado`. O corpo do 200 no log mostra `saldo_derivado_cents: 100000` onde o correto é `20000` — a inflação do BANK-001, capturada |
| M2 | A borda vira `posted_at < nova` (em vez de `<=`) | ✅ **1 failed** — só `..._na_data_exata_do_movimento_422`. O teste de borda é o único que morre, que é o que prova que ele mede a borda |
| M3 | A guarda passa a ignorar `status='ignored'` | ✅ **1 failed** — só `..._conta_movimento_ignorado` |

**Evidência (foreground, interpretador do venv — `scripts/check.sh` não usado, MNT-003):**

| Gate | Comando | Antes (baseline medida nesta sessão) | Depois |
|---|---|---|---|
| Backend | `pytest -q -m "not rls_e2e"` | `1045 passed, 1 skipped, 22 deselected` (310,67s) | **`1050 passed, 1 skipped, 22 deselected`** (305,45s) — +5, zero regressão |
| RLS e2e | `pytest -q -m rls_e2e` | 22 (gate) | **`22 passed`**, 102,92s |
| Lint backend | `ruff check .` | — | `All checks passed!` |
| Typecheck | `pnpm --filter @e1p/web typecheck` | — | `tsc --noEmit` **exit 0** |
| Lint frontend | `pnpm --filter @e1p/web lint` | — | `eslint . --max-warnings 0` **exit 0** |
| Frontend | `pnpm --filter @e1p/web test` | 288/50 | **`288 passed / 50 files`** (nada de `apps/web/` foi tocado) |

**Nenhuma mudança de frontend foi necessária** e nenhuma foi feita. O 422 já aparece na tela:
`AccountModal.save()` (`ContasSaldosPage.tsx`) tem `try/catch` com `setError(apiErrorMessage(err))`
e renderiza o texto no próprio modal — verificado por leitura. Isso é o oposto do **REL-001**
(MEDIUM, Story 8.7), que atinge `ignorar` / `desfazerIgnorar` / `removerDeclaracao`: **avaliado,
não corrigido** — é de outra story e a decisão de incluir agora ou virar follow-up foi devolvida ao
solicitante.

**Não tocados, por serem de outro dono:** UX-001 e SIG-001 (@architect), G-1 (adiado com registro
formal), MNT-001 (story de follow-up).

### File List

**Novos**
- `apps/api/migrations/versions/0058_bank_accounts.py`
- `apps/api/app/modules/bank/__init__.py`
- `apps/api/app/modules/bank/models.py`
- `apps/api/app/modules/bank/schemas.py`
- `apps/api/app/modules/bank/service.py`
- `apps/api/app/modules/bank/router.py`
- `apps/api/tests/test_bank_accounts.py`
- `apps/api/tests/test_money_planes.py`
- `apps/api/tests/test_bank_rls.py`

**Alterados**
- `apps/api/app/db/registry.py` (importa `BankAccount`)
- `apps/api/app/modules/__init__.py` (`bank_router` em `ALL_ROUTERS`)
- `apps/api/tests/test_platform.py` (IV6 — purga de `bank_accounts` no `delete_account`)
- `docs/stories/8.2.story.md` (esta story)

**Alterados na correção do gate (BANK-001, 2026-07-30)**
- `apps/api/app/modules/bank/service.py` (`_validate_opening_date_move` + chamada em
  `update_account`)
- `apps/api/tests/test_bank_accounts.py` (seção BANK-001: 5 testes + helper `_lancar`/`_conferencia`)

## QA Results

### Review Date: 2026-07-30
### Reviewed By: Quinn (@qa)

**Gate da onda (8.1–8.8): ❌ FAIL** → `docs/qa/epic-8-onda-0-1-gate-2026-07-30.md`
Veredito **da onda**, não desta story isoladamente: a Onda 0 + Onda 1 é um único PR e o gate julga o
conjunto. O bloqueio é **um** item, nomeado e barato (BANK-001, Story 8.2).

**Evidência executada (foreground, com o interpretador do venv — `scripts/check.sh` NÃO foi usado
como evidência, ver MNT-003):** backend `1045 passed, 1 skipped, 22 deselected`; `rls_e2e`
`22 passed`; frontend `288 tests / 50 files`; `ruff` limpo; `tsc --noEmit` exit 0;
`eslint --max-warnings 0` exit 0; `alembic heads` → `0060 (head)` (head único).

### Status desta story: **InReview → InProgress**

**Justificativa da mudança de status:** esta story é a dona do achado 🔴 HIGH que reprova a onda.
É a única das oito que muda de status; as demais seguem em `InReview` aguardando o gate da onda.

#### 🔴 BANK-001 (HIGH) — editar `opening_date` para frente produz divergência inventada

`update_account` (`apps/api/app/modules/bank/service.py:367-368`) aceita mover `opening_date` para
frente **sem guarda**. Como `_movements_sums` só soma `posted_at > opening_date`
(`service.py:186-192`), os movimentos anteriores à nova data param de contar no saldo derivado —
continuando **visíveis na lista**. A conferência então compara o checkpoint correto contra um saldo
inflado e **relata um furo que não existe**. Alcançável pela UI
(`ContasSaldosPage.tsx:699,715` envia o campo na edição).

Reprodução com saída real (probe descartado após a execução):

```
=== ANTES — abertura 01/06
    saldo_derivado=20000  movimentos_visiveis=1
    conferencia: banco=20000 sistema=20000 divergencia=0 dentro=True
=== PATCH opening_date -> 15/06 : 200
=== DEPOIS — abertura 15/06
    saldo_derivado=100000  movimentos_visiveis=1
    conferencia: banco=20000 sistema=100000 divergencia=-80000 dentro=False
```

A story já tem o argumento certo escrito — mas só no caminho de criação (`_validate_posted_at`,
`service.py:496-499`): *"Aceitar a data e não somar o movimento seria pior do que recusar: a linha
existiria, o saldo não mudaria, e ninguém entenderia por quê."* Falta espelhá-lo no `update`.

**Correção pedida:** recusar (422) mover `opening_date` para frente quando existir movimento com
`posted_at <= nova_data`, nomeando a data do movimento mais antigo. **+2 testes** (a recusa; e que
mover a data **para trás** continua permitido).

#### O que esta story acertou, e o gate confirmou por mutação

- `test_wallet_nao_importa_bank` — mutante `import app.modules.bank` dentro de `wallet/` **morto**.
- `derived_balance` é a **única** implementação da fórmula §3.1 no repositório (varredura confirmada).
- O rename `derived_balances` → `derived_balances_as_of` (D-4) tem a proibição escrita na docstring
  **e** um gate estático na conferência que reprova o uso (mutante M2 morto).
- Migration 0058: `ENABLE` + `FORCE ROW LEVEL SECURITY` + policy `tenant_isolation` com `USING` e
  `WITH CHECK`; índice único **parcial** com `tenant_id` na primeira coluna (índice único é global e
  não respeita RLS — o raciocínio está certo e documentado).
- `create_account` faz `db.flush()` **antes** do `audit.record` — é o único módulo do repositório que
  acerta isso (ver MNT-001: 17 call sites gravam `target=''`).

#### ⚠️ TEST-001 (MEDIUM) — corrigido por este gate, mas nasceu aqui

`_imported_modules` em `tests/test_money_planes.py:59-75` não apensava o alias do `ImportFrom`.
`from app.modules import bank` produzia só `"app.modules"` e **passava nos dois testes** (o AST não
via o `bank`; o teste por texto cru procura a string literal `"app.modules.bank"`, que essa forma não
contém). Com o import evasivo dentro de `wallet/service.py`, a suíte ficava verde: `6 passed`.

Isto atinge o teste que a própria docstring chama de *"o teste de maior valor da Story 8.2"* e
*"a única defesa permanente contra a reintrodução do bug original"* — a mitigação nomeada do risco
nº 1 do epic §7. **Corrigido pelo @qa** (só teste), e a correção foi verificada por mutação: os três
mutantes evasivos agora morrem.

---

## Story Draft Checklist — Resultado (River, modo autônomo)

| Categoria | Status | Observações |
|---|---|---|
| 1. Goal & Context Clarity | PASS | A story é a fundação do plano 3 e diz o que **não** entrega (movimentos, checkpoint, conferência, tela). A decisão do fundador (N contas PJ) está no topo das Dev Notes porque é ela que define o formato. |
| 2. Technical Implementation Guidance | PASS | Padrões a copiar citados por arquivo (`0049_investments.py::_enable_rls`, `0048_cost_centers.py`, `chart_of_accounts` para archive/409, `db/base.py` para mixins). O `sqlite_where` do índice parcial é o achado técnico que evita um teste verde por engano. |
| 3. Reference Effectiveness | PASS | design §1.2/§1.3/§2.1/§3.1/§6.4/§6.6/§7.4, epic §3.2/§4/§5/§6/§9, REQ-1/REQ-2, `dre.py:51,135-156`, `cockpit/service.py:130`, `tests/conftest.py`, `tests/test_tenancy_guard.py` — verificados nesta sessão. |
| 4. Self-Containment Assessment | PASS | Contrato público tabulado com as assinaturas exatas que 8.3–8.8 vão chamar, com três notas de alinhamento para a outra metade. A contradição "uma migration por onda" × três tabelas na Onda 1 está registrada com encaminhamento, não escondida. |
| 5. Testing Guidance | PASS | Os 3 testes da Regra dos Planos são AC (não recomendação); o `rls_e2e` é obrigatório e nomeado como ponto de extensão das 8.3/8.4 (economia de CI); IV1/IV5/IV6 têm teste nomeado. |
| 6. CodeRabbit Integration (conditional) | N/A | Disabled — skip notice presente. |

**Final Assessment: READY** (clareza 9/10). Maior risco residual: a Task 4 é densa (CRUD + primária + três funções de saldo). Se o @po avaliar que passa de um ciclo de `@dev`, o corte natural é extrair `derived_balances_as_of`/`active_balance_total`/`primary_account` para uma sub-story — mas **não** extrair `derived_balance` nem o `test_money_planes.py`, que são o núcleo do valor desta story.

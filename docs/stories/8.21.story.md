# Story 8.21: "Tenho a conta e não sei o saldo" — o modelo passa a registrar o ATO, não só o valor

## Status
Draft — ⚠️ **NÃO INICIAR SEM LER "Bloqueios de execução".** A redação está fechada e **todos os ACs
estão decididos** (o §Escalado 1 foi fechado pela @architect em 2026-08-07). Resta **um bloqueio
duro: a numeração da migration**, que não depende de ninguém desta story. Não impede a validação
do @po.

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["ruff (apps/api)", "pytest apps/api/tests -m 'not rls_e2e' (test_bank_accounts.py + test_financial_intelligence_projection.py + test_projection_saldo_misto.py + test_bank_reconciliation_report.py como não-regressão)", "pytest -m rls_e2e (migration contra Postgres REAL, como papel não-superusuário e1p_app)", "vitest apps/web (AccountModal + ContasSaldosPage)", "tsc --noEmit + eslint --max-warnings 0 (apps/web)", "as 4 etapas de scripts/check.sh rodadas INDIVIDUALMENTE e em FOREGROUND com o interpretador de apps/api/.venv", "os 3 agentes de QA (regression-tester, bug-hunter, dedup-checker)"]
```
> [AUTO-DECISION] `executor="@dev"`, `quality_gate="@architect"` — convenção de todo o Epic 8. Aqui o
> gate da @architect **não é formalidade**: esta story tem **migration** e muda o que a Projeção de
> Caixa afirma.
>
> ⚠️ **Divergência declarada, apontada pelo @po na v0.3.** A tabela de consistência da
> `validate-next-story` manda `Schema/DB/RLS/Migrations → executor @data-engineer, gate @dev`. Esta
> story é **multi-tipo** — a migration é **uma coluna aditiva** (Task 2), e o volume real está no
> consumidor (`_saldo_inicial`), nas duas rotas (AC5) e no formulário (AC6), que são território do
> @dev. Mantida em `@dev`/`@architect` **com a Task 2 co-assinada pela @data-engineer**, que já
> produziu o plano da migration (DDL, ordem das duas operações, análise de RLS) e cujo `rls_e2e`
> contra Postgres real já é `quality_gate_tool` obrigatório. **Se a Task 2 sair do recorte de uma
> coluna aditiva, a story volta ao @po para reassinar o executor.**

---

## Bloqueios de execução (verificados, não presumidos)

**1. A numeração da migration é insolúvel hoje.** Levantado pela @data-engineer (Dara):

| Migration | Onde está | `down_revision` |
|---|---|---|
| `0071_ai_usage.py` | **`main`** (PR #87) — head real de `main` | `0070` |
| `0072_tenant_timezone.py` | branch `fix/fuso-do-tenant-na-sessao` (`15727ac`) — **não mergeada** | `0071` |
| `0072_user_briefing_prefs.py` | branch `feat/vima-briefing-superficies` (`8dc3b49`) — **não mergeada** | `0071` |

Duas `0072` saíram da mesma `main` e apontam para `0071` — **head bifurcado** no instante em que as
duas mergearem. A segunda a entrar vira `0073`. Esta story é **`0074` no mínimo**, e só depois de as
duas pousarem. ⚠️ **Reconferir o head no momento de escrever, não agora** — a `0072_tenant_timezone`
já registra esta mesma colisão no cabeçalho dela, o que prova que reconferir na escrita não bastou:
reconferir **a cada merge de `main`**.

**2. ~~A @architect deixou um caso sem decidir~~ — ✅ FECHADO em 2026-08-07.** O caso da conta
parcialmente conhecida foi decidido: **qualquer elegível desconhecida suprime a Projeção inteira**,
com as duas obrigações que vêm junto (o número continua visível; a supressão nomeia a saída via
`notes`). O AC4 está redigido nessa forma e **não tem mais fork**. Razões completas no §Escalado 1 —
inclusive o fato que decidiu: `opening_balance_cents` **pode ser negativo**, então somar só as
conhecidas erra nas duas direções.

---

## O defeito, em fatos verificados no código

| # | Fato | Fonte verificada |
|---|---|---|
| 1 | O modelo guarda **o valor**, nunca **o ato** | `bank/models.py:102` — `opening_balance_cents: Mapped[int] = mapped_column(BigInteger, default=0, nullable=False)` |
| 2 | O formulário **oferece** o zero | `AccountModal.tsx:88` — `setOpening(centsToInput(editing?.opening_balance_cents ?? 0))`; no cadastro `editing` é `undefined`, então o campo nasce `"0,00"` |
| 3 | Logo, *"informei zero"* e *"não informei nada"* são **a mesma linha** | consequência dos dois acima; é o que a 8.19 registrou em §"O que sobrou sem solução" |
| 4 | `ORIGEM_INDISPONIVEL` existe desde a Onda 0 e **não tem gatilho** | `core/money_planes.py:72` — declarada; `projection.py` nunca a produz. `CLAUDE.md` registra como dívida |
| 5 | A decisão da Projeção é pela **existência** da conta | `projection.py:318` — `if not elegiveis:`; a docstring (`:287`) diz *"A decisão é pela EXISTÊNCIA da conta, jamais pelo valor"*, e isso está **certo** — não é o que esta story muda |

**A consequência, e ela é um falso NEGATIVO.** Uma conta cadastrada por quem não sabia o saldo entra
com `opening_balance_cents = 0`, vira `elegiveis`, e a Projeção passa a afirmar runway e alerta sobre
um saldo que **ninguém informou**. O produto não distingue *"o saldo é zero"* de *"não sei o saldo"*,
e afirma nos dois casos.

⚠️ **Isto NÃO é um defeito observado no tenant do fundador.** A conta C6 dele foi declarada com zero
**consciente** (fala dele, registrada na 8.19), e a Projeção dele está **certa**. É defeito
**possível**, e a higiene do formulário é o que o mantém improvável — e é por isso que a higiene está
nesta story, e não numa story de limpeza qualquer.
[Source: docs/stories/8.19.story.md §"O que sobrou sem solução — e é de modelo, não de regra"]

## Por que agora, e não "numa onda futura"

A 8.19 arquivou este item como sem pressa. A @architect reverteu essa leitura aplicando a regra que
ela mesma escreveu — [`controle-bancario-onda2-design.md:94`](../architecture/controle-bancario-onda2-design.md):

> *"Quando um design cria uma coluna nullable e a justifica com 'melhora X', pergunte se o que ela
> guarda é um **FATO** ou uma **PREFERÊNCIA**. Preferência é otimização e pode esperar. Fato tem hora
> marcada: o instante em que ele acontece. Perdido esse instante, ele não volta."*

*"O dono afirmou o saldo, ou aceitou o `0,00` que o formulário ofereceu?"* é **fato com hora
marcada**: acontece em `create_account` e nunca mais. Toda conta cadastrada sob o formulário atual
nasce com esse instante **perdido**, e nenhuma migration futura o reconstrói.

---

## Story

**As a** dono que acabou de cadastrar minha conta bancária mas **não tenho o saldo dela em mãos**,
**I want** poder dizer isso ao e1p — em vez de ser empurrado a um `0,00` que ele vai tratar como se
eu tivesse conferido no app do banco —,
**so that** a Projeção de Caixa **cale** o runway e o alerta enquanto não souber de onde partir, em
vez de afirmar sobre um número que eu nunca informei.

---

## Acceptance Criteria

1. **O modelo registra o ATO. `opening_balance_cents` NÃO é tocada.**
   `bank_accounts` ganha **`opening_balance_is_known BOOLEAN NOT NULL`**.
   - `true` → o número em `opening_balance_cents` é **afirmação do dono**. Comportamento de hoje.
   - `false` → o dono disse que **não sabe**; `opening_balance_cents` fica `0` e é **placeholder**.

   ⚠️ **`opening_balance_cents` anulável está REJEITADA pela @architect**, e o motivo é verificável:
   `_validate_opening_date_recuo` (`bank/service.py:404-448`) opera sobre **ausência**, e a docstring
   dela diz por quê — *"presença é a única coisa que a API consegue distinguir de 'não mudou'
   (`None` = campo ausente, em `BankAccountUpdate`)"*. Com a coluna anulável, o `None` de
   `schemas.py:109` passaria a significar **duas** coisas e a guarda da Story 8.11 morreria em
   silêncio. Ela também é a âncora da fórmula do design §3.1 (`service.py:766`), e anulá-la
   propagaria `None` para `derived_balance` → `active_balance_total` → Conferência → ContasSaldos.
   → veredito da @architect nesta sessão; `bank/service.py::_validate_opening_date_recuo`

2. **A migration é aditiva, sem backfill e sem janela de RLS — e isso é a razão de ela ser segura.**
   ```python
   op.add_column("bank_accounts", sa.Column(
       "opening_balance_is_known", sa.Boolean(), nullable=False, server_default=sa.true()))
   op.alter_column("bank_accounts", "opening_balance_is_known", server_default=None)
   ```
   - **`server_default=true` é o backfill**, e o único que não regride: toda conta existente continua
     afirmando, que é o comportamento correto hoje.
   - **O default cai logo em seguida, de propósito.** Mantido, todo `INSERT` que omitisse a coluna
     gravaria *"eu sei o saldo"* em silêncio — o defeito desta story reintroduzido pelo default.
     Derrubado, o esquecimento vira violação de `NOT NULL` na hora. Mesma disciplina de `ai.complete`
     exigir `db`/`tenant_id`/`task` e de `is_overdue` exigir `today`.
   - **Nenhuma janela de RLS.** `bank_accounts` tem `ENABLE`+`FORCE ROW LEVEL SECURITY` (`0058:51-52`),
     e as **seis** armadilhas registradas neste repo (`0046`, `0066`, `0067`, `0068`, `0069`, `0072`)
     são todas a mesma: `UPDATE` de backfill filtrado em silêncio pela RLS, completando com sucesso
     aparente. `ADD COLUMN ... NOT NULL DEFAULT` é **DDL, não DML** — a RLS não a alcança, e desde o
     PG 11 o default não-volátil vai ao catálogo (`pg_attribute.attmissingval`) sem reescrever linha.
     **O backfill que não existe é o backfill que não pode falhar em silêncio.**
   - **Custo verificado, não estimado:** `BankAccount(...)` é **construído** em **9 lugares** —
     **um** de produção (`bank/service.py::create_account`) e **8** em 6 arquivos de teste.
     (O `grep` devolve 10 ocorrências; a décima é a **declaração da classe** em `bank/models.py`,
     que não é construção. Correção do @po na v0.3.)
   → plano da @data-engineer nesta sessão

3. **A criação EXIGE a escolha; ela não tem default.**
   `BankAccountCreate` (`bank/schemas.py:66-73`) ganha o campo **sem valor padrão** — omitir é 422.
   Hoje `opening_balance_cents: int = 0` (`:72`) faz o zero nascer sozinho até pela API; o campo novo
   não repete isso. `create_account` (`service.py:785`) grava o que veio.
   ⚠️ Quando `opening_balance_is_known=false`, `opening_balance_cents` é **ignorado e gravado como
   `0`** — aceitar um valor junto de *"não sei"* seria guardar duas afirmações contraditórias na
   mesma linha.
   **Instanciação obrigatória:**
   - **MEMBRO** — `{"opening_balance_is_known": false}` sem `opening_balance_cents` → **201**, conta
     criada com `0` placeholder.
   - **NÃO-MEMBRO** — payload sem `opening_balance_is_known` → **422**, com mensagem que nomeia a
     escolha. (É a regressão que o AC2 tornou impossível no banco e que o schema torna impossível na
     borda.)

4. **A Projeção suprime quando QUALQUER elegível é desconhecida.** ✅ **Fechado pela @architect —
   ver §Escalado 1 para os dois argumentos que decidiram.**
   `projection._saldo_inicial` (`projection.py:281`, declarado *"um único lugar de decisão"*) passa a
   filtrar por conhecimento além de elegibilidade. Basta **uma** conta elegível com
   `opening_balance_is_known=false` para a origem virar **`ORIGEM_INDISPONIVEL`**
   (`money_planes.py:72`) — que existe desde a Onda 0 **sem gatilho**, e este é o gatilho.
   **Instanciação obrigatória:**
   - **MEMBRO** — Itaú `true` com R$ 10.000 **+** C6 `false` → `ORIGEM_INDISPONIVEL`, runway e
     alerta calados. (É o caso mais provável na prática: quem cadastra a segunda conta é quem já
     tem a primeira.)
   - **NÃO-MEMBRO** — as duas `true` → `ORIGEM_MISTO`, soma legítima, comportamento de hoje.
   ⚠️ **Duas obrigações que vêm junto, e sem as quais esta regra é PIOR que somar só as conhecidas:**
   **(a) O número continua visível.** `saldo_inicial_cents` e as duas parcelas continuam calculados
   e expostos; a parcela bancária segue sendo a soma das conhecidas. O que morre é `runway.days`
   (com `days_suprimido=True`) e `windows[].alert`. Princípio da Onda 0, literal: **suprimir a
   afirmação, nunca o número.**
   **(b) A supressão NOMEIA a saída.** `CashProjection.notes` (`projection.py:255`, já renderizado
   em `ProjecaoCaixaPage.tsx:99-104`) recebe uma nota dizendo **quantas e quais** contas estão sem
   saldo informado. **Zero campo novo.** Sem isso o dono vê o runway sumir e não descobre o que
   fazer — o mesmo beco sem saída do item 12(b) do WhatsApp, em que fora da janela de 24h a única
   saída oferecida era um template que não existia, e a conversa emudecia.
   A supressão de `runway.days` / `windows[].alert` / ícone **acontece por construção**: ela sempre
   foi condicionada à origem, e nenhum componente tem condicional própria (é o mesmo mecanismo que a
   8.8 usou para *restaurar* o runway sem tocar na lógica de supressão).
   ⚠️ **`_saldo_inicial` continua decidindo pela EXISTÊNCIA da conta, jamais pelo valor** — a
   docstring de `projection.py:287` está **certa** e não muda. O que entra é um terceiro fato
   (*"o dono afirmou?"*), não uma volta ao `total != 0` que aquela docstring rejeita.
   → `projection.py::_saldo_inicial`; `core/money_planes.py:72`

5. **Nenhuma superfície afirma um saldo que o dono não declarou. São DUAS rotas, e o lugar de
   mudar é o ROUTER — não o schema.** ⚠️ **Reescrito na v0.4 pelo veredito NO-GO do @po.**
   Para conta com `opening_balance_is_known=false`, a procedência do saldo derivado passa a ser
   `ORIGEM_INDISPONIVEL`. O mecanismo **já existe**: a Regra dos Planos §1.3c exige que todo saldo
   viaje com a procedência, e `indisponivel` é o valor para *"não consigo estabelecer"*.

   ⚠️ **O default do schema NÃO é o ponto de mudança, e confiar nele entrega um AC morto.**
   `saldo_derivado_origem: str = ORIGEM_BANCO` aparece em `schemas.py:154` e `:200` como **default**,
   mas os dois consumidores **atribuem o valor explicitamente** e a atribuição sobrescreve o default.
   Um @dev que mude só o schema entrega isto **verde na leitura e morto em produção**.

   | Rota | Constrói | Atribuição a mudar | Consumidor na tela |
   |---|---|---|---|
   | `GET /bank/accounts` | `BankAccountOut` | **`router.py:85`** — `saldo_derivado_origem=ORIGEM_BANCO` | `ContasSaldosPage.tsx:377` (saldo por conta) e `:1184`/`:1199` (seletores) |
   | `GET /bank/accounts/{id}/balance` | `BankBalanceOut` | **`router.py:327`** — mesma constante | mesma família; o payload afirma sozinho |

   ⚠️ **É obrigatório, não escopo a mais**, e as **duas** rotas entram. É o critério que a 8.20 AC5
   fixou: *"esta story existe para remover uma afirmação falsa; ela não pode trocar uma afirmação
   falsa por outra, uma camada acima."* Deixar a segunda rota de fora seria **cometer o defeito que
   este AC cita** — foi exatamente o que o @po pegou na v0.3.
   ⚠️ **`saldo_derivado_cents` continua sendo um número** (não vira `None`): anulá-lo é a opção (a) do
   AC1 pela porta dos fundos, com o mesmo custo de propagação. Quem diz *"não sei"* é a **origem**.
   ⚠️ **A constante nunca é escrita à mão** — `ORIGEM_INDISPONIVEL` vem de `core.money_planes`, como
   o comentário de `router.py:83-84` já exige para `ORIGEM_BANCO`.
   **Instanciação obrigatória:**
   - **MEMBRO** — conta `is_known=false` em `GET /bank/accounts` → `saldo_derivado_origem ==
     "indisponivel"`, e o mesmo em `GET /bank/accounts/{id}/balance`.
   - **NÃO-MEMBRO** — conta `is_known=true` → `"banco"` nas duas rotas, **byte a byte como hoje**.
   → `bank/router.py:85` e `:327`; `bank/schemas.py:154` e `:200` (só a docstring do campo);
     `ContasSaldosPage.tsx:377`

6. **O formulário nasce vazio e força a escolha — e esta metade é obrigatória.**
   `AccountModal.tsx`: no **cadastro**, o campo de saldo deixa de nascer `"0,00"` (`:88`) e passa a
   nascer vazio, com a escolha explícita *"sei o saldo" × "não sei o saldo"*.
   ⚠️ **Sem esta metade a coluna é inútil**, e o precedente é literal: a Story 8.11 provou que
   *"a metade de backend é necessária e INSUFICIENTE"* (`AccountModal.tsx:28-35`) — a UI mandava o
   valor pré-preenchido, o 422 nunca disparava, e a guarda virava código morto do ponto de vista de
   quem usa o produto. Aqui é idêntico: com o campo pré-preenchido, `is_known=true` sai sempre.
   ✅ **O padrão já está no arquivo:** ao recuar a data, `:106` limpa o campo e `:162-164` **omite** a
   chave do payload. Reusar, não reinventar.
   → `AccountModal.tsx:28-35`, `:88`, `:106`, `:162-164`

7. **Descobrir o saldo depois é um caminho de primeira classe.**
   `update_account` aceita virar `false → true` **junto com** `opening_balance_cents` no mesmo PATCH.
   ⚠️ Virar `false → true` **sem** informar o saldo é **422**, pela mesma razão de
   `_validate_opening_date_recuo`: afirmar *"agora eu sei"* mantendo o placeholder `0` produziria a
   **divergência inventada** que aquela guarda existe para impedir — e divergência inventada é pior
   que divergência escondida (`service.py:417-419`).
   Caminho `true → false` é permitido e **não apaga** `opening_balance_cents` (o número volta a valer
   se o dono voltar atrás; apagá-lo destruiria uma afirmação que foi verdadeira).

8. **Zero regressão para quem já existe.**
   Toda conta em produção entra com `true` pelo `server_default` do AC2 e **nada muda para ela**:
   Projeção, Conferência, Contas & Saldos, DRE, Lucratividade, Cockpit, Carteira e Fila de Pagamentos
   saem idênticos. É a razão de o default ser `true` e não `false` — `false` suprimiria a Projeção do
   fundador, que **hoje está correta**, exatamente a regressão que a 8.19 nomeia em §"Por que NÃO
   materializar", item 2.

9. **O gate: a coluna não pode existir sem o consumidor provado.**
   Teste dedicado que, com uma conta `is_known=false`, prova `saldo_inicial_origem ==
   ORIGEM_INDISPONIVEL` **e** `runway.days is None` **e** `days_suprimido is True` **e**
   `windows[].alert is False`.
   **Mais dois casos, ambos consequência do §Escalado 1 fechado:**
   - **o caso misto** (uma conta `true` + uma `false`) produz o **mesmo** resultado acima — é o que
     prova que a regra é *"qualquer desconhecida"* e não *"todas desconhecidas"*. Sem este teste, a
     implementação `if not conhecidas:` passa no CI e mente em produção;
   - **o número sobrevive à supressão**: `saldo_inicial_cents` e as duas parcelas continuam
     preenchidos e a invariante `total == banco + plataforma` continua valendo, **e** `notes` traz a
     nota nomeando as contas sem saldo. Asserir a **presença** da nota, não seu texto exato.
   ⚠️ **É o AC de maior valor da story**, e o motivo é histórico: `core/whatsapp/capabilities.py`
   existiu desde a Onda 0 com **zero call sites em produção**, e a docstring dele **afirmava** três
   consumidores que nunca foram escritos — foi essa afirmação que impediu alguém de notar a lacuna.
   `CLAUDE.md` derivou a contramedida: **capacidade nova nasce com o consumidor no mesmo passo**, e a
   lista de consumidores tem de ser verificável por `grep`.
   → `CLAUDE.md` §WhatsApp item 12

10. **Sem escrita em `bank_transactions`, sem checkpoint, sem tocar a fórmula do saldo derivado.**
    `_movements_sums`, `_movements_sum`, `derived_balance`, `derived_balances_as_of`,
    `active_balance_total`, `declare_balance`, `latest_checkpoint` e `reconciliation.py` saem **sem
    uma linha de lógica alterada**.
    ⚠️ **Materializar o saldo de abertura como checkpoint continua REJEITADO** (8.19 §"Por que NÃO
    materializar") — e a 8.20 já provou em produção que a comparação daí é degenerada. Se parecer
    necessário criar linha em `bank_balance_checkpoints`, **pare e reporte**.

### Integration Verification

- **IV0 — A LISTA FECHADA de testes que mudam de expectativa é levantada na Task 1, ANTES de
  qualquer edição, e registrada nas Completion Notes.** ⚠️ **Acrescentado na v0.4** — a 8.19 (IV7) e
  a 8.20 (IV2) tinham este IV e esta story não tinha; o @po pegou a falta.
  **Previsão desta redação (confirmar por `grep`, não por leitura desta story):** a lista deve sair
  **VAZIA**. As três asserções de `saldo_derivado_origem == ORIGEM_BANCO` que existem hoje —
  `test_bank_accounts.py:104`, `:137` e **`test_money_planes.py:544`** — usam contas que nascem
  `is_known=true` pelo `server_default` do AC2, então continuam verdes **sem edição**.
  ⚠️ **`test_money_planes.py` é gate ESTRUTURAL da Regra dos Planos.** Se ele mudar de resultado, a
  causa não é o teste: é o AC5 tendo vazado para o caminho `is_known=true`. **Pare e reporte.**
  Se a lista sair **não vazia**, o AC5 está errado — e é o AC5 que se conserta, nunca a asserção.
- **IV1 — a suíte inteira passa com `is_known=true` implícito.** Nenhum teste existente muda de
  expectativa por causa do AC2/AC5/AC8. Os que constroem `BankAccount(...)` à mão (**8 construções
  em 6 arquivos**) podem precisar do kwarg — **é ajuste de construção, nunca de asserção**. Se
  alguma asserção mudar, é regressão: **pare e reporte**.
- **IV2 — Conferência intacta.** `test_bank_reconciliation_report.py` e `test_bank_checkpoints.py`
  passam **sem edição**. Esta story não toca bloco 1 nem bloco 4; as 8.19 e 8.20 continuam válidas.
- **IV3 — `test_projection_saldo_misto.py` passa sem edição** para todos os cenários existentes (que
  são todos `is_known=true`). Os casos novos são **acréscimo**, não alteração.
- **IV4 — a migration é validada contra Postgres REAL**, em `rls_e2e`, rodando `alembic upgrade head`
  como o papel **não-superusuário `e1p_app`**, com contas legadas semeadas antes. Asserir: as legadas
  saem `true`, a RLS `FORCE` continua ativa em `bank_accounts`, e o isolamento cross-tenant continua
  fail-closed (sem GUC → 0 linhas). ⚠️ **O SQLite da suíte rápida não pega nada disto.**
- **IV5 — `downgrade()` é exercitado**, não só escrito. `op.drop_column` volta ao estado anterior; o
  que se perde é só o fato novo, e isso fica **registrado na docstring da migration**, não descoberto
  no meio de um rollback.
- **IV6 — DRE, Lucratividade, Cockpit, Carteira, Fila de Pagamentos e Agenda intactas.** Nenhuma lê
  `opening_balance_is_known` nem `saldo_derivado_origem`.
- **IV7 — `engine.py` permanece PURO e INTOCADO.** Os dois gates da 8.6 (AST + varredura de texto)
  passam sem edição. Se a mudança exigir importar qualquer coisa no motor, **pare**: escopo ultrapassado.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled`
> configurado localmente. Tratado como `false`, mesma decisão das Stories 5.7, 6.1, 6.2, 7.5, 8.1,
> 8.8, 8.19 e 8.20.
> Quality validation will use manual review process only (ver `quality_gate_tools` acima).

## Tasks / Subtasks

- [ ] **Task 0 — Resolver a numeração ANTES de tudo (Bloqueio 1)**
  - [ ] `git log origin/main -- apps/api/migrations/versions` e conferir o head **naquele momento**.
  - [ ] Confirmar se as duas `0072` já mergearam. Se não, **pare e reporte** — a story espera.
  - [ ] Só então numerar, com `down_revision` apontando para o head real.

- [ ] **Task 1 — Baseline medido e LISTA FECHADA levantada, ambos antes de editar (IV0, IV1)**
  - [ ] Rodar as três suítes e registrar os números **antes de qualquer edição**. ⚠️ Os baselines
        citados na 8.19 (1445/38/476) e na 8.20 (1338/34/433) **divergem entre si** e ambos estão
        defasados — meça.
  - [ ] `grep` por `BankAccount(` em `apps/api/` → as construções que podem precisar do kwarg (IV1).
        Descontar a **declaração da classe** em `bank/models.py`, que não é construção.
  - [ ] **`grep` por `saldo_derivado_origem` em `apps/api/` e `apps/web/src/` → a lista fechada do
        IV0.** ⚠️ **Acrescentado na v0.4 (veredito do @po).** Previsão: lista **vazia** — as três
        asserções existentes (`test_bank_accounts.py:104`, `:137`, `test_money_planes.py:544`) são
        de contas `is_known=true` e seguem verdes. Confirmar por execução, não por leitura.
  - [ ] **`grep` por `ORIGEM_BANCO` em `apps/api/app/modules/bank/`** → confirmar que os pontos de
        **atribuição** são exatamente `router.py:85` e `router.py:327` (AC5) e que `schemas.py:154`
        e `:200` são apenas **defaults** sobrescritos por eles. Se houver um terceiro ponto de
        atribuição, o AC5 está incompleto — **pare e reporte**.

- [ ] **Task 2 — Migration (AC: 1, 2; IV4, IV5)**
  - [ ] `add_column` + `alter_column(server_default=None)`, na ordem. Docstring no padrão da casa
        explicando o backfill-por-default e o porquê de **não** haver janela de RLS.
  - [ ] `downgrade()` com o custo escrito.
  - [ ] Teste `rls_e2e` contra Postgres real, com contas legadas semeadas.

- [ ] **Task 3 — Modelo, schemas e service (AC: 1, 3, 7)**
  - [ ] `bank/models.py`: a coluna, com comentário dizendo que ela guarda o **ato**, e que
        `opening_balance_cents` continua sendo o **valor** — os dois eixos, não um campo achatado.
  - [ ] `BankAccountCreate` **sem default**; `BankAccountUpdate` com `bool | None`.
  - [ ] `create_account`: grava; força `opening_balance_cents=0` quando `is_known=false`.
  - [ ] `update_account`: a guarda do AC7, ao lado das três guardas de data que já existem.

- [ ] **Task 4 — O consumidor (AC: 4, 5, 9)**
  - [ ] `_saldo_inicial`: o filtro novo + `ORIGEM_INDISPONIVEL`. ⚠️ **Não alterar o `until=today` de
        `active_balance_total`** — é o que impede a dupla contagem do dia D da 8.14 AC6, e trocá-lo
        por `None`/`SEM_CORTE` a reintroduz **em silêncio, pela porta oposta**
        (`service.py:670-683`; ratificação §C-7.3).
  - [ ] `BankAccountOut.saldo_derivado_origem` condicional.
  - [ ] O teste do AC9, e a verificação de que ele **não é vazio** (roda contra o código sem o
        consumidor e falha).

- [ ] **Task 5 — O formulário (AC: 6)**
  - [ ] `AccountModal.tsx`: campo vazio no cadastro + escolha explícita, reusando o padrão de `:106`
        e `:162-164`.
  - [ ] Aceite visual em ~360px **é exigido** — a escolha é elemento novo, e o AC9 da 8.13 está
        aberto justamente por isto ter sido pulado antes.

- [ ] **Task 6 — Testes e gates (AC: todas; IV1–IV7)**
  - [ ] Os dois membros/não-membros do AC3 e do AC4.
  - [ ] Não-regressão explícita: `test_projection_saldo_misto.py`, `test_bank_reconciliation_report.py`,
        `test_bank_checkpoints.py` **sem uma linha editada**.
  - [ ] As 4 etapas de `scripts/check.sh` **individualmente e em foreground** com o interpretador de
        `apps/api/.venv` (o script resolve `ruff`/`python` do PATH e mascara falha de frontend com
        `|| true` — dívida conhecida do `CLAUDE.md`) + os 3 agentes de QA.

## Dev Notes

### Por que um booleano, contra o padrão `*_at` da casa

`archived_at`, `paid_at`, `protested_at`, `last_read_at`, `title_checked_at` — a casa prefere
timestamp nullable a booleano. Aqui **não**, e a @architect deu dois motivos:

1. O que se guarda é **estado de conhecimento**, não evento.
2. É o `DEFAULT true` que faz o backfill ser um no-op. Um `*_at` nullable não carrega *"verdadeiro por
   omissão"*, e o legado cairia em `NULL` — que significa *"não sei se ele sabia"* e suprimiria a
   Projeção do fundador, hoje correta.

O instante do ato, para quem precisar, já está em `audit_entries` (`bank.account.create`).

### O que esta story NÃO faz

- **Não toca a Conferência.** Uma conta `is_known=false` não conferiu nada, mas o bloco 1 já responde
  `indisponivel` sem checkpoint e o bloco 4 já cai na `opening_date` (8.19 AC1). Nada a fazer.
- **Não mexe no 🟡 de completude.** Ver §Escalado 2 — é decisão de produto, não de arquitetura.
- **Não materializa checkpoint.** Ver AC10.

### File Locations

- **NOVO:** `apps/api/migrations/versions/00XX_bank_opening_balance_is_known.py` (numeração na Task 0)
- **ALTERAR:** `bank/models.py` (a coluna), `bank/schemas.py` (Create/Update + docstrings dos
  campos de procedência em `:154` e `:200`), `bank/service.py` (`create_account`, `update_account`),
  **`bank/router.py` — e SÓ as duas atribuições de `saldo_derivado_origem` (`:85` e `:327`)**,
  `financial_intelligence/projection.py` (`_saldo_inicial`),
  `apps/web/src/features/financeiro/AccountModal.tsx`
  > ⚠️ **`bank/router.py` saiu do NÃO TOCAR na v0.4, e o motivo importa.** A v0.3 o listava como
  > intocável *"fora do que o schema já expõe"* enquanto o AC5 exigia mudar a procedência — e a
  > procedência **só** é atribuída ali. A story se contradizia, e um @dev que a obedecesse ficaria
  > travado ou entregaria o AC morto. **O recorte é cirúrgico:** duas linhas, nenhuma rota nova,
  > nenhum campo novo, nenhuma outra linha do arquivo. Se o diff de `router.py` passar disso, o
  > escopo foi ultrapassado — **pare e reporte**.
- **NÃO TOCAR:** `bank/reconciliation.py`, `financial_intelligence/engine.py`,
  `financial_intelligence/schemas.py`, `core/money_planes.py`, `core/tenancy.py`,
  `ProjecaoCaixaPage.tsx` (exceto nada — `notes` já é renderizado, ver AC4b), `conferencia.ts`,
  `ConferenciaPage.tsx`, `bank/router.py` **fora das duas linhas do AC5**,
  `service.derived_balance` / `derived_balances_as_of` / `active_balance_total` (a **fórmula**)

### Technical Constraints

- **Nenhum filtro manual de `tenant_id`** — isolamento por RLS e só por RLS (Regra de Ouro nº 1).
- **`engine.py` permanece PURO** — os dois gates da 8.6 não podem ser tocados nem quebrados.
- **A Regra dos Planos §1.3 continua valendo:** `bank` não lê `transactions`, e todo campo de saldo
  viaja com o irmão `*_origem`. Os gates estruturais (`test_money_planes.py`) passam sem edição.
- **Datas são `date` de calendário, nunca `datetime`, nunca fuso** (`CLAUDE.md` §6.0).
- **`today` continua injetável** onde já é.

### Testing

- **Maior valor: o AC9** (o gate do consumidor). É o único modo de falha que faz a story inteira
  virar documentação em vez de comportamento.
- **Segundo: o IV4** (migration contra Postgres real). Seis migrations deste repo já falharam em
  silêncio por RLS; o SQLite da suíte rápida não pega nenhuma delas.
- **Terceiro: o IV1** (zero regressão). É o que prova que o `DEFAULT true` foi a escolha certa.
- Rodar o teste do AC9 **contra o código sem o consumidor** e registrar a falha nas Completion Notes.
  Um teste que já passava antes da correção não prova nada.

### Escalado (não implementar sem veredito)

1. ✅ **FECHADO pela @architect (2026-08-07) — a conta PARCIALMENTE conhecida suprime a Projeção
   inteira.** O AC4 está redigido nesta forma e não tem mais fork.

   O @sm recomendou o resultado certo pelo argumento mais fraco (*"a parcela que falta pode valer
   qualquer coisa"*). Os dois que de fato decidem:

   **(a) Somar só as conhecidas erra nas DUAS direções, e nada na tela diz em qual.**
   `opening_balance_cents` **pode ser negativo** — documentado em quatro lugares (`models.py:100`,
   `schemas.py:71`, `service.py:790`, `models.py:620`), porque conta no limite / cheque especial é
   saldo de partida legítimo. Logo:

   | Conta desconhecida | Somar só as conhecidas | Consequência |
   |---|---|---|
   | positiva | **subestima** o caixa | alerta dispara sem motivo — *"uma tela que grita por R$ 3 destrói a confiança no sinal"* (Regra 7) |
   | negativa (cheque especial) | **superestima** o caixa | alerta **calado** quando devia disparar — a máquina de falso negativo que a Onda 0 desmontou |

   Um instrumento que erra nos dois sentidos sem indicar qual não é instrumento. E o caso perigoso
   é o de quem tem cheque especial — quem mais precisa do alerta.

   **(b) A invariante da composição ficaria satisfeita na FORMA e violada no MATERIAL.**
   `CashProjection` contrata `saldo_inicial_cents == banco + plataforma` em todos os caminhos
   (`projection.py:242-254`), e a §1.3c manda *"somar sim; esconder a composição, nunca"*. Sob
   "somar só as conhecidas", `saldo_inicial_banco_cents` seria *"a soma das que eu conheço"* — um
   número com nome de parcela bancária que não é a parcela bancária, com **aparência de decomposição
   completa**. Mesma família do erro do gate que este epic já pagou uma vez
   (`CLAUDE.md` §"CORREÇÃO (2026-07-30)"): um número que mede a própria incompletude, com aparência
   de fato.

   **O custo desta escolha, pago por escrito:** cinco contas, quatro conhecidas, uma não → runway e
   alerta somem inteiros. É agressivo, e por isso a decisão vem com as duas obrigações do AC4 —
   o número continua visível **(a)** e a supressão nomeia a saída via `notes` **(b)**. **Sem as
   duas, esta escolha é pior que a alternativa que ela rejeita.**

2. **@pm / fundador — a conta com saldo desconhecido ainda conta para o 🟡 de completude?**
   Recomendação da @architect: **sim** (a conta existe; o que falta é o saldo). É decisão de produto,
   não de arquitetura. Não bloqueia: o 🟡 sai igual nos dois casos hoje, porque conta sem checkpoint
   já é 🟡.

3. **@pm — esta story não está no epic.** O §6 fecha em *"Total da Onda 2: 10 stories (8.9–8.18)"*;
   a 8.19 e a 8.20 já ficaram de fora, e o gate da Onda 2 registrou isso em `docs/prd/index.md:88`.
   Já escalado como **E-4** pelo @po. O epic é artefato do @pm; nem o @po nem o @sm o editam.

### Pendências registradas (fora de escopo, herdadas)

- **SIG-001** (a virada de mês apaga conferência recente) e **REL-002** (conta arquivada sai do
  relatório sem nota) continuam abertos, intocados.
- **G-1** — o gate global `test_todo_saldo_declara_origem` continua inexistente. Esta story acrescenta
  **mais um** campo de saldo com procedência variável (AC5), o que aumenta o valor daquele gate sem
  torná-lo pré-requisito.
- **`conferencia.ts:331`** — *"Esta conta nunca teve saldo informado."* virou código morto na 8.19
  (o backend não devolve mais `null`). Registrado lá, não corrigido, e **não** é desta story.
- **`packages/shared-types/src/generated.ts`** segue defasado e sem menção a `bank`. Esta story
  **muda contrato de API** (campo novo em Create/Update/Out), então **agrava** — registrar no gate.
- **`days_since_last_declared_balance` continua sem consumidor**, e esta story **não** o cria.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-08-07 | 0.5 | **Re-validação do @po — NO-GO (6/10), por UM ponto novo. Status permanece Draft.** ✅ **Os seis consertos da v0.4 estão corretos e reverificados no código:** `router.py:85`/`:327` são de fato os pontos de atribuição, a contradição do NÃO TOCAR saiu com recorte de duas linhas, o IV0 existe com o aviso certo sobre `test_money_planes.py` como gate estrutural, os dois `grep` entraram na Task 1, o executor está declarado em vez de silenciado e a contagem virou 9. **Nada a objetar neles.** ❌ **O bloqueio novo: `opening_balance_is_known` NUNCA é exposto na leitura.** Rastreadas as 8 ocorrências do campo na story — migration, criação, consumidor da Projeção, AC5 — e em nenhuma ele entra no `BankAccountOut`; a v0.4 inclusive **estreitou** o §File Locations de `bank/schemas.py (Create/Update/Out)` para `(Create/Update + docstrings)`. Três consequências: **(1)** o **AC7** (*"descobrir o saldo depois é um caminho de primeira classe"*) **não tem UI** — o AC6 cobre só *"no **cadastro**"*, e em modo edição a escolha nunca é renderizada porque a API não devolve o estado atual. **(2)** É o **`capabilities.py` de novo** — capacidade de backend sem consumidor —, precedente que a própria story cita no AC9 para justificar o gate: *"capacidade nova nasce com o consumidor no mesmo passo"*. **(3) O caminho existente vira armadilha SILENCIOSA, e é isto que bloqueia:** `AccountModal.tsx:88` pré-preenche o saldo e `:162-164` envia `opening_balance_cents` **sempre** fora do recuo de data — então o dono que marcou *"não sei"*, abre a edição, digita o saldo real e salva **grava o valor com `is_known` ainda `false`**, e a Projeção **permanece suprimida sem explicação**. Ele faz exatamente o que a nota do AC4(b) mandou e nada muda. **Isso derruba a razão de ser do AC4(b) da @architect**, que exigiu que a supressão *"nomeasse a saída"* precisamente para não repetir o beco sem saída do item 12(b) do WhatsApp: a nota nomeia a saída, e a saída não existe. **Conserto (um só, três partes):** expor `opening_balance_is_known` em `BankAccountOut`; estender o AC6 ao **modo edição** do `AccountModal` (o padrão de `:106`/`:162-164` já resolve — limpar o campo e omitir a chave); e o AC7 ganhar a asserção de que informar o saldo pela tela **vira o flag junto**, nunca só o valor. **Devolvida ao @sm.** Nada mais muda — AC1-AC5, AC8-AC10, a migration e o IV0 ficam como estão. | Pax (@po) |
| 2026-08-07 | 0.4 | **Correção do NO-GO do @po — os 6 pontos, todos fechados. Nenhum toca o AC4 nem a migration.** **(1) AC5 reescrito:** o ponto de mudança é o **router**, não o schema. `saldo_derivado_origem` é *atribuído* em `bank/router.py:85` (`BankAccountOut`) e `:327` (`BankBalanceOut`), e a atribuição sobrescreve o default de `schemas.py:154`/`:200` — mudar só o schema entregaria o AC verde na leitura e morto em produção. As **duas rotas** agora estão no AC, em tabela, com o consumidor de tela de cada uma, mais instanciação obrigatória (membro/não-membro). A segunda rota (`GET /bank/accounts/{id}/balance`) tinha ficado de fora, e deixá-la fora seria cometer o defeito que o próprio AC5 cita da 8.20. **(2) Contradição interna removida:** `bank/router.py` saiu do NÃO TOCAR e entrou em ALTERAR com **recorte cirúrgico de duas linhas** e `pare e reporte` se o diff passar disso — a v0.3 exigia mudar a procedência e proibia tocar o único arquivo que a atribui, travando qualquer @dev que obedecesse. **(3) IV0 novo — a lista fechada de testes que mudam de expectativa**, que a 8.19 (IV7) e a 8.20 (IV2) tinham e esta não tinha. Previsão registrada: **vazia**, porque as três asserções existentes (`test_bank_accounts.py:104`, `:137`, `test_money_planes.py:544`) são de contas `is_known=true` pelo `server_default`. ⚠️ `test_money_planes.py` é **gate estrutural** da Regra dos Planos: se ele mudar de resultado, a causa é o AC5 ter vazado para o caminho `true` — pare e reporte. **(4) Task 1** ganhou os dois `grep` que produzem o IV0 e que confirmam serem `router.py:85`/`:327` os únicos pontos de atribuição (terceiro ponto ⇒ AC5 incompleto ⇒ pare). **(5) Executor:** divergência da tabela `Schema/DB/RLS/Migrations → @data-engineer` declarada em vez de silenciada — story multi-tipo, migration é uma coluna aditiva, volume real no consumidor/rotas/formulário; mantida em `@dev`/`@architect` com a **Task 2 co-assinada pela @data-engineer**, e com a condição de que sair do recorte devolve a story ao @po. **(6)** *"10 lugares"* corrigido para **9 construções** (a décima ocorrência é a declaração da classe). | River (@sm) |
| 2026-08-07 | 0.3 | **Validação do @po — NO-GO (6/10). Status permanece Draft.** A story é forte em 9 dos 10 eixos e ~20 afirmações `arquivo:linha` foram conferidas contra o código e batem. **O AC5 não é implementável como escrito**, em três partes que se somam. **(1) Lugar errado:** o AC nomeia `bank/schemas.py:148-154`, mas ali existe só o **default** do campo — o valor é **atribuído explicitamente** em `bank/router.py:85` (`saldo_derivado_origem=ORIGEM_BANCO`) ao construir `BankAccountOut`, e a atribuição sobrescreve o default. Um @dev que mude só o schema entrega o AC **verde na leitura e morto em produção**. **(2) Contradição interna:** o único lugar onde a procedência é atribuída é `bank/router.py`, que esta mesma story lista em **NÃO TOCAR** (§File Locations) e cujo AC10 manda *"pare e reporte"* se o escopo for ultrapassado — um @dev que obedeça à story fica travado. **(3) Superfície não nomeada:** `BankBalanceOut` (`schemas.py:200` + `router.py:326-328`, o endpoint de saldo por conta) fixa o mesmo `ORIGEM_BANCO` e afirmaria igual sobre uma conta desconhecida. O AC5 invoca o critério da 8.20 AC5 (*"não pode trocar uma afirmação falsa por outra, uma camada acima"*) e então deixa uma camada de fora — o defeito que ele cita, cometido por ele. **Should-fix:** (4) a tabela de consistência da `validate-next-story` manda `Schema/DB/RLS/Migrations → executor @data-engineer, gate @dev`, e a story usa `@dev`/`@architect`; a substância está coberta (a @data-engineer já produziu o plano e o `rls_e2e` está nos `quality_gate_tools`), mas a atribuição formal diverge. (5) **Falta a lista fechada de testes que mudam de expectativa**, que a 8.19 (IV7) e a 8.20 (IV2) tinham e produziam na Task 1 antes de editar; aqui a Task 1 só faz `grep` de `BankAccount(`, e o IV1 afirma *"nenhum teste muda de expectativa"* com escopo só de AC2/AC8 — o AC5 toca `test_bank_accounts.py:104,137` e `test_money_planes.py:544` (**gate estrutural** da Regra dos Planos), que hoje passam por causa do default `true`, mas ninguém enumerou isso. **Nice-to-have:** (6) *"`BankAccount(...)` é construído em 10 lugares"* — 10 é a contagem de ocorrências e uma delas é a **declaração da classe** (`models.py`); construções reais são **9** (1 de produção + 8 em 6 arquivos de teste). **Devolvida ao @sm.** Nada além do AC5, do §File Locations, do IV e da Task 1 precisa mudar. | Pax (@po) |
| 2026-08-07 | 0.2 | **§Escalado 1 FECHADO — o AC4 não tem mais fork.** A conta parcialmente conhecida (uma `true` + uma `false`) **suprime a Projeção inteira**. O @sm recomendou o resultado certo pelo argumento mais fraco; os dois que decidiram são outros. **(a)** `opening_balance_cents` **pode ser negativo** — documentado em 4 lugares (`models.py:100`, `schemas.py:71`, `service.py:790`, `models.py:620`), porque cheque especial é saldo de partida legítimo. Logo "somar só as conhecidas" erra nas **duas** direções, sem nada na tela dizendo em qual: conta desconhecida positiva → subestima → alerta grita sem motivo (Regra 7); conta desconhecida **negativa** → superestima → alerta **calado** quando devia disparar, que é a máquina de falso negativo que a Onda 0 desmontou. E o caso perigoso é justamente o de quem tem cheque especial. **(b)** A invariante `saldo_inicial_cents == banco + plataforma` (`projection.py:242-254`) ficaria satisfeita na FORMA e violada no MATERIAL: `saldo_inicial_banco_cents` viraria "a soma das que eu conheço", um número com nome de parcela bancária e **aparência de decomposição completa** — mesma família do erro do gate que o epic já pagou. **Duas obrigações vieram junto, e sem elas a escolha seria pior que a rejeitada:** o número continua visível (`saldo_inicial_cents` e as duas parcelas seguem preenchidos; morre só `runway.days` e `windows[].alert`), e a supressão **nomeia a saída** via `CashProjection.notes` (`projection.py:255`), que já existe e **já é renderizado** (`ProjecaoCaixaPage.tsx:99-104`) — **zero campo novo**. Sem a segunda, o dono veria o runway sumir sem descobrir o que fazer: o beco sem saída do item 12(b) do WhatsApp. **AC4, AC9, Status, §Bloqueios e o checklist atualizados.** | Aria (@architect) |
| 2026-08-06 | 0.1 | Story criada a partir do §Escalado item 2 da Story 8.19 (*"Representar 'tenho a conta e não sei o saldo'"*), que estava aberto desde 30/07 e registrado no epic §"O que continua aberto e NÃO é do @pm". **Veredito da @architect nesta sessão:** `opening_balance_cents` anulável **rejeitada** (quebra `_validate_opening_date_recuo` da 8.11, cujo mecanismo é *"`None` = campo ausente"*, e propaga `None` pela âncora da fórmula do §3.1); **escolhida** a coluna irmã `opening_balance_is_known BOOLEAN NOT NULL DEFAULT true`, sem backfill. **Plano da @data-engineer:** o `server_default` cai logo após o `add_column` (mantido, o esquecimento gravaria *"eu sei"* em silêncio); **nenhuma janela de RLS**, porque `ADD COLUMN` é DDL e não DML — o que imuniza esta migration contra a armadilha que 6 outras deste repo pagaram. **Bloqueio de execução registrado:** duas `0072` em voo (`fix/fuso-do-tenant-na-sessao` e `feat/vima-briefing-superficies`), ambas commitadas, **nenhuma mergeada**, ambas com `down_revision="0071"` — head bifurcado, e esta story é `0074` no mínimo. **Achado desta redação (§Escalado 1):** o veredito decide o caso *"nenhuma conhecida"* e deixa aberto o caso **misto** (uma conta conhecida + uma não), que é o mais provável na prática; recomendação do @sm registrada com membro e não-membro escritos. | River (@sm) |

## Dev Agent Record
_(a preencher pelo @dev)_

## QA Results
_(a preencher pelo @qa)_

---

## Story Draft Checklist — Resultado (River, modo autônomo)

| Categoria | Status | Observações |
|---|---|---|
| 1. Goal & Context Clarity | PASS | Defeito verificado em `arquivo:linha` (5 fatos), o falso negativo nomeado, valor explícito (a Projeção cala em vez de afirmar sobre número não informado) e a urgência derivada de regra escrita, não de opinião (`onda2-design.md:94` — fato × preferência). |
| 2. Technical Implementation Guidance | PASS | DDL literal, ordem das duas operações justificada, ponto de inserção do consumidor nomeado (`_saldo_inicial`), lista **NÃO TOCAR** fechada, e a opção rejeitada (coluna anulável) com o motivo verificável em `service.py:404-448`. |
| 3. Reference Effectiveness | PASS | `[Source: …]` por seção com a informação crítica **resumida aqui**, não só linkada: 8.19 §Escalado 2, veredito da @architect, plano da @data-engineer, 8.11 (`AccountModal.tsx:28-35`), 8.20 AC5, `CLAUDE.md` §WhatsApp item 12. |
| 4. Self-Containment Assessment | PASS | Instanciação obrigatória aplicada ao AC3 **e** ao AC4 — membro e não-membro escritos nos dois. Bordas cobertas: legado (AC8), `false→true` sem saldo (AC7), `true→false` (AC7), conta parcialmente conhecida (AC4, decidida), saldo de abertura **negativo** (§Escalado 1 — foi o fato que decidiu o AC4). |
| 5. Testing Guidance | PASS | AC9 nomeado como o de maior valor com a razão histórica (`capabilities.py` sem consumidor), exigência de rodá-lo contra o código não corrigido, e IV4 exigindo Postgres real com o precedente das 6 migrations que falharam em silêncio. |
| 6. CodeRabbit Integration (conditional) | N/A | Disabled — skip notice presente. |

**Final Assessment (v0.4, pós-correção do NO-GO): pronta para RE-VALIDAÇÃO do @po.**
**Todos os ACs estão decididos e o AC5 agora é implementável.** Resta **um** bloqueio de execução,
escrito no topo e que não é de redação: a numeração da migration é insolúvel enquanto as duas
`0072` não pousarem em `main`.

⚠️ **O checklist acima é auto-avaliação do @sm e foi DESMENTIDO na v0.3.** Ele dava PASS em
"Technical Implementation Guidance" com a `lista NÃO TOCAR fechada` — e era justamente essa lista
que continha a contradição (`bank/router.py` proibido enquanto o AC5 o exigia). **A lição fica
escrita porque ela é o assunto desta story:** um checklist preenchido por quem escreveu o artefato
não tem consumidor que proteste. Foi o @po, lendo o **código** e não a story, que achou o defeito.
É a mesma classe da INSTANCIAÇÃO OBRIGATÓRIA e do `capabilities.py` sem consumidor.

**Riscos residuais, dois:**
1. **A supressão do AC4 é agressiva por construção** — quatro contas conhecidas e uma não bastam
   para calar runway e alerta. A @architect pagou esse custo por escrito e o compensou com as duas
   obrigações do AC4 (o número continua visível; a supressão nomeia a saída). Se o fundador achar a
   supressão pesada na prática, o ponto de reabertura é o §Escalado 1 — mas reabri-lo exige
   responder ao caso do **cheque especial**, que é o que fechou a decisão.
2. **O AC5 toca `bank/router.py`, que a story proibia até a v0.3.** O recorte é de duas linhas e há
   `pare e reporte` se o diff passar disso, mas é o ponto onde o escopo tem mais chance de crescer
   em silêncio. O IV0 e o `grep` de `ORIGEM_BANCO` na Task 1 existem para isso.

# Story 3.1: Ambiente de staging espelhando produção

## Status
InReview

## Executor Assignment
```yaml
executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: ["ruff (apps/api)", "pytest (apps/api/tests)", "manual infra review (compose/env isolation)", "validação manual do boot de staging (docker compose up + /health + smoke test)"]
```
> [AUTO-DECISION] Story classificada como Infra/CI/CD/Deploy (mapeamento da tabela "Executor Assignment" do template de story: "Infra/CI/CD/Deploy | @devops | @architect"). O escopo é majoritariamente arquivos de infra (`infra/docker-compose*.yml`, `.env.*`, `docs/HOSTINGER-DEPLOY.md`); o pequeno trecho Python (`app/seed_staging.py`) é parte do mesmo esforço de infra (dado sintético para o staging), não uma feature de negócio nova — por isso não foi classificada como Code/Features/Logic (@dev). Reason: consistência com a tabela oficial de mapeamento executor↔tipo de trabalho e com o objetivo do Epic 3 (esteira de deploy).

## Story
**As a** operador do go-live,
**I want** um ambiente de staging com a mesma imagem de produção e dados sintéticos,
**so that** cada deploy seja validado antes de ir para produção, sem risco ao dado real.

## Acceptance Criteria
1. Existe um staging que sobe a mesma imagem/compose de produção (`docker-compose.prod.yml` ou `.traefik.yml`), com `.env` próprio e dados sintéticos (seed), isolado da produção.
2. O fluxo de release documenta: deploy em staging → validação (smoke test dos módulos-chave) → promoção para produção.
3. Staging usa segredos próprios e nunca aponta para o banco/credenciais de produção.

### Integration Verification
- IV1: Subir o staging roda `alembic upgrade head` + seed e responde no `/health` (mesma topologia do dev/prod).
- IV2: O papel `e1p_app` non-superuser é criado no staging (RLS válida), igual à produção.
- IV3: Nenhuma configuração de staging afeta produção (redes/volumes/segredos isolados).

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false` por instrução explícita do orquestrador desta missão (mesmo tratamento das Stories 1.1-1.4).
> Quality validation will use manual review process only (ver `quality_gate_tools` acima e `scripts/check.sh`).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [x] **Task 1 — Documentar e viabilizar a reutilização EXATA do compose/imagem de produção para staging (AC: 1, 3)**
  - [x] **Achado técnico (inspeção de código, importante para quem implementar):** `infra/docker-compose.prod.yml` (linhas 27-28) e `infra/docker-compose.traefik.yml` (linhas 37-38) têm `env_file: - .env.prod` **hardcoded como string literal** — isso NÃO é substituído por `${...}` de variável de ambiente do shell (`docker compose --env-file` só afeta interpolação `${VAR}` *dentro* do YAML, como `${DOMAIN}`; a chave `env_file:` é lida literalmente). Ou seja: **não dá para simplesmente rodar `docker compose -f docker-compose.prod.yml --env-file .env.staging up`** e esperar que os containers recebam os segredos de staging — eles continuariam tentando ler um arquivo chamado `.env.prod`.
  - [x] **[AUTO-DECISION] Reutilizar os arquivos de compose SEM NENHUMA alteração** (zero diff em `docker-compose.prod.yml`/`.traefik.yml` além do Task 2): o staging deve rodar a partir de um **diretório separado** (ex.: clone próprio do repo em `/opt/e1p-staging` numa VPS dedicada — preferível — ou, se o custo obrigar, na mesma VPS de produção com nome de projeto Compose isolado, ver subtask abaixo) onde o arquivo local se chama, literalmente, `infra/.env.prod` — só que preenchido com **segredos e domínio de staging**, não os de produção. Assim `docker-compose.prod.yml`/`.traefik.yml` continuam sendo usados **byte-a-byte** iguais à produção (satisfaz "mesma imagem/compose de produção" da AC1 sem risco de editar o arquivo que já está em produção real). Reason: menor risco (não mexe no compose que já roda em `e1p.doroeventos.com.br`), e evita inventar uma sintaxe de `env_file` parametrizada que não existe hoje no projeto.
  - [x] Criar `infra/.env.staging.example` (novo arquivo, espelhando `infra/.env.prod.example`) com: `DOMAIN=staging.seudominio.com`, placeholders de segredos **PRÓPRIOS**, `FRONTEND_URL=https://staging.seudominio.com`, e a nova flag `SEED_SYNTHETIC_DATA=true`. Comentário no topo instruindo a copiar para `infra/.env.prod` **dentro do diretório do deploy de staging**.
  - [x] Em `infra/.env.prod.example` (produção real), adicionar a nova variável `SEED_SYNTHETIC_DATA=` (vazia = false/produção) com comentário apontando para `.env.staging.example`.
  - [x] **Isolamento de rede/volume (AC3, IV3):** documentado no §8 do runbook que, se compartilhar a MESMA VPS, cada `docker compose` usa um **nome de projeto distinto** (`-p e1p-staging`) — isola redes/volumes nomeados sem editar o YAML. Preferência declarada: VPS dedicada barata.

- [x] **Task 2 — Seed de dados sintéticos para staging, auto-protegido contra rodar em produção (AC: 1, 3)**
  - [x] `apps/api/app/config.py`: adicionado campo `seed_synthetic_data: bool = False` à classe `Settings` (default `False`).
  - [x] Criado `apps/api/app/seed_staging.py` (novo módulo, mesmo padrão de `apps/api/app/seed.py`): função `seed_synthetic_data()` que:
    - Se `settings.seed_synthetic_data` for `False`, loga `"[seed_staging] SEED_SYNTHETIC_DATA=false — nada a fazer."` e retorna (no-op silencioso — único gate; independente de `settings.is_production`).
    - Se `True`: cria (idempotente por sentinela) um tenant `slug="staging-demo"` + 1 owner com senha conhecida + registros mínimos (funil CRM padrão + 1 cliente na etapa Entrada, 1 evento de Agenda amanhã, 1 cobrança em aberto em Contas a Receber).
    - Usa o helper `ensure_stages` do CRM + ORM (não INSERT cru). **[AUTO-DECISION] RLS:** tenant/usuário via `SessionLocal` (tabelas globais); registros de negócio via `tenant_session(tenant_id)` (fixa a GUC `app.current_tenant_id`, senão o INSERT falharia fail-closed no Postgres real, onde a app conecta como `e1p_app` non-superuser). O guidance de teste da story falava só em monkeypatchar `SessionLocal`; foi estendido para monkeypatchar também `tenant_session`, porque usar `SessionLocal` puro nas tabelas RLS quebraria em staging real — que é justamente o ponto da story (espelhar produção com RLS). Reason: correção de RLS > seguir a letra do guidance de teste.
  - [x] Encadeada a chamada `&& python -m app.seed_staging` logo após `python -m app.seed` em AMBOS `infra/docker-compose.prod.yml` e `infra/docker-compose.traefik.yml` — único ponto onde os composes de produção são tocados; seguro em produção real porque `seed_synthetic_data()` é no-op lá (flag ausente/false).

- [x] **Task 3 — Documentar o fluxo de release (staging → validação → promoção) (AC: 2)**
  - [x] Adicionada a seção `## 8. Staging (validar antes de produção)` em `docs/HOSTINGER-DEPLOY.md`, entre a `## 7. Diferenças em relação ao dev` e a `## Dívida / próximos passos`. Cobre: isolamento (VPS dedicada preferida / `-p e1p-staging` como fallback), como subir (`cp .env.staging.example .env.prod`), checklist de smoke test dos módulos-chave (login sintético `owner@staging-demo.e1p.com`; Agenda; CRM/Kanban; Cockpit; Contas a Receber + "simular pgto"; `/api/health`), e fluxo de promoção (só após smoke test, aplicar o §5 no ambiente de produção). Reforça AC3 (staging nunca aponta para segredos de produção).

- [x] **Task 4 — Testes (AC: 1, 2, 3)**
  - [x] Criado `apps/api/tests/test_seed_staging.py` (padrão de `test_seed.py`: SQLite em memória via `StaticPool` + monkeypatch de `SessionLocal` e `tenant_session`): Caso 1 no-op com flag False; Caso 2 cria tenant/owner/cliente/evento/cobrança com flag True; Caso 3 idempotência (2x sem duplicar).
  - [x] Em `apps/api/tests/test_config.py`, adicionado `test_seed_synthetic_data_defaults_false` (default seguro em dev e em produção).
  - [x] **Fora do escopo automatizável nesta story:** o boot real do staging via `docker compose` multi-container (IV1 `/health`; IV2 papel `e1p_app`; IV3 isolamento de rede/volume) não é exercível por `pytest` — validação manual pelo operador via `docs/HOSTINGER-DEPLOY.md#8-staging-validar-antes-de-producao`.

- [x] **Task 5 — Checklist de qualidade (AC: todas)**
  - [~] `ruff check .` (apps/api) — **NÃO executado neste ambiente** (sem interpreter Python do projeto / ruff instalável — ver Dev Agent Record). Código revisado manualmente contra a config ruff (line-length=100; regras E,F,I,B,UP,ASYNC,S): imports ordenados por `order-by-type`, `# noqa: S105` na senha sintética, nenhuma linha >100.
  - [~] `python -m pytest` (apps/api) — **NÃO executado neste ambiente** (sem pip/venv/ensurepip no WSL, sem interpreter no host; deps pinadas de 2024 sem wheels cp314). Sintaxe validada via `py_compile`. Testes escritos seguindo o padrão validado de `test_seed.py`. Requer validação num ambiente com o venv do projeto (Python 3.13 + `requirements.txt`).
  - [x] Confirmado que **nenhuma migration Alembic nova** é necessária (a story só adiciona seed de dados via camada de aplicação, config e arquivos de infra/documentação; última migration na branch permanece `0031`).

## Dev Notes

### Previous Story Insights
- Stories 1.1-1.4 (Epic 1, Segurança & Compliance) estabeleceram o padrão de teste para módulos de seed: `monkeypatch.setattr(seed_module, "SessionLocal", TestSession)` com SQLite em memória (`StaticPool`) — reaproveitado aqui para `seed_staging.py`. [Source: apps/api/tests/test_seed.py]
- Story 1.4 já documentou e testou o guard `_guard_production_secrets` (`apps/api/app/config.py`) e o padrão `is_production` — usado como base para a decisão desta story sobre `ENVIRONMENT=production` em staging (ver seção "Decisão de ENVIRONMENT em staging" abaixo). [Source: docs/stories/1.4.story.md]
- Esta story roda em paralelo com outras stories do Epic 3 (3.2-3.5) e possivelmente de outros epics — não assumir que nenhuma delas já foi implementada. O arquivo `docs/HOSTINGER-DEPLOY.md` pode ter merges concorrentes (Story 3.3 provavelmente edita a seção de backup) — revisar conflitos ao integrar.
- Migrations em paralelo: a numeração mais alta na branch de base é `0031`. Esta story **não precisa de migration nova** (ver Task 5).

### Decisão de ENVIRONMENT em staging (achado técnico + AUTO-DECISION)
- [AUTO-DECISION] O `.env.staging.example` define `ENVIRONMENT=production` (não um valor novo como `staging`), para que o staging espelhe 100% o comportamento de produção real: `_guard_production_secrets` valida os mesmos segredos fortes, e `forgot_password` não vaza `dev_reset_token` — ambos controlados por `settings.is_production`. Reason: o objetivo do Epic 3 é matar o "drift" entre ambientes; um terceiro valor de `ENVIRONMENT` criaria exatamente o comportamento divergente que a esteira de staging deveria eliminar. A separação real fica por conta de: diretório/host diferente, `.env.prod` com CONTEÚDO diferente, e a flag independente `SEED_SYNTHETIC_DATA`.
- Consequência: o gate do seed sintético (`seed_synthetic_data()`) **não pode** depender de `is_production` (seria sempre `True` em staging) — por isso usa exclusivamente a flag explícita `SEED_SYNTHETIC_DATA`, ausente/false em produção real por padrão.

### File Locations (novos/alterados)
- NOVO: `infra/.env.staging.example`
- ALTERAR: `infra/.env.prod.example` (nova variável `SEED_SYNTHETIC_DATA=`, comentário)
- ALTERAR: `infra/docker-compose.prod.yml` (comando do `api`: `&& python -m app.seed_staging`)
- ALTERAR: `infra/docker-compose.traefik.yml` (idem)
- ALTERAR: `apps/api/app/config.py` (novo campo `seed_synthetic_data: bool = False`)
- NOVO: `apps/api/app/seed_staging.py`
- NOVO: `apps/api/tests/test_seed_staging.py`
- ALTERAR: `apps/api/tests/test_config.py` (teste de default `seed_synthetic_data=False`)
- ALTERAR: `docs/HOSTINGER-DEPLOY.md` (nova seção `## 8. Staging`)

### Technical Constraints
- Não editar `infra/docker-compose.prod.yml`/`.traefik.yml` além da única linha de comando necessária (Task 2).
- `seed_synthetic_data()` DEVE ser no-op seguro por padrão (flag `False`) — único mecanismo real de proteção contra rodar dados sintéticos num banco de produção real.
- Não introduzir OCR/IA/integrações externas reais no seed sintético — dados gerados localmente, sem chamadas de rede.

### Testing
- Framework backend: `pytest` (`apps/api/tests`), mesmo padrão de `test_seed.py` (SQLite em memória via `StaticPool` + `monkeypatch` do `SessionLocal` do módulo de seed; aqui, também de `tenant_session`, pelas tabelas RLS).
- Testes novos: `apps/api/tests/test_seed_staging.py` (3 casos) + `test_config.py` (default seguro).
- A validação do boot real do staging (IV1/IV2/IV3) é manual, via o runbook da Task 3.

## Story Draft Checklist Results (@sm, execução autônoma via `story-draft-checklist.md`)

**Story readiness: READY** · **Clarity score: 9/10**

| Category | Status | Issues |
|---|---|---|
| 1. Goal & Context Clarity | PASS | — |
| 2. Technical Implementation Guidance | PASS | — |
| 3. Reference Effectiveness | PASS | Todas as referências citam arquivo/linha e usam formato consistente `[Source: ...]`. |
| 4. Self-Containment Assessment | PASS | Decisão de `ENVIRONMENT=production` em staging e o achado do `env_file` literal estão documentados no próprio Dev Notes. |
| 5. Testing Guidance | PASS | Cenários automatizáveis (seed) e a limitação de boot real de containers (manual, IV1-IV3) estão ambos explícitos. |
| 6. CodeRabbit Integration (conditional) | N/A | `coderabbit_integration` tratado como disabled neste ambiente; skip notice presente. |

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-10 | 0.1 | Story criada a partir do Epic 3 (Deploy, Storage & Observabilidade) | River (@sm) |
| 2026-07-10 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-07-10 | 0.2 | Implementação (config flag + seed_staging + compose + runbook §8 + testes). Status: Ready → InReview | Dex (@dev) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.8 (claude-opus-4-8[1m]) — @dev (Dex), modo YOLO autônomo.

### Debug Log References
- `py_compile` de `app/seed_staging.py`, `app/config.py`, `tests/test_seed_staging.py`, `tests/test_config.py` → OK (sintaxe válida).
- Verificação de line-length (≤100, config ruff) em todos os arquivos Python novos/alterados → OK.

### Completion Notes List
- **Config:** `Settings.seed_synthetic_data: bool = False` (env `SEED_SYNTHETIC_DATA`). Default seguro — produção nunca semeia dados sintéticos.
- **Seed:** `app/seed_staging.py` — no-op quando a flag é false; quando true, cria tenant `staging-demo` + owner (`owner@staging-demo.e1p.com` / `staging-demo-owner`, `must_reset_password=False` para login direto no smoke test) + funil CRM padrão (reusa `crm.service.ensure_stages`) + 1 cliente na etapa Entrada + 1 reunião na Agenda (amanhã 14h) + 1 cobrança Pix em aberto (R$150). Tudo idempotente por sentinelas (`slug`/`email`/`external_ref`).
- **RLS correta:** tabelas globais (tenant/usuário) via `SessionLocal`; tabelas de negócio (RLS) via `tenant_session(tenant_id)` — sem a GUC do tenant, o INSERT falharia fail-closed em Postgres real (papel `e1p_app`). Testes monkeypatcham ambos.
- **Compose:** `&& python -m app.seed_staging` adicionado ao comando do `api` em `docker-compose.prod.yml` e `docker-compose.traefik.yml` (após `python -m app.seed`). Seguro em produção (no-op).
- **Runbook:** `docs/HOSTINGER-DEPLOY.md` §8 documenta isolamento (VPS dedicada preferida / `-p e1p-staging` fallback), subida (`cp .env.staging.example .env.prod`), smoke test dos módulos-chave e promoção.
- **Sem migration:** confirmado — nenhuma alteração de schema. Última migration na branch: `0031`.

### Limitações de validação (precisam de ambiente/infra real)
- **`ruff check` e `pytest` NÃO foram executados neste ambiente.** O worktree não tem venv; o host Windows não tem interpreter Python (só o alias da Microsoft Store); o WSL tem apenas Python 3.14 bare, sem `pip`/`venv`/`ensurepip`, e as deps pinadas do projeto (2024) não têm wheels para cp314. Foi feita validação estática: `py_compile` (sintaxe) + revisão manual contra a config ruff. **Ação requerida pelo QA/operador:** rodar `cd apps/api && ruff check . && pytest` no venv do projeto (Python 3.13 + `requirements.txt`) — esperado: suíte anterior (~254) + 4 novos testes (3 em `test_seed_staging.py` + 1 em `test_config.py`) verdes.
- **Boot real do staging (IV1/IV2/IV3) — validação manual:** subir `docker compose` de staging numa VPS/host com Docker (não disponível para este agente), conferir `/api/health` (IV1), papel `e1p_app` non-superuser criado (IV2) e isolamento de redes/volumes/segredos vs produção (IV3), seguindo `docs/HOSTINGER-DEPLOY.md` §8.
- **Sem credenciais externas necessárias:** o seed sintético gera dados localmente, sem chamadas de rede (Regra: nenhuma integração externa no seed).

### File List
- `apps/api/app/config.py` (alterado — campo `seed_synthetic_data`)
- `apps/api/app/seed_staging.py` (novo)
- `apps/api/tests/test_seed_staging.py` (novo)
- `apps/api/tests/test_config.py` (alterado — teste de default)
- `infra/.env.staging.example` (novo)
- `infra/.env.prod.example` (alterado — flag `SEED_SYNTHETIC_DATA=`)
- `infra/docker-compose.prod.yml` (alterado — comando do `api`)
- `infra/docker-compose.traefik.yml` (alterado — comando do `api`)
- `docs/HOSTINGER-DEPLOY.md` (alterado — nova seção §8)
- `docs/stories/3.1.story.md` (este arquivo — Status/Tasks/Dev Agent Record)

## QA Results
_(a preencher pelo @qa)_

# Story 3.2: CI testando a imagem Docker de produção

## Status
InReview

## Executor Assignment
```yaml
executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: ["ruff (apps/api)", "pytest (apps/api/tests)", "docker build (apps/api/Dockerfile)", "revisão manual do workflow GitHub Actions"]
```
> [AUTO-DECISION] Story de CI/CD/Infra (cria pipeline GitHub Actions que builda e testa a imagem de produção) → pelo mapeamento "Work Type → Executor" do template (`Infra/CI/CD/Deploy → @devops`, quality_gate `@architect`), executor=@devops, quality_gate=@architect. Reason: consistente com `agent-authority.md` ("CI/CD pipeline management" é operação EXCLUSIVA de @devops) e com o template de story (`story-tmpl.yaml`, seção `executor-assignment`).

## Story
**As a** operador do go-live,
**I want** um CI que rode a suíte de testes DENTRO da imagem Docker de produção e o e2e de isolamento cross-tenant,
**so that** o drift venv↔produção seja pego antes do deploy e o isolamento RLS seja garantido automaticamente.

## Acceptance Criteria
1. O CI builda a imagem de produção e roda a suíte completa (`scripts/check.sh`/pytest) **dentro** dela, não só no venv local.
2. O CI executa o teste de isolamento cross-tenant no Postgres real (testcontainers), hoje só validado manualmente.
3. O CI é **gate obrigatório**: um deploy só é promovido se o CI passar; falha bloqueia o deploy.

### Integration Verification
- IV1: Todos os ~252 testes existentes continuam passando na imagem de produção.
- IV2: O teste de isolamento reproduz o cenário "João não vê dados da Maria" no Postgres real.
- IV3: O gate não introduz falso-positivo que trave deploys legítimos (pipeline estável).

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este ambiente de execução da missão trata `coderabbit_integration` como indisponível por instrução explícita do orquestrador (sem integrações externas neste ambiente), independentemente do valor default do framework (`.aiox-core/core-config.yaml` global tem `coderabbit_integration.enabled: true`, mas este projeto (e1p) não tem um `core-config.yaml` próprio que o sobrescreva, e a missão instruiu tratar como disabled mesmo assim).
> Quality validation will use manual review process only (ver `quality_gate_tools` acima e `scripts/check.sh`).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [x] **Task 1 — Criar o workflow de CI que builda e testa a imagem de produção dentro dela mesma (AC: 1)**
  - [x] Confirmado por busca no repo: **não existe nenhum workflow GitHub Actions hoje** (`.github/workflows/` só aparece dentro de `node_modules/**` de dependências — nada no projeto). Criar **NOVO** arquivo `.github/workflows/ci.yml`.
  - [x] Trigger: `on: push` (branches `main`) e `on: pull_request` (target `main`) — o repo é hospedado em `https://github.com/educacaocriativa/escritorio-1-pessoa` (confirmado via `git remote -v`), branch de trabalho atual é `docs/go-live-prd-epics-stories`.
  - [x] Job `test-in-prod-image` (roda em `ubuntu-latest`):
    - `actions/checkout@v4`.
    - `docker build -f apps/api/Dockerfile -t e1p-api:ci apps/api` — contexto de build é `apps/api` (mesmo padrão de `infra/docker-compose.prod.yml` linha `build: ../apps/api`, que resolve para o diretório `apps/api` a partir de `infra/`). `apps/api/Dockerfile` já copia `COPY . .` (linha 18), incluindo `apps/api/tests/` (não está no `.dockerignore` — confirmado em `apps/api/.dockerignore`), então a suíte de testes viaja dentro da imagem.
    - `docker run --rm e1p-api:ci sh -c "ruff check . && python -m pytest -q -m 'not rls_e2e'"` — roda lint + a suíte completa (SQLite em memória via `apps/api/tests/conftest.py`, sem precisar de Postgres real) **dentro** da imagem de produção, pegando o mesmo tipo de drift que já quebrou uma vez (FastAPI mais novo no venv local que o pinado em `requirements.txt`, ver `CLAUDE.md#6.1`). O marker `rls_e2e` (Task 3) é excluído aqui porque o teste de isolamento real precisa subir um container Postgres via `testcontainers` — rodar Docker-in-Docker dentro do `docker run` da imagem adicionaria complexidade/instabilidade desnecessária; ver Task 3 para onde esse teste roda.
  - [x] **[AUTO-DECISION]** Não usar `scripts/check.sh` literalmente dentro do `docker run` (o script também tenta rodar etapas de frontend via `pnpm`, que não existe na imagem `apps/api`, só backend). Reason: a imagem de produção testada nesta AC é a do backend (`apps/api/Dockerfile`, a mesma buildada por `docker-compose.prod.yml` para o serviço `api`); replicar só as duas linhas relevantes do script (`ruff check .` + `python -m pytest -q`) dentro do container evita invocar um script que assume um ambiente Node/pnpm que a imagem não tem. O frontend não é o alvo do drift descrito na AC (o bug histórico foi no backend).

- [x] **Task 2 — Ajustar `scripts/check.sh` para não quebrar localmente sem Docker (AC: 1, 2)**
  - [x] ALTERAR `scripts/check.sh` linha `( cd apps/api && python -m pytest -q )` → `( cd apps/api && python -m pytest -q -m "not rls_e2e" )`, espelhando o mesmo filtro de marker usado no `docker run` da Task 1. Reason: sem esse ajuste, qualquer dev/CI rodando `scripts/check.sh` num host sem Docker disponível (ex.: dentro do próprio container `python:3.13-slim` já usado nas Stories 1.1-1.4 para rodar pytest sem Postgres) quebraria ao coletar o novo teste `test_rls_isolation.py` (Task 3), que depende de `testcontainers` conseguir falar com um daemon Docker.

- [x] **Task 3 — Criar o teste de isolamento cross-tenant no Postgres real via testcontainers (AC: 2)**
  - [x] ALTERAR `apps/api/requirements.txt`, seção "Dev / testes" (linhas ~19-22, junto de `pytest==8.3.4`/`ruff==0.8.4`): adicionar `testcontainers[postgres]` (pin de versão a definir pelo @dev/@devops que implementar — checar última versão estável compatível com Python 3.13 no momento da implementação, já que este draft não deve fixar uma versão sem verificar disponibilidade real). → **Implementado como `testcontainers[postgres]==4.9.0`, validado instalando dentro da imagem `python:3.13-slim` (build da imagem de produção OK).**
  - [x] NOVO arquivo `apps/api/tests/test_rls_isolation.py`, marcado inteiro com `pytestmark = pytest.mark.rls_e2e` (ver Task 4 para o registro do marker). Deve reproduzir literalmente o cenário descrito em `CLAUDE.md#3` ("João não vê dados da Maria") e a Regra de Ouro nº 1 do projeto:
    1. Sobe `testcontainers.postgres.PostgresContainer("postgres:16-alpine")`.
    2. Contra o container, roda o bootstrap de RLS **exatamente como em produção** (mesmo SQL de `infra/docker/initdb/01-rls-enforce.sql`, linhas 11-13: `CREATE ROLE e1p_app WITH LOGIN PASSWORD ... NOSUPERUSER`, `GRANT ALL PRIVILEGES ON DATABASE ... TO e1p_app`, `GRANT ALL ON SCHEMA public TO e1p_app`) — via psycopg/SQLAlchemy usando as credenciais superuser do container de teste.
    3. Roda `alembic upgrade head` (via `alembic.config.Config` + `alembic.command.upgrade`, apontando `apps/api/alembic.ini`/`apps/api/migrations`) usando a **connection string do papel `e1p_app`** (não o superuser do testcontainers) — isso é CRÍTICO: a Regra de Ouro nº 1 (`CLAUDE.md#3`, item 1) explica que a app "DEVE conectar como papel NÃO-superusuário, senão a RLS é IGNORADA (superusuários fazem bypass, mesmo com FORCE)"; para o teste ser fiel a produção, `e1p_app` precisa ser o DONO das tabelas (quem roda as migrations), exatamente como acontece no `docker-compose.prod.yml` (serviço `api`, `DATABASE_URL: postgresql+psycopg://e1p_app:...`).
    4. Cria dois tenants sintéticos ("joao"/"maria") e, para cada um, grava uma linha numa tabela com RLS (ex.: `audit_entries`, criada e protegida por `_enable_rls()` já na migration `0001_initial.py`) usando `set_config('app.current_tenant_id', <tenant_id>, false)` antes do INSERT — mesmo padrão de `apps/api/app/db/session.py::tenant_session`.
    5. Assert 1 (cenário nomeado): com a GUC setada para o tenant do João, a query só retorna a linha do João — a linha da Maria NUNCA aparece.
    6. Assert 2 (fail-closed, IV2 implícito pela Regra de Ouro nº 1): sem GUC setada (ou GUC vazia), a query retorna **zero linhas** (não todas) — replicando o comentário de `0001_initial.py::_enable_rls` ("o segundo arg `true` em `current_setting` evita erro... retorna NULL => nenhuma linha visível, fail-safe").
    7. Assert 3: conectar como o **superuser** do testcontainers (não `e1p_app`) e confirmar que ele consegue ver as duas linhas — documentando por que a Regra de Ouro nº 1 exige que a app NUNCA use esse papel.
  - [x] **[AUTO-DECISION]** Implementar a lógica de `set_config`/sessão do teste com SQLAlchemy "cru" (engine própria do teste, criada a partir da URL do container), em vez de importar e reusar `app.db.session.tenant_session` diretamente. Reason: `app.db.session` cria um `engine`/`SessionLocal` module-level ligado a `settings.database_url` no momento do import (lido de `.env`/defaults) — reapontar esse engine global para a URL efêmera do container exigiria monkeypatch de estado compartilhado do processo, arriscando vazar para outros testes que rodam no mesmo processo pytest (mesmo risco documentado no próprio `conftest.py` sobre isolar overrides por teste). Uma engine local ao teste, criada e descartada dentro do próprio `test_rls_isolation.py`, é mais segura e mais fácil de auditar — ao custo de duplicar ~10 linhas da lógica de `set_config` do `tenant_session` (aceitável, é teste, não código de produção).

- [x] **Task 4 — Job de CI dedicado ao teste de isolamento + registro do marker (AC: 2)**
  - [x] ALTERAR `apps/api/pyproject.toml`, seção `[tool.pytest.ini_options]` (linhas ~17-19): adicionar `markers = ["rls_e2e: teste de isolamento cross-tenant via testcontainers, exige Docker disponível"]` para registrar o marker (evita `PytestUnknownMarkWarning`; o projeto não usa `--strict-markers` hoje, então isso não quebra nada, é só higiene).
  - [x] No `.github/workflows/ci.yml` (Task 1), adicionar o job `cross-tenant-rls` (roda em `ubuntu-latest`, que já vem com Docker Engine nativo — GitHub-hosted runners suportam `testcontainers` sem setup extra):
    - `actions/checkout@v4`.
    - `actions/setup-python@v5` (`python-version: "3.13"`, para casar com `requires-python = ">=3.13"` de `apps/api/pyproject.toml`).
    - `pip install -r apps/api/requirements.txt`.
    - `pytest -q -m rls_e2e apps/api/tests/test_rls_isolation.py` (a partir da raiz do repo, ou `cd apps/api && pytest -q -m rls_e2e`).
  - [x] **[AUTO-DECISION]** Este job roda direto no runner do GitHub Actions (não dentro do `docker run` da imagem de produção da Task 1), porque `testcontainers` precisa falar com o daemon Docker do host para subir o container Postgres efêmero — nesting Docker-in-Docker dentro de outro container adicionaria complexidade (montar `/var/run/docker.sock`, permissões) sem ganho real para o objetivo da AC2 (validar RLS num Postgres real), que já é satisfeito rodando no runner com as mesmas versões de dependências de `requirements.txt`. O drift venv↔produção (razão de ser da AC1) já é coberto separadamente pelo job `test-in-prod-image`.

- [x] **Task 5 — Tornar o CI gate obrigatório para deploy (AC: 3)**
  - [x] O workflow `.github/workflows/ci.yml` (Tasks 1 e 4) já falha (status vermelho) se qualquer um dos dois jobs (`test-in-prod-image`, `cross-tenant-rls`) falhar — comportamento padrão do GitHub Actions, sem `continue-on-error`.
  - [x] **GAP conhecido, fora do escopo de código desta story:** para que isso vire de fato um **gate que bloqueia merge/deploy** (não só um workflow que fica vermelho), é preciso habilitar em GitHub → Settings → Branches → regra de proteção da branch `main` com "Require status checks to pass before merging", marcando `test-in-prod-image` e `cross-tenant-rls` como checks obrigatórios. Essa é uma operação de configuração do repositório GitHub — **exclusiva de @devops** (`agent-authority.md`: "CI/CD pipeline management" e config de repositório GitHub são operações exclusivas de @devops; nenhum outro agente deve tocar `gh api repos/.../branches/.../protection` ou equivalente). Registrar como tarefa de follow-up para @devops após esta story ser implementada e mergeada. → **Documentado explicitamente no Dev Agent Record (Follow-up @devops) e em `docs/HOSTINGER-DEPLOY.md` §"Dívida / próximos passos".**
  - [x] ALTERAR `docs/HOSTINGER-DEPLOY.md`, seção `## 5. Atualizações (deploy de uma nova versão)`: adicionar uma linha explícita "Antes de `git pull` + rebuild em produção, confirme que o CI (`.github/workflows/ci.yml`) está verde no commit/branch que será deployado" — hoje essa seção não menciona CI (confirmado: o arquivo diz literalmente, na seção "Dívida / próximos passos", "CI/CD (hoje é `git pull` + rebuild manual na VPS)"); atualizar essa linha da dívida para refletir que o CI passa a existir (mas o deploy em si continua manual — não há pipeline de deploy automático nesta story, só o gate de teste).

- [x] **Task 6 — Validação e qualidade (AC: todas; IV1, IV2, IV3)**
  - [x] Rodar `ruff check apps/api` e `python -m pytest -q -m "not rls_e2e"` localmente (ou no container `python:3.13-slim` usado nas Stories 1.1-1.4, já que o host de dev não tem Python/venv) para confirmar que o filtro de marker (Task 2/4) não quebra a suíte existente (IV1 — ~252 testes existentes continuam passando). → **IV1 OK: `286 passed, 1 deselected` dentro da imagem de produção (a suíte cresceu além dos ~252 originais; o único deselecionado é o novo `rls_e2e`). ruff limpo.**
  - [x] Rodar `docker build -f apps/api/Dockerfile -t e1p-api:ci apps/api` localmente e depois `docker run --rm e1p-api:ci sh -c "ruff check . && python -m pytest -q -m 'not rls_e2e'"` para validar a Task 1 antes de subir o workflow (evita descobrir erro de path/contexto só dentro do GitHub Actions). → **Executado exatamente assim; build OK, ruff+pytest verdes na imagem.**
  - [x] Rodar `pytest -m rls_e2e apps/api/tests/test_rls_isolation.py -v` localmente com Docker disponível, confirmando os 3 asserts da Task 3 (IV2 — cenário "João não vê dados da Maria" reproduzido no Postgres real). → **IV2 OK: `1 passed`; migrations rodadas como `e1p_app` (non-superuser), os 3 asserts passam (isolamento, fail-closed sem GUC, bypass do superuser).**
  - [x] Validar IV3 manualmente: rodar o workflow completo (ex.: via `gh workflow run` ou um PR de teste) e confirmar que passa de forma consistente (sem flakiness) em pelo menos 2 execuções consecutivas antes de considerar o gate estável o bastante para @devops habilitar a branch protection (Task 5). → **Parcial: o teste `rls_e2e` passou em 2 execuções consecutivas localmente (sem flakiness). A execução do workflow COMPLETO no GitHub Actions exige push + runner real e é follow-up de @devops (ver Dev Agent Record — Limitações do ambiente).**

## Dev Notes

### Previous Story Insights
- Não há stories do Epic 3 anteriores a esta (3.2 é a primeira sendo draftada no epic; 3.1 — ambiente de staging — pode estar sendo desenvolvida em paralelo por outro agente/sessão). **Sem dependência AC-a-AC entre 3.1 e 3.2**: ambas usam a mesma imagem de produção (`apps/api/Dockerfile`, `infra/docker-compose.prod.yml`), mas o epic não declara pré-requisito explícito entre elas — esta story foi draftada de forma autocontida, sem assumir que 3.1 já está implementada. [AUTO-DECISION] Caso 3.1 já exista quando esta story for implementada, nada aqui precisa mudar (o CI builda a imagem diretamente, não depende do ambiente de staging estar de pé).
- Padrão de execução de testes sem Python/venv local, usado nas Stories 1.1-1.4: container `python:3.13-slim` com `requirements.txt` instalado e o worktree montado. Reutilizar esse padrão para validar a Task 6 desta story antes de confiar só no CI.

### Contexto do sistema (fonte: epic)
- "A app é 100% conteinerizada (12-factor). Já existe um bundle de deploy commitado (`59ff179`) para VPS Hostinger... Lacunas conhecidas: não há staging, não há CI rodando os testes na imagem de produção (houve um bug de drift venv↔produção — versão do FastAPI divergente escondeu um erro de rota 204 que só quebrou no container)... e o teste de isolamento cross-tenant (RLS é Postgres-only) só foi validado manualmente." [Source: docs/prd/epic-3-deploy-storage-observabilidade.md — "Contexto do sistema existente"]
- "Manter 12-factor para não acoplar à VPS específica (caminho AWS aberto). A app sempre roda como papel non-superuser `e1p_app` (RLS válida). Nenhuma mudança pode quebrar os ~252 testes existentes; o CI passa a ser gate obrigatório antes do deploy." [Source: docs/prd/epic-3-deploy-storage-observabilidade.md — "Integration Requirements"]

### Regra de Ouro nº 1 (RLS) — por que o teste precisa rodar como `e1p_app`
- "Isolamento de tenant é sagrado... Acesso a dados SEMPRE passa pela camada de tenancy (RLS no Postgres)... ⚠️ A app DEVE conectar como papel NÃO-superusuário (`e1p_app`), senão a RLS é IGNORADA (superusuários fazem bypass, mesmo com FORCE)... Validado por teste e2e no Postgres real (João não vê dados da Maria)." [Source: CLAUDE.md#3 "Regras de ouro (NUNCA violar)", item 1]
- "O GUC do tenant é setado em escopo de SESSÃO (`set_config(..., false)`) em `db/session.py`, não de transação." [Source: CLAUDE.md#3, item 1]

### Dívida técnica que esta story fecha (fonte: CLAUDE.md)
- "Teste de isolamento cross-tenant: RLS é Postgres-only; os testes unitários usam SQLite e não a exercem. ✅ Validado manualmente via e2e no Docker (Postgres real). TODO: automatizar com testcontainers no CI." [Source: CLAUDE.md#6.1 "Dívida técnica / TODO de segurança"] — esta é literalmente a AC2 desta story.
- "Drift de versão venv↔produção: o venv local tinha FastAPI mais novo que o pinado (0.115.5), o que escondeu um erro de rota 204 que só quebrou no container... Considerar CI que rode os testes na imagem." [Source: CLAUDE.md#6.1] — esta é literalmente a AC1 desta story.

### Arquivos e padrões relevantes (verificado diretamente no código)
- `apps/api/Dockerfile` (22 linhas) — imagem `python:3.13-slim`, instala `tesseract-ocr`/`build-essential`/`libpq5`, `COPY requirements.txt .` + `pip install -r requirements.txt`, depois `COPY . .` (copia tudo, inclusive `tests/`, não excluído por `apps/api/.dockerignore`).
- `infra/docker-compose.prod.yml` (linhas 24-43) — serviço `api`: `build: ../apps/api` (contexto = `apps/api`), `DATABASE_URL: postgresql+psycopg://e1p_app:${APP_DB_PASSWORD}@postgres:5432/e1pdb`, `command: sh -c "alembic upgrade head && python -m app.seed && uvicorn ..."`. Confirma o contexto de build e o padrão de rodar `alembic upgrade head` como `e1p_app`.
- `infra/docker/initdb/01-rls-enforce.sql` (dev) e `infra/docker/initdb-prod/01-rls-enforce.sh` (prod) — criam o papel `e1p_app NOSUPERUSER` e concedem privilégios. O teste da Task 3 precisa replicar esse bootstrap (não há um script Python reusável para isso hoje — é só SQL puro).
- `apps/api/app/db/session.py` (linhas 20-47) — `tenant_session(tenant_id)`: abre conexão dedicada (`engine.connect()`), seta `set_config('app.current_tenant_id', tid, false)`, valida `tenant_id` não-vazio (fail-closed), reseta a GUC no `finally`. Padrão a replicar (não a importar diretamente — ver Task 3 AUTO-DECISION) no teste de isolamento.
- `apps/api/migrations/versions/0001_initial.py` (linhas 19-31, 65-77) — `_enable_rls(table)` helper (`ENABLE ROW LEVEL SECURITY` + `FORCE ROW LEVEL SECURITY` + `CREATE POLICY tenant_isolation ... USING (tenant_id = current_setting('app.current_tenant_id', true))`), aplicado a `audit_entries` — tabela candidata natural para o teste da Task 3 (existe desde a primeira migration, RLS já habilitada, schema simples).
- `apps/api/tests/conftest.py` (79 linhas) — hoje só SQLite em memória; comentário explícito no topo: "RLS é específica do Postgres e NÃO é exercida aqui — é validada em ambiente com Postgres". O novo teste (Task 3) é o primeiro a fechar essa lacuna de forma automatizada.
- `apps/api/requirements.txt` — seção final "# Dev / testes" já mistura deps de teste/lint na imagem de produção (`pytest`, `ruff`) — mesmo padrão a seguir para `testcontainers[postgres]` (Task 3), não há separação `requirements-dev.txt`/`requirements.txt` neste projeto.
- `apps/api/pyproject.toml` — `[tool.pytest.ini_options]` hoje só tem `testpaths = ["tests"]` e `asyncio_mode = "auto"`, sem `markers` declarados nem `--strict-markers` (Task 4 adiciona o marker `rls_e2e`).
- `scripts/check.sh` — script de qualidade local (`ruff check .` + `pytest -q` no backend, typecheck/vitest no frontend via `pnpm`). Precisa do ajuste de marker da Task 2 para não quebrar sem Docker.
- `.github/workflows/` — **não existe no projeto** (confirmado via busca; só aparece dentro de `node_modules/**` de dependências de terceiros). Este é o primeiro workflow de CI do repositório.
- `apps/api/app/main.py` (linha 30) — `GET /health` existe (mencionado no epic e em `docs/HOSTINGER-DEPLOY.md` §4 como ponto de validação pós-deploy; não é usado diretamente pelo CI desta story, mas relevante para a Story 3.1/3.4 que consomem o mesmo endpoint).
- `apps/api/app/config.py` (linhas 8-46) — `Settings` com `is_production` gate (`_guard_production_secrets`); por default `environment="development"`, então o guard não interfere nos testes do CI (nenhuma variável de produção precisa ser setada para rodar a suíte).
- `docs/HOSTINGER-DEPLOY.md` — guia de deploy manual; seção "Dívida / próximos passos" já lista "CI/CD (hoje é `git pull` + rebuild manual na VPS)" como pendência — Task 5 atualiza esse trecho.

### File Locations (novos/alterados)
- NOVO: `.github/workflows/ci.yml`
- NOVO: `apps/api/tests/test_rls_isolation.py`
- ALTERAR: `apps/api/requirements.txt` (+`testcontainers[postgres]`)
- ALTERAR: `apps/api/pyproject.toml` (+`markers = [...]` em `[tool.pytest.ini_options]`)
- ALTERAR: `scripts/check.sh` (filtro de marker `-m "not rls_e2e"`)
- ALTERAR: `docs/HOSTINGER-DEPLOY.md` (§5 Atualizações + §"Dívida / próximos passos")

### Technical Constraints
- Não relaxar a Regra de Ouro nº 1 em nenhum ponto do teste novo: o teste da Task 3 DEVE rodar as migrations e as queries de isolamento como `e1p_app`, nunca como o superuser do testcontainers — senão o teste "passaria" sem validar nada de real (falso-positivo perigoso, o oposto do que a IV3 pede).
- Não introduzir Docker-in-Docker (montar `/var/run/docker.sock` dentro de um container já rodando dentro do runner) — decisão explícita da Task 4 de rodar o job `cross-tenant-rls` direto no runner, não aninhado na imagem de produção.
- Nenhuma orientação técnica específica encontrada nos docs disponíveis sobre versionamento/pin exato do pacote `testcontainers` — o @dev que implementar deve checar a versão estável mais recente compatível com Python 3.13 no momento da implementação (não travar aqui um número que pode já estar desatualizado quando a story for pega).
- Nenhuma orientação técnica específica encontrada nos docs disponíveis sobre um pipeline de **deploy automático** (CD) — esta story cobre só o **CI de teste/gate** (AC1-AC3); o deploy em produção continua manual (`git pull` + `docker compose up`, conforme `docs/HOSTINGER-DEPLOY.md` §5), só que agora condicionado (por processo, não por automação) a um CI verde.

### Testing
- Framework backend: `pytest` (`apps/api/tests`), já usado nas Stories 1.1-1.4.
- Teste novo: `apps/api/tests/test_rls_isolation.py`, marcado `rls_e2e`, roda contra Postgres real via `testcontainers` — não roda no `pytest -q` padrão (filtrado por marker, Tasks 2/4), só no job dedicado `cross-tenant-rls` do CI (Task 4) ou manualmente com Docker disponível.
- Suíte existente (~252 testes, todos SQLite in-memory via `conftest.py`) deve continuar passando inalterada dentro da imagem de produção — é literalmente a IV1 desta story.
- Nenhuma orientação técnica específica encontrada nos docs disponíveis sobre um framework de teste de workflow GitHub Actions em si (ex.: `act` para rodar Actions localmente) — a Task 6 usa validação manual (`docker build`/`docker run` local + execução real do workflow) na ausência de tooling específico documentado no projeto.

## Story Draft Checklist Results
_(executado por River/@sm em modo YOLO/autônomo — `.aiox-core/product/checklists/story-draft-checklist.md`)_

**Story readiness: READY** | **Clarity score: 9/10**

| Category | Status | Issues |
|---|---|---|
| 1. Goal & Context Clarity | PASS | — |
| 2. Technical Implementation Guidance | PASS | — |
| 3. Reference Effectiveness | PASS | Referências citam linhas/seções específicas (`CLAUDE.md#3`, `CLAUDE.md#6.1`, epic §Story 3.2, arquivos com nº de linha). |
| 4. Self-Containment Assessment | PASS | Termos de domínio (GUC, RLS FORCE, `set_config`) explicados inline; premissas marcadas como `[AUTO-DECISION]` com razão. |
| 5. Testing Guidance | PASS | Cenários de teste explícitos (3 asserts na Task 3), critério de sucesso = IV1-IV3. |
| 6. CodeRabbit Integration (conditional) | N/A | Tratado como disabled por instrução da missão; skip notice presente. |

**Gaps / riscos sinalizados (não-bloqueantes):**
- Versão exata do pacote `testcontainers[postgres]` não foi travada neste draft (checar disponibilidade/compat Python 3.13 no momento da implementação) — documentado em Technical Constraints.
- AC3 ("gate obrigatório") tem uma parte de código (workflow falha se os jobs falharem — coberto pela Task 1/4) e uma parte de **configuração de repositório GitHub** (branch protection com status checks obrigatórios) que é exclusiva de @devops e não pode ser feita via código desta story — documentada explicitamente como follow-up na Task 5, para não ficar implícita/perdida.
- Sem `docs/architecture/` neste projeto: todo o contexto técnico veio de `CLAUDE.md` + inspeção direta do código (Dockerfile, docker-compose.prod.yml, session.py, migrations) — mais trabalho de verificação do que citar um documento de arquitetura pronto, mas nenhuma lacuna de informação identificada.

**Developer perspective:** um @devops/@dev competente consegue implementar as 6 tasks sem precisar pedir esclarecimento adicional — os únicos pontos de julgamento (pin de versão do `testcontainers`, nome exato dos jobs) já vêm com uma decisão default sugerida e o porquê.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-10 | 0.1 | Story criada a partir do Epic 3 (Deploy, Storage & Observabilidade) | River (@sm) |
| 2026-07-10 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-07-10 | 1.0 | Implementação completa (Tasks 1-6). CI (ci.yml) + teste RLS e2e (testcontainers) + ajustes de marker/check.sh/docs. Status: Ready → InReview | Dex (@dev) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.8 (claude-opus-4-8[1m]) — agente Dex (@dev), modo YOLO autônomo.

### Debug Log References
- `docker build -f apps/api/Dockerfile -t e1p-api:ci-3.2 apps/api` → build OK; `testcontainers-4.9.0` (+ `docker-7.2.0`, `wrapt-2.2.2`) instalados na imagem de produção sem conflito.
- `docker run --rm e1p-api:ci-3.2 sh -c "ruff check . && python -m pytest -q -m 'not rls_e2e'"` → ruff limpo; **286 passed, 1 deselected** (o deselecionado é `tests/test_rls_isolation.py`, filtrado pelo marker). IV1 OK.
- `docker run --rm -v //var/run/docker.sock:/var/run/docker.sock -e TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal -e TESTCONTAINERS_RYUK_DISABLED=true --add-host host.docker.internal:host-gateway e1p-api:ci-3.2 sh -c "python -m pytest -q -m rls_e2e tests/test_rls_isolation.py"` → **1 passed** em 2 execuções consecutivas (6.56s / 9.28s). IV2 OK, sem flakiness observada. (Os flags de socket/host override foram só para validar localmente no Docker Desktop Windows; no runner `ubuntu-latest` do GitHub Actions o `testcontainers` fala com o Docker nativo sem esses flags — ver job `cross-tenant-rls` em `ci.yml`.)

### Completion Notes List
- **AC1 (suíte dentro da imagem de produção):** job `test-in-prod-image` builda `apps/api/Dockerfile` e roda `ruff check . && pytest -q -m 'not rls_e2e'` DENTRO da imagem. Validado localmente exatamente como o CI faz — pega o drift venv↔produção descrito em `CLAUDE.md#6.1`.
- **AC2 (isolamento cross-tenant no Postgres real):** novo `tests/test_rls_isolation.py` (marker `rls_e2e`) sobe `postgres:16-alpine` via `testcontainers`, cria o papel `e1p_app NOSUPERUSER` (mesmo bootstrap de `initdb/01-rls-enforce.sql`), roda `alembic upgrade head` **como `e1p_app`** e verifica 3 asserts: (1) João só vê a linha do João (e o simétrico p/ Maria), (2) fail-closed sem GUC → zero linhas, (3) superuser faz bypass e vê as duas linhas. Job `cross-tenant-rls` roda esse teste no runner (Docker nativo).
- **AC3 (gate obrigatório):** os dois jobs sem `continue-on-error` → qualquer falha deixa o workflow vermelho. A parte de **branch protection** (tornar os checks obrigatórios p/ merge) é config de repositório GitHub, **exclusiva de @devops** — registrada como follow-up (abaixo) e em `docs/HOSTINGER-DEPLOY.md`.
- **Pin do `testcontainers`:** fixado em `4.9.0` (compatível com Python 3.13, valida no build da imagem). `driver="psycopg"` no `PostgresContainer` para casar com `psycopg[binary]` do projeto (não `psycopg2`).
- **Sem migration nova:** a story não pede alteração de schema; reusa `audit_entries` (RLS já habilitada desde a migration `0001`). Última migration na base (`0031`) permanece intocada.
- **IDS:** `.github/workflows/` não existia no projeto (CREATE justificado, é o 1º workflow do repo); `test_rls_isolation.py` novo (ADAPTA o padrão de `tenant_session`/`_enable_rls` sem importar código de produção — ver AUTO-DECISION da Task 3); demais arquivos foram ALTERADOS in-place seguindo os padrões existentes.

### Follow-up para @devops (fora do escopo de código desta story)
1. **Branch protection (fecha o loop da AC3):** GitHub → Settings → Branches → regra na `main` com "Require status checks to pass before merging", marcando `test-in-prod-image` e `cross-tenant-rls` como obrigatórios. Só @devops pode tocar `gh api repos/.../branches/.../protection` (`agent-authority.md`).
2. **IV3 end-to-end:** rodar o workflow COMPLETO no GitHub Actions (via PR real) e confirmar 2 execuções verdes consecutivas antes de ativar a branch protection.

### Limitações do ambiente (não pôde ser validado aqui)
- **Execução real do workflow GitHub Actions** (`ubuntu-latest`, `actions/checkout`, `actions/setup-python`): não há acesso ao GitHub Actions neste ambiente. O conteúdo do `ci.yml` foi revisado manualmente e cada comando dos jobs foi reproduzido localmente via Docker (build + run da imagem; teste `rls_e2e` com socket Docker montado). A validação end-to-end no runner real é follow-up de @devops (IV3).
- **Sem Python/venv no host:** toda a validação de ruff/pytest rodou dentro de containers (`e1p-api:ci-3.2` = a própria imagem de produção), fiel ao padrão das Stories 1.1-1.4.

### File List
- NOVO: `.github/workflows/ci.yml`
- NOVO: `apps/api/tests/test_rls_isolation.py`
- ALTERADO: `apps/api/requirements.txt` (+`testcontainers[postgres]==4.9.0`)
- ALTERADO: `apps/api/pyproject.toml` (+`markers = ["rls_e2e: ..."]` em `[tool.pytest.ini_options]`)
- ALTERADO: `scripts/check.sh` (filtro `-m "not rls_e2e"` no pytest do backend)
- ALTERADO: `docs/HOSTINGER-DEPLOY.md` (§5 Atualizações: confirmar CI verde antes do deploy; §Dívida: CI existe + follow-up de branch protection/CD)
- ALTERADO: `docs/stories/3.2.story.md` (Status + tasks + Dev Agent Record)

## QA Results
_(a preencher pelo @qa)_

# Story 2.1: Provedor de e-mail transacional (SMTP/SES)

## Status
Done

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["ruff (apps/api)", "pytest (apps/api/tests)", "manual security review", "validação manual do fluxo de recuperação de senha e convite em staging com SMTP real"]
```
> [AUTO-DECISION] Story troca um stub interno (`core/email.py`) por integração real via SMTP genérico (biblioteca padrão `smtplib`, sem SDK novo — reduz custo/dependência, alinhado à Regra de Ouro nº 4 do CLAUDE.md) + ajustes de config/gating de resposta. É trabalho de backend sem UI nova → executor=@dev, quality_gate=@architect (mesmo padrão das Stories 1.1-1.4). Reason: consistência com o restante do Epic 1 e não há trabalho de schema/DB nesta story.

## Story
**As a** profissional e seus clientes/funcionários convidados,
**I want** receber de verdade os e-mails de recuperação de senha e de convite (com a senha temporária),
**so that** o onboarding e a recuperação de acesso funcionem em produção sem expor tokens/senhas na resposta.

## Acceptance Criteria
1. `core/email.py` passa a enviar via provedor real (SMTP genérico ou SES), configurado por `SMTP_HOST` e credenciais em `.env.prod`/SSM; sem chave configurada, mantém o comportamento de log (graceful degradation).
2. Os fluxos existentes passam a entregar por e-mail: link de recuperação (`/auth/forgot-password`) e senha temporária de convite de conta/funcionário (`/admin/accounts`, `/admin/accounts/{tenant_id}/users`).
3. Em produção, o corpo da resposta **não** retorna mais `dev_reset_token` nem `temp_password` (só em dev).

### Integration Verification
- IV1: Os fluxos de convite/1º acesso e recuperação de senha continuam funcionando ponta a ponta (testes existentes passam; token/senha deixam de vazar no corpo em prod).
- IV2: Sem chave de e-mail configurada, os endpoints não quebram (falham fechado/log).
- IV3: Sem regressão de latência perceptível nos endpoints de auth/convite.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false` por instrução explícita do orquestrador desta missão.
> Quality validation will use manual review process only (ver `quality_gate_tools` acima e `scripts/check.sh`).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [x] **Task 1 — Adicionar configuração SMTP completa em `Settings` (AC: 1)**
  - [x] `apps/api/app/config.py`: hoje só existe `smtp_host: str = ""` (linha 34). Adicionar os campos que faltam para uma integração SMTP genérica real: `smtp_port: int = 587`, `smtp_user: str = ""`, `smtp_password: str = ""`, `smtp_from: str = ""` (endereço de remetente; se vazio, usar `smtp_user` como fallback no envio), `smtp_use_tls: bool = True`.
  - [x] Manter o contrato "sem chave = log": a condição de gating em `send_email` continua sendo `if not settings.smtp_host` (nenhuma mudança de comportamento quando `SMTP_HOST` vazio).
  - [x] Atualizar `.env.example` com o novo bloco de variáveis (seguir o padrão de comentário já usado para `WHATSAPP_TOKEN`/`WHATSAPP_PHONE_ID`, que hoje são as únicas integrações documentadas nesse arquivo): `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD`, `SMTP_FROM`, `SMTP_USE_TLS`.
  - [x] Nenhuma migration necessária (mudança é só de configuração de app, sem schema/DB).

- [x] **Task 2 — Implementar envio real em `core/email.py` (AC: 1, IV2, IV3)**
  - [x] `apps/api/app/core/email.py` (arquivo inteiro, 28 linhas hoje): substituir o comentário-placeholder `# Integração real (SMTP/SES) entra aqui quando as credenciais existirem.` (linha 22) por uma implementação real usando **`smtplib` + `email.message.EmailMessage`** (biblioteca padrão do Python, sem dependência nova no `requirements.txt` — evita custo de SDK adicional como `boto3`; um SMTP genérico também cobre SES via o endpoint SMTP do SES, satisfazendo a AC1 "SMTP genérico ou SES" sem bifurcar a implementação).
  - [x] Preservar a assinatura pública `send_email(*, to: str, subject: str, body: str) -> str` (mesmo contrato usado por `platform/service.py::_send_invite`, linha 267 e 277) e o retorno `"sent" | "logged" | "failed"`.
  - [x] Manter o `if not settings.smtp_host: ... return "logged"` (graceful degradation, IV2) inalterado.
  - [x] No caminho de envio real: montar `EmailMessage` com `From=settings.smtp_from or settings.smtp_user`, `To=to`, `Subject=subject`, corpo texto simples (`set_content(body)`); conectar via `smtplib.SMTP(settings.smtp_host, settings.smtp_port)`, `starttls()` se `settings.smtp_use_tls`, `login(settings.smtp_user, settings.smtp_password)` se houver usuário configurado, `send_message(msg)`. Envolver no `try/except Exception` já existente para preservar `"failed"` em caso de erro do provedor (IV3).
  - [x] Usar timeout explícito na conexão SMTP (`timeout=10`, mesmo padrão de `core/whatsapp.py`) para não travar a request em caso de provedor lento/indisponível (IV3).

- [x] **Task 3 — Ligar `/auth/forgot-password` ao envio real de e-mail (AC: 2, IV1)**
  - [x] `apps/api/app/modules/auth/router.py::forgot_password`: agora, quando `raw` existir, monta um corpo de e-mail (texto simples) com o código de redefinição e instrução para colá-lo na tela "Definir nova senha" do app (o front atual tem campo de colar token manualmente — não existe rota `/reset-password?token=...`, não inventada nesta story) e chama `send_email(to=str(data.email), subject="Redefinição de senha", body=...)`.
  - [x] Import local (`from app.core.email import send_email`) dentro da função, seguindo o padrão de import tardio de `platform/service.py::_send_invite`.

- [x] **Task 4 — Gatear `temp_password` em produção (AC: 3, IV1)**
  - [x] `apps/api/app/modules/platform/schemas.py`: `temp_password` tornado opcional em ambos os schemas — `temp_password: str | None = None` (`AccountInviteOut` e `StaffInviteOut`).
  - [x] `apps/api/app/modules/platform/router.py`: em `create_account` e `create_staff`, gatear a atribuição: `temp_password=None if settings.is_production else temp` (import `from app.config import settings`).
  - [x] Confirmado que `dev_reset_token` em `/auth/forgot-password` **já satisfaz a AC3 para esse endpoint** — nenhuma mudança de gating necessária ali (só os dois endpoints `/admin/accounts*`).

- [x] **Task 5 — Testes (AC: 1, 2, 3; IV1, IV2, IV3)**
  - [x] `apps/api/tests/test_email.py` (novo): (a) sem `smtp_host` → `"logged"` sem exceção (IV2); (b) com `smtp_host` e `smtplib.SMTP` mockado → `"sent"` + mock chamado com host/porta/TLS/login/send_message esperados + validação do `EmailMessage` (From/To/Subject/corpo); (c) `smtp_from` sobrepõe `smtp_user` e TLS desligado não chama `starttls`; (d) `smtplib.SMTP` levantando exceção → `"failed"` sem propagar (IV3).
  - [x] `apps/api/tests/test_auth.py`: novo teste confirma que `send_email` é chamado (patch em `app.core.email.send_email`) quando há conta válida e que o token cru vai no corpo; e que sem conta nada é enviado. Testes existentes (`dev_reset_token`) seguem passando (ambiente default `development`).
  - [x] `apps/api/tests/test_platform.py`: novos testes forçam `settings.environment="production"` (monkeypatch) e confirmam `temp_password is None` na resposta de `POST /admin/accounts` e `POST /admin/accounts/{tenant_id}/users`. Testes existentes que usam `temp_password` seguem passando sem alteração (ambiente default `development`).
  - [x] Suíte completa (`pytest`, `apps/api/tests`) verde: **294 passed** (286 anteriores + 8 novos), sem regressão.

- [x] **Task 6 — Checklist de qualidade (AC: todas)**
  - [x] `ruff check .` (apps/api) — **All checks passed!**
  - [x] `pytest` (apps/api/tests) — **294 passed** (rodado no container `e1p-api-test:local`, sem venv no host — mesma limitação das Stories 1.1-1.4).
  - [x] Validação de entrega SMTP real ponta-a-ponta NÃO é possível sem provedor configurado em staging (`SMTP_HOST`/credenciais em `.env.prod`/SSM) — documentado como limitação; a validação automatizada usa mocks (Task 5).

## Dev Notes

### Previous Story Insights (Stories 1.1 - 1.4, Epic 1)
- Primeiro story do Epic 2 (Integrações Reais) — autocontido, não depende de outras stories em paralelo.
- Import tardio (`from app.core.email import send_email` dentro da função) reusado nas novas chamadas.
- Gating por `settings.is_production` reusado (não reimplementado).

### Arquivos e padrões relevantes (verificado diretamente no código)
- `apps/api/app/core/email.py` — stub trocado por envio real via `smtplib`.
- `apps/api/app/core/whatsapp.py` — padrão irmão (gating por config vazia → log, `try/except` → `"failed"`, `timeout=10`) usado como referência.
- `apps/api/app/config.py` — `Settings` (Pydantic Settings), `is_production` é `@property`.
- `apps/api/app/modules/auth/router.py` — `forgot_password` agora envia e-mail; `dev_reset_token` já gateado.
- `apps/api/app/modules/platform/router.py` / `schemas.py` — gating de `temp_password` (produção → `None`).
- `.env.example` (raiz do repo, não `apps/api/.env.example`) — bloco SMTP adicionado seguindo o padrão de comentários das integrações.

### File Locations (novos/alterados)
- ALTERADO: `apps/api/app/config.py` (campos SMTP)
- ALTERADO: `apps/api/app/core/email.py` (implementação real via `smtplib`)
- ALTERADO: `apps/api/app/modules/auth/router.py` (`forgot_password` envia e-mail)
- ALTERADO: `apps/api/app/modules/platform/router.py` (gating de `temp_password`)
- ALTERADO: `apps/api/app/modules/platform/schemas.py` (`temp_password: str | None = None`)
- ALTERADO: `.env.example` (bloco SMTP)
- NOVO: `apps/api/tests/test_email.py`
- ALTERADO: `apps/api/tests/test_auth.py`
- ALTERADO: `apps/api/tests/test_platform.py`

### Migration
- **Nenhuma migration necessária.** Sem mudança de schema/DB. O número `0032` fica reservado (não usado nesta story).

### Technical Constraints
- Contrato de `send_email` (`"sent" | "logged" | "failed"`) preservado — `platform/service.py::_send_invite` consome como caixa-preta, sem alteração.
- Graceful degradation preservada: `smtp_host` vazio → `"logged"` sem exceção, em qualquer ambiente (IV2).
- Sem rota de front para token via query string (fora do escopo, 100% backend).
- Sem SDK/dependência paga nova — `smtplib` da stdlib (cobre SMTP genérico e endpoint SMTP do SES — Regra de Ouro nº 4).
- Resposta genérica de `/auth/forgot-password` preservada (não revela existência do e-mail).

### Testing
- Framework backend: `pytest` (`apps/api/tests`), execução via container (imagem `e1p-api-test:local`), sem venv no host.
- `smtplib.SMTP` mockado nos testes de `core/email.py` (não depende de servidor SMTP real).
- Testes de produção forçam `settings.environment="production"` via monkeypatch (mesma abordagem de `test_config.py`).

## Story Draft Checklist Results

_(executado por River/@sm, modo autônomo, contra `.aiox-core/product/checklists/story-draft-checklist.md`)_

| Category | Status | Issues |
|---|---|---|
| 1. Goal & Context Clarity | PASS | — |
| 2. Technical Implementation Guidance | PASS | — |
| 3. Reference Effectiveness | PASS | — |
| 4. Self-Containment Assessment | PASS | — |
| 5. Testing Guidance | PASS | — |
| 6. CodeRabbit Integration (conditional) | N/A | `coderabbit_integration` não configurado neste projeto (tratado como `false`); skip notice presente |

**Final Assessment: READY** (clarity score 9/10)

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-10 | 0.1 | Story criada a partir do Epic 2 (Integrações Reais) | River (@sm) |
| 2026-07-10 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-07-10 | 1.0 | Implementação: SMTP real (`smtplib`), envio em forgot-password, gating de `temp_password` em produção, testes (8 novos). Status: Ready → InReview | Dex (@dev) |
| 2026-07-11 | 1.1 | QA Gate: PASS (7 checks verdes, 523 passed/9 skipped, ruff limpo). Status: InReview → Done | Quinn (@qa) |

## Dev Agent Record

### Agent Model Used
claude-opus-4-8 (Dex / @dev)

### Debug Log References
- `ruff check .` (apps/api, container `e1p-api-test:local`) → All checks passed!
- `pytest -q` (apps/api/tests, mesmo container) → 294 passed (286 baseline + 8 novos), ~94s.

### Completion Notes List
- **AC1 (envio real + graceful degradation):** `core/email.py` agora usa `smtplib` + `email.message.EmailMessage` (stdlib, sem dependência nova). `smtp_host` vazio continua retornando `"logged"` sem tocar em `smtplib`. Novos campos em `Settings`: `smtp_port`, `smtp_user`, `smtp_password`, `smtp_from` (fallback para `smtp_user`), `smtp_use_tls`. `timeout=10` na conexão (IV3).
- **AC2 (fluxos entregam por e-mail):** `forgot_password` passou a chamar `send_email` (import tardio) com o código de redefinição no corpo, instruindo a colar na tela existente do front (sem inventar rota de link clicável). O convite de conta/funcionário já chamava `send_email`/`whatsapp` via `_send_invite` — inalterado.
- **AC3 (não vazar em produção):** `temp_password` tornado `str | None` em `AccountInviteOut`/`StaffInviteOut` e gateado por `settings.is_production` nos dois endpoints `/admin/accounts*`. `dev_reset_token` já era gateado.
- **Testes:** `test_email.py` (novo, 4 casos: logged/sent/from-override/failed) + 2 em `test_auth.py` (send_email chamado / não chamado) + 2 em `test_platform.py` (gating em produção via monkeypatch de `settings.environment`).
- **Sem migration** (config + integração + gating apenas). Número `0032` reservado e não usado.
- **LIMITAÇÃO (precisa de credencial/infra real para validação end-to-end):** a entrega SMTP real (recuperação de senha e convite chegando de fato numa caixa de entrada) só pode ser validada com `SMTP_HOST` + credenciais de um provedor real (SMTP genérico ou endpoint SMTP do Amazon SES) configurados em `.env.prod`/SSM num ambiente de staging. Este ambiente não tem esse provedor; a cobertura automatizada usa `smtplib.SMTP` mockado. Recomenda-se, ao configurar o provedor, um smoke test manual dos dois fluxos (forgot-password e convite de conta/funcionário) em staging antes do go-live.
- **Nota de branch:** a story `2.1.story.md` não existia na base `origin/docs/go-live-prd-epics-stories` (só as stories do Epic 1 estavam commitadas). O arquivo foi criado/versionado nesta feature branch com o conteúdo original + este Dev Agent Record.

### File List
- `apps/api/app/config.py` (alterado)
- `apps/api/app/core/email.py` (alterado)
- `apps/api/app/modules/auth/router.py` (alterado)
- `apps/api/app/modules/platform/router.py` (alterado)
- `apps/api/app/modules/platform/schemas.py` (alterado)
- `.env.example` (alterado)
- `apps/api/tests/test_email.py` (novo)
- `apps/api/tests/test_auth.py` (alterado)
- `apps/api/tests/test_platform.py` (alterado)
- `docs/stories/2.1.story.md` (novo nesta branch)

## QA Results

- **Data:** 2026-07-11 · **Gate:** @qa (Quinn) · **Veredito: PASS** · **Status → Done**

### 7 Quality Checks
| # | Check | Resultado | Evidência |
|---|-------|-----------|-----------|
| 1 | Requirements traceability (AC→teste) | PASS | AC1→`test_email.py` (logged/sent/from-override/failed); AC2→`test_auth.py::test_forgot_password_sends_email_when_account_exists`; AC3→`test_platform.py::test_temp_password_hidden_in_production_{account,staff}` |
| 2 | Code quality / standards | PASS | `ruff check .` limpo; `smtplib` da stdlib (sem dependência nova — Regra de Ouro nº 4) |
| 3 | Test coverage & pass | PASS | Suíte completa **523 passed, 9 skipped** (~166s) na imagem `e1p-api-test:local` |
| 4 | Security review | PASS | Token de reset e `temp_password` NÃO retornam no corpo em produção (gateados por `settings.is_production`); confirmado por teste |
| 5 | Graceful degradation / NFR | PASS | `if not settings.smtp_host → "logged"` sem exceção (IV2); `timeout=10` na conexão SMTP (IV3); exceção do provedor → `"failed"` sem propagar |
| 6 | No regression | PASS | Baseline mantida (523), nenhum teste do Epic 1 quebrado |
| 7 | Documentation / evidence | PASS | `.env.example` com bloco SMTP; Dev Agent Record completo; limitação de validação e2e (SMTP real em staging) documentada |

### Verificação manual (equivalente aos QA agents §5, sem sub-agente disponível)
- **regression:** suíte completa verde, sem regressão.
- **bug-hunter:** caminho de envio real e o gating de dev/prod revisados no código real (`core/email.py`, `auth/router.py`, `platform/router.py`) — coerentes com os testes.
- **dedup:** reusa `settings.is_production` e o import tardio de `send_email` (padrão de `_send_invite`); sem duplicação.

### Observações não bloqueantes (dívida já reconhecida)
- Validação end-to-end da entrega SMTP/SES real é passo manual do operador antes do go-live (credenciais em `.env.prod`/SSM). Cobertura automatizada usa `smtplib.SMTP` mockado — adequado ao estágio.
- Link clicável dedicado (`/reset-password?token=`) fica como dívida de UX (fora de escopo, 100% backend).

**Concordo com o gate do @architect (Aria).** AC1/AC2/AC3 e IV1/IV2/IV3 atendidos.

## Architect Review (Aria)

- **Data:** 2026-07-11 · **Gate:** @architect (Aria) · **Veredito: PASS (APPROVED)**

### Escopo verificado (código real, não só a story)
- `core/email.py` — envio real via `smtplib` + `EmailMessage` (stdlib, sem dependência nova, alinhado à Regra de Ouro nº 4). `timeout=10`, `starttls()` condicional, `login()` só se houver usuário, `From = smtp_from or smtp_user`. Confirmado.
- `config.py` — campos SMTP presentes (`smtp_host/port/user/password/from/use_tls`). Contrato "sem host = log" intacto.
- `auth/router.py::forgot_password` — envia e-mail com o código; import tardio; resposta genérica preservada (não revela existência do e-mail).
- `platform/router.py` — `temp_password = None if settings.is_production else temp` em `create_account` e `create_staff`. Schemas com `temp_password: str | None`.

### AC / IV
- **AC1 (envio real + graceful degradation):** PASS. `if not settings.smtp_host → "logged"` em qualquer ambiente (IV2). Exceção do provedor → `"failed"` sem propagar (IV3).
- **AC2 (fluxos entregam):** PASS. forgot-password chama `send_email`; convite reusa `_send_invite`.
- **AC3 (não vaza em produção):** **PASS — ponto crítico da missão confirmado.** `dev_reset_token` só é anexado quando `not settings.is_production` (`auth/router.py:112`); em produção NÃO volta na resposta, o usuário recebe apenas pelo e-mail. `temp_password` idem, gateado por `is_production` nos dois endpoints `/admin/accounts*`. Sem SMTP configurado, o comportamento de dev é integralmente preservado (o token continua no log/resposta dev, graceful).

### Segurança / trade-offs
- Sem vazamento de token/senha em produção; comportamento de dev preservado — o objetivo declarado da story está atendido.
- **Observação (não bloqueante):** comparação/entrega do código de reset por corpo de e-mail em texto simples é adequada ao estágio; um link clicável dedicado (`/reset-password?token=`) fica como dívida de UX já reconhecida (fora de escopo, 100% backend).
- **Validação end-to-end contra SMTP/SES real** = passo manual do operador antes do go-live (credenciais em `.env.prod`/SSM). Não bloqueia o gate — cobertura automatizada usa `smtplib.SMTP` mockado.

### Qualidade
- `ruff check .` → **All checks passed!** (container `e1p-api-test:local`).
- `pytest` (suíte completa) → **523 passed, 9 skipped** (rodado uma vez cobrindo as 3 stories do Epic 2). Sem regressão.

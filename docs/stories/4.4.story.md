# Story 4.4: Wildcard de subdomínio por tenant (TLS DNS-01)

## Status
InReview

## Executor Assignment
```yaml
executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: ["docker compose config (validação de sintaxe do Caddyfile/compose)", "ruff (apps/api, se o middleware de resolução de subdomínio for adicionado)", "pytest (apps/api/tests, se aplicável)", "teste manual de emissão/renovação do certificado wildcard (IV3)", "revisão manual de segurança do isolamento RLS (IV2)"]
```
> [AUTO-DECISION] Story é majoritariamente Infra/CI/CD/Deploy (TLS wildcard via DNS-01, Caddyfile/labels de proxy) com uma fatia pequena de backend (resolução de subdomínio → tenant, Task 3) → executor=@devops, quality_gate=@architect, conforme a matriz Work Type → Executor do template (`Infra/CI/CD/Deploy | @devops | @architect`), mesmo padrão da Story 3.4 (monitoramento) que também misturou infra + pequena superfície de app. Reason: o núcleo da story (certificado wildcard, roteamento do proxy) é 100% infraestrutura; a resolução de tenant por subdomínio (Task 3) é pequena o bastante (uma função pura + um dependency opcional) para não justificar trocar o executor para @dev — @architect como quality_gate cobre a revisão de segurança do ponto mais sensível (não deixar o Host header, que é controlável pelo cliente, virar fonte de verdade da RLS).

## Story
**As a** plataforma white-label,
**I want** servir cada tenant em seu subdomínio (`joaosilva.e1p.com`) com TLS wildcard,
**so that** o white-label por subdomínio funcione em produção.

## Acceptance Criteria
1. TLS wildcard (`*.e1p.com`) via desafio DNS-01 (Caddy com plugin de provedor DNS) configurado.
2. O roteamento por subdomínio resolve o tenant correto (mantendo o isolamento RLS).
3. O go-live single-domain atual continua funcionando durante a transição.

### Integration Verification
- IV1: O domínio único atual permanece acessível.
- IV2: Isolamento por tenant preservado no roteamento por subdomínio.
- IV3: Emissão/renovação de certificado wildcard validada.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false` por instrução explícita do orquestrador desta missão (mesmo padrão das Stories 1.1-1.4 e 3.4/3.5).
> Quality validation usa processo manual (ver `quality_gate_tools` acima e `scripts/check.sh`).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [x] **Task 1 — TLS wildcard via DNS-01 no Caddy próprio (`docker-compose.prod.yml`) (AC: 1, 3; IV1, IV3)**
  - [x] Trocar a imagem `caddy:2-alpine` (build oficial, sem plugins) por uma imagem Caddy com o plugin de DNS necessário — Caddy não suporta desafio DNS-01 nativamente, precisa do módulo `github.com/caddy-dns/<provedor>` compilado no binário (via `xcaddy` ou uma imagem community já buildada, ex. `docker.io/caddy:2-alpine` custom via `Dockerfile` próprio em `infra/caddy/Dockerfile` com `xcaddy build --with github.com/caddy-dns/cloudflare`, novo arquivo). **[AUTO-DECISION]** Provedor DNS: **Cloudflare** — reason: é a única sugestão técnica já registrada no projeto para este exato ponto ("Cloudflare é o mais simples se você migrar o DNS pra lá" [Source: docs/HOSTINGER-DEPLOY.md#Dívida / próximos passos]); não inventa provedor novo, apenas segue a orientação já documentada. Pré-requisito operacional (fora do escopo de código desta story): migrar o DNS de `e1p.com` para a Cloudflare, se ainda não estiver lá.
  - [x] Atualizar `infra/Caddyfile` (hoje só `{$DOMAIN} { ... }`, domínio único — [Source: infra/Caddyfile]) para servir DOIS blocos de site: o domínio único atual (IV1, mantém `handle_path /api/*` + fallback pro `web`, sem alteração de comportamento) **e** um bloco wildcard `*.{$ROOT_DOMAIN}` (ex.: `*.e1p.com`) com o mesmo roteamento `/api/*` → `api:8000` / resto → `web:80`, usando `tls { dns cloudflare {$CLOUDFLARE_API_TOKEN} }` no bloco wildcard para forçar o desafio DNS-01 (obrigatório para wildcard — Let's Encrypt não emite wildcard via HTTP-01). Documentar no próprio Caddyfile (comentário) por que só o bloco wildcard usa DNS-01: o domínio único (IV1) continua com o desafio HTTP-01 automático que já funciona hoje, sem regressão.
  - [x] Adicionar `CLOUDFLARE_API_TOKEN` (escopo mínimo: **Zone.DNS Edit** na zona `e1p.com`, seguindo o princípio de menor privilégio) e `ROOT_DOMAIN` (default `e1p.com`, distinto de `DOMAIN` que hoje aponta pro host único de produção real `e1p.doroeventos.com.br` — ver Dev Notes) como novas variáveis em `infra/.env.prod.example` (nunca commitar valor real, mesma regra do `.env.prod`). **Nota:** `ROOT_DOMAIN` já existia em `.env.prod.example` (linha 24) e em `app/config.py` (`root_domain`) — reaproveitado; só `CLOUDFLARE_API_TOKEN` era novo.
  - [x] Atualizar `infra/docker-compose.prod.yml`: trocar `image: caddy:2-alpine` por `build: ./caddy` (novo Dockerfile do subtask 1), e propagar `CLOUDFLARE_API_TOKEN`/`ROOT_DOMAIN` do `.env.prod` pro serviço `caddy` (já usa `env_file: .env.prod`, então só precisa garantir que as novas chaves estejam lá).

- [x] **Task 2 — Preparar (documentar) o caminho wildcard na topologia real de produção (Traefik compartilhado) (AC: 1, 3; IV1)**
  - [x] `infra/docker-compose.traefik.yml` é a topologia realmente usada em produção hoje (`e1p.doroeventos.com.br`, Traefik compartilhado vivendo em `/opt/infra/proxy/`, **fora deste repositório** — [Source: infra/docker-compose.traefik.yml (comentário de cabeçalho)]). Como o certresolver/DNS provider do Traefik compartilhado é configurado no repo de infra externo (não neste), esta story **não pode** alterar a emissão de certificado por lá diretamente — documentado como dependência externa explícita (ver Dev Notes → Riscos e HOSTINGER-DEPLOY.md §8.3).
  - [x] Ainda assim, atualizar os labels do serviço `web`/`api` em `infra/docker-compose.traefik.yml` para aceitar o wildcard quando o Traefik compartilhado tiver o certresolver wildcard configurado: trocar `Host(\`${DOMAIN}\`)` por `Host(\`${DOMAIN}\`) || HostRegexp(\`{subdomain:[a-z0-9-]+}.${ROOT_DOMAIN}\`)` (Traefik v2/v3 `HostRegexp`) nos routers `e1p-api` e `e1p-web`, preservando a prioridade existente (`/api` com priority=20 antes do catch-all `web` com priority=1 — [Source: infra/docker-compose.traefik.yml linhas 62-88]) para não regredir o roteamento atual (IV1).
  - [x] Adicionar `ROOT_DOMAIN` a `infra/.env.prod.example` (mesma variável do Task 1, reaproveitada aqui) e documentar em `docs/HOSTINGER-DEPLOY.md` que a emissão do certificado wildcard nesta topologia depende de configuração no repo `/opt/infra/proxy/` (fora deste repo) — atualizar o certresolver do Traefik compartilhado para um resolver com desafio DNS-01/Cloudflare (mesmo provedor do Task 1, para não introduzir dois fluxos de credencial DNS diferentes).

- [x] **Task 3 — Resolução de tenant por subdomínio no backend (AC: 2; IV2)**
  - [x] Criar `apps/api/app/core/subdomain.py` (novo arquivo) com uma função pura `extract_tenant_slug(host: str, root_domain: str) -> str | None`: recebe o header `Host` da requisição (ex.: `joaosilva.e1p.com` ou `joaosilva.e1p.com:8000`), remove a porta se houver, e devolve o primeiro rótulo como slug candidato SE o host terminar em `.{root_domain}` e não for exatamente `root_domain`/`www.{root_domain}` — senão devolve `None` (cobre o caso do domínio único do IV1/IV3: `Host` não bate no wildcard, função devolve `None`, comportamento atual preservado).
  - [x] **[AUTO-DECISION][CRÍTICO DE SEGURANÇA]** O slug resolvido pelo `Host` header **NUNCA** deve virar a fonte da GUC `app.current_tenant_id` (a RLS já é fixada exclusivamente a partir do `tenant_id` do JWT validado — `apps/api/app/core/tenancy.py::get_current_user`/`get_tenant_db`, [Source: apps/api/app/core/tenancy.py]). `Host` é um header controlado pelo cliente — usá-lo para decidir isolamento de dados quebraria a Regra de Ouro nº 1 (`CLAUDE.md §3.1`). Documentado como docstring de aviso no topo de `subdomain.py` e no `__all__` reexportado em `tenancy.py`.
  - [x] Adicionar uma dependency opcional `get_tenant_by_subdomain(request: Request, db: Session = Depends(get_db)) -> Tenant | None` em `apps/api/app/core/subdomain.py` (reexportada em `tenancy.py`): chama `extract_tenant_slug`, se houver slug faz `db.scalar(select(Tenant).where(Tenant.slug == slug))` (consulta na tabela global `tenants`, sem RLS) e devolve o `Tenant` ou `None`. Uso previsto: rotas **públicas** para branding/UX — não para autorizar escrita/leitura de dado sensível.
  - [x] Adicionar `"monitor"` (subdomínio já usado pela Story 3.4 para o painel Uptime Kuma) à lista `RESERVED_SLUGS` em `apps/api/app/modules/auth/schemas.py` — evita que um tenant novo registre o slug `monitor` e colida com o subdomínio de monitoramento de infraestrutura.
  - [x] Testes unitários para `extract_tenant_slug` (novo `apps/api/tests/test_subdomain.py`): casos — subdomínio válido, domínio raiz exato → `None`, `www` → `None`, host com porta, domínio diferente → `None`, subdomínio de dois níveis → primeiro rótulo, além de case-insensitive, root com ponto final, e host semelhante mas não-subdomínio (`note1p.com` → `None`). 12 testes, todos verdes.

- [x] **Task 4 — Documentação e validação (AC: 1, 2, 3; IV1, IV2, IV3)**
  - [x] Atualizar `docs/HOSTINGER-DEPLOY.md`: substituída a linha de dívida "Wildcard subdomínio por tenant (...)" por uma seção "## 8. Wildcard de subdomínio por tenant" descrevendo: pré-requisito de migrar DNS para Cloudflare, gerar o `CLOUDFLARE_API_TOKEN` (escopo mínimo Zone.DNS Edit), build da imagem Caddy customizada (Task 1), e a dependência externa do Traefik compartilhado (Task 2).
  - [x] Runbook de validação IV3 (emissão/renovação) documentado em §8.4: subir `docker-compose.prod.yml` com `.env.prod` preenchido, acessar `https://qualquerslug.${ROOT_DOMAIN}` e confirmar via `openssl s_client` que o certificado cobre `*.${ROOT_DOMAIN}`; conferir logs do Caddy para a emissão via `dns.providers.cloudflare`. **[LIMITAÇÃO]** Emissão/renovação real do certificado NÃO validada neste ambiente (sem `CLOUDFLARE_API_TOKEN` real, sem DNS gerido pela Cloudflare, sem infra viva) — exige validação humana em staging/prod (ver Dev Agent Record).
  - [x] Confirmar IV1 (domínio único continua funcionando): bloco `{$DOMAIN}` do Caddyfile preservado sem alteração de comportamento (só HTTP-01, não DNS-01); labels do Traefik mantêm `Host(${DOMAIN})` como primeira alternativa e as prioridades (20/1). Validação de login end-to-end exige infra viva (documentada no runbook §8.5).
  - [x] Confirmar IV2 (isolamento preservado): revisão de código confirma que `get_tenant_by_subdomain`/`extract_tenant_slug` não seta a GUC `app.current_tenant_id` nem substitui `get_tenant_db`/`tenant_session` — apenas `get_current_user` (via JWT) alimenta tenant_id de RLS. Item explícito no checklist de quality gate (HOSTINGER-DEPLOY.md §8.5).
  - [x] **Sem migration Alembic nesta story** — `tenants.slug` já existe, é `unique` e já é a chave usada para resolução de subdomínio. Nenhuma mudança de schema foi necessária; nenhuma migration criada (número reservado 0043 permanece livre).

## Dev Notes

### Contexto do sistema (fonte: CLAUDE.md / epic / HOSTINGER-DEPLOY.md — verificado no código)
- "White-label por subdomínio: o go-live é single-domain; wildcard `*.e1p.com` exige TLS via desafio DNS-01." [Source: docs/prd/epic-4-backlog-pos-lancamento.md#Contexto do sistema existente]
- Dívida já registrada explicitamente: "Wildcard subdomínio por tenant (`*.seudominio.com`) — exige TLS via desafio DNS-01 (Caddy suporta plugins de provedor de DNS; Cloudflare é o mais simples se você migrar o DNS pra lá). Reavaliar quando o white-label por subdomínio for pro ar." [Source: docs/HOSTINGER-DEPLOY.md#Dívida / próximos passos]
- Cada usuário/tenant já tem um `slug` único, pensado desde o início como subdomínio. O campo `Tenant.slug` (String(63), unique, index) já existe em `apps/api/app/modules/auth/models.py` linha 21 e é validado/reservado em `apps/api/app/modules/auth/schemas.py`. [Source: apps/api/app/modules/auth/models.py; apps/api/app/modules/auth/schemas.py]
- Regra de ouro nº 1 (isolamento de tenant): a GUC `app.current_tenant_id` é setada **apenas** em `tenant_session()`/`get_tenant_db()` a partir do `tenant_id` do JWT já validado — nunca de um header não autenticado. [Source: CLAUDE.md §3.1; apps/api/app/db/session.py; apps/api/app/core/tenancy.py]
- Decisão de arquitetura já registrada: "seguimos RLS como ÚNICA garantia de isolamento — o código NÃO adiciona filtro manual de tenant." Reforça por que o Host header não pode virar atalho de autorização nesta story. [Source: CLAUDE.md §6.0 nota final]

### Topologia de proxy real (verificado no código)
- **Duas topologias de produção coexistem no repo:**
  - `infra/docker-compose.prod.yml` + `infra/Caddyfile`: Caddy PRÓPRIO publicando 80/443, TLS automático via Let's Encrypt HTTP-01 (domínio único). [Source: infra/docker-compose.prod.yml; infra/Caddyfile]
  - `infra/docker-compose.traefik.yml`: topologia **realmente usada em produção** (`e1p.doroeventos.com.br`), Traefik COMPARTILHADO vivendo em `/opt/infra/proxy/` **fora deste repositório**. [Source: infra/docker-compose.traefik.yml linhas 1-14, 56-88]

### Migrations (contagem verificada)
- Maior migration existente no repo no momento deste draft: `apps/api/migrations/versions/0031_platform_audit_entries.py`. Número reservado exclusivo desta story: **0043** — NÃO foi necessário (sem mudança de schema).

### Riscos / lacunas técnicas
- **Dependência externa não versionada:** a topologia de produção real (Traefik compartilhado, Task 2) vive em `/opt/infra/proxy/`, fora deste repositório — a emissão real do certificado wildcard ali depende de mudança de config externa (certresolver DNS-01) que só pode ser documentada aqui, não implementada. Bloqueio operacional (não de código).
- **Migração de DNS para Cloudflare é pré-requisito, não coberto por código.**
- **Imagem Caddy customizada:** precisa ser buildada (via `xcaddy`/Dockerfile próprio) — pequeno aumento de complexidade de build/CI.

### Testing
- Backend: `pytest` em `apps/api/tests` (SQLite em memória). Novo `apps/api/tests/test_subdomain.py` cobre `extract_tenant_slug` (função pura). 298 testes no total (286 + 12 novos), todos verdes.
- Infra: sem suíte automatizada — validação via `docker compose config` (sintaxe, feita) + runbook de emissão de certificado (manual, exige infra viva).

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-10 | 0.1 | Draft inicial criado a partir do Epic 4 | River (@sm) |
| 2026-07-10 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-07-10 | 1.0 | Implementação completa (Tasks 1-4) — Status: Ready → InReview | Dex (@dev) |

## Dev Agent Record

### Agent Model Used
claude-opus-4-8 (Dex / @dev, modo YOLO autônomo em git worktree isolado)

### Debug Log References
- Ruff: `ruff check` nos 4 arquivos backend tocados → All checks passed (após remover aspas redundantes de anotação com `from __future__ import annotations`, UP037).
- Pytest: suíte completa `apps/api` em container `python:3.13-slim` (sem Python no host Windows) → **298 passed** in 89s (12 novos de `test_subdomain.py`).
- Validação de compose: `docker compose --env-file .env.prod -f docker-compose.prod.yml config` → OK (caddy resolve `build: ./caddy`); `docker compose --env-file .env.prod -f docker-compose.traefik.yml config` → OK (labels `HostRegexp` interpolam corretamente). `.env.prod` temporário criado a partir do `.example` só para validar sintaxe e removido em seguida (nunca commitado).

### Completion Notes List
- **IDS:** REUSE `ROOT_DOMAIN` (já em `.env.prod.example` e `config.py::root_domain`) e a tabela global `tenants`/`Tenant.slug` (já consultada em auth/service). CREATE `infra/caddy/Dockerfile`, `apps/api/app/core/subdomain.py`, `apps/api/tests/test_subdomain.py` (não havia equivalente). ADAPT `Caddyfile`, `docker-compose.prod.yml`, `docker-compose.traefik.yml`, `schemas.py`, `tenancy.py`, `HOSTINGER-DEPLOY.md`.
- **Decisão de segurança (não-negociável):** `extract_tenant_slug`/`get_tenant_by_subdomain` são apenas branding/UX; o Host header NUNCA alimenta RLS. Documentado como docstring de aviso e no `__all__`. Nenhum ponto do código usa a resolução por subdomínio para setar tenant de sessão.
- **Provedor DNS:** Cloudflare (única orientação já registrada nos docs). Segue o princípio de menor privilégio no token (Zone.DNS Edit).
- **Sem migration** — `tenants.slug` já é `unique`; 0043 permanece reservado e não utilizado.
- **[LIMITAÇÃO — precisa infra/credencial real para validação end-to-end]:**
  - IV3 (emissão/renovação do certificado wildcard) NÃO pôde ser validado: exige `CLOUDFLARE_API_TOKEN` real, DNS de `e1p.com`/`${ROOT_DOMAIN}` gerido pela Cloudflare e uma VPS viva. Runbook documentado em HOSTINGER-DEPLOY.md §8.4 para validação humana.
  - Topologia B (Traefik compartilhado): a emissão real do cert depende de config no repo externo `/opt/infra/proxy/` (certresolver DNS-01), fora do alcance desta story. Só os labels de roteamento foram ajustados aqui.
  - IV1/login end-to-end e IV2/RLS em Postgres real são validações manuais (RLS não é exercida em SQLite — padrão do projeto).
- **Sintaxe HostRegexp:** usei a forma prescrita pela story (Traefik v2, named group `{subdomain:[a-z0-9-]+}`) e documentei a variante v3 (`^[a-z0-9-]+\.${ROOT_DOMAIN}$`) em comentário, já que o header do compose não fixa a versão do Traefik.

### File List
- `infra/caddy/Dockerfile` (novo) — imagem Caddy com plugin `caddy-dns/cloudflare` via xcaddy.
- `infra/Caddyfile` (mod) — 2º bloco de site `*.{$ROOT_DOMAIN}` com `tls { dns cloudflare ... }` (DNS-01).
- `infra/docker-compose.prod.yml` (mod) — serviço caddy: `image` → `build: ./caddy`.
- `infra/docker-compose.traefik.yml` (mod) — routers `e1p-api`/`e1p-web` aceitam `HostRegexp` wildcard.
- `infra/.env.prod.example` (mod) — nova var `CLOUDFLARE_API_TOKEN` (escopo mínimo documentado).
- `apps/api/app/core/subdomain.py` (novo) — `extract_tenant_slug` + `get_tenant_by_subdomain` (branding/UX; nunca RLS).
- `apps/api/app/core/tenancy.py` (mod) — reexporta as funções de subdomínio com aviso no `__all__`.
- `apps/api/app/modules/auth/schemas.py` (mod) — `"monitor"` em `RESERVED_SLUGS`.
- `apps/api/tests/test_subdomain.py` (novo) — 12 testes de `extract_tenant_slug`.
- `docs/HOSTINGER-DEPLOY.md` (mod) — nova §8 (wildcard) + runbook IV3/IV1/IV2; dívida atualizada.

## QA Results

## Story Draft Checklist — Resultado (executado em modo autônomo por @sm)

| Categoria | Status | Notas |
|---|---|---|
| 1. Goal & Context Clarity | PASS | Objetivo, valor de negócio e vínculo com o Epic 4 explícitos. Autocontida. |
| 2. Technical Implementation Guidance | PASS | Arquivos-alvo e tecnologia (Caddy + plugin DNS Cloudflare) especificados; ponto crítico de segurança explicitado. |
| 3. Reference Effectiveness | PASS | Referências citam arquivo+seção/linha específicos. |
| 4. Self-Containment Assessment | PASS | Topologia dupla de proxy e decisão de segurança explicadas; riscos declarados. |
| 5. Testing Guidance | PARTIAL | Testes unitários da função pura claros; validação de certificado é manual/runbook (natureza de infra). |
| 6. CodeRabbit Integration (conditional) | N/A | `coderabbit_integration` disabled por instrução da missão. |

**Final Assessment: READY** — contexto suficiente para implementação; risco operacional externo sinalizado para decisão humana; decisão de segurança (não usar Host header para RLS) documentada como guard-rail não-negociável.

# Story 3.4: Monitoramento e alertas

## Status
InReview

## Executor Assignment
```yaml
executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: ["docker compose config (validação de sintaxe)", "teste manual de queda simulada (IV2)", "revisão manual de custo/topologia (IV3)"]
```
> [AUTO-DECISION] Story é Infra/CI/CD/Deploy (stack de monitoramento self-hosted, compose, cron, sem código de aplicação novo em `apps/api`/`apps/web`) → executor=@devops, quality_gate=@architect, conforme a matriz Work Type → Executor do template (`Infra/CI/CD/Deploy | @devops | @architect`). Reason: não há endpoint/UI de produto a implementar; o trabalho é 100% infraestrutura (compose, Caddyfile/Traefik labels, cron, configuração de canal de alerta).

## Story
**As a** operador do go-live,
**I want** monitorar disponibilidade (health da API, TLS, disco, restart de container) com alerta acionável,
**so that** eu saiba de uma queda antes do cliente e possa reagir.

## Acceptance Criteria
1. Há monitoramento de uptime/health da API (`/health`) e da validade do TLS (Caddy/Traefik), com alerta (ex.: Uptime Kuma + notificação).
2. Sinais básicos de host são observados: disco e reinícios de container.
3. Um alerta chega por um canal acionável (e-mail/WhatsApp/Telegram) quando um check falha.

### Integration Verification
- IV1: O monitoramento não impacta a performance da aplicação (checks leves).
- IV2: Um teste de queda simulada (parar o container) dispara o alerta.
- IV3: Custo do monitoramento respeita o guard-rail (NFR3) — solução barata/self-hosted preferida.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false` por instrução explícita do orquestrador desta missão (mesmo padrão das Stories 1.1-1.4).
> Quality validation usa processo manual (ver `quality_gate_tools` acima).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [x] **Task 1 — Subir Uptime Kuma como stack de monitoramento self-hosted (AC: 1, 2; IV3)**
  - [x] Criar `infra/docker-compose.monitoring.yml` (novo arquivo, separado de `docker-compose.prod.yml`/`docker-compose.traefik.yml` para não acoplar o release da app ao release do monitoramento — mesmo princípio 12-factor citado no Epic Goal). Serviço `uptime-kuma` (imagem `louislam/uptime-kuma:1`), volume nomeado `uptime_kuma_data:/app/data`, `restart: always`.
  - [x] **[AUTO-DECISION]** Escolher **Uptime Kuma** (não Prometheus/Grafana nem SaaS pago) porque: (a) já é a sugestão registrada em `docs/HOSTINGER-DEPLOY.md` linha 122-123 ("Dívida / próximos passos") e em `docs/prd.md` FR12/IV3 como exemplo direto; (b) é self-hosted/gratuito, único container, sem custo adicional (satisfaz NFR3 — `docs/prd.md#NFR3`); (c) tem monitor nativo de tipo **HTTP(s)** (para `/health`), **Certificate Expiry** (para validade de TLS) e **Docker Container** (para reinícios/estado de container via `docker.sock`), cobrindo AC1+AC2 sem escrever código de aplicação novo. Reason: menor custo operacional e reaproveita decisão já documentada no projeto (REUSE > CREATE, IDS).
  - [x] Montar `docker.sock` **somente leitura** (`/var/run/docker.sock:/var/run/docker.sock:ro`) no serviço `uptime-kuma` para habilitar o monitor tipo "Docker Container" (reinícios/estado dos containers `api`, `web`, `postgres`, `caddy`/proxy).
  - [x] Expor o painel do Kuma **apenas** por trás do proxy existente, nunca direto na porta do host: labels Traefik para `monitor.${DOMAIN}` (rede externa `edge`, `entrypoints=websecure`, `tls.certresolver=letsencrypt`, `middlewares=security-headers@file`) foram colocadas no próprio serviço em `docker-compose.monitoring.yml` (subdomínio dedicado). Bloco de rota `monitor.{$DOMAIN}` adicionado ao `infra/Caddyfile` para a topologia B (Caddy). **[AUTO-DECISION]** Labels ficam no serviço em `docker-compose.monitoring.yml` (NÃO em `docker-compose.traefik.yml`) para preservar a separação 12-factor exigida pela própria Task 1 — evita duplicar/acoplar a definição do serviço ao compose da app.
  - [x] **[AUTO-DECISION]** Proteger o acesso ao painel do Kuma com **autenticação própria do Uptime Kuma** (usuário/senha configurado no primeiro acesso à UI) — não expor sem login (documentado no runbook §8.1). Reason: painel mostra topologia/portas internas; não há orientação técnica específica nos docs sobre SSO/basic-auth adicional, então usar o mecanismo de auth nativo da ferramenta é a opção mais simples e já embutida (evita inventar camada extra).

- [x] **Task 2 — Configurar os checks (monitors) no Uptime Kuma (AC: 1, 2)** _(documentado como runbook em §8.2; monitors são configurados na UI/volume do Kuma, não em código)_
  - [x] Monitor **HTTP(s)**: `https://<DOMAIN>/api/health`; resposta esperada JSON `{"status":"ok","service":"e1p-api","env":"production"}` do endpoint real `apps/api/app/main.py`. Intervalo **[AUTO-DECISION]** 60s (leve, IV1). Documentado na tabela do §8.2.
  - [x] Monitor **Certificate Expiry** apontando para `https://<DOMAIN>` — documentado no §8.2 (parte "TLS" do AC1).
  - [x] Monitor(es) **Docker Container** para `api`, `web`, `postgres` e o proxy via `docker.sock` (`:ro`) — documentado no §8.2 (AC2).
  - [x] Monitor de **disco** tipo **Push** alimentado por `infra/scripts/monitoring/disk-check.sh` (novo, cron do host — mesmo padrão do backup em §6). Script lê `df` do filesystem do Docker e faz `curl` para a Push URL do Kuma (`up`/`down`).
  - [x] **[AUTO-DECISION]** Limiar de disco: 85% (configurável via `DISK_THRESHOLD`). Documentado no runbook §8.2.

- [x] **Task 3 — Canal de alerta acionável (AC: 3)**
  - [x] Notification do Uptime Kuma vinculada a todos os monitors — documentado no runbook §8.3 (configuração na UI, sem código).
  - [x] **[AUTO-DECISION]** Canal primário: **Telegram** (bot + chat id), gratuito, sem depender de SMTP (`SMTP_HOST` vazio hoje) nem da WhatsApp Cloud API (PENDENTE). Variáveis em `infra/.env.monitoring.example`.
  - [x] **Não reaproveitado** `apps/api/app/core/whatsapp.py` — nenhum arquivo de `apps/api` foi tocado. O alerta operacional vive 100% na config do Kuma (domínio infra vs. tenant).
  - [x] Documentar variáveis do canal em novo `infra/.env.monitoring.example` (`KUMA_TELEGRAM_BOT_TOKEN`, `KUMA_TELEGRAM_CHAT_ID`, `KUMA_PUSH_URL_DISK`, `DISK_THRESHOLD`) — exceção `!.env.monitoring.example` adicionada ao `.gitignore`; nunca commitar valores reais.

- [x] **Task 4 — Runbook e validação (AC: 1, 2, 3; IV1, IV2, IV3)**
  - [x] `docs/HOSTINGER-DEPLOY.md`: dívida em §7 atualizada ("implementado, ver §8") + nova seção "## 8. Monitoramento e alertas" (subir compose, monitors, notificação Telegram, limiar de disco 85%).
  - [x] Runbook de **teste de queda simulada** (IV2) documentado em §8.4 (`docker stop`/`docker start` + confirmação do alerta).
  - [x] IV1 (checks leves): intervalo de 60s no HTTP(s) sobre `/health` (não toca banco) documentado em §8.2/§8.5.
  - [x] IV3 (custo): Uptime Kuma gratuito/self-hosted no mesmo VPS, sem serviço pago novo — documentado em §8.5, referenciando NFR3.
  - [x] **Sem migration Alembic nesta story** — confirmado: nenhuma mudança de schema/dados; trabalho 100% infra. Número reservado (0038) NÃO foi usado.

## Dev Notes

### Contexto do sistema (fonte: CLAUDE.md / epic / prd)
- "não há monitoramento/alertas" é uma lacuna conhecida explicitamente listada no "Contexto do sistema existente" do Epic 3. [Source: docs/prd/epic-3-deploy-storage-observabilidade.md#Contexto do sistema existente]
- FR12: "Instrumentar monitoramento e alertas de disponibilidade: health da API (`/health`), validade do TLS (Caddy/Traefik), disco, e reinícios de container — com alerta acionável (ex.: Uptime Kuma + notificação)." [Source: docs/prd.md#FR12]
- NFR3 (guard-rail de custo): "~US$30–45/mês no plano AWS Fase A, ou 1 VPS Hostinger. Não introduzir serviço pago sem justificar (Regra de Ouro nº 4). Preferir Graviton/ARM, cache agressivo, S3 lifecycle." [Source: docs/prd.md#NFR3]
- Dívida já registrada no guia de deploy: "Monitoramento/alertas (hoje não há; considerar `docker compose logs` + algo simples tipo Uptime Kuma, ou migrar para AWS Fase B quando o tráfego justificar)." [Source: docs/HOSTINGER-DEPLOY.md#7. Diferenças em relação ao dev / Dívida]
- A VPS de produção real (`e1p.doroeventos.com.br`) roda um **Traefik compartilhado** entre vários projetos (fora deste repo, `/opt/infra/proxy/`); esta é a topologia a priorizar para o monitor real, embora `docker-compose.prod.yml` (Caddy próprio) também deva continuar suportado como opção B. [Source: infra/docker-compose.traefik.yml (comentário de cabeçalho); docs/HOSTINGER-DEPLOY.md]
- Esta story roda em paralelo com as demais do Epic 3 (3.1, 3.2, 3.3, 3.5) e possivelmente epics diferentes — não assumir que qualquer uma delas já foi implementada; a story foi redigida para ser autocontida.

### Endpoint `/health` (verificado no código)
```python
# apps/api/app/main.py:30-32
@app.get("/health", tags=["infra"])
def health() -> dict[str, str]:
    return {"status": "ok", "service": "e1p-api", "env": settings.environment}
```
Não toca banco de dados — checar leve, compatível com IV1. [Source: apps/api/app/main.py]

### Arquivos de infra relevantes (verificados diretamente no repo)
- `infra/docker-compose.prod.yml` — topologia com Caddy próprio publicando 80/443; serviços `postgres`, `api`, `web`, `caddy`; rede única `internal`.
- `infra/docker-compose.traefik.yml` — topologia real de produção (Traefik compartilhado da VPS); `api`/`web` só entram na rede externa `edge` com labels Traefik; `api` roteia por `PathPrefix(/api)` com `stripprefix`, prioridade 20; `web` é catch-all, prioridade 1; certresolver `letsencrypt`; middleware `security-headers@file`.
- `infra/Caddyfile` — `handle_path /api/* { reverse_proxy api:8000 }` e `handle { reverse_proxy web:80 }` (topologia Caddy próprio).
- `infra/.env.prod.example` — modelo de segredos de produção (`DOMAIN`, `JWT_SECRET`, `SMTP_HOST` vazio, `WHATSAPP_TOKEN`/`WHATSAPP_PHONE_ID` vazios, etc.); nenhum segredo real deve ser commitado.
- `docs/HOSTINGER-DEPLOY.md` — guia de deploy atual, com cron de backup (§6) como padrão de referência para o script de disco (Task 2), e a dívida de monitoramento (§7) que esta story resolve.
- `apps/api/app/core/whatsapp.py` — envio de WhatsApp via Cloud API, hoje "logged" sem credenciais (`settings.whatsapp_token`/`whatsapp_phone_id` vazios); é o canal **do produto para o cliente do tenant**, não deve ser reaproveitado para o alerta operacional desta story (ver Task 3).
- `apps/api/app/core/email.py` — stub de e-mail (log em dev, `SMTP_HOST` vazio hoje); mesma lógica: canal do produto, não o de infra.

### File Locations (novos/alterados previstos)
- NOVO: `infra/docker-compose.monitoring.yml`
- NOVO: `infra/scripts/monitoring/disk-check.sh`
- NOVO (opcional): `infra/.env.monitoring.example`
- ALTERAR: `infra/docker-compose.traefik.yml` (labels Traefik do `uptime-kuma`) e/ou `infra/Caddyfile` (rota do subdomínio de monitoramento) — dependendo de qual topologia for usada no ambiente-alvo.
- ALTERAR: `docs/HOSTINGER-DEPLOY.md` (nova seção "8. Monitoramento e alertas" + atualização da dívida em §7).

### Technical Constraints
- Não expor Postgres/API diretamente ao host (regra já estabelecida no guia Hostinger, §1) — o Uptime Kuma deve alcançar `/health` pela mesma rede interna/edge dos demais serviços, não por porta nova publicada.
- Não introduzir serviço pago (NFR3) — Uptime Kuma é gratuito/self-hosted; qualquer alternativa paga (Datadog, Better Uptime, etc.) precisaria de justificativa explícita, que não foi dada — por isso não foi escolhida.
- Regra de Ouro nº 1 (isolamento de tenant/RLS) não é afetada por esta story — o monitor não faz queries de negócio, apenas HTTP/TCP/Docker-socket checks.
- `docker.sock` montado **read-only** no container do Kuma — nenhuma orientação técnica adicional de hardening foi encontrada nos docs disponíveis além disso; se um hardening mais forte for exigido (ex.: `docker-socket-proxy`), fica registrado como dívida técnica.

### Testing
- Nenhuma orientação técnica específica encontrada nos docs disponíveis sobre testes automatizados para stacks de monitoramento self-hosted (não há suíte `pytest`/`vitest` aplicável — este é trabalho 100% de infraestrutura, sem código Python/TypeScript novo em `apps/api`/`apps/web`).
- Validação é manual/runbook (Task 4): (a) `docker compose -f infra/docker-compose.monitoring.yml config` para validar sintaxe do compose antes de subir; (b) subir a stack e confirmar os 2+ monitors (HTTP/Certificate/Docker Container) reportando "up"; (c) teste de queda simulada (IV2) parando um container e confirmando o alerta chega no canal Telegram configurado; (d) inspecionar visualmente que os ~252 testes existentes de `apps/api/tests` não foram afetados (nenhum arquivo de `apps/api` é tocado nesta story, então não é esperado impacto — mas rodar `scripts/check.sh` continua sendo boa prática antes de considerar a story concluída, conforme `CLAUDE.md` §5).

## Story Draft Checklist Results (River / @sm, modo autônomo)

| Category | Status | Issues |
| --- | --- | --- |
| 1. Goal & Context Clarity | PASS | — |
| 2. Technical Implementation Guidance | PASS | — |
| 3. Reference Effectiveness | PASS | — |
| 4. Self-Containment Assessment | PASS | — |
| 5. Testing Guidance | PARTIAL | Sem suíte automatizada aplicável (story 100% infra, sem código Python/TS novo); validação é runbook manual (Task 4). Aceitável dado o tipo de story, mas fica registrado como lacuna estrutural do projeto (não há testcontainers/e2e de infra hoje). |
| 6. CodeRabbit Integration (conditional) | N/A | Disabled — skip notice presente. |

**Final Assessment: READY** — a story tem contexto técnico suficiente (arquivos reais citados com linha, decisões técnicas com justificativa AUTO-DECISION, e runbook de validação) para um dev/devops agent implementar sem precisar releitura de docs externos. Único ponto de atenção: Testing Guidance é PARTIAL por natureza do trabalho (infraestrutura), não por lacuna de redação.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-10 | 0.1 | Story criada a partir do Epic 3 (Deploy, Storage & Observabilidade) | River (@sm) |
| 2026-07-10 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready | @po |

## Dev Agent Record

### Agent Model Used
Dex (@dev) — claude-opus-4-8[1m]

### Debug Log References
- `sh -n infra/scripts/monitoring/disk-check.sh` → OK (sintaxe do script válida).
- `DOMAIN=example.com docker compose -f infra/docker-compose.monitoring.yml config` → OK (Docker 29.6.1; sintaxe/topologia do compose válida — rede externa `edge` referenciada, volume `uptime_kuma_data`, `docker.sock:ro`, labels Traefik).
- `git check-ignore infra/.env.monitoring.example` → não ignorado (exceção `!.env.monitoring.example` no `.gitignore` funciona; só o `*.example` é versionado).
- `apps/api` / `apps/web` NÃO foram tocados → nenhuma suíte `ruff`/`pytest`/`tsc`/`vitest` aplicável (story 100% infra, sem código Python/TS novo). Validação é manual/runbook (§8), conforme seção "Testing" da story.

### Completion Notes List
- Story 100% infra (compose, proxy, cron, config de terceiros). Zero mudança em `apps/api`/`apps/web`; sem migration Alembic (número reservado 0038 NÃO usado).
- **[AUTO-DECISION]** Labels Traefik do `uptime-kuma` ficaram no próprio serviço em `docker-compose.monitoring.yml` (não em `docker-compose.traefik.yml`), preservando a separação 12-factor exigida pela Task 1. As File Locations previam `ALTERAR docker-compose.traefik.yml` como opção ("e/ou"); optei por manter o serviço isolado no compose de monitoramento, que é o requisito mais forte.
- Compose default = topologia A (Traefik compartilhado, produção real, `monitor.${DOMAIN}` via rede `edge`). Topologia B (Caddy próprio) coberta por: bloco `monitor.{$DOMAIN}` no `Caddyfile` + instruções no cabeçalho do compose para trocar a rede.
- **[AUTO-DECISION]** `git check-ignore` mostrou que `.env.*` capturaria `infra/.env.monitoring.example`; adicionei a exceção `!.env.monitoring.example` ao `.gitignore` (mesmo padrão de `.env.prod.example`) para que o template seja versionado.
- **Nota de ambiente (worktree):** os arquivos foram inicialmente escritos no working dir do repo principal por engano e depois migrados para este worktree (`feature/3.4-monitoring-alerts`); o repo principal foi restaurado ao estado original. O commit final contém apenas os arquivos desta story neste worktree.

#### Requer credencial/infra real para validação end-to-end (NÃO testável neste ambiente)
- **AC1/AC2 (monitors ativos):** exige subir o Kuma numa VPS com Docker + DNS `monitor.<DOMAIN>` apontado e Traefik/Caddy emitindo TLS. `docker compose config` valida a sintaxe, mas não sobe a stack aqui (sem VPS/DNS/`docker.sock` de produção).
- **AC3 (alerta chega no canal):** exige um bot Telegram real (`KUMA_TELEGRAM_BOT_TOKEN`/`KUMA_TELEGRAM_CHAT_ID`) configurado na UI do Kuma — não há credencial de Telegram neste ambiente.
- **IV2 (teste de queda simulada):** procedimento `docker stop`/`docker start` documentado no runbook §8.4; precisa da stack viva + canal configurado para confirmar a chegada do alerta ponta a ponta.
- **Monitor de disco (Push):** a Push URL só existe após criar o monitor na UI do Kuma; o `disk-check.sh` foi validado apenas por sintaxe (`sh -n`), não por execução contra um Kuma real.

### File List
**Novos:**
- `infra/docker-compose.monitoring.yml` — stack Uptime Kuma (self-hosted), labels Traefik para `monitor.${DOMAIN}`, `docker.sock:ro`, volume `uptime_kuma_data`.
- `infra/scripts/monitoring/disk-check.sh` — monitor de disco do host via Push (cron), limiar `DISK_THRESHOLD` (default 85%).
- `infra/.env.monitoring.example` — template de segredos/config do canal Telegram e do Push de disco (nunca commitar valores reais).

**Alterados:**
- `infra/Caddyfile` — bloco de rota `monitor.{$DOMAIN}` (topologia B, Caddy próprio).
- `.gitignore` — exceção `!.env.monitoring.example`.
- `docs/HOSTINGER-DEPLOY.md` — §7 (dívida atualizada) + nova seção §8 "Monitoramento e alertas" (runbook completo: subir, monitors, Telegram, teste de queda, custo).
- `docs/stories/3.4.story.md` — Status, checkboxes, Dev Agent Record, File List.

## QA Results
_(a preencher pelo @qa)_

# Story 8.9: Fundação da Regra da Origem (`sync_origin_movement` + migration aditiva)

## Status
Draft

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["ruff (apps/api)", "pytest apps/api/tests (novos test_bank_origin.py, test_bank_origin_rls.py; alterado test_money_planes.py)", "pytest -m rls_e2e (Postgres real, testcontainers — job cross-tenant-rls)", "alembic upgrade head + downgrade num Postgres real", "bash scripts/check.sh (lint+types+testes, CLAUDE.md §5) — ⚠️ rodar as etapas individualmente (X9)", "os 3 agentes de QA (regression-tester, bug-hunter, dedup-checker — CLAUDE.md §5)"]
```
> [AUTO-DECISION] Story de **backend de contrato puro** (migration aditiva + uma função + dois gates estruturais), **sem nenhum chamador e sem nenhuma tela** → `executor="@dev"`. `quality_gate="@architect"` seguindo o padrão histórico deste projeto para todo artefato do eixo financeiro-analítico (5.3–5.9, 8.2–8.8). Reason: esta story define o **ponto único de escrita** de `source ∈ SOURCES_SISTEMA` e o índice que é a garantia de idempotência de toda a Onda 2 — errar a semântica aqui contamina as Stories 8.12 a 8.18 de uma vez, e a decisão (índice único parcial em vez de `dedup_hash`, ausência de `origin_type`, hash sem a conta) é arquitetural, não de implementação.

## Story
**As a** dono da empresa de 1 pessoa cujo razão bancário nasceu vazio porque nenhum evento do sistema escrevia nele,
**I want** que o e1p ganhe **uma única função** capaz de manter um movimento bancário coerente com o lançamento de negócio que o originou — e o índice que impede esse movimento de existir duas vezes,
**so that** as stories seguintes (baixa de Contas a Pagar, recebimento fora do trilho, transferência, payout) possam **ligar os eventos que o sistema já conhece** sem que cada uma invente o seu próprio caminho de escrita.

> **Esta story entrega o CONTRATO, não o comportamento.** Ao final dela, `sync_origin_movement`
> existe, é testada e **não tem um único chamador em produção**. Isso é deliberado (epic §6, critério
> de corte (iii): *"migration e backend de contrato vêm antes de qualquer chamador"*).

## Acceptance Criteria

1. **Migration aditiva encadeada no head REAL, confirmado programaticamente.** Uma migration nova
   (esperada **0061**, mas o número é **confirmado**, nunca fixado — ver Task 1) com
   `down_revision = "0060"`. Ela é **estritamente aditiva**: só `ADD COLUMN` nullable e `CREATE INDEX`.
   **Zero backfill, zero `UPDATE`, zero linha lida ou escrita.** Colunas:
   - `bank_transactions.origin_id VARCHAR(64) NULL`
     > ⚠️ **`VARCHAR(64)`, não 36 — RATIFICADO pela @architect em 2026-07-30**
     > ([`controle-bancario-onda2-ratificacao.md`](../architecture/controle-bancario-onda2-ratificacao.md)
     > §C-3.3, bloqueio 1 da §8). O bloco ilustrativo do design dizia 36 porque escrevia `origin_id`
     > como *"o id do lançamento"*; a §3.2 o **redefiniu como CHAVE DE ORIGEM**, e para origem de
     > múltiplas pernas ela é `f"{id}:{perna}"` — `uuid4` (36) + `":out"` (4) = **40** (Story 8.18).
     > A razão da folga é **assimetria de custo, não conforto**: em Postgres `VARCHAR(n)` é
     > armazenamento variável (64 e 36 custam o mesmo em disco; o `n` é restrição, não reserva),
     > enquanto errar para menos custa `ALTER COLUMN` sobre tabela com dado sob `FORCE ROW LEVEL
     > SECURITY` — a armadilha da 0046 que o ADR 0003 nomeia como o único ponto desse tipo do épico.
     > **Esta largura tem de entrar antes de a migration ser escrita.**
   - `payables.bank_account_id VARCHAR(36) NULL` e `payables.bank_transaction_id VARCHAR(36) NULL`
   - `charges.bank_account_id VARCHAR(36) NULL` e `charges.bank_transaction_id VARCHAR(36) NULL`
   **Sem FK dura** em nenhuma delas — padrão do projeto (`charges.client_id`, `payables.cost_center_id`):
   integridade no service, sob RLS. As três tabelas **já têm** RLS `FORCE` (0011, 0010, 0059); a
   migration **não** mexe em policy nenhuma.
   → design Onda 2 §3.2, §3.3, §3.4; epic itens 2.1, 2.2, 2.3

2. **O índice único parcial — e é ele, não o `dedup_hash`, a garantia de idempotência.**
   ```sql
   CREATE UNIQUE INDEX uq_bank_transactions_origin
       ON bank_transactions (tenant_id, source, origin_id)
       WHERE origin_id IS NOT NULL;
   ```
   `tenant_id` é a **primeira** coluna porque índice único é global e **não respeita RLS** — sem isso
   o tenant B receberia uma violação inexplicável por causa de um dado do tenant A, que é bug **e**
   vazamento de existência (mesma lição que a Story 8.2 já pagou em `uq_bank_accounts_tenant_ident`).
   Mais o índice de leitura `ix_payables_bank_account` em `(tenant_id, bank_account_id)`.
   **Teste obrigatório:** duas linhas com o mesmo `(tenant_id, source, origin_id)` → o banco recusa a
   segunda; duas linhas com `origin_id IS NULL` na mesma conta → **ambas passam** (é o `WHERE` parcial
   fazendo o trabalho; sem ele, todo movimento manual colidiria com todo outro movimento manual).
   ⚠️ Este teste **só vale no Postgres** (índice parcial não existe no SQLite dos unitários) → vive em
   `test_bank_origin_rls.py`, marcado `pytest.mark.rls_e2e`.
   → design Onda 2 §3.2; epic §4.8(b)

3. **`SOURCES` ganha `payable` e `charge`; nascem os DOIS conjuntos.** Em `bank/models.py`:
   ```python
   SOURCE_PAYABLE = "payable"   # NOVO — baixa de Contas a Pagar
   SOURCE_CHARGE  = "charge"    # NOVO — recebimento fora do trilho
   SOURCES_SISTEMA = (SOURCE_PAYABLE, SOURCE_CHARGE, SOURCE_TRANSFER, SOURCE_YIELD, SOURCE_PAYOUT)
   SOURCES_EXTERNA = (SOURCE_MANUAL, SOURCE_OFX, SOURCE_CSV)
   ```
   `SOURCES` continua existindo e passa a ser exatamente `SOURCES_EXTERNA + SOURCES_SISTEMA` — com
   **teste** de que a união é a tupla inteira e a interseção é vazia (senão um valor novo pode entrar
   em um conjunto e sumir do outro em silêncio). `source` continua `String(16)`: `"payable"` (7) e
   `"charge"` (6) cabem — **sem migration de tipo**.
   **Regra normativa desta story em diante:** nenhuma regra do repositório é escrita contra um valor
   **solto** de `source`; toda regra é escrita contra `SOURCES_SISTEMA` / `SOURCES_EXTERNA`.
   → design Onda 2 §3.1; epic §4.8

4. **A Invariante da Origem, testada nas DUAS direções.**
   > `source ∈ SOURCES_SISTEMA` ⟺ `origin_id IS NOT NULL`
   `test_origem_do_sistema_sempre_tem_origin_id` verifica os dois sentidos: (a) nenhum movimento com
   `source ∈ SOURCES_SISTEMA` tem `origin_id` nulo; (b) nenhum movimento com `source ∈ SOURCES_EXTERNA`
   tem `origin_id` preenchido. Testar só um lado deixa passar exatamente o estado que a Onda 4
   (importação) vai criar por acidente. A invariante é **aplicada no service** (não por
   `CheckConstraint`), no padrão do projeto — integridade no service, onde ela pode explicar.
   → design Onda 2 §3.1; epic §4.8

5. **`bank/origin.py::sync_origin_movement` — a ÚNICA função do repositório que escreve
   `source ∈ SOURCES_SISTEMA`.** Assinatura **canônica** (é contrato consumido pelas Stories
   8.12, 8.15, 8.18 e, fora desta onda, pelas 2b e 3):
   ```python
   # apps/api/app/modules/bank/origin.py — CONTRATO
   def sync_origin_movement(
       db: Session, *, tenant_id: str, actor: str,
       source: str,                    # ∈ SOURCES_SISTEMA — 422 fora disso
       origin_id: str,
       bank_account_id: str | None,    # None ⇒ o lançamento não está mais liquidado ⇒ APAGA
       posted_at: date | None,
       amount_cents: int | None,       # COM SINAL: − para payable, + para charge
       description: str,
       counterparty_name: str = "",
       counterparty_document: str = "",
       operation_nature: str | None = None,
       transfer_id: str | None = None,   # pareia pernas irmãs; só `source='transfer'` usa nesta onda
   ) -> BankTransaction | None: ...
   ```
   > ⚠️ **`transfer_id` é kwarg desta função, e isso é decisão desta validação, não do design.**
   > **[CORREÇÃO DO @PO, 2026-07-30]** O design §8 (e a ratificação §C-3.1) exigem que as **duas
   > pernas** de uma transferência nasçam com `transfer_id = transfer.id`; o contrato da §3.5 não
   > previa por onde esse valor entra. A Story 8.18 registrava a lacuna como uma **disjunção não
   > decidida** (*"que grava `transfer_id` **ou** que esta story o grava logo depois"*) — e a segunda
   > metade dessa disjunção **contradiz a docstring normativa desta própria AC**: gravar depois é um
   > segundo escritor da mesma linha, e é exatamente o que torna a Regra da Origem inauditável.
   > Um kwarg fecha a lacuna sem abrir caminho novo. `transfer_id` é `VARCHAR(36)` e **já existe** na
   > tabela (`bank/models.py:278`), declarada no design-mãe como *"preenchido quando `source in
   > (transfer, yield, payout)`"* — **nenhuma migration é necessária para ele.**
   > **Membro:** as duas pernas da 8.18, com `transfer_id` preenchido e `origin_id` sufixado.
   > **Não-membro:** o movimento de `payable` da 8.12 — perna única, `transfer_id=None`.
   > ⚠️ Se a @architect preferir outra forma, é ela que decide; o que **não** pode ficar é a disjunção.
   Comportamento, os três casos e nada além deles:
   - **ausente → cria** — `status='matched'` (nasce conciliado: o e1p originou os dois lados, não há
     julgamento a fazer), `origin_id` preenchido, `raw_description = description`,
     `fitid=None`, `import_batch_id=None`, `dedup_hash` do AC6;
   - **presente → atualiza** a **mesma linha** (conta, data, valor, descrição, contraparte,
     `dedup_hash`). **Move, nunca duplica** — os dois saldos derivados se corrigem sozinhos porque são
     derivados;
   - **`bank_account_id is None` → apaga** (DELETE), sob a guarda do AC8.
   **Não commita**, pelo mesmo motivo de `build_payable`/`apply_paid`/`build_charge`: o movimento e o
   lançamento entram na **mesma transação**. Um dos dois sem o outro é o estado que esta story existe
   para tornar impossível. Faz `db.flush()` antes de `audit.record` (o `id` tem default Python-side —
   sem o flush a trilha nasceria com `target=''`, o defeito MNT-001 que o módulo `bank` **já evita**).
   → design Onda 2 §3.5; epic §4.8, item 2.4

6. **`dedup_hash` de origem de sistema = `sha256(f"{source}|{origin_id}")` — SEM a conta.** Helper
   público `bank/origin.py::origin_dedup_hash(source: str, origin_id: str) -> str`
   **[SUPOSIÇÃO DO @SM: o nome da função; a fórmula é contrato do design §3.2]**.
   A ausência de `bank_account_id` na fórmula é **deliberada e testável**: trocar a conta de um
   lançamento é **UPDATE da mesma linha**, não uma linha nova — se o hash carregasse a conta, a troca
   exigiria reidratá-lo e a operação deixaria de ser um UPDATE puro.
   **Teste:** `origin_dedup_hash` é **estável sob troca de conta** (mesmo `source`+`origin_id`, contas
   diferentes → mesmo hash). ⚠️ Não confundir com `service._manual_dedup_hash`, que **chaveia na
   conta e no UUID da linha** e continua valendo para `SOURCES_EXTERNA` — são duas variantes com
   contratos diferentes, e **não** se "harmoniza" uma com a outra retroativamente (seria migration
   com backfill sob `FORCE RLS`, a armadilha da 0046).
   → design Onda 2 §3.2

7. **Regra de autoridade escrita no código, não só no design.** Docstrings normativas:
   - `payables.bank_account_id` / `charges.bank_account_id` = **decisão do usuário, autoritativa**;
   - `bank_transactions.bank_account_id` = **derivada** dela pelo sincronizador, escrita por
     **nenhum outro caminho**;
   - `payables.bank_transaction_id` / `charges.bank_transaction_id` = **cache de leitura**. Se
     divergir do movimento cujo `origin_id = payable.id`, **quem manda é o `origin_id`**.
   > ⚠️ **`python -m app.scripts.bank_audit` NÃO EXISTE — a obrigação desta story é TESTE, não
   > script. RATIFICADO pela @architect em 2026-07-30**
   > ([`controle-bancario-onda2-ratificacao.md`](../architecture/controle-bancario-onda2-ratificacao.md)
   > §C-4, bloqueio 2 da §8). **Verificado nesta validação:** `grep -rn "bank_audit" apps/` → **zero
   > ocorrências**; `apps/api/app/scripts/` contém `__init__.py`, `migrate_attachments_to_s3.py` e
   > `scan_orphan_storage.py`, e nada mais. A versão 0.1 desta story mandava o @dev *"acrescentar uma
   > varredura"* a um arquivo inexistente — ele criaria o script inteiro em silêncio (escopo
   > inventado, Art. IV) ou pararia. Nos dois casos, um ciclo perdido.
   >
   > **O que fica no lugar, e por que um teste basta aqui:** a divergência entre o cache e o
   > `origin_id` só é alcançável **por bug**. `sync_origin_movement` é o **único** escritor do
   > movimento, devolve a linha na mesma chamada e na mesma transação, e o chamador grava o cache com
   > o que recebeu — não há segundo caminho, não há concorrência, não há materialização assíncrona.
   > **Condição alcançável só por bug se prova com teste, não com script; um script que ninguém tem
   > gatilho para rodar não é garantia, é intenção documentada.**
   >
   > **Obrigação desta story:** `test_cache_de_movimento_nunca_diverge_do_origin_id`, exercitando os
   > **cinco** caminhos de mutação — **baixar, trocar conta, trocar data, estornar, repagar** — e
   > afirmando em cada um que `payable.bank_transaction_id` aponta para o movimento com
   > `origin_id = payable.id`, **ou** que os dois são `NULL`.
   > ⚠️ Os cinco caminhos só passam a existir na **8.12**; nesta story o teste os exercita chamando
   > `sync_origin_movement` diretamente e escrevendo o cache à mão, e a **8.12 o estende** para os
   > caminhos reais. **A regra de autoridade nas docstrings fica** — ela é correta e é o que importa.
   >
   > O **script** volta a ser necessário na **Onda 5**, e como **pré-requisito dela**: é lá que existe
   > `_refresh_status` (**também inexistente hoje** — `bank/service.py:826` o descreve como trabalho
   > da onda de conciliação; verificado nesta validação) e é lá que a divergência passa a ser
   > alcançável **sem bug**, por matcher concorrente e vínculo parcial.
   → design Onda 2 §3.3 (corrigida); ratificação §C-4; epic item 2.2

8. **`origin_type` NÃO é criado, e `payment_route` NÃO é criada.** Para todo valor de
   `SOURCES_SISTEMA`, `source` já responde *"qual tipo de lançamento"*; um segundo campo dizendo a
   mesma coisa seria a terceira encarnação do defeito D-3. Idem para a rota de pagamento: ela é
   **derivada** de `transaction_id`/`bank_account_id`, nunca rotulada. Esta story **cria os dois
   ponteiros** que a Invariante do Trilho (§4.9) usará na 8.15 — mas **não** implementa a invariante:
   sem `settle_off_rail` não há charge fora do trilho para varrer.
   **Teste de ausência:** varredura AST/texto reprovando `origin_type` e `payment_route` em
   `app/modules/**` (é barato, e a "melhoria" é óbvia para quem chegar depois).
   → design Onda 2 §3.2, §3.4; epic §4.9

9. **A guarda do DELETE, escrita AGORA porque escrevê-la depois é tarde.** O caminho de apagar
   (`bank_account_id=None`) só apaga enquanto a linha for **puramente sintética**:
   `source ∈ SOURCES_SISTEMA` **e** `fitid IS NULL` **e** `import_batch_id IS NULL`. Se a linha já
   tiver sido **enriquecida** pela importação (Onda 4), o sincronizador **não apaga**: ele **desliga a
   origem** — `origin_id = NULL`, `source = SOURCE_OFX`, `status = STATUS_UNMATCHED` — e a linha volta
   a ser um movimento órfão do extrato, o que é **verdade** (o dinheiro saiu mesmo; o sistema é que
   não sabe mais por quê). Degradação honesta.
   Hoje esse ramo é **inalcançável** (não existe importação) — e é exatamente por isso que ele entra
   agora, com teste que monta a linha enriquecida à mão. Escrevê-lo na Onda 4 significaria descobrir
   a regra depois de já ter perdido dado bancário real.
   → design Onda 2 §4.5; epic item 2.11

10. **Gate estrutural novo: `bank` NÃO importa `payables`/`receivables`.** `test_money_planes.py`
    ganha `test_bank_nao_importa_payables` — a **asserção positiva** que fecha o §1.3d: a dependência
    é **de negócio para banco, nunca a volta**. `payables`/`receivables` **podem** importar
    `app.modules.bank` (e vão, a partir da 8.12); `app.modules.bank` **nunca** importa
    `payables`/`receivables`.
    Reusar `_imported_modules` (o helper por AST já existente) **mais** a varredura de texto cru — os
    dois, pelo mesmo motivo que a 8.2 documentou e que o re-gate provou por mutação: `from app.modules
    import payables` é a forma evasiva, e ela precisa casar pelo alias do `ImportFrom` (TEST-001).
    ⚠️ **Consequência de projeto que esta story trava:** o aviso pró-ativo da Story 8.11 e qualquer
    leitura de `payables` a partir do módulo `bank` são **proibidos** — quem precisa cruzar os dois
    lados faz isso do lado do negócio.
    → design Onda 2 §3.5; epic §4.1(d), item 2.4

11. **Isolamento validado no Postgres real.** `apps/api/tests/test_bank_origin_rls.py`
    (`pytest.mark.rls_e2e`, testcontainers, `alembic upgrade head` rodado como o papel
    **não-superusuário** `e1p_app`): `sync_origin_movement` do tenant A nunca alcança movimento,
    `payable` ou `charge` do tenant B; escrita com `tenant_id` de outro tenant é barrada pelo
    `WITH CHECK`; sem a GUC `app.current_tenant_id` a leitura é fail-closed; **e o índice único
    parcial do AC2 é exercitado aqui** (não existe no SQLite).
    → epic §4.7; job `cross-tenant-rls` do CI

12. **`CLAUDE.md` §Financeiro reconciliado com a numeração nova (conflito X1) — RESÍDUO REDUZIDO A
    UM ITEM.**
    > ⚠️ **[CORREÇÃO DO @PO, 2026-07-30] Dois dos três itens desta AC já estavam FEITOS quando a
    > story foi escrita, e mandá-los de novo induziria o @dev a "corrigir" texto correto.**
    > Verificado nesta validação contra o arquivo e contra o histórico: o commit **`0474808`**
    > (*"docs: fecha o residuo da renumeracao de ondas no CLAUDE.md"*) já fechou os dois — a mensagem
    > dele nomeia exatamente *"regra 1 dizia payout = Onda 6; agora e Onda 3"* e *"regra 7 repetia a
    > leitura de gate que a §3.1.1 do epic invalidou"*. Hoje `CLAUDE.md:223` (regra 1) diz **"Onda 3"**
    > e a regra 7 já carrega a correção do gate. **Não reabrir.**

    **O único resíduo que sobra, e que esta story fecha:** o título da seção
    (`CLAUDE.md:216`) diz *"Financeiro: Controle Bancário e Conferência (Epic 8 — **Ondas 0 e 1**)"* →
    passa a nomear também a **Onda 2 em andamento**.
    ⚠️ `CLAUDE.md` é **memória viva do projeto**, não documento de PM: a edição é do @dev, é pequena,
    e é a única mudança fora de `apps/api/` nesta story. **Se ao abrir o arquivo você encontrar as
    regras 1 e 7 já corretas, isso é o esperado** — registre no Dev Agent Record e siga.
    → epic §11.6 X1; commit `0474808`

### Integration Verification

- **IV1 — DRE e Lucratividade intactas.** `dre.py` agrega exatamente `charges` + `payables` +
  `transactions` (`dre.py:51,135-156`) e filtra `status != canceled` nas 4 agregações. Esta story
  **não cria status novo, não muda status de nada e não escreve em `charges`/`payables`** — só
  acrescenta colunas nullable que nascem `NULL`. Teste de snapshot: `dre_report(start, end)` idêntico
  **campo a campo** antes e depois de a migration rodar e de `sync_origin_movement` criar um
  movimento. `profitability.py` deriva da DRE → coberto pelo mesmo teste.
- **IV2 — Projeção de Caixa intacta.** `projection.py` não é tocado. Um movimento criado por
  `sync_origin_movement` com `posted_at <= hoje` **entra** no `active_balance_total(until=today)` —
  o que é correto e é o objetivo do épico — mas nesta story **nenhum chamador existe**, logo a
  resposta de `GET /financial-intelligence/projection` é **byte a byte a mesma**. Teste explícito
  (é o acoplamento acidental mais provável).
  ⚠️ **[CORREÇÃO DO @PO, 2026-07-30] A dependência da 8.19 escrita aqui está DESATUALIZADA.** A
  versão **0.2** da Story 8.19 teve a premissa **refutada pelo fundador** (*"é o saldo real hoje"*) e
  **removeu `projection.py` do escopo** — o AC6 dela diz *"A Projeção de Caixa NÃO é tocada. Nada."*
  e `projection.py` está na lista **NÃO TOCAR**. Logo: **a 8.19 não corrige `_saldo_inicial`, e
  `ORIGEM_MISTO` pela mera existência de conta permanece — corretamente**, porque a Projeção está
  certa. Esta story continua **não tocando `projection.py`**; o que muda é que não há conserto
  alheio a esperar, e **a 8.19 não é pré-requisito de nada desta onda**.
- **IV3 — Cockpit intacto.** `cockpit/service.py:130` segue calculando `net_revenue` a partir da
  Carteira; nada aqui o alcança.
- **IV4 — Fila de Pagamentos e Contas a Pagar/Receber intactas.** `payables/service.py`,
  `payables/router.py`, `payables/receipts.py` e `receivables/service.py` **não são editados** nesta
  story (só `models.py` de cada um, e só para as colunas nullable). `POST /payables/bills/{id}/pay`
  continua funcionando **sem** `bank_account_id` — quem o torna obrigatório é a 8.12.
  `FilaPagamentosPage` e `PagarPage` seguem intocadas.
- **IV5 — Carteira intacta.** `wallet_summary` inalterado; nenhum arquivo de `app/modules/wallet/` é
  editado. `test_wallet_nao_importa_bank` (8.2) continua passando, e o AC10 acrescenta a asserção
  simétrica **sem** afrouxar a existente.
- **IV6 — Conferência e Diagnóstico intactos.** `bank/reconciliation.py` **não é editado**. O
  relatório de conferência segue lendo `derived_balance(until=<reference_date>)` (regra 6 do
  `CLAUDE.md`), e a **nota do gate** no bloco 4 é escopo da 8.16, não desta.
  `financial_intelligence/engine.py` continua **puro** — esta story não lhe entrega dado novo.
- **IV7 — Contas & Saldos intacta.** `GET /bank/accounts` e as telas `/financeiro/contas` e
  `/financeiro/conferencia` não mudam de comportamento. **Nenhum arquivo de `apps/web/` é tocado**
  nesta story (frontend segue 304/50).
- **IV8 — Exclusão de conta (LGPD) continua cobrindo.** `platform.delete_account` purga
  dinamicamente as subclasses de `TenantMixin`; esta story **não cria tabela**, só colunas, então a
  purga segue cobrindo por construção — mas o teste de `delete_account` com movimento de origem
  presente é **verificado**, não assumido.
- **IV9 — Zero regressão.** Baseline: backend **1051 passed, 1 skipped**; `rls_e2e` **22**; frontend
  **304 / 50**. Ao final: os mesmos números **+ os testes novos**, zero falha nova.
  `alembic upgrade head` **e** o `downgrade` aplicam limpo num Postgres real.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false`, mesma decisão já documentada nas Stories 5.7, 6.1, 6.2, 7.5 e 8.2–8.8.
> Quality validation will use manual review process only (ver `quality_gate_tools` acima e `scripts/check.sh`).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [ ] **Task 1 — Confirmar o head das migrations ANTES de escrever uma linha (AC: 1)**
  - [ ] Rodar, com o Python do venv (`apps/api/.venv`), **não** o do PATH:
        `ScriptDirectory.from_config(Config("alembic.ini")).get_heads()`
  - [ ] Confirmar que o retorno é **uma tupla de UM elemento**. Se vierem dois, **PARE**: há
        múltiplos heads e `alembic upgrade head` já está quebrado — a suíte `rls_e2e` inteira cai
        junto, não só esta story
  - [ ] Encadear `down_revision` no head **retornado**, nunca no número que este documento supõe.
        Head em 2026-07-30: `['0060']` → esperado `0061`
  - [ ] Registrar no cabeçalho da migration que o head foi **confirmado programaticamente**, com a
        data — no formato que a `0060_bank_balance_checkpoints.py` estabeleceu
  - [ ] ⚠️ Anotar no Dev Agent Record: a Story **8.18** (outro @sm, transferências) também tem
        migration própria e o epic §6.1 manda mergeá-la **por último** justamente para não disputar
        head com esta

- [ ] **Task 2 — A migration aditiva (AC: 1, 2)**
  - [ ] 5 `ADD COLUMN` nullable, nas 3 tabelas. **Nenhum `UPDATE`, nenhum `SELECT`**
  - [ ] `CREATE UNIQUE INDEX uq_bank_transactions_origin ... WHERE origin_id IS NOT NULL`, com
        `tenant_id` como **primeira** coluna
  - [ ] `CREATE INDEX ix_payables_bank_account ON payables (tenant_id, bank_account_id)`
  - [ ] `downgrade()` simétrico (drop dos índices e das colunas)
  - [ ] Copiar do cabeçalho da `0060` o **aviso** sobre a armadilha do backfill sob `FORCE RLS`
        (0046) — declarando que esta migration **não faz backfill** e por isso não é atingida. Copiar
        a estrutura é correto; copiar a ausência de disciplina, não
  - [ ] Aplicar `upgrade` **e** `downgrade` contra um Postgres real antes de seguir

- [ ] **Task 3 — Vocabulário: `SOURCES_SISTEMA` / `SOURCES_EXTERNA` (AC: 3)**
  - [ ] `SOURCE_PAYABLE`, `SOURCE_CHARGE` + as duas tuplas em `bank/models.py`
  - [ ] `SOURCES` passa a ser derivada das duas (união), nunca uma terceira lista escrita à mão
  - [ ] Teste: união == `SOURCES`, interseção == vazio, `len` bate
  - [ ] Docstring normativa: *"toda regra deste módulo é escrita contra os CONJUNTOS, nunca contra um
        valor solto de `source`"* — com o porquê (o eixo duplo já está em produção na 0059 e **não
        será consertado**: exigiria reescrever coluna com dado sob `FORCE RLS` por benefício estético)

- [ ] **Task 4 — Colunas nos models + regra de autoridade (AC: 1, 7)**
  - [ ] `BankTransaction.origin_id`; `Payable.bank_account_id` / `.bank_transaction_id`;
        `Charge.bank_account_id` / `.bank_transaction_id`
  - [ ] Docstrings de autoridade nos 3 lugares (autoritativa / derivada / cache de leitura)
  - [ ] Nenhum `setattr` genérico ganha acesso a esses campos — a guarda é dupla **de propósito**
        (o campo não existe no schema de entrada **e** a função não faz `setattr` genérico), mesma
        disciplina do `update_transaction` do módulo `bank`

- [ ] **Task 5 — `bank/origin.py::sync_origin_movement` (AC: 5, 6)**
  - [ ] Arquivo **novo** `apps/api/app/modules/bank/origin.py`
  - [ ] `origin_dedup_hash(source, origin_id)` — `sha256(f"{source}|{origin_id}")`, **sem a conta**
  - [ ] Os 3 ramos: cria / atualiza a mesma linha / apaga. **Sem commit.** `db.flush()` antes do
        `audit.record`
  - [ ] 422 quando `source ∉ SOURCES_SISTEMA` — a função **não** é uma porta genérica de escrita
  - [ ] Validar `posted_at` contra a `opening_date` da conta de destino (reusar a guarda existente do
        service; **não** duplicar a fórmula) e devolver erro **acionável** — quem transforma isso em
        422 de API é a 8.12, mas a mensagem nasce aqui
  - [ ] Docstring declarando, em voz alta: **"é a ÚNICA função do repositório que escreve
        `source ∈ SOURCES_SISTEMA`; qualquer segundo caminho torna a Regra da Origem inauditável"**

- [ ] **Task 6 — A guarda da linha puramente sintética (AC: 9)**
  - [ ] No ramo de apagar: `fitid IS NULL AND import_batch_id IS NULL` → **DELETE**
  - [ ] Caso contrário → **desliga a origem** (`origin_id=None`, `source=SOURCE_OFX`,
        `status=STATUS_UNMATCHED`) e **preserva a linha**
  - [ ] Teste que monta a linha enriquecida **à mão** (hoje não há importação que a produza) e prova
        os dois ramos
  - [ ] Comentário no código dizendo que o segundo ramo é **inalcançável hoje** e por que mesmo assim
        ele existe agora

- [ ] **Task 7 — Os gates estruturais (AC: 4, 8, 10)**
  - [ ] `test_bank_nao_importa_payables` em `test_money_planes.py` (AST **e** texto cru, reusando
        `_imported_modules` com o alias do `ImportFrom` — a evasão que o TEST-001 achou)
  - [ ] `test_origem_do_sistema_sempre_tem_origin_id` — **as duas direções**
  - [ ] `test_origin_id_cabe_na_coluna` — **normativo (ratificação §C-3.3)**: para **cada** forma de
        chave de origem construída no repositório (`payable.id`, `charge.id`, `f"{transfer.id}:out"`,
        `f"{transfer.id}:in"`), afirmar `len(chave) <= <largura declarada no model>`. Uma origem de
        várias pernas nova reprova **em CI**, não no `ALTER COLUMN` sob `FORCE RLS`
  - [ ] Varredura reprovando `origin_type` e `payment_route` em `app/modules/**`
  - [ ] Verificar que `test_wallet_nao_importa_bank` (8.2) **continua** passando sem afrouxamento

- [ ] **Task 8 — O teste que substitui o script inexistente (AC: 7)**
  - [ ] ⚠️ **NÃO criar `app/scripts/bank_audit.py`.** Ele não existe, nunca existiu, e o epic que
        mandava *"não recriar"* está errado (escalado ao @pm — ratificação §3.1 E-1). Se você se
        pegar escrevendo um script aqui, **pare e releia o AC7**
  - [ ] `test_cache_de_movimento_nunca_diverge_do_origin_id` em `test_bank_origin.py`, cobrindo os
        **cinco** caminhos de mutação: baixar, trocar conta, trocar data, estornar, repagar
  - [ ] Em cada um: `payable.bank_transaction_id == <movimento com origin_id = payable.id>.id`
        **ou** os dois `NULL`. Nunca um preenchido e o outro não
  - [ ] Nesta story os caminhos são exercitados chamando `sync_origin_movement` direto e escrevendo o
        cache à mão (não existe chamador ainda); anotar no Dev Agent Record que a **8.12 estende**
        este teste para os caminhos reais
  - [ ] Registrar no Dev Agent Record que o script fica como **pré-requisito da Onda 5**, junto de
        `_refresh_status` — os dois inexistentes hoje

- [ ] **Task 9 — Testes de isolamento no Postgres real (AC: 2, 11)**
  - [ ] `apps/api/tests/test_bank_origin_rls.py`, `pytest.mark.rls_e2e`, testcontainers
  - [ ] O índice único parcial (as duas metades: colide com `origin_id`; **não** colide com `NULL`)
  - [ ] Cross-tenant: A não alcança o movimento/`payable`/`charge` de B; `WITH CHECK`; fail-closed
        sem GUC

- [ ] **Task 10 — `CLAUDE.md` §Financeiro (AC: 12)**
  - [ ] Nomear a **Onda 2 em andamento** no título da seção (`CLAUDE.md:216`)
  - [ ] ⚠️ **Conferir, e NÃO editar:** regra 1 (payout = **Onda 3**) e regra 7 (leitura do gate) já
        foram corrigidas pelos commits `c9a20f4` e **`0474808`**. Se estiverem corretas, registre a
        conferência no Dev Agent Record e **não toque nelas**
  - [ ] **Não** reescrever nada além do título

- [ ] **Task 11 — Fechamento (AC: todos)**
  - [ ] Rodar as etapas de `scripts/check.sh` **individualmente** (X9: o script mascara falha de
        frontend com `|| true` e resolve `ruff`/`python` do PATH)
  - [ ] Confirmar backend `1051 + novos`, `rls_e2e 22 + novos`, frontend **304/50 inalterado**
  - [ ] Os 3 agentes de QA (`regression-tester`, `bug-hunter`, `dedup-checker`)
  - [ ] Preencher a File List e o Dev Agent Record

## Dev Notes

### Por que a Onda 2 existe — e por que esta story vem primeiro

O fundador, ao ver as Ondas 0 e 1 já em produção:

> *"o que vc tem que entender, que é um sistema integrado, não tem o motivo de tudo começar do zero."*

O módulo `bank` nasceu com **lançamento manual como porta primária**, quando `payables` **já carrega a
informação**: valor, data, fornecedor, plano de contas e centro de custo. Falta só *de qual conta o
dinheiro saiu*. **Confirmado por leitura de código:** `payables` tem **zero** referência a `bank`
(grep de `apps/api/app/modules/payables/`; design Onda 2 §13). Em produção: **45 `payables` pagas, 0
`charges`, saldo derivado R$ 0,00** — e o único caminho para encher o razão seria **redigitar 45
contas** como movimento manual. Digitação dupla é exatamente o peso que este produto promete não impor.

A @architect nomeia a falha e a regra de método que fica (design Onda 2 §1.1(4)):

> **Antes de desenhar a porta de entrada de um plano de dados novo, enumere TODOS os eventos que o
> sistema já emite e que significam um fato desse plano. Ligue-os primeiro. A porta manual e a
> importação existem para o resíduo — e só o resíduo justifica o custo delas.**

Esta story é a **infraestrutura dessa ligação**. Ela não liga nada ainda — de propósito.

### O que esta story NÃO faz, e por que a lista importa

| Não faz | Onde está |
|---|---|
| Mudar `apply_paid` | **8.12** |
| Tocar em qualquer tela | **8.13** (e 8.14, 8.17) |
| `settle_off_rail` / Invariante do Trilho | **8.15** (esta story só cria os dois ponteiros) |
| Estado `scheduled` | **8.14** |
| Corte de data (`until=None` = hoje) | **8.10** |
| Guarda do `opening_date` | **8.11** |
| Qualquer backfill | **nenhuma story** — o backfill das 45 é **manual** (F8), e o único backfill do épico ficou na Onda 2b |
| Consertar a mistura de eixos de `source` | **nunca** — decisão registrada (design §3.1) |

### A idempotência é o ÍNDICE, não o `dedup_hash`

É o ponto mais fácil de errar desta story, e ele já foi errado uma vez no desenho.

`dedup_hash` foi criado (0059) como *"mecanismo universal de idempotência"* para o pipeline de
**importação**. Ele não serve aqui: `service._manual_dedup_hash` chaveia no **UUID da própria linha**,
que é único por construção e portanto **nunca** deduplica coisa nenhuma. A garantia que o requisito do
fundador pede — *preencher a conta duas vezes, reprocessar a mesma baixa, um retry de request* — vem
do **banco**, pelo índice único parcial `(tenant_id, source, origin_id) WHERE origin_id IS NOT NULL`.
Fail-closed, no espírito da RLS.

E a fórmula do hash de origem-sistema é `sha256(f"{source}|{origin_id}")` **sem `bank_account_id`**
por uma razão operacional, não estética: **trocar a conta é UPDATE da mesma linha**. Com a conta no
hash, a troca exigiria reidratá-lo, e o que é uma correção viraria uma recriação.

### Por que coluna direta e não `bank_reconciliations` — e por que isso não contradiz o design-mãe

O design-mãe §2.3 **rejeitou** a coluna direta em favor da tabela de ligação, por causa de **N:N** (um
Pix quitando duas contas) e do estado **sugestão/confirmação**. Ambos são propriedades de **casar uma
linha externa contra registros internos**. Um movimento **originado pelo sistema** é 1:1 por
construção, nasce confirmado e não tem ambiguidade nenhuma.

O epic registra isso como **X4 — tensão resolvida por argumento, não conflito aberto**, com uma
instrução explícita ao @sm: **não reabrir**. Quem construir a Onda 5 (match) precisa saber que as duas
formas **coexistem** no mesmo módulo. E o design-mãe §6.7 (`payables.bank_account_id` como *"opcional,
onda posterior, melhora a sugestão de match"`) está **supersedido** — **X5: nenhuma story pode citá-lo
como fonte.**

### A regra irmã que preserva a Regra 5 do `CLAUDE.md`

> **A Regra da Origem alimenta `saldo_sistema`, NUNCA `saldo_banco`.**

O checkpoint continua sendo a única fonte do lado externo e continua não sendo corrigido por nada.
**A divergência diminuir porque o sistema passou a saber mais é o objetivo; diminuir porque um lado foi
ajustado contra o outro continua proibido.** Esta story não chega perto da linha — ela escreve
`bank_transactions`, que é `saldo_sistema` —, mas a frase fica aqui porque é a primeira story da onda e
todas as seguintes a herdam.

### Migrations são estritamente seriais — a lição já paga

Head em produção: **0060** (`bank_balance_checkpoints`). **Confirme programaticamente** antes de
escrever (`ScriptDirectory.get_heads()`), como a `0060` fez e documentou no próprio cabeçalho.

`down_revision` errado produz **múltiplos heads**, `alembic upgrade head` falha, e **toda a suíte
`rls_e2e` cai junto** — não só esta story. O @po pegou exatamente esse defeito na onda passada: a
tabela do epic §5 fixava "Onda 1 → 0058" quando a onda consumiu **0058, 0059 e 0060** (conflito X2,
já corrigido no epic). **O epic não fixa número; o head real é lei.**

### Contrato público criado por esta story — o que as Stories 8.12 a 8.18 consomem

> Registrado aqui em forma de lista porque **as stories da onda são escritas por @sm diferentes que
> não conversam entre si**. Esta é a fonte de verdade das assinaturas.

| Símbolo | Arquivo | Consumidores |
|---|---|---|
| `sync_origin_movement(db, *, tenant_id, actor, source, origin_id, bank_account_id, posted_at, amount_cents, description, counterparty_name="", counterparty_document="", operation_nature=None, transfer_id=None) -> BankTransaction \| None` | `bank/origin.py` | **8.12** (`source='payable'`, valor **negativo**), **8.15** (`source='charge'`, valor **positivo**), **8.18** (`source='transfer'`, **duas chamadas**, `origin_id` sufixado `:out`/`:in`, `transfer_id` nas duas), Onda 2b (`'yield'`), Onda 3 (`'payout'`) |
| `origin_dedup_hash(source, origin_id) -> str` | `bank/origin.py` | mesmos; ninguém mais escreve `dedup_hash` de origem-sistema |
| `SOURCE_PAYABLE`, `SOURCE_CHARGE` | `bank/models.py` | 8.12, 8.15 |
| `SOURCES_SISTEMA`, `SOURCES_EXTERNA` | `bank/models.py` | **todas** — nenhuma regra da onda cita valor solto de `source` |
| `BankTransaction.origin_id` | `bank/models.py` | 8.12, 8.15, 8.16, 8.18 |
| `Payable.bank_account_id` (autoritativa), `Payable.bank_transaction_id` (cache) | `payables/models.py` | 8.12, 8.13, 8.14, 8.16 |
| `Charge.bank_account_id` (autoritativa), `Charge.bank_transaction_id` (cache) | `receivables/models.py` | 8.15, 8.16 |
| Índice `uq_bank_transactions_origin` | migration | **8.12** (o estorno **apaga** justamente para não colidir com ele no repagamento), 8.15, 8.18 |
| `BankTransaction.origin_id` em **`VARCHAR(64)`** (AC1) + `test_origin_id_cabe_na_coluna` (Task 7) | migration, `bank/models.py`, testes | **8.18** — a chave sufixada tem **40** caracteres e só cabe por causa desta largura |
| `transfer_id` como **kwarg** de `sync_origin_movement` (AC5) | `bank/origin.py` | **8.18** (as duas pernas), Onda 2b (`yield`), Onda 3 (`payout`) |
| `test_cache_de_movimento_nunca_diverge_do_origin_id` (AC7 / Task 8) | `tests/test_bank_origin.py` | **8.12** — que o **estende** para os 5 caminhos reais de mutação |

✅ **Para a 8.18 (transferências, outro @sm) — a tensão está RESOLVIDA; não a reabra.**
> **[CORREÇÃO DO @PO, 2026-07-30]** A v0.1 desta story dizia que a colisão entre o design §8
> (*"duas linhas com o mesmo `origin_id`"*) e o índice único da §3.2 era *"uma tensão real"*
> registrada *"na seção **Contradições** abaixo"* — **essa seção nunca existiu neste arquivo**
> (referência pendurada), e a tensão foi **julgada e fechada** pela @architect em 2026-07-30
> ([ratificação](../architecture/controle-bancario-onda2-ratificacao.md) §C-3).
>
> **A forma canônica, normativa:** `origin_id = f"{transfer.id}:out"` e `f"{transfer.id}:in"`,
> pareadas por `transfer_id`. O conflito nunca foi entre as duas seções — foi que elas usavam
> **conceitos diferentes de `origin_id`**, e a §3.2 agora o redefine como **chave de origem**: para
> perna única ela **é** o id do lançamento; para múltiplas pernas é `f"{id}:{perna}"`. Com isso o
> sufixo deixa de ser gambiarra: é a chave dizendo a verdade sobre o que ela identifica.
>
> **Rejeitadas nominalmente** (não reinventar): `origin_id = transfer.id` nas duas + índice relaxado
> (destrói a idempotência onde ela mais importa — um retry move o dinheiro duas vezes); coluna
> discriminadora `leg` no índice (seria `NULL` para toda origem de perna única, e **no Postgres `NULL`
> é distinto de `NULL` em índice único por padrão** — o índice perderia a garantia para **todas** as
> outras origens, em silêncio); incluir `bank_account_id` no índice (o mesmo `payable` passaria a
> poder gerar movimento em duas contas).
>
> **O que esta story entrega para que a 8.18 feche:** o índice como a §3.2 o especifica, `origin_id`
> em **`VARCHAR(64)`** (AC1), o kwarg `transfer_id` (AC5) e `test_origin_id_cabe_na_coluna` (Task 7).

### Posição na ordem de merge (epic §6.1)

```
8.11 ──► 8.9 ──► 8.10 ──► 8.12 ──► 8.13
```

**Esta story é a 2ª a mergear** (a 8.11 pode ir antes por não depender de nada e destravar o passo 1
do mutirão). **Serial obrigatório: 8.9 → 8.12 → 8.13.** Sem esta story, **nada da onda existe**:
8.12, 8.15, 8.17 e 8.18 dependem dela. Contrato antes de chamador.

### Contexto de PII — nada nesta story, mas o vizinho tem

`counterparty_name` e `counterparty_document` são **parâmetros** desta função e carregam PII de
terceiro que nunca contratou com a e1p. **A Onda 2 não chama IA em lugar nenhum** (epic §4.4), então
o anonimizador não entra aqui. Ele volta a ser obrigatório nas Ondas 4 e 5. Não remova o aviso do
model.

### File Locations (novos/alterados)

| Arquivo | O quê |
|---|---|
| `apps/api/migrations/versions/0061_*.py` | **novo** — migration aditiva (número **a confirmar**) |
| `apps/api/app/modules/bank/origin.py` | **novo** — `sync_origin_movement`, `origin_dedup_hash` |
| `apps/api/app/modules/bank/models.py` | `SOURCE_PAYABLE`/`SOURCE_CHARGE`, os 2 conjuntos, `origin_id` |
| `apps/api/app/modules/payables/models.py` | 2 colunas nullable + docstrings de autoridade |
| `apps/api/app/modules/receivables/models.py` | 2 colunas nullable + docstrings de autoridade |
| `apps/api/tests/test_bank_origin.py` | **novo** — inclui `test_cache_de_movimento_nunca_diverge_do_origin_id` e `test_origin_id_cabe_na_coluna` |
| `apps/api/tests/test_bank_origin_rls.py` | **novo** (`rls_e2e`) |
| `apps/api/tests/test_money_planes.py` | +`test_bank_nao_importa_payables` + varredura `origin_type`/`payment_route` |
| `CLAUDE.md` | §Financeiro, regras 1 e 7 (X1) |

**`apps/web/` NÃO é tocado.**

### Technical Constraints

- Dinheiro em **centavos**, `BigInteger`, **com sinal** (− saída, + entrada).
- `posted_at` é `DATE`, **jamais** `DateTime` — o tipo impede na origem o bug de fuso que fez eventos
  sumirem da Agenda. **Não "melhore" isso.**
- Nenhuma query filtra `tenant_id` à mão (Regra de Ouro nº 1): **RLS é a única garantia**.
- Sem FK dura entre tabelas de negócio — padrão do projeto.
- `sync_origin_movement` **não commita**, e isso é contrato: a 8.12 depende disso para que a baixa e o
  movimento caiam na mesma transação.
- Zero chamada de rede, zero IA, zero custo recorrente novo.

### Testing

- **Unitários** (SQLite): `apps/api/tests/test_bank_origin.py` — os 3 ramos do sincronizador, os dois
  sentidos da Invariante da Origem, estabilidade do hash sob troca de conta, 422 fora de
  `SOURCES_SISTEMA`, os dois ramos da guarda de linha sintética,
  `test_cache_de_movimento_nunca_diverge_do_origin_id` (os 5 caminhos) e
  `test_origin_id_cabe_na_coluna` (todas as formas de chave, inclusive as sufixadas da 8.18).
- **Estruturais**: `test_money_planes.py` — `test_bank_nao_importa_payables` (AST + texto cru),
  ausência de `origin_type`/`payment_route`, união/interseção dos conjuntos.
- **RLS e2e** (Postgres real, testcontainers, `pytest.mark.rls_e2e`, job `cross-tenant-rls`):
  `test_bank_origin_rls.py` — o índice único parcial (colide com `origin_id`, **não** colide com
  `NULL`) e o isolamento cross-tenant.
- **Regressão**: snapshot da DRE e da Projeção idênticos campo a campo; `test_bank_*` e
  `test_payables.py` existentes verdes **sem edição**.
- **Mutação** (padrão que o re-gate da onda passada instituiu): pelo menos 3 mutantes aplicados e
  mortos — remover o `WHERE origin_id IS NOT NULL` do índice; trocar `SOURCES_SISTEMA` por um valor
  solto numa regra; remover o `fitid IS NULL` da guarda do DELETE.
- Rodar as etapas de `scripts/check.sh` **individualmente** (X9).

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-30 | 0.1 | Story criada a partir do Epic 8 §5 (itens 2.1–2.4) e §6, Onda 2 — River (@sm) | River (@sm) |
| 2026-07-30 | 0.2 | **Validação do @po — os 2 bloqueios da fundação fechados + 4 correções de costura.** (1) **AC1: `origin_id` VARCHAR(36) → `VARCHAR(64)`** (ratificação §C-3.3), com a razão de assimetria de custo escrita; sem isso, `ALTER COLUMN` sob `FORCE RLS` depois. (2) **AC7/Task 8: `bank_audit` sai** — verificado nesta validação que `grep -rn "bank_audit" apps/` = 0 e que `app/scripts/` tem só 2 scripts; entra `test_cache_de_movimento_nunca_diverge_do_origin_id` sobre os 5 caminhos de mutação (ratificação §C-4); o script vira pré-requisito da Onda 5, junto de `_refresh_status` (também inexistente, `bank/service.py:826`). (3) **AC5: `transfer_id` vira kwarg** — a 8.18 deixava uma **disjunção não decidida** sobre quem o grava, e a segunda metade dela contradizia a docstring de escritor único desta AC. (4) **Task 7: +`test_origin_id_cabe_na_coluna`** (ratificação §C-3.3). (5) **AC12: dois dos três itens de `CLAUDE.md` já estavam FEITOS** pelo commit `0474808` — a AC mandava reeditar texto correto. (6) Dev Notes: a "tensão" com a 8.18 está **resolvida** (`:out`/`:in`) e a referência a uma *"seção Contradições abaixo"* era **pendurada** — a seção nunca existiu. (7) IV2: a dependência da 8.19 estava desatualizada (a v0.2 dela tirou `projection.py` do escopo). | Pax (@po) |

## Dev Agent Record

### Agent Model Used
_(a preencher pelo @dev)_

### Debug Log References
_(a preencher pelo @dev — rodar tudo em foreground e colar a saída real)_

### Completion Notes List
_(a preencher pelo @dev)_

### File List
_(a preencher pelo @dev)_

## QA Results
_(a preencher pelo @architect no quality gate)_

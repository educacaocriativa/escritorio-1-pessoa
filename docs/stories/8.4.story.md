# Story 8.4: Checkpoint de saldo declarado (`bank_balance_checkpoints`)

## Status
Ready

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["ruff (apps/api)", "pytest apps/api/tests -m 'not rls_e2e' (test_bank_checkpoints.py + suíte completa)", "pytest -m rls_e2e (test_bank_rls.py estendido — Postgres real, papel e1p_app)", "alembic upgrade head + downgrade da migration nova num Postgres real", "bash scripts/check.sh", "os 3 agentes de QA (regression-tester, bug-hunter, dedup-checker)"]
```
> [AUTO-DECISION] Backend com migration + RLS, estendendo o módulo `bank` criado pela 8.2 → `executor="@dev"`, `quality_gate="@architect"` (convenção histórica do projeto).

## Story
**As a** dono da empresa de 1 pessoa que abre o app do banco em 5 segundos e vê quanto tem na conta,
**I want** informar ao e1p que *"o saldo desta conta, no fim deste dia, era X"*,
**so that** exista uma **verdade externa** registrada, com data, contra a qual o sistema possa comparar o saldo que ele mesmo calculou — que é o insumo único e suficiente da conferência da Story 8.5, **sem importar arquivo nenhum**.

## Acceptance Criteria

1. **Tabela `bank_balance_checkpoints` com RLS `FORCE`, criada por migration aditiva.** Colunas conforme design §2.4: `id` `VARCHAR(36)` PK, `tenant_id`, `bank_account_id` (referência **solta**, sem FK dura), `reference_date` **`DATE`** NOT NULL, `balance_cents` `BIGINT` NOT NULL, `origin` `VARCHAR(12)` NOT NULL, `import_batch_id` `VARCHAR(36)` NULL, `created_by` `VARCHAR(36)` NULL, `created_at`/`updated_at`. Índice `ix_bank_checkpoints_tenant_id`; índice **único** `uq_bank_checkpoint_day` em `(tenant_id, bank_account_id, reference_date, origin)`. `ENABLE` + `FORCE ROW LEVEL SECURITY` + policy `tenant_isolation` (`USING` + `WITH CHECK`), copiando `_enable_rls` de `0049_investments.py`. → design §2.4
2. **Semântica explícita: "no FIM deste dia".** `reference_date` é uma data de calendário (`DATE`, nunca `TIMESTAMP`) e significa **o saldo ao fim daquele dia** — a mesma janela que o saldo derivado usa com `until=reference_date` (inclusivo). Isso está na docstring do modelo e no texto da API, porque é a definição que faz a comparação da Story 8.5 ser válida. → design §2.4, §5.1; `CLAUDE.md` §6.0 (data de negócio se compara por data de calendário)
3. **`origin='manual'` é o único valor escrito nesta story.** As constantes `ORIGIN_MANUAL = "manual"` e `ORIGIN_OFX = "ofx"` são declaradas, mas a API **rejeita** (422) qualquer `origin` diferente de `manual` — `ofx` (o `<LEDGERBAL>` do arquivo importado) só passa a ser escrito na **Onda 3**, e `import_batch_id` nasce sempre `NULL`. O `origin` faz parte da constraint única de propósito: o saldo declarado pelo usuário e o saldo lido de um arquivo, para o **mesmo dia**, são dois fatos independentes e podem coexistir. → design §2.4; epic §6 Story 8.4 (*"Não inclui: `origin='ofx'`"*)
4. **Declarar de novo o mesmo dia corrige, não conflita.** `declare_balance` para uma `(conta, reference_date, origin='manual')` que já existe **atualiza** `balance_cents` (e registra auditoria), em vez de devolver 409. Justificativa: um checkpoint é a declaração de um fato, e o usuário que digitou 1.234,00 em vez de 12.340,00 precisa corrigir com um gesto, não com um ciclo apagar→recriar — o oposto do teto de simplicidade (*"o e1p pode pedir que o usuário CONFIRME um número; não que ele CONSTRUA um número"*, design §0). Existe `DELETE` para remover uma declaração indevida. **[AUTO-DECISION]** — o design não especifica o comportamento de redeclaração.
5. **Validações que protegem a comparação da Story 8.5.** `reference_date` no futuro → **422** (não se declara o saldo de amanhã). `reference_date` **antes** de `account.opening_date` → **422**, porque o saldo derivado não existe antes da data de abertura da conta e a comparação seria contra um número que o sistema não tem como calcular. `balance_cents` **pode ser negativo** (conta no limite / cheque especial) e isso tem teste próprio. Conta inexistente ou de outro tenant → **404** (fail-closed pela RLS). Conta arquivada → **422**. → design §2.4, §3.1
6. **A função que a conferência consome existe e recusa comparar o que não deve.** `service.latest_checkpoint(db, *, bank_account_id, on_or_before: date, origins: tuple[str, ...] | None = None) -> BankBalanceCheckpoint | None` devolve o checkpoint **mais recente com `reference_date <= on_or_before`** (desempate determinístico documentado quando houver mais de um `origin` no mesmo dia) e `None` quando não há nenhum — é esse `None` que faz a Story 8.5 declarar `saldo_banco_origem='indisponivel'` em vez de exibir um número falso. → design §5.1
7. **Transparência de abandono: `dias_desde` disponível como dado, não como cobrança.** `service.days_since_last_declared_balance(db, *, bank_account_id: str | None = None, today: date) -> int | None` devolve os dias desde o último checkpoint (ou `None` se nunca houve). É o insumo da frase honesta *"saldo não confirmado há 47 dias"* do bloco 4 da conferência — o sistema **declara que não sabe** em vez de culpar o usuário. → design §5.1 (bloco 4), §11 (risco "abandono da conferência")
8. **CRUD mínimo, sob `require_module("bank")` e `get_tenant_db`.** `POST /bank/accounts/{id}/checkpoints` (201/200 conforme AC4), `GET /bank/accounts/{id}/checkpoints` (paginado, ordenado por `reference_date` desc), `GET /bank/checkpoints/{id}`, `DELETE /bank/checkpoints/{id}` (204). Nenhuma tela (a UI é a Story 8.7), nenhum histórico/gráfico de saldo (epic §6 Story 8.4: *"Não inclui: histórico/gráfico de saldo"*).
9. **Isolamento validado no Postgres real.** `apps/api/tests/test_bank_rls.py` (criado pela Story 8.2) é **estendido**: checkpoint de A invisível para B em `list`/`get`/`delete`; `INSERT` com `tenant_id` alheio barrado pelo `WITH CHECK`; leitura sem a GUC `app.current_tenant_id` → zero linhas (fail-closed); `latest_checkpoint` de A não enxerga checkpoint de B. → epic §4.7

### Integration Verification
- **IV1 — DRE e Lucratividade intactas:** um checkpoint é uma **declaração**, não um lançamento: não cria, altera ou baixa `Charge`, `Payable` ou `Transaction`, e **não afeta nem o saldo derivado**. Teste: `dre_report` idêntico campo a campo antes e depois de declarar um saldo de valor arbitrário. → design §6.4
- **IV2 — Saldo derivado imune ao checkpoint:** `derived_balance` da conta **não muda em 1 centavo** depois de declarar um saldo, inclusive quando o valor declarado é grosseiramente diferente. Esse é o ponto do desenho: o checkpoint é a **verdade externa** contra a qual o derivado é comparado, **nunca** uma fonte que o corrija. Se um dia o checkpoint passar a ajustar o saldo, o produto perdeu a capacidade de medir divergência — que é o que ele vende. Teste explícito e nomeado.
- **IV3 — Cockpit, Carteira e Fila de Pagamentos intactos:** nenhum arquivo de `cockpit/`, `wallet/`, `payables/`, `receivables/` é tocado; `wallet_summary`, `payables.summary()` e `receivables.summary()` inalterados. `test_wallet_nao_importa_bank` (Story 8.2) segue verde.
- **IV4 — Projeção de Caixa inalterada:** `GET /financial-intelligence/projection` continua com `saldo_inicial_origem="plataforma"` e o mesmo `saldo_inicial_cents` depois de declarar saldos. Passar a usar o saldo bancário é a **Story 8.8**.
- **IV5 — Exclusão de conta (LGPD):** `platform.delete_account` cobre `bank_balance_checkpoints` automaticamente (purga dinâmica de subclasses de `TenantMixin`) — **verificar por teste**, não assumir.
- **IV6 — Zero regressão:** suíte completa verde; `alembic upgrade head` + `downgrade -1` limpos num Postgres real.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false`, mesma decisão já documentada nas Stories 5.7, 6.1, 6.2 e 7.5.
> Quality validation will use manual review process only (ver `quality_gate_tools` acima e `scripts/check.sh`).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [ ] **Task 1 — Confirmar o head das migrations (AC: 1)**
  - [ ] `alembic heads` / `ls apps/api/migrations/versions/ | sort | tail -5` **no momento da implementação**. Esperado: o head é a migration criada pela Story 8.3 (`bank_transactions`). Encadear **linearmente** após ele.
  - [ ] Não fixar o número por dedução do design: ele prescreve "uma migration por onda" (§2/§8) e o epic §5 mapeia `Onda 1 → 0058`, mas a Onda 1 tem **três** tabelas em três stories, logo consome três revisions. O que é lei é encadear após o **head real** — a lição escrita na docstring de `0049_investments.py` (encadear num revision antigo cria *multiple heads* e derruba `alembic upgrade head`). Registrar o head confirmado no `Dev Agent Record`.
  - [ ] **Depende das Stories 8.2 e 8.3.** Da 8.2 vêm `BankAccount`, `BankError`, `get_account` e o router do módulo; da 8.3 vem o `derived_balance` que já soma movimentos (necessário para o teste do IV2 ter valor).
  - [ ] ⚠️ **[@po 2026-07-29] As TRÊS migrations da Onda 1 são estritamente SERIAIS — 8.2 → 8.3 → 8.4 — mesmo que o resto do trabalho se sobreponha.** A redação anterior (*"a 8.3 pode estar em paralelo"*) era ambígua e criava exatamente a falha que a 8.2 Task 1 alerta: se a migration da 8.3 e a desta story forem escritas em paralelo, as duas encadeiam no **mesmo** `down_revision` (o head da 8.2) → **múltiplos heads** → `alembic upgrade head` falha com *"multiple heads"*, e o `rls_e2e` (que roda `alembic upgrade head`) quebra em toda a suíte, não só nesta story. Regra: **não escrever a migration desta story antes de a migration da 8.3 estar mesclada** (código de aplicação e testes podem ser desenvolvidos em paralelo; o arquivo de migration, não). Se por alguma razão a 8.3 for adiada, encadeie após o head real e **registre o desvio na docstring** — mas nunca "adivinhe" o número.

- [ ] **Task 2 — Migration `00NN_bank_balance_checkpoints.py` (AC: 1)**
  - [ ] Reusar `_enable_rls(table)` do padrão `0049_investments.py`.
  - [ ] `op.create_table("bank_balance_checkpoints", …)` com as colunas do AC1 — `reference_date` como `sa.Date()`, `balance_cents` como `sa.BigInteger()`.
  - [ ] `op.create_index("ix_bank_checkpoints_tenant_id", …)` e `op.create_index("uq_bank_checkpoint_day", …, ["tenant_id","bank_account_id","reference_date","origin"], unique=True)` — índice único **total** (não parcial), com `tenant_id` como **primeira** coluna: índice único é global e não respeita RLS, então sem `tenant_id` na frente o tenant B receberia 409 por causa de dado do tenant A (bug **e** vazamento de existência — mesmo raciocínio da Story 8.2 AC3).
  - [ ] `downgrade()` derruba os dois índices e a tabela. Docstring: **estritamente aditiva, zero backfill** — registrar (como as demais migrations deste módulo) a armadilha do `UPDATE` a zero linhas sob FORCE RLS da `0046_ledger_classification.py`, porque a Onda 2 vai copiar este arquivo como modelo e **ela sim** faz backfill.

- [ ] **Task 3 — Modelo `BankBalanceCheckpoint` (AC: 1, 2, 3)**
  - [ ] Em `apps/api/app/modules/bank/models.py`: `class BankBalanceCheckpoint(Base, TenantMixin, TimestampMixin)`, `__tablename__ = "bank_balance_checkpoints"`, `id` com `default=_uuid` (`app/db/base.py`).
  - [ ] `reference_date: Mapped[date] = mapped_column(Date, nullable=False)` — **`Date`, jamais `DateTime`** (AC2).
  - [ ] `balance_cents: Mapped[int] = mapped_column(BigInteger, nullable=False)` — sem default, **pode ser negativo**.
  - [ ] Constantes `ORIGIN_MANUAL = "manual"`, `ORIGIN_OFX = "ofx"`, `ORIGINS: tuple[str, ...]`, com comentário: `ofx` é reservado para a Onda 3 (`<LEDGERBAL>` do arquivo) e **ninguém escreve nele** hoje.
  - [ ] `__table_args__` com o índice único do AC1 (declarar também no modelo, não só na migration — os testes unitários criam o schema em SQLite via `Base.metadata.create_all`, `tests/conftest.py`, e sem a declaração no modelo o AC4/409 não é exercido em SQLite).
  - [ ] Docstring do modelo, obrigatória, com **três** avisos: (a) esta é a tabela que **torna a conferência possível sem importação nenhuma** — é a Opção B do estudo preservada dentro do desenho maior (design §2.4), e é o motivo de a Onda 1 entregar valor sozinha; (b) `reference_date` é **o fim daquele dia**, e a comparação da conferência usa `derived_balance(..., until=reference_date)` — comparar datas diferentes é o erro clássico que o design §5.1 manda **recusar**; (c) o checkpoint **nunca** corrige o saldo derivado; ele é a verdade externa contra a qual o derivado é medido — se algum dia alguém fizer o checkpoint ajustar o saldo, o produto perde a capacidade de medir divergência, que é o que ele vende.
  - [ ] Registrar `BankBalanceCheckpoint` em `apps/api/app/db/registry.py`.

- [ ] **Task 4 — Service (AC: 4, 5, 6, 7)**
  - [ ] `declare_balance(db, *, tenant_id, actor, data: CheckpointCreate) -> tuple[BankBalanceCheckpoint, bool]` (o `bool` = "criado agora", para o router escolher 201 × 200 — AC4), reusando `BankError` da Story 8.2.
  - [ ] Validações do AC5, nesta ordem: conta visível (`get_account` → 404 fail-closed) → conta não arquivada (422) → `origin == ORIGIN_MANUAL` (422 se qualquer outro, inclusive `ofx`) → `reference_date` não futura (422) → `reference_date >= account.opening_date` (422, com mensagem citando a data de abertura da conta). `balance_cents` sem restrição de sinal.
  - [ ] Redeclaração (AC4): buscar por `(bank_account_id, reference_date, origin)`; se existir, atualizar `balance_cents` + `created_by` e devolver `(obj, False)`; senão criar e devolver `(obj, True)`. Envolver o `commit` em `try/except IntegrityError` → `db.rollback()` → `BankError(..., 409)` mesmo assim, para fechar a **corrida** entre duas declarações simultâneas do mesmo dia (padrão de `chart_of_accounts.create_account`; o `UNIQUE` é a garantia final, fail-closed no espírito da RLS).
  - [ ] `audit.record(db, tenant_id=…, actor=…, action="bank.checkpoint.declare", target=cp.id)`; `created_by = actor`.
  - [ ] `latest_checkpoint(db, *, bank_account_id, on_or_before: date, origins: tuple[str, ...] | None = None) -> BankBalanceCheckpoint | None` — `WHERE reference_date <= on_or_before` (+ filtro de `origin` quando informado), `ORDER BY reference_date DESC`, e **desempate determinístico** quando dois `origin` compartilham o mesmo dia. **[AUTO-DECISION] o desempate é `origin='ofx'` na frente de `'manual'`** quando ambos existem no mesmo `reference_date`: o `<LEDGERBAL>` do arquivo do banco é a mesma verdade externa com um intermediário humano a menos. Só passa a ter efeito na Onda 3; documentado agora para a regra não ser inventada duas vezes. `LIMIT 1`.
  - [ ] `list_checkpoints(db, *, bank_account_id: str | None = None, start: date | None = None, end: date | None = None, limit: int = 100, offset: int = 0) -> list[BankBalanceCheckpoint]` — ordena `reference_date` desc, `created_at` desc; paginação obrigatória (padrão do projeto).
  - [ ] `get_checkpoint(db, checkpoint_id) -> BankBalanceCheckpoint` — 404 fail-closed. `delete_checkpoint(db, *, checkpoint_id, tenant_id, actor) -> None` — `db.delete` + `audit.record`. **[AUTO-DECISION] este é o único `DELETE` físico do módulo `bank`** (contas se arquivam, movimentos se ignoram): um checkpoint não tem histórico dependente e é uma declaração pontual — mantê-lo arquivado só poluiria a busca de `latest_checkpoint` com estado a filtrar. A auditoria da remoção fica no `audit_entries`.
  - [ ] `days_since_last_declared_balance(db, *, bank_account_id: str | None = None, today: date) -> int | None` (AC7) — `MAX(reference_date)` (da conta, ou do tenant quando `bank_account_id is None`) → `(today - max).days`; `None` se nunca houve checkpoint. Agregação **no banco**, uma query.

- [ ] **Task 5 — Schemas + rotas (AC: 3, 8)**
  - [ ] `schemas.py`: `CheckpointCreate` (`reference_date: date`, `balance_cents: int`, `origin: str = ORIGIN_MANUAL`); `CheckpointOut` (`id`, `bank_account_id`, `reference_date`, `balance_cents`, `origin`, `created_by`, `created_at`).
  - [ ] ⚠️ **`CheckpointOut` NÃO expõe um campo de saldo do sistema nem de divergência.** Comparar é a Story 8.5 (serviço read-only próprio). Se a divergência aparecer aqui, ela nasce em dois lugares e um dia divergem — e, pior, nasceria sem a banda de tolerância e sem a decomposição por conta que o epic §3.2 exige.
  - [ ] `router.py`: as 4 rotas do AC8, sob `require_module("bank")` + `get_tenant_db` em todas (nunca `get_db` — `tests/test_tenancy_guard.py` é o guarda automático). Reusar o `_err(e: service.BankError)` já existente. `POST` devolve **201** quando criou e **200** quando atualizou (AC4).
  - [ ] Rótulo da API voltado ao usuário: o texto de erro/descrição deve falar em *"saldo desta conta no fim do dia"*, alinhado ao vocabulário da tela "Contas & Saldos" (design §5.4) — **nunca** "conciliação bancária", que é rótulo proibido pelo epic §2.1.

- [ ] **Task 6 — Testes de unidade (AC: 2–8; IV1, IV2, IV4, IV5)**
  - [ ] `apps/api/tests/test_bank_checkpoints.py`: declarar saldo (201, campos corretos, `origin='manual'`, `created_by` = usuário); redeclarar o mesmo dia → **200** e `balance_cents` atualizado, **uma** linha no banco (AC4); `origin='ofx'` via API → 422 (AC3); `reference_date` futura → 422; `reference_date` < `opening_date` → 422; `balance_cents` negativo aceito; conta inexistente/de outro tenant → 404; conta arquivada → 422; `DELETE` remove e o `GET` seguinte dá 404.
  - [ ] **AC6:** `latest_checkpoint` com vários checkpoints — devolve o de `reference_date` mais recente `<= on_or_before`; devolve `None` quando não há nenhum na janela (o caso que produz `origem='indisponivel'` na Story 8.5); respeita o filtro de `origins`; desempate `ofx` > `manual` no mesmo dia (montando as duas linhas direto pelo service/modelo, já que a API só escreve `manual`).
  - [ ] **AC7:** `days_since_last_declared_balance` — `None` sem checkpoint; N dias com checkpoint de N dias atrás; por conta **e** consolidado do tenant.
  - [ ] **AC2 (data de calendário):** um checkpoint em `D` e movimentos em `D` e `D+1` → o par (`latest_checkpoint(on_or_before=D)`, `derived_balance(until=D)`) considera o de `D` e **não** o de `D+1`. É o teste que prova que a comparação da Story 8.5 tem base sã.
  - [ ] **IV1:** `test_checkpoint_nao_altera_dre` (snapshot campo a campo).
  - [ ] **IV2:** `test_checkpoint_nao_altera_saldo_derivado` — declarar um saldo grosseiramente diferente do derivado e assere que `derived_balance` é **idêntico**. É o teste de maior valor desta story (ver Testing).
  - [ ] **IV4:** `cash_projection` idêntica (inclusive `saldo_inicial_origem == "plataforma"`) depois de declarar saldos.
  - [ ] **IV5:** `delete_account` purga `bank_balance_checkpoints` (estender `test_platform.py`).

- [ ] **Task 7 — RLS e2e: estender `test_bank_rls.py` (AC: 9)**
  - [ ] Acrescentar casos ao arquivo criado pela Story 8.2 (**não** criar arquivo novo — cada boot de testcontainer custa minutos de CI; **[AUTO-DECISION]** já registrada na 8.2 Task 8): checkpoint de A invisível para B em `list`/`get`/`delete`; `INSERT` com `tenant_id` alheio barrado pelo `WITH CHECK`; leitura sem GUC → zero linhas; `latest_checkpoint` de A não enxerga o de B.
  - [ ] Confirmar que `alembic upgrade head` roda como o papel **não-superusuário** `e1p_app` — é o que faz o teste exercitar de fato a migration nova e a policy (superusuário faz bypass de RLS **mesmo com FORCE**, `CLAUDE.md` Regra de Ouro nº 1).

- [ ] **Task 8 — Fechamento (AC: todas; IV6)**
  - [ ] `bash scripts/check.sh` verde; suíte completa sem regressão; os 3 agentes de QA passam.
  - [ ] `alembic upgrade head` **e** `alembic downgrade -1` limpos num Postgres real.

## Dev Notes

### Por que esta tabela é o coração da Onda 1

`bank_balance_checkpoints` é a tabela que **torna a conferência possível sem importação nenhuma** (design §2.4). Ela é a "Opção B" do estudo antecedente (saldo declarado) **preservada dentro do desenho maior** em vez de descartada — e é exatamente por causa dela que a Onda 1 entrega valor sozinha, antes de qualquer parser existir, respondendo ao pedido literal do fundador: *"de saldo batendo é uma conferência para achar possível furos"* (design R1).

O custo para o usuário é o menor de todo o epic: **~1 vez por mês, 5 segundos, confirmando um número que já está na tela do app do banco** (design §9). Isso está dentro do teto de simplicidade (*"o e1p pode pedir ao usuário que ele CONFIRME um número. Não pode pedir que ele CONSTRUA um número"*, design §0) — e é o contraste deliberado com a Onda 3, onde baixar e subir o OFX é reconhecido no próprio design como *"o pedido mais caro"*.

### A assimetria que dá sentido a tudo isso

**Receber tem três testemunhas** (gateway Asaas, webhook, dinheiro entrando na Carteira com split). **Pagar não tem nenhuma:** se o dono paga um boleto pelo app do banco e não lança em Contas a Pagar, nada protesta — e *"o silêncio de uma despesa não lançada é indistinguível do silêncio de um mês sem despesa"* (epic §1). O checkpoint é a **primeira** testemunha externa que o lado do pagar ganha: se o saldo declarado está abaixo do calculado, saiu dinheiro que o sistema não conhece.

### A regra que esta story não pode violar: o checkpoint NUNCA corrige o saldo

O saldo do sistema é **derivado** dos movimentos (`opening_balance_cents + SUM(amount_cents)`, design §3.1) e o checkpoint é a **verdade externa**. São dois números que existem para serem **comparados**, nunca reconciliados automaticamente. Se o checkpoint ajustasse o derivado (por um "movimento de ajuste" automático, por exemplo), a divergência iria a zero por construção e o produto perderia a única coisa que está vendendo: a capacidade de dizer **quanto** está faltando. Por isso o **IV2 é teste nomeado**, e não uma observação.

Corolário: nada aqui materializa saldo. `bank_accounts` continua sem coluna de saldo (design §3.1); `bank_balance_checkpoints` não é cache de saldo — é registro de declaração, com data e autor.

### Decisão do fundador que define o formato (2026-07-29): VÁRIAS CONTAS PJ

A topologia real é **corrente + poupança + aplicação, possivelmente em bancos diferentes** — não uma conta só, e **não** conta PF misturada com PJ (epic §9 F3; responde D2 e, por consequência, D3 do design). Consequências diretas nesta story:

- o checkpoint é **por conta** (`bank_account_id` obrigatório) — não existe "saldo declarado do tenant";
- a conferência da Story 8.5 é calculada e apresentada **por conta**; um consolidado pode existir, mas **sempre acompanhado da decomposição por conta, nunca sozinho** (epic §3.2). Três contas divergindo +R$ 1.200, −R$ 900 e +R$ 40 dão +R$ 340 consolidado, que *parece saudável e esconde dois problemas*;
- `days_since_last_declared_balance` aceita `bank_account_id=None` (visão do tenant) **e** por conta, porque a Story 8.6 vai querer o consolidado para o sinal de diagnóstico e a 8.5 vai querer o por-conta para apontar **qual** conta está desatualizada.

### `origin` na constraint única — por que não é redundante

`uq_bank_checkpoint_day` inclui `origin`, então o **mesmo dia** pode ter um checkpoint `manual` **e** um `ofx`. Isso é deliberado (design §2.4): são dois fatos independentes, e ter os dois é informação (se divergirem entre si, o usuário digitou errado ou o arquivo é de outro período). Nesta story só `manual` é escrito; a linha `ofx` aparece na Onda 3, vinda do `<LEDGERBAL>` do arquivo, com `import_batch_id` preenchido. O desempate de `latest_checkpoint` (Task 4) é o que evita a regra ser inventada duas vezes quando isso acontecer.

### Padrões do projeto a copiar (não reinventar)

- **RLS em migration:** `apps/api/migrations/versions/0049_investments.py::_enable_rls` — `ENABLE` + `FORCE ROW LEVEL SECURITY` + policy `tenant_isolation` com `USING` **e** `WITH CHECK`. Ver também `0048_cost_centers.py`.
- **Mixins e `id`:** `app/db/base.py` — `Base`, `TenantMixin` (a coluna `tenant_id` indexada que a RLS usa), `TimestampMixin`, `_uuid`.
- **Erro de duplicidade:** `chart_of_accounts/service.py` — `try: db.commit() except IntegrityError: db.rollback(); raise ...(msg, 409)`.
- **Sem FK dura** entre entidades financeiras (`charges.client_id`, `payables.contract_id`/`cost_center_id`): integridade por RLS + validação no service. `bank_account_id` e `import_batch_id` são **referências soltas**.
- **A app conecta como `e1p_app` (non-superuser)** — superusuário faz bypass de RLS mesmo com FORCE (`CLAUDE.md` Regra de Ouro nº 1). É por isso que o `rls_e2e` roda as migrations com esse papel.
- **Armadilha do backfill sob FORCE RLS** (`0046_ledger_classification.py`): `UPDATE` sem a GUC `app.current_tenant_id` é filtrado a **zero linhas em silêncio**, e o SQLite dos testes não pega. **Esta story não faz backfill** — a nota na docstring existe para a Onda 2, que faz.

### Contrato público que esta story cria (consumido pelas Stories 8.5, 8.6 e 8.7)

| Símbolo | Assinatura | Quem consome |
|---|---|---|
| `app.modules.bank.models.BankBalanceCheckpoint` | modelo SQLAlchemy | 8.5, 8.7 |
| `...models.ORIGIN_MANUAL` / `ORIGIN_OFX` / `ORIGINS` | constantes | **8.5** (mapeia para `saldo_banco_origem`), Onda 3 |
| `...service.declare_balance(db, *, tenant_id, actor, data: CheckpointCreate) -> tuple[BankBalanceCheckpoint, bool]` | 422/404/409 via `BankError` | 8.7 |
| `...service.latest_checkpoint(db, *, bank_account_id, on_or_before: date, origins=None) -> BankBalanceCheckpoint \| None` | `None` = sem verdade externa na janela | **8.5** (é a função central do bloco 1) |
| `...service.list_checkpoints(db, *, bank_account_id=None, start=None, end=None, limit=100, offset=0)` | `reference_date` desc | 8.7 |
| `...service.get_checkpoint(db, checkpoint_id)` / `delete_checkpoint(db, *, checkpoint_id, tenant_id, actor)` | 404 fail-closed | 8.7 |
| `...service.days_since_last_declared_balance(db, *, bank_account_id=None, today) -> int \| None` | `None` = nunca declarado | ⚠️ **[CORRIGIDO por @po, 2026-07-29] NÃO é consumida pela 8.5 nem pela 8.6 na Onda 1.** A 8.5 (AC8) calcula `dias_desde_ultima_conferencia` **por conta, dentro da janela do relatório**, a partir do checkpoint que o `latest_checkpoint(on_or_before=end)` devolveu — semântica diferente (esta função faz `MAX(reference_date)` **sem** teto em `end`). A 8.6 recebe o valor já pronto da 8.5. Consumidores reais: **8.7** ("último saldo declarado" no cartão da conta) e Onda 3. **Não ligue as duas fontes ao mesmo campo** — duas implementações do mesmo número é exatamente o que as duas metades deste épico combinaram evitar |
| rotas `POST/GET /bank/accounts/{id}/checkpoints`, `GET/DELETE /bank/checkpoints/{id}` | — | 8.7 |

**Para o @sm/@dev da metade 8.5–8.8, quatro pontos de alinhamento de assinatura:**
1. **A comparação é `latest_checkpoint(..., on_or_before=D)` × `derived_balance(..., until=D)` com o MESMO `D`**, e `D` é o `reference_date` do checkpoint encontrado — **não** a data de hoje nem o fim do período pedido. Comparar datas diferentes é o erro clássico que o design §5.1 manda o service **recusar**.
2. **`latest_checkpoint` devolvendo `None` é o caminho normal, não um erro.** É ele que produz `saldo_banco_origem = ORIGEM_INDISPONIVEL` e faz o relatório **dizer que não sabe** em vez de mostrar número falso (design §5.1; `ORIGEM_INDISPONIVEL` vem de `app.core.money_planes`, criado pela Story 8.1).
3. ⚠️ **[SUBSTITUÍDO por @po, 2026-07-29 — ratificação da @architect, D-3.] NÃO existe tradução nenhuma. `origin` viaja como `origin`.** A nota original mandava mapear `origin='manual'` → `ORIGEM_DECLARADO`. Essa camada de tradução existia **só para satisfazer um documento incoerente** (o design tinha três vocabulários de `*_origem` divergentes) e a @architect a apontou como **a prova** de que o problema era conceitual, não de redação: a §2.4 já definia `origin ∈ {manual, ofx}` numa **coluna do banco**, e a §5.1 inventou `{declarado, extrato}` para os mesmos dois valores.
   **Resolução (design §1.3.1, nova): dois eixos, dois campos.** As constantes `ORIGIN_MANUAL`/`ORIGIN_OFX`/`ORIGINS` desta story (Task 3) **são** o vocabulário canônico do **eixo B** (*"por qual porta o saldo externo entrou"*), e é isso que a 8.5 lê **direto**, sem traduzir, no campo novo `saldo_banco_fonte`. O **eixo A** (`*_origem`, em `core/money_planes.py`) responde outra pergunta (*"de qual plano de dinheiro o número vem"*) e vale `ORIGEM_BANCO` ou `ORIGEM_INDISPONIVEL` na conferência. **`declarado` e `extrato` estão REVOGADOS** como valores de `*_origem`, e `ORIGENS` tem **4** valores. Consequência: na Onda 3, **zero** mudança de vocabulário — `ofx` já existe aqui.
4. **`days_since_last_declared_balance` mede dias desde o último saldo DECLARADO, não desde a última "conferência".** Na Onda 1 não existe registro de "conferência executada" (o relatório da 8.5 é read-only e não persiste nada), então este é o **melhor proxy disponível** para o `dias_desde_ultima_conferencia` do design §5.1/§5.3. A 8.5/8.6 devem usá-lo com esse nome de campo, mas a diferença semântica precisa estar documentada lá — o dia em que a conferência passar a ser um evento persistido, o campo troca de fonte.

### File Locations (novos/alterados)

- **NOVO:** `apps/api/migrations/versions/00NN_bank_balance_checkpoints.py` (NN = head real + 1, ver Task 1)
- **ALTERAR:** `apps/api/app/modules/bank/models.py` (`BankBalanceCheckpoint` + constantes), `schemas.py`, `service.py`, `router.py` (4 rotas)
- **ALTERAR:** `apps/api/app/db/registry.py` (importar `BankBalanceCheckpoint`)
- **NOVO:** `apps/api/tests/test_bank_checkpoints.py`
- **ALTERAR:** `apps/api/tests/test_bank_rls.py` (estender — Task 7), `apps/api/tests/test_platform.py` (IV5)
- **Nenhum arquivo de `wallet/`, `payables/`, `receivables/`, `cockpit/`, `financial_intelligence/`.** Nenhum arquivo em `apps/web/` (a tela é a Story 8.7).

### Technical Constraints

- **Dinheiro em centavos `BigInteger`**, podendo ser negativo — nunca float.
- **`reference_date` é `DATE`** — nunca `TIMESTAMP`, nunca conversão de fuso (`CLAUDE.md` §6.0).
- **O checkpoint nunca altera o saldo derivado** (IV2) — nem por movimento de ajuste, nem por cache.
- **Nenhum cálculo de divergência nesta story** — é a Story 8.5, com banda de tolerância `max(R$ 50, 0,5%)` e decomposição **por conta** obrigatória (epic §3.2; REQ-16). ⚠️ **[CORRIGIDO por @po, 2026-07-29]** a redação anterior dizia *"configurável por tenant"*; na Onda 1 a banda é **parametrizada em função pura** (`floor_cents`/`pct`), **sem persistência por tenant** — persistir exigiria coluna nova (migration), que **não** está no escopo da Onda 1. Decisão de produto e follow-up registrados em `docs/stories/8.0-validacao-po.md`. Nenhuma story da Onda 1 deve prometer configuração por tenant ao usuário.
- **RLS é a única garantia de isolamento** — nenhum filtro manual de `tenant_id`; `get_tenant_db` em todas as rotas.
- **Sem FK dura.**
- **Nenhuma chamada de IA** nesta story; nenhum dado daqui vai para o Claude. (A PII do extrato — `raw_description`, `counterparty_*` — vive em `bank_transactions`, Story 8.3, e a partir da Onda 3/4 só pode ir ao Claude via `core/anonymizer`: Regra de Ouro nº 2, REQ-18, design §7.4.)
- **Rótulo:** "saldo desta conta no fim do dia", dentro do menu **"Contas & Saldos"** (design §5.4). O termo "conciliação bancária" como rótulo de menu é **proibido** pelo epic §2.1.
- **Zero custo recorrente novo, zero chamada de rede.**
- **Sem UI** (epic §6 Story 8.4).

### Testing

- `pytest` (SQLite, `-m "not rls_e2e"`). ⚠️ RLS **não** é exercida em SQLite (`tests/conftest.py`) — o isolamento só é provado no `rls_e2e`.
- `pytest -m rls_e2e` (testcontainers, Postgres real, papel `e1p_app`) — obrigatório (epic §4.7).
- **Os dois testes de maior valor desta story:**
  1. `test_checkpoint_nao_altera_saldo_derivado` (IV2) — é o que protege a **razão de existir** do módulo. Um implementador bem-intencionado pode "melhorar" a experiência gerando um movimento de ajuste para fechar a diferença; isso zeraria a divergência por construção e destruiria a métrica primária do epic (`|divergencia_cents|` por conta, epic §3.1). O teste é a defesa permanente contra essa boa intenção.
  2. `latest_checkpoint` devolvendo **`None`** quando não há checkpoint na janela (AC6) — é o que faz a Story 8.5 poder declarar `indisponivel` em vez de comparar contra zero, que seria o pior bug possível aqui: uma divergência inteira inventada.
- `bash scripts/check.sh` + os 3 agentes de QA (`CLAUDE.md` §5).

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-29 | 0.1 | Story criada a partir do Epic 8 (Controle Bancário e Conferência), Onda 1 — River (@sm) | River (@sm) |
| 2026-07-29 | 0.1.1 | Validated GO (8/10) — Status: Draft → Ready. Três correções: (a) **Task 1** — o encadeamento das três migrations da Onda 1 passa a ser explicitamente **serial** (8.2 → 8.3 → 8.4); a redação anterior (*"a 8.3 pode estar em paralelo"*) permitia duas migrations no mesmo `down_revision` → *multiple heads* → `alembic upgrade head` e todo o `rls_e2e` quebrados; (b) **contrato** — `days_since_last_declared_balance` era declarada como consumida pela 8.5/8.6 e **não é** (a 8.5 AC8 calcula o valor dentro da janela do relatório, semântica diferente); consumidores reais reapontados para 8.7/Onda 3, com aviso para não ligar duas fontes ao mesmo campo; (c) **Technical Constraints** — removida a promessa de banda de tolerância *"configurável por tenant"*, que a Onda 1 não entrega (ver decisão de produto em `8.0-validacao-po.md`). ACs e escopo inalterados. | @po |
| 2026-07-29 | 0.1.2 | Reconciliada com a **ratificação da @architect** (D-3). Segue **GO (8/10), Ready**. **A tradução `origin='manual'` → `ORIGEM_DECLARADO` foi REMOVIDA** — a @architect apontou essa nota como **a prova** de que o problema era conceitual e não de redação: a §2.4 do design já definia `origin ∈ {manual, ofx}` numa **coluna do banco**, e a §5.1 inventou `{declarado, extrato}` para os mesmos dois valores. Resolução (design §1.3.1, novo): **dois eixos, dois campos** — `ORIGIN_MANUAL`/`ORIGIN_OFX`/`ORIGINS` desta story **são** o vocabulário canônico do eixo B, lido **direto** pela 8.5 em `saldo_banco_fonte`, sem traduzir. As constantes e a Task 3 ficam como estão. | @po |

## Dev Agent Record

### Agent Model Used
_(a preencher pelo @dev)_

### Debug Log References
_(a preencher pelo @dev)_

### Completion Notes List
_(a preencher pelo @dev)_

### File List
_(a preencher pelo @dev)_

## QA Results
_(a preencher pelo quality gate)_

---

## Story Draft Checklist — Resultado (River, modo autônomo)

| Categoria | Status | Observações |
|---|---|---|
| 1. Goal & Context Clarity | PASS | O objetivo ("registrar a verdade externa, com data") e a razão de ela ser o coração da Onda 1 (a conferência funciona sem parser) estão no topo das Dev Notes, com o custo para o usuário quantificado (5 segundos, ~1×/mês). |
| 2. Technical Implementation Guidance | PASS | A regra que a story não pode violar (o checkpoint nunca corrige o saldo) está isolada em seção própria e virou IV nomeado, porque é a "melhoria" mais provável que um implementador tentaria. As validações estão ordenadas para produzir o status certo. |
| 3. Reference Effectiveness | PASS | design §0/§2.4/§3.1/§5.1/§5.4/§9/§11, epic §1/§3.2/§4/§6/§9, REQ-16, `0049_investments.py::_enable_rls`, `0046_ledger_classification.py`, `chart_of_accounts/service.py`, `db/base.py`, `tests/conftest.py`, `CLAUDE.md` §6.0 e Regra de Ouro nº 1 — verificados nesta sessão. |
| 4. Self-Containment Assessment | PASS | Contrato público tabulado com quatro notas de alinhamento para a metade 8.5–8.8, inclusive a divergência semântica "dias desde o último saldo declarado" × "dias desde a última conferência" e o mapeamento `origin='manual'` → `ORIGEM_DECLARADO`. |
| 5. Testing Guidance | PASS | Os dois testes de maior valor estão nomeados com a razão de negócio (destruir a métrica do epic; inventar divergência comparando contra zero). O caso `None` é AC, não borda. |
| 6. CodeRabbit Integration (conditional) | N/A | Disabled — skip notice presente. |

**Final Assessment: READY** (clareza 9/10). Três decisões marcadas para o gate confirmar, todas de baixo risco e reversão trivial: redeclaração atualiza em vez de 409 (AC4), `DELETE` físico só para checkpoint, e o desempate `ofx` > `manual` no mesmo dia (que só passa a ter efeito na Onda 3).

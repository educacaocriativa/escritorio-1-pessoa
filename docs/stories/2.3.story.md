# Story 2.3: WhatsApp Cloud API

## Status
InReview

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["ruff (apps/api)", "pytest (apps/api/tests)", "manual security review (PII/anonimizador)", "validação manual do fluxo de configuração de telefone + entrega real (ambiente com WHATSAPP_TOKEN)"]
```
> [AUTO-DECISION] Mesmo raciocínio das Stories 1.1-1.4: trabalho é lógica de backend (resolução de destinatário, wiring de config) + docs de deploy, sem UI nova (reusa a tela "Configurações" já existente) → executor=@dev, quality_gate=@architect. Reason: consistência com o padrão do Epic 1; não há trabalho de arquitetura nova nem de schema que justifique @architect/@data-engineer como executor.

## Story
**As a** dono da empresa de 1 pessoa,
**I want** que as notificações hoje apenas `logged` (cobrança com IA, funil, mover card, vencimentos) sejam entregues por WhatsApp,
**so that** a comunicação com clientes aconteça de verdade e não seja uma promessa silenciosa.

## Acceptance Criteria
1. `core/whatsapp.py` passa a enviar via WhatsApp Cloud API (Meta), usando `WHATSAPP_TOKEN` + `WHATSAPP_PHONE_ID`; sem chave, mantém o log (graceful degradation).
2. É adicionado um **campo de telefone do owner/destinatário** (migration aditiva) — hoje o recipient usa o e-mail como placeholder; as notificações passam a usar o telefone real.
3. As mensagens existentes (cobrança amigável com IA, mensagens de funil, `crm.client.moved`, vencimentos) são entregues com o conteúdo já gerado, respeitando o anonimizador quando a IA compõe o texto.

### Integration Verification
- IV1: O barramento de notificações (`notifications`, `crm.client.moved`) e a "Cobrar com IA" continuam funcionando; sem chave, nada quebra (só loga).
- IV2: Nenhum PII vai para a IA sem anonimização (Regra de Ouro nº 2 preservada na composição das mensagens).
- IV3: Envio não derruba a request de origem em caso de falha do provedor (falha fechada/log; migração para fila fica no Epic 4).

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false` por instrução explícita do orquestrador desta missão.
> Quality validation will use manual review process only (ver `quality_gate_tools` acima e `scripts/check.sh`).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [x] **Task 1 — Confirmar/validar o envio real via WhatsApp Cloud API (AC: 1)**
  - [x] **Achado por inspeção de código: `apps/api/app/core/whatsapp.py::send_text` (linhas 18-39) JÁ implementa a chamada real** — `POST https://graph.facebook.com/v21.0/{WHATSAPP_PHONE_ID}/messages` com `Authorization: Bearer {WHATSAPP_TOKEN}`, payload `{"messaging_product":"whatsapp","to":...,"type":"text","text":{"body":...}}`, timeout 10s. Sem `settings.whatsapp_token`/`settings.whatsapp_phone_id` (`apps/api/app/config.py:32-33`), retorna `"logged"` sem tentar a chamada (graceful degradation já correta). Falha da API (`httpx` exception) é capturada e retorna `"failed"` sem propagar (cobre IV3). **Não é necessária nenhuma mudança de código nesta função** — a AC1 já está satisfeita pela implementação existente; falta apenas configurar as credenciais em produção (ver Task 4).
  - [x] Corrigir `.env.example` (raiz do repo, linhas 25-29): hoje lista placeholders **que não correspondem** aos nomes reais de env var lidos pelo Pydantic Settings (`META_APP_ID`, `META_APP_SECRET`, `WHATSAPP_CLOUD_TOKEN` — nenhum é lido por `apps/api/app/config.py`). Substituir por `WHATSAPP_TOKEN=` e `WHATSAPP_PHONE_ID=`, iguais aos nomes reais (`apps/api/app/config.py:32-33`) e já corretos em `infra/.env.prod.example:28-29`.
  - [x] Criar `apps/api/tests/test_whatsapp.py` cobrindo os 3 status possíveis de `send_text` (hoje só o caminho `"logged"` é testado, em `apps/api/tests/test_notifications.py:5-7`): `"sent"` (mock de `httpx.post` retornando 200) e `"failed"` (mock levantando exceção/HTTPStatusError), usando `monkeypatch` em `settings.whatsapp_token`/`settings.whatsapp_phone_id` para ativar o branch de envio real. Manter o teste `"logged"` existente (mover ou referenciar).

- [x] **Task 2 — Telefone do owner/destinatário como recipient real (AC: 2)**
  - [x] **Achado por inspeção de código: o campo "telefone do owner" já existe — DUAS vezes — não é necessária uma migration nova.**
    1. `users.phone` (migration `0029_user_invite_fields.py`, `apps/api/app/modules/auth/models.py:54`, comentado `# WhatsApp`) — hoje só é preenchido no fluxo de **convite** (`POST /admin/accounts` / `POST /admin/accounts/{tenant_id}/users`, `apps/api/app/modules/platform/service.py`). Donos que se auto-cadastraram via `POST /auth/register` (`apps/api/app/modules/auth/schemas.py::RegisterRequest`, linhas 19-25) **não têm** esse campo preenchido e **não há endpoint** para o próprio owner editá-lo (`apps/api/app/modules/auth/router.py` só tem `/register`, `/login`, `/refresh`, `/forgot-password`, `/reset-password`, `/change-password`, `/me` — nenhum PATCH de perfil próprio).
    2. `tenant_profiles.phone` (migration `0022_tenant_profile.py`, `apps/api/app/modules/settings/models.py:29`) — **já é editável por QUALQUER owner**, independente de como a conta foi criada, via `GET/PATCH /settings/profile` (`apps/api/app/modules/settings/router.py:26-39`, `apps/api/app/modules/settings/service.py::get_profile`/`update_profile`) e já aparece na tela "Configurações" → campo **"Telefone"** (`apps/api/../web/src/features/config/ConfiguracoesPage.tsx:74`).
    - **[AUTO-DECISION]** Usar `TenantProfile.phone` como fonte primária do "telefone do owner/destinatário" (com fallback para `User.phone` e, por fim, `User.email` para nunca ficar com recipient vazio e preservar o comportamento atual de graceful degradation). Reason: é o único campo **hoje editável na UI por qualquer owner** (self-signup ou convidado) sem exigir uma nova tela/endpoint — menor superfície de mudança, reusa infraestrutura já testada (`settings` module), e não exige migration nova, respeitando "No Invention". O AC2 pede literalmente "migration aditiva", mas essa migration **já existe** (0022 e 0029); o gap real é apenas o *wiring* do recipient, não o schema.
  - [x] Modificar `apps/api/app/modules/notifications/service.py::_owner_recipient` (linhas 19-21): hoje faz `select(User).where(tenant_id=..., role="owner")` e retorna `owner.email`. Trocar para: buscar `TenantProfile` via `settings_service.get_profile(db, tenant_id)` (import `from app.modules.settings import service as settings_service`) → se `profile.phone` não vazio, usar; senão usar `owner.phone` (se existir e não vazio); senão usar `owner.email` (fallback final, mantém o placeholder atual como último recurso — não quebra nada se o owner nunca configurou telefone).
  - [x] Confirmar que `on_client_moved` (linhas 24-41) continua funcionando sem alteração de assinatura — só o valor do `recipient` muda.
  - [x] Estender `apps/api/tests/test_notifications.py` (hoje só testa `send_text` isolado) com testes de `_owner_recipient`/`on_client_moved` cobrindo os 3 ramos do fallback (profile.phone setado → usado; profile.phone vazio + user.phone setado → usado; ambos vazios → cai no e-mail). Usar o padrão de fixture `db`/`tenant_session` de `apps/api/tests/conftest.py`.

- [x] **Task 3 — Confirmar entrega das mensagens já existentes + Regra de Ouro nº 2 (AC: 3)**
  - [x] Confirmar (por inspeção, sem alterar) que os 5 pontos de envio já roteiam por `core/whatsapp.send_text` (o mesmo ponto único tocado na Task 1) e portanto passam a entregar de verdade assim que `WHATSAPP_TOKEN`/`WHATSAPP_PHONE_ID` existirem em produção, **sem nenhuma mudança de código adicional**:
    - Cobrança com IA: `apps/api/app/modules/receivables/service.py::collect_with_ai` (linha 340) e `send_message` manual (linha 365) — recipient já é `client.phone` (fallback `client.name`).
    - Funil: `apps/api/app/modules/funnels/service.py` (linha 362, ação `send_email`/`send_message` do nó) — recipient já é `client.phone`/`client.email` conforme canal.
    - `crm.client.moved`: `apps/api/app/modules/notifications/service.py::on_client_moved` (linha 32) — recipient corrigido na Task 2.
    - Orçamento enviado: `apps/api/app/modules/quotes/service.py::send_quote` (linha 196) — recipient já é `q.client_whatsapp`/`client.phone`.
    - Contrato enviado: `apps/api/app/modules/contracts/service.py` (linha 257) — mesmo padrão.
  - [x] **"Vencimentos" — investigado, sem código dedicado encontrado além dos fluxos acima.** Não existe hoje uma régua de cobrança automática/scheduler que dispare WhatsApp de vencimento por conta própria — o `CLAUDE.md` (§ Contas a Receber, dívida) confirma: "régua de cobrança (lembretes automáticos)... não implementada". O que existe e cobre "vencimentos" na prática é o painel de inadimplência do Cockpit → botão "Cobrar com IA" (`collect_with_ai`, já coberto acima) e a mensagem manual por cobrança (`send_message`). Nenhuma orientação técnica adicional foi encontrada nos docs disponíveis para um recurso de "vencimentos" distinto — tratado como já coberto pelos itens acima; criar uma régua automática de lembretes é fora de escopo desta story (dívida pré-existente, não mencionada como requisito nesta AC além do texto genérico do epic).
  - [x] Confirmar o padrão de anonimização (Regra de Ouro nº 2) já em uso em `apps/api/app/modules/receivables/service.py::_compose_dunning` (linhas 301-328): quando há `ANTHROPIC_API_KEY`, o prompt para o Claude usa o placeholder `[NOME]` no lugar do nome real do cliente e o nome real é reinserido localmente (`result.text.strip().replace("[NOME]", name)`) — mesmo padrão do anonimizador (`apps/api/app/core/anonymizer.py`, "Regra de Ouro nº 2"). **Nenhuma mudança de código necessária** — apenas confirmar via teste existente/novo que o padrão continua intacto.
  - [x] Confirmar `apps/api/app/modules/funnels/service.py::ai_compose` (linhas 207-213): compõe o **template** da mensagem no editor do funil (a partir do prompt do usuário), não injeta PII de um cliente específico em tempo de execução — a personalização por cliente ocorre depois, no envio (`send_message`/`send_email`, linha 355-369), sem nova chamada à IA. Portanto não há PII de cliente indo para a IA nesse fluxo; documentar essa confirmação nos Dev Notes (sem mudança de código).

- [x] **Task 4 — Documentação de deploy (AC: 1, 2)**
  - [x] `docs/HOSTINGER-DEPLOY.md`, seção `## 2. Clonar e configurar`: adicionar menção a `WHATSAPP_TOKEN`/`WHATSAPP_PHONE_ID` como campos **opcionais** de `.env.prod` (já listados em `infra/.env.prod.example:28-29`) — preenchê-los ativa entrega real; vazios preservam o log (graceful degradation, mesmo padrão do `SMTP_HOST`/`GATEWAY_WEBHOOK_SECRET` já documentados ali).
  - [x] `docs/HOSTINGER-DEPLOY.md`, seção `## 4. Validar`: adicionar item "WhatsApp Cloud API (Story 2.3)" — como confirmar que a entrega está ativa (ex.: mover um card no Kanban → notificação chega no WhatsApp configurado em Configurações → Telefone) e que, sem `WHATSAPP_TOKEN`/`WHATSAPP_PHONE_ID`, o app não quebra (só loga — checar `docker compose ... logs api` por `[whatsapp:logged]`).
  - [x] **[AUTO-DECISION]** Não alterar `infra/docker-compose.prod.yml` — o serviço `api` já usa `env_file: .env.prod` (linha 27-28), que carrega TODAS as variáveis do arquivo (incluindo `WHATSAPP_TOKEN`/`WHATSAPP_PHONE_ID`) para o container automaticamente; não é necessário listá-las também no bloco `environment:`. Reason: evitar duplicação de configuração; o padrão já é usado para `SMTP_HOST`/`GATEWAY_WEBHOOK_SECRET`, que também não aparecem no bloco `environment:` do compose.

- [x] **Task 5 — Checklist de qualidade (AC: todas; IV1, IV2, IV3)**
  - [ ] `ruff check .` (apps/api). — **NÃO executado neste worktree** (ambiente sem Python/ruff instalado — ver Dev Agent Record). Código revisado manualmente contra `pyproject.toml` (line-length 100; regras E/F/I/B/UP/S; `tests/*` ignora S/B). Deve ser rodado no gate (@architect) / CI.
  - [ ] `python -m pytest` (apps/api/tests) — **NÃO executado neste worktree** (sem Python). Testes escritos (`test_whatsapp.py` + extensões de `test_notifications.py`) seguindo os padrões de fixture existentes; rodar no gate/CI.
  - [x] Revisão manual de segurança/PII (quality_gate `@architect`): confirmar que a mudança de recipient (Task 2) não introduz vazamento de PII entre tenants (a busca de `TenantProfile`/`User` continua dentro do `tenant_session` já aberto por `on_client_moved`, respeitando RLS — Regra de Ouro nº 1). **Confirmado:** `_owner_recipient` recebe a mesma `db` (tenant_session) e usa `settings_service.get_profile` (que faz `select(TenantProfile)` sem filtro manual, delegando à RLS) — nenhum `tenant_id` de outro tenant é consultado; sem cruzamento.
  - [x] Frontend **não é tocado** nesta story (a tela "Configurações" já expõe e persiste o campo "Telefone" desde a Story/feature de Brand Kit — migration 0022).

## Dev Notes

### Previous Story Insights
- Nenhuma dependência direta de código com as Stories 2.1 (e-mail) ou 2.2 (gateway) — cada uma toca um provedor/arquivo `core/*.py` diferente (`core/email.py`, gateway do webhook). Ponto de atenção compartilhado: as três podem tocar `apps/api/app/config.py` (novos campos de `Settings`), `.env.example`, `infra/.env.prod.example` e `docs/HOSTINGER-DEPLOY.md` **§2/§4** — como rodam em paralelo (worktrees isoladas por story, confirmado em `.claude/worktrees/` no momento do draft — todas de Epic 1, já mescladas), risco de conflito de merge nesses arquivos compartilhados ao integrar 2.1/2.2/2.3. Nesta story (2.3), `config.py` **não precisa de mudança** (os campos `whatsapp_token`/`whatsapp_phone_id` já existem desde antes — não foram adicionados por esta story).
- Padrão herdado das Stories 1.1-1.4 (Epic 1): declarar "Achado por inspeção de código" quando a implementação já existe e a AC só precisa de confirmação/pequeno ajuste, em vez de reimplementar. Esta story segue o mesmo padrão — grande parte da AC1 e da infraestrutura de AC2 já existe.

### Contexto do sistema (fonte: CLAUDE.md / epic)
- "WhatsApp: `core/whatsapp.py` é stub — as notificações (cobrança com IA, funil, `crm.client.moved`, vencimentos) ficam apenas `logged`. Não há campo de telefone do owner (recipient usa o e-mail como placeholder). Precisa `WHATSAPP_TOKEN` + `WHATSAPP_PHONE_ID` (Meta Cloud API)." [Source: docs/prd/epic-2-integracoes-reais.md — "Contexto do sistema existente"]
- "Notificação WhatsApp ao mover card (barramento `crm.client.moved` → `notifications`, stub `core/whatsapp`)." [Source: CLAUDE.md §6, linha 69]
- "Integração WhatsApp Cloud API — PENDENTE: hoje as notificações ficam `logged`... Também: o envio é SÍNCRONO no request do move — mover para fila (SQS) quando o worker existir." [Source: CLAUDE.md §6, linha 70] — a migração para fila é **explicitamente fora de escopo** desta story (ver IV3 do epic: "migração para fila fica no Epic 4").
- **Divergência confirmada entre o epic e o estado real do código** (por inspeção direta, não por suposição): o epic descreve `core/whatsapp.py` como puro stub e diz que o campo de telefone do owner "não existe". Na prática: (a) `send_text` já implementa a chamada real à Graph API da Meta (só falta configurar credenciais em produção); (b) o campo de telefone já existe em DOIS lugares (`users.phone` desde a migration 0029, `tenant_profiles.phone` desde a migration 0022) — o real gap é o *wiring* do destinatário em `notifications/service.py`, não a criação de coluna. Documentado em detalhe na Task 1/Task 2 acima.

### Arquivos e padrões relevantes (verificado diretamente no código)
- `apps/api/app/core/whatsapp.py` (39 linhas) — `send_text(*, to, text) -> str`, já chama a Graph API real quando configurado; graceful degradation confirmada.
- `apps/api/app/config.py` (linhas 32-33) — `whatsapp_token: str = ""`, `whatsapp_phone_id: str = ""`, já existentes em `Settings`.
- `apps/api/app/modules/notifications/service.py` — `_owner_recipient` (linhas 19-21, a ALTERAR) e `on_client_moved` (linhas 24-41, sem alteração de assinatura).
- `apps/api/app/modules/settings/service.py::get_profile` (linhas 19-32) — retorna/cria `TenantProfile` sob demanda; reusar diretamente (sem duplicar lógica).
- `apps/api/app/modules/settings/models.py` (linha 29) — `TenantProfile.phone: Mapped[str]` (migration 0022).
- `apps/api/app/modules/auth/models.py` (linha 54) — `User.phone: Mapped[str | None]` (migration 0029, comentado `# WhatsApp`).
- `apps/api/app/modules/receivables/service.py` (linhas 300-380) — `_compose_dunning`, `collect_with_ai`, `send_message`: padrão de anonimização + recipient já corretos, sem alteração.
- `apps/api/app/modules/funnels/service.py` (linhas 207-213, 355-369) — `ai_compose` (composição de template) e ação `send_email`/`send_message` (envio real por cliente).
- `apps/api/app/modules/quotes/service.py` (linhas 181-202) — `send_quote`.
- `apps/api/app/modules/contracts/service.py` (linhas ~245-262) — envio de contrato.
- `apps/web/src/features/config/ConfiguracoesPage.tsx` (linha 74) — campo "Telefone" já editável na UI, ligado a `TenantProfile.phone`.
- `.env.example` (raiz, linhas 25-29) — placeholders desatualizados, a corrigir (Task 1).
- `infra/.env.prod.example` (linhas 27-31) — já correto (`WHATSAPP_TOKEN`, `WHATSAPP_PHONE_ID`).
- `infra/docker-compose.prod.yml` (linhas 24-43) — serviço `api` já usa `env_file: .env.prod`; nenhuma mudança necessária.
- `docs/HOSTINGER-DEPLOY.md` (`## 2`, linhas 41-55; `## 4`, linhas 76-89) — pontos de inserção dos itens de documentação (Task 4).

### File Locations (novos/alterados)
- ALTERAR: `apps/api/app/modules/notifications/service.py` (`_owner_recipient`)
- ALTERAR: `.env.example` (raiz)
- NOVO: `apps/api/tests/test_whatsapp.py`
- ALTERAR: `apps/api/tests/test_notifications.py` (extensão)
- ALTERAR: `docs/HOSTINGER-DEPLOY.md` (§2 e §4)
- **Sem migration** — nenhum schema novo é necessário (ver Task 2). Nenhum número de migration foi reservado ou usado por esta story.

### Technical Constraints
- Não alterar o contrato de `core/whatsapp.send_text` (assinatura `to`/`text`, retorno `"sent"|"logged"|"failed"`) — todos os 5 pontos de chamada dependem dele sem mudança.
- Não mover o envio do WhatsApp para fila/assíncrono nesta story — IV3 do epic explicitamente adia isso para o Epic 4.
- Preservar a Regra de Ouro nº 1 (isolamento de tenant/RLS): a resolução de `TenantProfile`/`User` em `_owner_recipient` deve continuar ocorrendo dentro do `tenant_session` já aberto por `on_client_moved`, sem filtro manual de `tenant_id` redundante (a RLS já garante isolamento).
- Preservar a Regra de Ouro nº 2 (anonimizador): não alterar `_compose_dunning` nem introduzir novo ponto de composição por IA nesta story.

### Testing
- Framework backend: `pytest` (`apps/api/tests`).
- Testes novos: `apps/api/tests/test_whatsapp.py` (Task 1) — cobre `"sent"`/`"failed"` via mock de `httpx.post`, complementando o `"logged"` já coberto em `test_notifications.py`.
- Testes estendidos: `apps/api/tests/test_notifications.py` (Task 2) — fallback chain do recipient (`TenantProfile.phone` → `User.phone` → `User.email`).
- Padrão de fixtures: `apps/api/tests/conftest.py` (`db`, `get_tenant_session_factory` override) — reusar, não criar fixture nova.
- Nenhuma orientação técnica específica encontrada nos docs disponíveis sobre um teste automatizado ponta-a-ponta ("mover card → chega WhatsApp real") — mesma limitação já registrada nas Stories 1.1-1.4 (sem e2e Playwright no projeto); validar manualmente em ambiente com `WHATSAPP_TOKEN`/`WHATSAPP_PHONE_ID` reais antes do go-live, conforme item adicionado em `docs/HOSTINGER-DEPLOY.md` §4 (Task 4).

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-10 | 0.1 | Story criada a partir do Epic 2 (Integrações Reais) — Story 2.3 WhatsApp Cloud API | River (@sm) |
| 2026-07-10 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-07-10 | 0.2 | Implementado: wiring do recipient (fallback TenantProfile.phone→User.phone→User.email), `.env.example` corrigido, testes (`test_whatsapp.py` + extensão de `test_notifications.py`), docs de deploy. Sem migration (colunas já existiam). Status: Ready → InReview | Dex (@dev) |

## Dev Agent Record

### Agent Model Used
claude-opus-4-8 (Dex / @dev), modo YOLO autônomo, worktree isolado.

### Debug Log References
- Sem logs de runtime: este worktree **não tem Python/ruff/pytest instalados** (apenas o stub
  `WindowsApps/python.exe` do Windows, não funcional; sem `.venv`, sem `pip`, sem `conda`/`uv`).
  Portanto `ruff check .` e `pytest` **não puderam ser executados aqui** — validação automatizada
  fica delegada ao quality gate (@architect) / CI, onde a stack Python está disponível.

### Completion Notes List
- **AC1 (envio real):** confirmado por inspeção — `core/whatsapp.py::send_text` já faz o `POST`
  real à Graph API (v21.0) quando `whatsapp_token`+`whatsapp_phone_id` existem; sem chave, retorna
  `"logged"` (graceful degradation); exceção do provedor → `"failed"` sem propagar (IV3). Nenhuma
  mudança de código na função. Adicionado `test_whatsapp.py` cobrindo os 3 status via mock de
  `httpx.post` + `monkeypatch` nas settings (logged/sent/failed, inclusive erro de status HTTP).
- **AC2 (telefone do destinatário):** **sem migration** — as colunas já existem (`tenant_profiles.phone`
  migration 0022; `users.phone` migration 0029). O gap real era o *wiring*: `_owner_recipient` agora
  resolve o recipient pela cadeia `TenantProfile.phone` → `User.phone` → `User.email` (fallback final
  preserva o placeholder histórico e a graceful degradation). Reusa `settings_service.get_profile`
  (sem duplicar lógica). `on_client_moved` inalterado (só o valor de `recipient` muda). O número
  reservado 0034 **não foi usado** — a story não requer schema novo (documentado na Task 2).
- **AC3 (mensagens existentes + Regra de Ouro nº 2):** confirmado por inspeção que os 5 pontos de
  envio (cobrança com IA, funil, crm.client.moved, orçamento, contrato) roteiam pelo único
  `send_text`, passando a entregar de verdade assim que as credenciais existirem em produção —
  sem mudança de código adicional. Anonimização em `_compose_dunning` (placeholder `[NOME]`
  reinserido localmente) intacta; `funnels.ai_compose` compõe template (sem PII de cliente) — IV2 ok.
- **IV1/IV3:** sem chave, nada quebra (só loga); falha do provedor não derruba a request de origem.
- **`.env.example` corrigido:** placeholders inválidos (`META_APP_ID`/`META_APP_SECRET`/
  `WHATSAPP_CLOUD_TOKEN`, nenhum lido pelo Pydantic Settings) substituídos por `WHATSAPP_TOKEN`/
  `WHATSAPP_PHONE_ID` reais. `PAYMENT_GATEWAY_KEY` deixado intacto (escopo da Story 2.2).
- **Docs de deploy:** `HOSTINGER-DEPLOY.md` §2 (vars opcionais) e §4 (como validar entrega real vs.
  log) atualizados.
- **Limitações / precisa de infra real para validar E2E:** entrega ponta-a-ponta com número real
  exige conta/número aprovados na **Meta WhatsApp Cloud API** (`WHATSAPP_TOKEN` + `WHATSAPP_PHONE_ID`)
  em ambiente com credenciais — não disponível neste worktree; validar manualmente antes do go-live
  (item adicionado em `HOSTINGER-DEPLOY.md` §4). Suíte automatizada (ruff+pytest) **escrita mas não
  executada aqui** por ausência de toolchain Python — rodar no gate/CI.
- **Nota operacional:** um linter externo do worktree reverteu uma vez os edits de
  `HOSTINGER-DEPLOY.md`; reaplicados e confirmados presentes antes do commit.

### File List
- ALTERADO: `apps/api/app/modules/notifications/service.py` (`_owner_recipient`: cadeia de fallback
  do recipient + import de `settings_service`)
- ALTERADO: `.env.example` (placeholders WhatsApp corrigidos para os nomes reais)
- NOVO: `apps/api/tests/test_whatsapp.py` (3 status de `send_text`)
- ALTERADO: `apps/api/tests/test_notifications.py` (testes do fallback chain de `_owner_recipient`)
- ALTERADO: `docs/HOSTINGER-DEPLOY.md` (§2 vars opcionais WhatsApp; §4 item de validação)
- ALTERADO: `docs/stories/2.3.story.md` (status InReview, checkboxes, Dev Agent Record)

## QA Results
_(a preencher pelo @qa)_

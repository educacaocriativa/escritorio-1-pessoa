# Story 8.11: Guarda do `opening_date` + aviso pró-ativo no cadastro da conta

## Status
Draft

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["ruff (apps/api)", "pytest apps/api/tests (alterado test_bank_accounts.py; novo test_payables_paid_before.py)", "pytest -m rls_e2e (Postgres real, testcontainers)", "pnpm --filter @e1p/web test (ContasSaldosPage.test.tsx)", "tsc --noEmit + eslint --max-warnings 0", "validação manual: recuar a data de abertura pela UI real e conferir que o saldo é redeclarado, não herdado", "os 3 agentes de QA (regression-tester, bug-hunter, dedup-checker — CLAUDE.md §5)"]
```
> [AUTO-DECISION] Story **sem migration**, com uma guarda de service, um endpoint read-only novo e uma mudança de formulário → `executor="@dev"`. `quality_gate="@architect"` porque a guarda é a **gêmea do BANK-001 pela porta oposta** — o defeito 🔴 HIGH que o gate das Ondas 0–1 pegou — e porque a colocação do endpoint do aviso é decidida por uma **restrição normativa de arquitetura** (§4.1d: `bank` nunca importa `payables`), não por conveniência.

## Story
**As a** dono que vai recuar a data de abertura da minha conta para conseguir trazer as **45 contas já pagas** para o extrato do e1p,
**I want** que o sistema **exija** o saldo daquele dia mais antigo — e que, já no cadastro da conta, ele me diga **quantas contas pagas ficariam de fora** e **qual número ir buscar** no app do banco,
**so that** eu não produza uma divergência inventada nem descubra a parede só depois de estornar 45 contas e não conseguir repagar nenhuma.

> **Esta story é o PASSO 1 do mutirão do founder** (epic §7.2). Ela precisa estar **em produção antes**
> de ele recuar o `opening_date` do C6. Por isso pode mergear **antes de todo o resto da onda**,
> inclusive antes da 8.9.

## Acceptance Criteria

1. **Recuar `opening_date` passa a EXIGIR `opening_balance_cents` no mesmo PATCH.**
   Em `bank/service.update_account`: se `data.opening_date is not None` e
   `data.opening_date < account.opening_date` (recuo) e `data.opening_balance_cents is None` →
   **422**, com a mensagem nomeando **as duas datas**:
   > *"O saldo de abertura que você informou era o saldo de **{opening_date atual}**. Para abrir esta
   > conta em **{nova data}**, informe o saldo daquele dia — o número está no extrato do seu banco."*
   Avançar a data continua caindo em `_validate_opening_date_move` (a guarda do BANK-001, criada na
   8.2 e fechada no re-gate); **não** mexer nela. `opening_date` igual à atual **não é recuo** e não
   dispara nada.
   → design Onda 2 §4.3; epic item 2.12

2. **⚠️ A guarda do AC1 é NECESSÁRIA e INSUFICIENTE — e é isto que dá trabalho nesta story.**
   Verificado no código em 2026-07-30: `ContasSaldosPage.tsx::AccountModal.save()` **sempre** envia
   `opening_balance_cents` no PATCH, e o campo vem **pré-preenchido com o valor antigo**
   (`setOpening(centsToInput(editing?.opening_balance_cents ?? 0))`). Ou seja: pela UI real, o campo
   **nunca está ausente**, o 422 do AC1 **nunca dispara**, e recuar a data grava o **saldo antigo na
   data nova** — que é exatamente a divergência inventada que a guarda existe para impedir.
   **Portanto esta story entrega as duas metades, e nenhuma sozinha é aceitável:**
   - **(a) backend** — o 422 do AC1, que protege a API contra qualquer cliente (Atalho do iOS,
     script, curl, cliente futuro);
   - **(b) frontend** — ao **recuar** a data no formulário, o campo "Saldo de abertura" é **limpo** e
     marcado como obrigatório, o botão de salvar fica **desabilitado** até haver um valor digitado, e
     aparece a frase dizendo **de qual dia** é o saldo pedido. Reaproveitar o valor antigo é
     **proibido**.
   ⚠️ Sem (b), esta story é decorativa. Sem (a), qualquer outro cliente reintroduz o defeito.
   → **[ACHADO DO @SM, 2026-07-30]** — verificado em `ContasSaldosPage.tsx:725-750`; o design §4.3
   especifica só a metade (a), porque descreve o contrato de API, não o formulário

3. **Aviso pró-ativo no cadastro: o e1p diz QUAL número buscar, e não inventa nenhum.**
   Ao cadastrar uma conta nova, quando existirem `Payable` com `status='paid'` e data de pagamento
   **anterior** à `opening_date` escolhida, o formulário mostra, **antes de salvar**:
   > *"Você tem **{N}** contas pagas entre **{DD/MM da mais antiga}** e **{DD/MM da mais recente}**
   > (**{R$ total}**). Se esta conta abrir em {data escolhida}, elas **não** vão entrar no extrato do
   > e1p. Quer abrir em **{dia anterior à mais antiga}** e informar o saldo daquele dia?"*
   O aviso **não bloqueia** o cadastro — informa e oferece a data. O botão que aceita a sugestão
   **só** preenche o campo de data; o **saldo continua sendo digitado pelo usuário**.
   → design Onda 2 §1.2(c); epic item 2.13

4. **O saldo de abertura NUNCA é derivado — é a Regra 5, e o design a nomeia explicitamente.**
   O e1p **não** calcula, sugere ou pré-preenche `opening_balance_cents` a partir do histórico de
   `payables`. Derivá-lo seria *"somar o que o sistema sabe e chamar de o que o banco diz"* — a
   circularidade que faria a divergência ir a zero **por construção no dia um**, matando a métrica
   primária do épico. **Confirmar, não derivar.**
   **Teste de ausência:** nenhuma resposta desta story (API ou UI) contém um valor de saldo calculado;
   varredura reprovando qualquer soma de `amount_cents` de `payables` que desemboque num campo de
   saldo de abertura.
   → design Onda 2 §1.2(c); `CLAUDE.md` regra 5

5. **O aviso é servido pelo lado do NEGÓCIO, nunca pelo módulo `bank`.** Endpoint **novo**,
   read-only, em `payables`:
   ```
   GET /payables/bills/paid-before?date=YYYY-MM-DD
   → PayablesPaidBeforeOut { count: int, total_cents: int, oldest_paid_on: date|None, newest_paid_on: date|None }
   ```
   **[SUPOSIÇÃO DO @SM: a rota, o nome do schema e o formato da resposta.** O design §1.2(c) especifica
   *o que* o aviso diz, não *onde* o dado é servido. **A colocação, porém, é forçada por restrição
   normativa**, não escolhida: o epic §4.1(d) e o gate `test_bank_nao_importa_payables` (Story 8.9)
   proíbem `app.modules.bank` de importar `payables`. Um endpoint em `bank/router.py` que contasse
   contas a pagar violaria o gate. `payables` **pode** importar `bank`; a volta, nunca.**]**
   O endpoint **não escreve nada**, é paginação-livre (devolve agregado, não lista) e respeita RLS
   como todo o resto.
   ⚠️ Se a Story 8.9 já tiver mergeado, o gate estrutural dela **reprova** a alternativa — o que é o
   comportamento desejado. Se ainda não tiver, **não** aproveite a janela.
   → epic §4.1(d), item 2.13; design Onda 2 §3.5

6. **A contagem usa o eixo de CAIXA, não o de competência.** "Conta paga antes de DD/MM" é
   `status = 'paid'` **e** `paid_at::date < DD/MM`. **Nunca** `due_date`, **nunca**
   `competence_date` — regra dura do projeto (`payables/models.py:6-9`): fluxo de caixa usa `paid_at`,
   DRE/lucratividade usam `competence_date`, **nunca invertidos**. O que interessa aqui é o dia em que
   o dinheiro **saiu da conta**, porque é ele que precisa ser posterior à abertura para somar no saldo
   derivado (`_movements_sums`: `posted_at > opening_date`).
   ⚠️ `paid_at` é `TIMESTAMPTZ`; a comparação é por **data de calendário**, com a mesma disciplina do
   `CLAUDE.md` §6.0. Conta com `status='paid'` e `paid_at IS NULL` (legado) **não** entra na contagem,
   e o teste fixa isso.
   → `payables/models.py:6-9`; `bank/service._movements_sums`

7. **A borda é `<`, e ela combina com a guarda que já existe.** Uma conta paga **exatamente na**
   `opening_date` **fica de fora do saldo** (`_movements_sums` usa `posted_at > opening_date`,
   estritamente) e `_validate_posted_at` a recusa com 422. Portanto a data sugerida pelo AC3 é o **dia
   anterior** à conta paga mais antiga, não o mesmo dia. É a mesma assimetria que a Story 8.4 fixou em
   `_validate_reference_date` e que o re-gate da 8.2 confirmou como certa. **Teste de borda
   obrigatório.**
   → `bank/service.py:287-306, 605-628`; gate das Ondas 0–1, BANK-001

8. **Nada em `_validate_posted_at` é tocado.** Esta story **não** afrouxa a guarda de lançamento
   (`posted_at > opening_date` → 422; `posted_at` futuro → 422). Ela ataca o problema pelo lado
   **certo**: mover a parede, não furá-la. Quem mexe no caminho de `posted_at` do lado da **origem** é
   a 8.12; quem libera o futuro é a 8.14.
   → epic §6, delimitação da 8.11 (*"Não inclui: mexer em `_validate_posted_at`"*)

9. **Isolamento validado no Postgres real.** O endpoint novo entra na suíte `rls_e2e`: o tenant A
   nunca conta `payables` do tenant B; sem a GUC `app.current_tenant_id` a leitura é fail-closed. E a
   guarda do AC1 é exercitada sob RLS (recuo com saldo → 200; recuo sem saldo → 422), como o design
   pede: **+2 testes**.
   → epic §4.7; job `cross-tenant-rls`; design Onda 2 §4.3

10. **Aceite manual em ~360px do formulário de conta.** O `AccountModal` ganha campo condicional,
    frase de aviso e botão de sugestão — três elementos novos num modal que já tem 9 campos. **G-4
    está aberto desde a Onda 1** e o fundador decidiu que ele **bloqueia release** (F-D8). Esta story
    **não** é a que o epic marca como bloqueante em 360px (essa é a 8.13), mas o formulário precisa ser
    conferido em ~360px e o resultado registrado no Dev Agent Record — porque **dois PRs de fix de
    campo já foram pagos** por elemento fora da área visível (PRs #56 e #58).
    → epic §9.2 F-D8/G-4; `CLAUDE.md` (PRs #56, #58)

### Integration Verification

- **IV1 — Conferência intacta em comportamento, e é ela que esta story protege.** `reconciliation.py`
  **não é editado**. O ponto: recuar `opening_date` **legitimamente** (com saldo redeclarado) muda o
  saldo derivado — e **deve** mudar. Teste em duas metades: (a) recuo **com** saldo correto → a
  divergência **permanece a mesma** (o saldo de partida e o conjunto de movimentos mudam juntos, e é
  isso que torna o recuo "o caminho de reparo"); (b) recuo **sem** redeclarar → **422**, e nada muda.
  ⚠️ A metade (a) é o teste mais importante desta story: é ele que prova que a guarda **não** quebrou
  o caminho de reparo que o design chama de legítimo.
- **IV2 — DRE e Lucratividade intactas.** `dre.py` agrega `charges` + `payables` + `transactions` por
  **competência** e **não lê `bank_accounts`**. Mudar `opening_date` ou `opening_balance_cents` não
  toca a DRE em hipótese nenhuma. Snapshot campo a campo mesmo assim.
- **IV3 — Projeção de Caixa.** `projection.py` **não é editado**. `_saldo_inicial` soma
  `active_balance_total`, que **depende** do saldo derivado — então recuar a abertura **com** saldo
  redeclarado muda a Projeção, corretamente. Teste: recuo sem saldo (422) → Projeção **idêntica**.
  ⚠️ **Dependência registrada:** a Story **8.19** (outro @sm) corrige `_saldo_inicial`, que decide
  `ORIGEM_MISTO` pela **mera existência de conta** e não por saldo conhecido. **A 8.19 entra antes
  desta onda.** Esta story **não antecipa** o conserto e **não toca `projection.py`**.
- **IV4 — Cockpit intacto.** Nada aqui alcança `net_revenue`.
- **IV5 — Carteira intacta.** Nenhum arquivo de `app/modules/wallet/`. Os gates da Regra dos Planos
  seguem verdes.
- **IV6 — Fila de Pagamentos e Contas a Pagar intactas.** `payables/service.py` ganha **uma função
  de leitura** e `payables/router.py` **uma rota GET**; nenhum caminho de escrita é tocado.
  `POST /payables/bills/{id}/pay`, `/cancel`, `/reverse` e `PATCH /bills/{id}` **inalterados**.
  `FilaPagamentosPage` e `PagarPage` **não são editadas** nesta story.
- **IV7 — Diagnóstico intacto.** `engine.py` continua **puro**; nenhum sinal novo. A regra de
  completude consome o serviço da 8.5, que não mudou.
- **IV8 — Contas & Saldos: o formulário muda, a lista não.** Os dois totais (`TOTAL_EM_CONTAS_LABEL`,
  `DISPONIVEL_CAIXA_LABEL`) e o cálculo por trás deles **não são tocados**. Os testes de **colisão de
  rótulo** do UX-001 (`contas.test.ts:157-168`) seguem verdes — a frase do aviso é texto de
  formulário, **não** um rótulo de saldo, e **não pode conter** `ROTULO_BANCO`,
  `TOTAL_EM_CONTAS_LABEL` nem `DISPONIVEL_CAIXA_LABEL`. Verificar explicitamente.
- **IV9 — Zero regressão.** Baseline: backend **1051 passed, 1 skipped**; `rls_e2e` **22**; frontend
  **304 / 50**. Ao final: os mesmos + os novos. **Sem migration.**

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false`, mesma decisão já documentada nas Stories 5.7, 6.1, 6.2, 7.5 e 8.2–8.8.
> Quality validation will use manual review process only (ver `quality_gate_tools` acima e `scripts/check.sh`).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [ ] **Task 1 — Reler as duas guardas antes de acrescentar a terceira (AC: 1, 7, 8)**
  - [ ] `bank/service.py::_validate_opening_date` (data futura), `_validate_opening_date_move`
        (avanço por cima de movimento — a correção do BANK-001) e `_validate_posted_at`
  - [ ] Confirmar que `_validate_opening_date_move` deixa o **recuo** passar direto e que a docstring
        o descreve como *"o caminho de reparo"* — a guarda nova **não pode** fechar esse caminho, só
        exigir o saldo junto
  - [ ] Confirmar a assimetria de borda (`>` no saldo, `<=` recusado no lançamento) e derivar dela a
        data que o AC3 sugere: **dia anterior**, nunca o mesmo dia

- [ ] **Task 2 — A guarda do recuo (AC: 1)**
  - [ ] Em `update_account`, **antes** de qualquer escrita em `acc`: detectar recuo e exigir
        `opening_balance_cents`
  - [ ] ⚠️ **Ordem importa** — hoje `opening_date` é escrito no início e `opening_balance_cents` só
        depois, no laço de campos. As duas guardas comparam contra a data **atual** da conta; nenhuma
        escrita pode acontecer antes de as três passarem
  - [ ] Mensagem nomeando as **duas datas** e dizendo **onde** buscar o número
  - [ ] Docstring nova: por que exigir, e por que isto é o gêmeo do BANK-001 pela porta oposta

- [ ] **Task 3 — O endpoint do aviso, do lado do negócio (AC: 5, 6)**
  - [ ] `payables/service.py::paid_before(db, *, date_: date) -> dict` — agregado, read-only
  - [ ] `payables/schemas.py::PayablesPaidBeforeOut`
  - [ ] `GET /payables/bills/paid-before` em `payables/router.py`
  - [ ] ⚠️ Registrar a rota **antes** de `GET /bills/{payable_id}` (`router.py:67`), senão o FastAPI
        casa `paid-before` como um `payable_id` e a rota nova nunca é alcançada
  - [ ] `status='paid'` **e** `paid_at IS NOT NULL` **e** `paid_at::date < :date` — nunca `due_date`
  - [ ] **Nenhuma soma de valor vira saldo**: `total_cents` é o total **pago**, rotulado como tal na
        UI, e nunca é oferecido como saldo de abertura (AC4)

- [ ] **Task 4 — O formulário: as duas metades (AC: 2, 3, 4)**
  - [ ] **(a)** No `AccountModal`, ramo de **edição**: comparar a data escolhida com
        `editing.opening_date`. Se recuou → **limpar** o campo de saldo, marcá-lo obrigatório,
        desabilitar o salvar até haver valor, e mostrar de **qual dia** é o saldo pedido
  - [ ] **(b)** No ramo de **criação**: ao escolher/alterar a data de abertura, chamar
        `GET /payables/bills/paid-before` e montar a frase do AC3, com o botão que preenche **só a
        data** sugerida
  - [ ] Debounce/aborto na chamada — o campo é `type="date"` e dispara a cada tecla em alguns
        navegadores
  - [ ] Falha da chamada (rede, 401, módulo não permitido) → **o aviso simplesmente não aparece**;
        cadastrar conta **nunca** pode ficar bloqueado por causa dele (mesmo padrão fail-safe de
        `PagarPage`, que já trata `require_module` sem quebrar a tela)
  - [ ] Zero contas pagas anteriores → **nenhum aviso** (silêncio; a disciplina anti-ruído da banda de
        tolerância vale aqui também)

- [ ] **Task 5 — Testes de backend (AC: 1, 5, 6, 7, 9)**
  - [ ] Recuo **com** `opening_balance_cents` → **200**, e o saldo gravado é o **novo**
  - [ ] Recuo **sem** `opening_balance_cents` → **422**, e **nada** é gravado (nem a data)
  - [ ] Avanço → segue caindo em `_validate_opening_date_move` (BANK-001 **não** regride)
  - [ ] Mesma data → não é recuo, passa
  - [ ] `paid-before`: contagem por `paid_at`, borda `<` estrita, `paid_at IS NULL` fora, conta
        `canceled`/`open` fora, tenant vazio → zeros
  - [ ] `rls_e2e`: os +2 casos do design + isolamento cross-tenant do endpoint novo

- [ ] **Task 6 — Testes de frontend (AC: 2, 3, 10)**
  - [ ] Recuar a data **limpa** o saldo e **desabilita** salvar até haver valor
  - [ ] Avançar a data **não** limpa nada
  - [ ] Aviso aparece com N > 0 e **não** aparece com N == 0
  - [ ] Botão de sugestão preenche **só** a data
  - [ ] Falha do endpoint → o modal segue funcional
  - [ ] Colisão de rótulo: a frase do aviso **não** contém `ROTULO_BANCO`, `TOTAL_EM_CONTAS_LABEL`
        nem `DISPONIVEL_CAIXA_LABEL`

- [ ] **Task 7 — Aceite em ~360px (AC: 10)**
  - [ ] Conferir o `AccountModal` inteiro em ~360px: o campo de saldo, a frase do aviso e o botão de
        salvar **visíveis sem rolagem escondida**
  - [ ] Registrar o resultado (com evidência) no Dev Agent Record — G-4 está aberto e é decisão do
        fundador

- [ ] **Task 8 — Fechamento (AC: todos)**
  - [ ] Etapas de `scripts/check.sh` **individualmente** (X9)
  - [ ] Backend `1051 + novos`; `rls_e2e 22 + novos`; frontend `304/50 + novos`
  - [ ] Os 3 agentes de QA
  - [ ] File List e Dev Agent Record

## Dev Notes

### Por que esta story vem primeiro, e por que ela é operacional antes de ser técnica

O epic §7.2 registra uma **situação verificada**: o fundador cadastrou a conta **C6 em 2026-07-30**,
com `opening_date` = hoje. O saldo derivado só soma `posted_at > opening_date`, e `_validate_posted_at`
recusa `posted_at <= opening_date` com 422. **As duas guardas estão certas** — o que aconteceu até a
abertura já está dentro de `opening_balance_cents`, e contar de novo dobraria o valor.

**Consequência CERTA, não provável:** com `opening_date = 30/07`, **as 45 baixas retroativas levam 422,
todas**.

A ordem do mutirão **não é negociável** (epic §7.2):

| Passo | Ação | Se pular |
|---|---|---|
| **1** | **Recuar o `opening_date` do C6**, declarando o saldo daquele dia | Todo repagamento leva 422 |
| **2** | **Só então estornar** as 45 | — |
| **3** | **Repagar** informando conta e data | — |

> ⚠️ **Invertendo a ordem, ele fica com 45 contas estornadas e nenhuma repagável.**

**Esta story é o pré-requisito de software do passo 1.** Sem ela, recuar a data sem redeclarar o saldo
produz uma **divergência inventada** — o BANK-001 pela porta oposta. E o passo 3 depende da **Story
8.12**, que ainda não existe. Por isso a 8.11 pode e deve mergear cedo, inclusive **antes da 8.9**.

### O gêmeo do BANK-001, e por que ele é mais perigoso agora

O BANK-001 (🔴 HIGH, gate das Ondas 0–1) era: mover `opening_date` **para frente** fazia os movimentos
anteriores pararem de contar no saldo **continuando visíveis na lista** — a conferência relatava um
furo que não existe. Foi corrigido por `_validate_opening_date_move` (5 testes, 4 mutantes mortos,
fechado no re-gate).

O que sobrou é a **porta oposta**, e o design a nomeia (§4.3):

> `update_account` permite recuar `opening_date` **sem exigir um novo `opening_balance_cents`** — e o
> saldo de abertura antigo era o saldo *na data antiga*. Recuar sozinho produz um saldo de partida
> errado e, portanto, **uma divergência inventada** — a mesma classe do BANK-001, pela porta oposta.

E a probabilidade muda de patamar com esta onda: antes, recuar a data era operação rara; a partir do
mutirão das 45, é **o primeiro passo obrigatório**. O epic classifica o risco como **"Alta depois desta
onda"**.

**Por que divergência inventada é pior que divergência escondida** (a razão está escrita no próprio
service, na docstring do `_validate_opening_date_move`): *"depois de duas caçadas frustradas ele para de
confiar no sinal, e o sinal é o produto"*.

### ⚠️ O achado que muda o tamanho desta story

**Verificado no código, não deduzido do design.** `ContasSaldosPage.tsx::AccountModal` é usado para
**criar e editar**, e o `save()` monta o corpo assim:

```
opening_balance_cents: parseCentsBRL(opening),   // ← pré-preenchido com o valor ANTIGO
opening_date: openingDate,
```

para **os dois** caminhos (`POST` e `PATCH`). Ou seja, o campo **nunca chega ausente** pela UI real.
Um 422 "obrigatório se ausente" seria, do ponto de vista do usuário do produto, **código morto**: ele
recuaria a data, o formulário mandaria o saldo velho, o backend responderia 200, e a divergência
inventada aconteceria **exatamente como se a guarda não existisse**.

O design §4.3 não está errado — ele descreve o **contrato de API**, e a guarda é indispensável ali (o
Atalho do iOS, um script, um cliente futuro). Mas a guarda **sozinha não resolve o problema do
produto**, e é o problema do produto que o epic §7.2 diz que bloqueia o mutirão.

**Por isso o AC2 exige as duas metades**, e a de frontend não é polimento: é a metade que efetivamente
protege o fundador na semana em que ele vai fazer o mutirão.

### O aviso pró-ativo — "sistema integrado" aplicado a um dado externo, sem violar a Regra 5

Esta é a distinção mais fina da story, e a @architect a escreveu explicitamente (design §1.2(c)):

> O saldo de abertura é *o saldo do banco numa data*: um fato **sobre o banco**, que o sistema por
> definição não conhece. Derivá-lo do histórico de `payables` seria somar o que o sistema sabe e chamar
> de "o que o banco diz" — a circularidade da Regra 5, com a divergência indo a zero por construção
> **no dia um**. **Confirmar, não derivar.**

**Mas há uma coisa adjacente que o sistema pode e deve fazer:** quando o dono cadastra a conta, o e1p
**já sabe** a data da conta a pagar mais antiga que ele vai querer trazer.

> **O e1p não inventa o número — ele diz qual número ir buscar.**

Essa frase é o AC3 e o AC4 juntos, e a diferença entre eles é a diferença entre esta onda e um bug.

A @architect também respondeu, no mesmo lugar, a pergunta de se o saldo de abertura não seria mais um
caso de "o sistema já sabe": **não é**, e confundi-lo com os cinco eventos da §1.2 quebraria a Regra 5.

### Por que o endpoint do aviso mora em `payables` e não em `bank`

Restrição **normativa**, não preferência. Epic §4.1(d), a partir da Onda 2:

> a dependência é **de negócio para banco, nunca a volta** — `payables`/`receivables` **podem**
> importar `app.modules.bank`; `app.modules.bank` **nunca** importa `payables`/`receivables`.

A Story **8.9** transforma isso em gate executável (`test_bank_nao_importa_payables`, AST + texto cru).
Um endpoint em `bank/router.py` que contasse contas a pagar **falharia o gate** — e é para isso que o
gate existe: *"sem ela, o primeiro atalho de conveniência recria um ciclo"*.

⚠️ **Nota de sequenciamento:** esta story pode mergear **antes** da 8.9, ou seja, num momento em que o
gate ainda não existe. **Não aproveite a janela.** A restrição já é normativa no epic; o gate só a
torna auditável.

### Caixa, nunca competência — a regra dura que a contagem tem de respeitar

`payables/models.py:6-9`, verbatim:

> Regra determinística (Story 5.2): fluxo de caixa usa `paid_at` (regime de caixa);
> DRE/lucratividade/relatórios analíticos usam `competence_date` (regime de competência).
> **Nunca inverter.**

Aqui a pergunta é *"o dinheiro saiu da conta antes de a conta abrir no e1p?"* — pergunta de **caixa**.
Usar `due_date` daria a contagem errada para toda conta paga em atraso ou adiantada, que é justamente o
caso do fundador (ele mesmo disse: *"se estiver fazendo retroativo, pq não deu certo no dia"*).

### Posição na ordem de merge (epic §6.1)

```
8.11 ──► 8.9 ──► 8.10 ──► 8.12 ──► 8.13
```

**1ª a mergear, e a única que pode ir antes da 8.9.** O epic justifica: *"Não depende de nada e
destrava o passo 1 do mutirão (§7.1). Pode ir primeiro, inclusive antes da 8.9."*
**Paralelizável com segurança:** `{8.11}` sozinha no conjunto.

### Contrato público criado por esta story

> Registrado porque as stories da onda são escritas por @sm diferentes que não conversam.

| Símbolo | Arquivo | Consumidores |
|---|---|---|
| `GET /payables/bills/paid-before?date=` → `PayablesPaidBeforeOut {count, total_cents, oldest_paid_on, newest_paid_on}` | `payables/router.py`, `payables/schemas.py` | `ContasSaldosPage` (esta story). **8.13** pode reusar para explicar o 422 do `opening_date` na baixa; **8.16** **não** deve — a nota do gate no bloco 4 é sobre *lançamentos sem conta informada*, não sobre *lançamentos anteriores à abertura*, e são coisas diferentes |
| `payables/service.py::paid_before(db, *, date_)` | `payables/service.py` | idem |
| 422 de `update_account` no recuo sem saldo | `bank/service.py` | **8.12/8.13** — o 422 do `opening_date` na baixa (design §4.3) é **outro** 422, disparado por `_validate_posted_at`; não confundir as mensagens |

### File Locations (novos/alterados)

| Arquivo | O quê |
|---|---|
| `apps/api/app/modules/bank/service.py` | guarda do recuo em `update_account` (AC1) |
| `apps/api/app/modules/payables/service.py` | `paid_before` (read-only) |
| `apps/api/app/modules/payables/schemas.py` | `PayablesPaidBeforeOut` |
| `apps/api/app/modules/payables/router.py` | `GET /bills/paid-before` — **antes** de `/bills/{payable_id}` |
| `apps/api/tests/test_bank_accounts.py` | +casos do recuo |
| `apps/api/tests/test_payables_paid_before.py` | **novo** |
| `apps/api/tests/test_bank_rls.py` (ou novo `_rls`) | +2 casos (`rls_e2e`) |
| `apps/web/src/features/financeiro/ContasSaldosPage.tsx` | `AccountModal`: campo condicional + aviso |
| `apps/web/src/features/financeiro/ContasSaldosPage.test.tsx` | casos novos |

**Nenhuma migration.** `reconciliation.py`, `projection.py`, `engine.py`, `wallet/`, `PagarPage` e
`FilaPagamentosPage` **não são tocados**.

### Technical Constraints

- **Não derivar saldo.** Nenhum caminho desta story produz um valor de `opening_balance_cents`.
- `paid_at` é `TIMESTAMPTZ`; comparar por **data de calendário** (`CLAUDE.md` §6.0).
- A borda do saldo derivado é `>` **estrita**; a data sugerida é o **dia anterior**.
- Nenhuma query filtra `tenant_id` à mão — RLS é a única garantia.
- O aviso **degrada em silêncio**: falhar nele nunca bloqueia o cadastro da conta.
- Sem migration, sem tabela, sem coluna.

### Testing

- **Unitários**: as 4 combinações de `opening_date` (recuo com saldo, recuo sem saldo, avanço, igual);
  `paid_before` (borda `<`, `paid_at IS NULL`, status errado, tenant vazio).
- **Regressão de guarda**: os 5 testes do BANK-001 e o `test_patch_opening_date_ignora_movimento_de_
  OUTRA_conta_do_mesmo_tenant` (RG-1) seguem verdes **sem edição**.
- **RLS e2e** (Postgres real, `pytest.mark.rls_e2e`): os +2 casos do design §4.3 + isolamento do
  endpoint novo.
- **Frontend** (vitest): as duas metades do AC2, o aviso do AC3, a degradação silenciosa, a colisão de
  rótulo.
- **Snapshot de não-regressão**: DRE, Projeção e Conferência (nas duas metades da IV1).
- **Mutação**: pelo menos 3 mutantes mortos — remover a condição de recuo (avanço passaria a exigir
  saldo indevidamente); trocar `<` por `<=` na contagem do `paid_before`; remover a limpeza do campo no
  frontend (o teste do AC2b tem de cair).
- **Manual**: recuar a data pela UI real e conferir que o saldo é **redeclarado**, não herdado; e o
  aceite em ~360px (AC10).
- Etapas de `scripts/check.sh` rodadas **individualmente** (X9).

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-30 | 0.1 | Story criada a partir do Epic 8 §5 (itens 2.12, 2.13), §6 e §7.2, Onda 2 — River (@sm). Inclui o **[ACHADO DO @SM]** do AC2: a guarda de API do design §4.3 é inócua contra o `AccountModal` atual, que sempre envia `opening_balance_cents` pré-preenchido — a story passa a exigir as duas metades | River (@sm) |

## Dev Agent Record

### Agent Model Used
_(a preencher pelo @dev)_

### Debug Log References
_(a preencher pelo @dev — rodar tudo em foreground e colar a saída real)_

### Completion Notes List
_(a preencher pelo @dev)_

### File List
_(a preencher pelo @dev)_

## QA Results
_(a preencher pelo @architect no quality gate)_

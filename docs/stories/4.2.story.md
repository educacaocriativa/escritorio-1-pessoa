# Story 4.2: Upload real de logo e imagens

## Status
Done

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["pytest apps/api/tests (test_attachments.py, test_settings.py, test_quotes.py, test_marketing.py, test_pages.py)", "vitest apps/web (novo, feature sem testes de UI ainda)", "bash scripts/check.sh (lint+types+testes, CLAUDE.md §5)", "revisão manual de isolamento (IV2): confirmar que a nova tabela pública NÃO expõe `Attachment` (documentos privados) nem outras tabelas com RLS"]
```
> [AUTO-DECISION] Story mistura backend (FastAPI: nova tabela/rota + reuso de 4 módulos) e frontend (4 features React) sem introduzir infraestrutura nova → mapeia para "Code/Features/Logic" na matriz Work Type → Executor (`Code/Features/Logic | @dev | @architect`). quality_gate=@architect (em vez de @qa) porque a Task 1 introduz uma **decisão de arquitetura não trivial** (servir imagem publicamente sem autenticação, ver Dev Notes) que merece revisão de padrão, não só de bug. Reason: nenhuma orientação técnica encontrada nos docs sobre esse ponto específico — decisão registrada como AUTO-DECISION na Task 1 e deve ser validada por @architect antes de "Done".

## Story
**As a** dono da empresa de 1 pessoa,
**I want** subir logo e imagens de verdade (Brand Kit, propostas, carrossel, sites) em vez de colar URL,
**so that** eu não dependa de hospedar imagens externamente.

## Acceptance Criteria
1. Brand Kit, propostas, carrossel e sites aceitam upload real de imagem/logo (reusando a camada de storage do Epic 3.5), mantendo a prévia ao vivo.
2. Campos de URL antigos continuam aceitos (retrocompatível) durante a transição.
3. O reuso de Brand Kit (proposta/carrossel herdam cores/logo) continua funcionando com o upload real.

### Integration Verification
- IV1: Propostas/carrosséis/sites existentes (com URL) continuam renderizando.
- IV2: Isolamento por tenant no storage preservado.
- IV3: Guarda de esquema de `<img src>` (segurança já existente) mantida.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false` por instrução explícita do orquestrador desta missão (mesmo padrão das Stories 1.1-1.4 e 3.4).
> Quality validation usa processo manual (ver `quality_gate_tools` acima).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [x] **Task 1 — Endpoint de imagem pública, reaproveitando a camada de Anexos do Epic 3.5 (AC: 1; fundação das Tasks 2-5)**
  - [x] **[AUTO-DECISION]** Problema identificado por leitura direta do código: o módulo de Anexos existente (`apps/api/app/modules/attachments/`) é **privado por design** — `download_attachment` exige `CurrentUser` autenticado (`require_module("attachments")`) e o frontend só consegue exibir o arquivo baixando-o via `axios` (Bearer token) e criando um Blob URL (`apps/web/src/components/Attachments.tsx:52-55`, função `open()`). Isso funciona para o caso de uso atual (boleto/contrato, abertos sob clique, dentro do app autenticado), mas **não funciona** para um `<img src="...">` direto — nem dentro do app autenticado (o browser não anexa o header `Authorization` em tags `<img>`) nem, principalmente, nas **páginas públicas sem login** que esta story precisa alimentar. Decisão: reaproveitar a camada de Anexos como base, mas criar uma **segunda tabela GLOBAL sem RLS** dedicada a imagens intencionalmente públicas (`public_images`). **@architect deve confirmar esta decisão** antes de "Done".
  - [x] Modelo novo em `apps/api/app/modules/attachments/models.py`: `PublicImage(Base, TimestampMixin)` (SEM `TenantMixin` — tabela global). Colunas: `id`, `tenant_id` (rastreio/auditoria, NÃO usada para leitura), `content_type`, `size`, `data` (`LargeBinary`). Tabela: `public_images`.
    - [x] **[AUTO-DECISION]** Restringir `content_type` a `{"image/jpeg", "image/png"}` (constante nova `PUBLIC_IMAGE_TYPES`).
    - [x] Reaproveitar `MAX_BYTES = 10 * 1024 * 1024` (10MB).
  - [x] Service novo em `apps/api/app/modules/attachments/service.py`: `create_public_image` e `get_public_image` (404 se não encontrado).
  - [x] Rotas em `apps/api/app/modules/attachments/router.py`: `POST /attachments/public-images` (autenticado) → `{id, url}`; novo `public_router` com `GET /public-images/{id}` **sem autenticação** via `get_db`, retornando `Response(content, media_type)` inline.
  - [x] Registrar o novo `public_router` em `apps/api/app/modules/__init__.py` (`attachments_public_router` em `ALL_ROUTERS`).
  - [x] Migration Alembic nova: `apps/api/migrations/versions/0041_public_images.py` — `down_revision="0031"` (head confirmado via inspeção: nenhuma migration revisa 0031 nesta branch).
  - [x] Adicionar tipo `PublicImage { id, url }` em `packages/shared-types/src/index.ts`.
  - [x] Criar helper `apps/web/src/lib/uploadPublicImage.ts` — `uploadPublicImage(file): Promise<string>` devolve a URL renderável.
  - [x] Testes backend novos em `apps/api/tests/test_attachments.py`: upload válido → 201 + url; tipo fora de {jpeg,png} → 415; >10MB → 413; `GET /public-images/{id}` sem auth → 200; inexistente → 404; upload sem auth → 401.

- [x] **Task 2 — Brand Kit (Configurações): upload real de logo (AC: 1, 3)**
  - [x] `apps/web/src/features/config/ConfiguracoesPage.tsx`: botão de upload ao lado do campo `Logo (URL)` que chama `uploadPublicImage` e faz `set({ logo_url: url })`. Campo de URL mantido (AC2).
  - [x] Prévia ao vivo (safeSrc + `<img>`) funciona sem mudança (URL relativa `/api/public-images/{id}` passa na guarda).
  - [x] AC3 (reuso do Brand Kit): confirmado por leitura — herança de `logo_url` continua sendo string simples, sem mudança de fluxo (validação e2e manual documentada).

- [x] **Task 3 — Propostas (Central de Orçamentos): upload de logo + galeria de imagens (AC: 1, 2, 3)**
  - [x] `QuoteBuilderPage.tsx` aba "Aparência": upload ao lado do input de URL do logo.
  - [x] Aba "Imagens": upload por linha da galeria via prop opcional aditiva `uploadField` no `ListTab` genérico (Cronograma não passa nada → sem mudança de comportamento).
  - [x] `ProposalView.tsx`/`PublicProposalPage.tsx` continuam funcionando sem mudança (strings simples + nova rota pública).
  - [x] IV1: nenhuma migration toca `quotes`/`published_proposals`.

- [x] **Task 4 — Carrossel: upload de foto por slide (AC: 1, 2)**
  - [x] `CarrosselBuilderPage.tsx`: botão de upload por slide que chama `uploadPublicImage` e `setSlide(i, { photo_url: url })`.
  - [x] **[AUTO-DECISION]** Aplicada a guarda `safeSrc` em `CarouselSlideView.tsx` (gap pré-existente — usava `slide.photo_url` direto em background-image e `<img>`), uniformizando o padrão nos 4 consumidores (IV3).
  - [x] Export html2canvas: nova rota é same-origin via `/api` (Vite/Caddy) — sem CORS/tainted canvas (validação manual documentada).

- [x] **Task 5 — Sites: upload de logo (estilo) + imagem em bloco (AC: 1, 2)**
  - [x] `PageBuilderPage.tsx`: adicionado campo de logo (URL) + upload no painel de Aparência (o `logo_url` já era salvo no payload, mas não tinha UI de edição).
  - [x] `PageBuilderPage.tsx` (`BlockFields`, bloco `image`): upload no editor do bloco de imagem. `PageBlocks.tsx` já renderiza com `safeSrc` — sem mudança necessária.
  - [x] `PublicPage.tsx` continua funcionando sem mudança.

- [x] **Task 6 — Retrocompatibilidade, isolamento e regressão (AC: 2; IV1, IV2, IV3)**
  - [x] Confirmado (revisão de código): `logo_url`/`photo_url`/`gallery[].url`/`block.url` continuam string/JSON livre — nenhuma mudança de schema nessas tabelas, dados antigos válidos por construção (AC2, IV1).
  - [x] Confirmado que `Attachment` (privado) não foi alterado — só recebeu tabela/rota adicional ao lado (IV2).
  - [x] IV2 documentado: `public_images` é deliberadamente pública na leitura (mesmo padrão de `published_*`); isolamento aplica-se à escrita (upload autenticado) e à separação total de `Attachment`. `test_tenancy_guard.py` allowlist atualizada com `attachments` (uso legítimo de `get_db`).
  - [x] IV3: guarda `safeSrc` validada nos 4 pontos de renderização (config, proposta, sites, carrossel após Task 4).
  - [x] Suíte backend completa verde (292 testes) + ruff limpo. Frontend: tsc + vitest verdes.
  - [x] Teste vitest do helper `uploadPublicImage` (mock do axios) adicionado.

## Dev Notes

### Contexto do sistema (fonte: Epic 4 / CLAUDE.md)
- "Logo/imagens: vários módulos (Brand Kit, propostas, carrossel, sites) aceitam apenas **URL**, não upload." — enumeração oficial dos 4 alvos desta story. [Source: docs/prd/epic-4-backlog-pos-lancamento.md]
- Módulo de Anexos (Epic 3.5): upload real PDF/JPEG/PNG (≤10MB), bytes no Postgres, RLS por tenant. "migrar para S3 é dívida" (fora do escopo). [Source: CLAUDE.md#Anexos]
- Regra de Ouro nº 1 (RLS) + padrão "snapshot público global sem RLS" (`published_proposals`/`published_contracts`/`published_pages`) = base da decisão da Task 1.

### File Locations (novos/alterados)
- NOVO: `apps/api/migrations/versions/0041_public_images.py`
- ALTERAR: `apps/api/app/modules/attachments/models.py` (`PublicImage`, `PUBLIC_IMAGE_TYPES`)
- ALTERAR: `apps/api/app/modules/attachments/service.py` (`create_public_image`, `get_public_image`)
- ALTERAR: `apps/api/app/modules/attachments/schemas.py` (`PublicImageOut`)
- ALTERAR: `apps/api/app/modules/attachments/router.py` (`POST /attachments/public-images`, `public_router`)
- ALTERAR: `apps/api/app/modules/__init__.py`, `apps/api/app/db/registry.py`
- ALTERAR: `packages/shared-types/src/index.ts` (`PublicImage`)
- NOVO: `apps/web/src/lib/uploadPublicImage.ts` + `.test.ts`, `apps/web/src/components/ImageUploadButton.tsx`
- ALTERAR: `ConfiguracoesPage.tsx`, `QuoteBuilderPage.tsx`, `CarrosselBuilderPage.tsx`, `CarouselSlideView.tsx`, `PageBuilderPage.tsx`
- ALTERAR: `apps/api/tests/test_attachments.py`, `apps/api/tests/test_tenancy_guard.py`

### Technical Constraints
- Não modificar `Attachment` (modelo/rota/service) — apenas adicionar código novo ao lado (IV2).
- `public_images` é tabela GLOBAL (sem RLS) por design.
- Bytes continuam no Postgres (S3 é dívida, fora do escopo).

## Story Draft Checklist Results

| Category | Status |
|---|---|
| 1. Goal & Context Clarity | PASS |
| 2. Technical Implementation Guidance | PASS |
| 3. Reference Effectiveness | PASS |
| 4. Self-Containment Assessment | PASS |
| 5. Testing Guidance | PASS |
| 6. CodeRabbit Integration | N/A |

**Avaliação final: READY.**

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-10 | 0.1 | Story criada a partir do Epic 4 (Backlog Pós-Lançamento) | River (@sm) |
| 2026-07-10 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-07-10 | 0.2 | Implementada (backend + frontend + testes) — Status: Ready → InReview | Dex (@dev) |
| 2026-07-11 | 1.0 | QA Gate PASS (7 checks; `public_images` global sem RLS confirmado em código; suíte 523 passed/9 skipped) — Status: InReview → Done | Quinn (@qa) |

## Dev Agent Record

### Agent Model Used
claude-opus-4-8 (Dex / @dev)

### Debug Log References
- Backend rodado via imagem Docker `e1p-api-test:local` (Python 3.13 + deps) com o código do worktree bind-mounted, pois não há interpretador Python nativo neste ambiente Windows. Testes usam SQLite in-memory (conftest), sem necessidade de Postgres.
- Frontend: `pnpm install` no worktree + `pnpm --filter @e1p/web typecheck` (verde) + `pnpm --filter @e1p/web test` (vitest, verde).

### Completion Notes List
1. **Decisão de arquitetura (Task 1) — REQUER CONFIRMAÇÃO DE @architect (quality_gate):** criada tabela GLOBAL sem RLS `public_images` (mesmo padrão de `published_*`) em vez de expor `Attachment` (privado, guarda boletos/contratos) sem autenticação. Escrita autenticada por tenant (`tenant_id` gravado só para rastreio); leitura pública deliberada via `GET /public-images/{id}` (rota `public_router`, `get_db` com guarda documentada). `Attachment` permanece 100% intacto/RLS.
2. **Desvio da orientação literal da story sobre a URL retornada (verificado contra a infra real):** a story sugeria `url = "/public-images/{id}"` usado direto no `<img src>`. Isso **NÃO renderiza** no navegador: o backend não tem prefixo `/api`, e tanto o Vite dev (`vite.config.ts` `rewrite`) quanto o Caddy de produção (`infra/Caddyfile` `handle_path /api/*`) **removem** o prefixo `/api` antes de repassar. Portanto o valor que é persistido em `logo_url`/`gallery[].url`/`photo_url`/`block.url` e renderizado cru (inclusive nas páginas públicas de snapshot) precisa ser `/api/public-images/{id}`. Solução: o backend devolve o caminho da sua própria árvore de rotas (`/public-images/{id}`) e o helper `uploadPublicImage` prefixa com `api.defaults.baseURL` (`/api`, fonte única de verdade) → URL renderável. Passa na guarda `safeSrc` (`^(https?://|/)`).
3. **`ListTab` (QuoteBuilderPage):** para a galeria, em vez de duplicar toda a renderização da lista, adicionei uma prop **opcional e retrocompatível** `uploadField` — quando setada, cada linha ganha um `ImageUploadButton` que preenche o campo. O consumidor "Cronograma" não passa a prop, então seu comportamento é idêntico ao anterior.
4. **`CarouselSlideView.tsx` (IV3):** aplicada a guarda `safeSrc` (gap pré-existente) em `background-image` e `<img src>`, uniformizando os 4 consumidores.
5. **`PageBuilderPage.tsx`:** o `logo_url` já era enviado no payload de salvar, mas não havia UI para editá-lo — adicionei o campo (URL) + upload no painel de Aparência.
6. **Reuso (IDS):** criado 1 componente reutilizável `ImageUploadButton.tsx` (baseado no padrão visual de `UploadButton` em `Attachments.tsx`) + 1 helper `uploadPublicImage.ts`, consumidos pelas 4 telas — evita duplicar a lógica de file-input/upload.
7. **`test_tenancy_guard.py`:** a nova rota pública usa `get_db` (legítimo, tabela global sem RLS) → `attachments` adicionado à allowlist do guarda estático, com justificativa documentada.

### Limitações / o que precisa de validação end-to-end (não pôde ser testado neste ambiente)
- **Validação manual e2e (sem suíte e2e automatizada para isto)** — requer stack viva (Docker + Postgres real + Vite): (a) upload de logo no Brand Kit atualiza a prévia; (b) proposta nova herda o logo do Brand Kit após upload (AC3); (c) abrir link público de proposta/site em aba anônima e ver a imagem enviada renderizar (confirma a leitura pública sem token); (d) export PNG do carrossel (html2canvas) com foto vinda de `/api/public-images/{id}` (same-origin, sem tainted canvas). O código foi construído para funcionar nesses fluxos, mas a confirmação visual depende de infra viva.
- **RLS real:** os testes usam SQLite (não exercem RLS). O isolamento de escrita e a inexistência de RLS em `public_images` (por design) devem ser confirmados no Postgres real, como já é a prática do projeto (docs/AWS-DEPLOYMENT.md).
- **Lint (eslint):** `pnpm --filter @e1p/web lint` falha em TODO o repo por ausência de `eslint.config.js`/`.eslintrc` (eslint v9 pin, config flat nunca commitada) — problema **pré-existente**, não introduzido por esta story, e **não** faz parte do gate `scripts/check.sh` (que usa ruff + pytest + tsc + vitest, todos verdes).

### File List
- `apps/api/app/modules/attachments/models.py` (M)
- `apps/api/app/modules/attachments/service.py` (M)
- `apps/api/app/modules/attachments/schemas.py` (M)
- `apps/api/app/modules/attachments/router.py` (M)
- `apps/api/app/modules/__init__.py` (M)
- `apps/api/app/db/registry.py` (M)
- `apps/api/migrations/versions/0041_public_images.py` (A)
- `apps/api/tests/test_attachments.py` (M)
- `apps/api/tests/test_tenancy_guard.py` (M)
- `packages/shared-types/src/index.ts` (M)
- `apps/web/src/lib/uploadPublicImage.ts` (A)
- `apps/web/src/lib/uploadPublicImage.test.ts` (A)
- `apps/web/src/components/ImageUploadButton.tsx` (A)
- `apps/web/src/features/config/ConfiguracoesPage.tsx` (M)
- `apps/web/src/features/orcamentos/QuoteBuilderPage.tsx` (M)
- `apps/web/src/features/marketing/CarrosselBuilderPage.tsx` (M)
- `apps/web/src/features/marketing/CarouselSlideView.tsx` (M)
- `apps/web/src/features/sites/PageBuilderPage.tsx` (M)
- `docs/stories/4.2.story.md` (A — story trazida para esta branch + atualizada)

## QA Results

### Gate Decision: **PASS** — Quinn (@qa), 2026-07-11 (SDC Phase 4, QA Gate)

Suíte canônica rodada UMA vez na imagem `e1p-api-test:local`: `ruff check .` **All checks passed**; `python -m pytest -q` **523 passed, 9 skipped** (inclui `test_attachments.py`, `test_tenancy_guard.py`).

**7 Quality Checks**
| # | Check | Resultado |
|---|-------|-----------|
| 1 | AC coverage (AC1-3, IV1-3) | PASS — upload real nos 4 alvos (Brand Kit/propostas/carrossel/sites) + campos URL antigos preservados; testes de upload válido/415/413/404/401 em `test_attachments.py` |
| 2 | Code quality / padrões | PASS — 1 componente reutilizável (`ImageUploadButton`) + 1 helper (`uploadPublicImage`); prop `uploadField` aditiva/retrocompatível no `ListTab` |
| 3 | Testes | PASS — casos de upload público + vitest do helper; suíte verde |
| 4 | Segurança (foco da missão) | PASS — verificado em código: `PublicImage(Base, TimestampMixin)` SEM `TenantMixin` (tabela global, migration 0041 sem RLS **por design**, padrão idêntico a `published_*`); `Attachment(Base, TenantMixin, ...)` intacto e com RLS (IV2); escrita autenticada, `tenant_id` só rastreio; leitura pública deliberada; `content_type` restrito a jpeg/png; guarda `safeSrc` nos 4 pontos de render (IV3, gap em `CarouselSlideView` fechado) |
| 5 | Performance / custo | PASS — bytes no Postgres (S3 é dívida fora de escopo); URL same-origin `/api/public-images/{id}` evita CORS/tainted canvas |
| 6 | Confiabilidade | PASS — nenhuma migration toca `quotes`/`published_proposals`/`pages`/`carousels`; dados antigos válidos por construção (IV1/AC2) |
| 7 | Documentação | PASS — decisão de arquitetura (tabela pública global) documentada e confirmada pelo @architect; validação e2e visual é manual |

**Verificação de código real:** confirmado por leitura direta — `models.py:43` `class PublicImage(Base, TimestampMixin)` (sem TenantMixin); `PUBLIC_IMAGE_TYPES = {"image/jpeg", "image/png"}`; migration 0041 comentário explícito "SEM RLS por design", `down_revision="0040"` (cadeia linear). A decisão-chave da missão (`public_images` global sem RLS é o padrão correto) está **confirmada em código**, alinhada com o @architect.

**Decisão: PASS → Status InReview → Done.**

## Architect Review (Aria)

**Veredito: PASS** — 2026-07-11 (quality_gate=@architect). Status NÃO alterado (permanece InReview). **Decisão de arquitetura da Task 1 CONFIRMADA** (era o item que exigia validação explícita do @architect).

### Gates de qualidade
- `ruff check .`: **All checks passed**. `pytest`: **523 passed, 9 skipped** (inclui `test_attachments.py`, `test_tenancy_guard.py`).

### Decisão de arquitetura confirmada (Task 1)
CONFIRMO a criação da tabela GLOBAL `public_images` **sem RLS**, separada de `Attachment`, em vez de expor o módulo privado de Anexos sem autenticação:
- É o padrão correto e já validado no projeto — idêntico a `published_proposals`/`published_pages`/`published_contracts` (snapshots públicos globais). Coerência arquitetural preservada.
- `Attachment` (boletos/contratos privados) permanece **100% intacto e com RLS** — só recebeu tabela/rota ao lado (IV2).
- **Isolamento correto:** a ESCRITA é autenticada (`POST /attachments/public-images` sob `require_module("attachments")`, `tenant_id` carimbado só para rastreio); a LEITURA é pública por design (`serve_public_image` via `get_db` sobre tabela global — não toca `users` nem tabelas de negócio por tenant). Uso de `get_db` legítimo e documentado; allowlist do `test_tenancy_guard` atualizada com justificativa. Seguro.
- `content_type` restrito a jpeg/png; `MAX_BYTES` 10MB reaproveitado.

### Foco da missão — retrocompatibilidade com campos de URL antigos
Confirmado: `logo_url` / `photo_url` / `gallery[].url` / `block.url` continuam string/JSON livre — **nenhuma migration toca** `quotes`/`published_proposals`/`pages`/`carousels`. Dados antigos válidos por construção (AC2/IV1). Propostas/carrosséis/sites existentes com URL continuam renderizando sem mudança.

### Segurança adicional
- IV3: guarda `safeSrc` (`^(https?://|/)`) confirmada nos 4 pontos de renderização; o gap pré-existente em `CarouselSlideView` foi fechado (uniformização correta, ganho líquido).
- URL retornada (`/public-images/{id}` + prefixo `/api` no helper) resolve o roteamento real (Vite/Caddy removem `/api`) — desvio da orientação literal da story bem justificado contra a infra real. Aprovado.

### Dívidas aceitas
- Bytes no Postgres (S3 é dívida, fora de escopo). Validação e2e visual (upload → herança Brand Kit → render em aba anônima → export PNG html2canvas) é manual (sem suíte e2e). Limitações de ambiente, não de código.

# Story 8.5: Conferência bloco 1 — a divergência em uma frase, por conta

## Status
Ready

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["ruff (apps/api)", "pytest apps/api/tests (novo test_bank_reconciliation_report.py)", "pytest -m rls_e2e (novo test_bank_reconciliation_report_rls.py — Postgres real, testcontainers)", "bash scripts/check.sh (lint+types+testes, CLAUDE.md §5)", "os 3 agentes de QA (regression-tester, bug-hunter, dedup-checker — CLAUDE.md §5)", "validação manual: relatório read-only (snapshot antes/depois) + recusa de comparar datas diferentes"]
```
> [AUTO-DECISION] Story de **backend puro de agregação read-only** (nenhuma tela, nenhuma escrita, nenhuma migration) → `executor="@dev"`. `quality_gate="@architect"` seguindo o padrão histórico do projeto para todo serviço do eixo analítico-financeiro (5.3–5.9). Reason: esta story define o **contrato numérico** que a 8.6 (motor de diagnóstico) e a 8.7 (tela) consomem — errar a semântica aqui contamina as duas, e a decisão de modelagem (comparar na mesma data, banda de tolerância, decomposição obrigatória por conta) é arquitetural, não de implementação.

## Story
**As a** dono da empresa de 1 pessoa com **várias contas PJ** (corrente + poupança + aplicação, possivelmente em bancos diferentes),
**I want** um serviço que compare, **por conta**, o saldo que o banco mostra com o saldo que o e1p calcula **na mesma data de referência**, aplicando uma banda de tolerância,
**so that** eu saiba **em reais e por conta** onde há furo de lançamento — sem ser incomodado por diferença irrelevante e sem receber um número consolidado que esconde dois problemas se anulando.

## Acceptance Criteria

1. **Serviço read-only, por conta.** Novo `apps/api/app/modules/bank/reconciliation.py` com
   `reconciliation_report(db, *, start: date, end: date, bank_account_id: str | None = None, today: date | None = None) -> ConferenciaReport`.
   O relatório traz **uma entrada por conta** (`contas: list[ConferenciaConta]`) — todas as contas
   ativas (`bank.service.list_accounts(db)`, que já esconde `archived_at IS NOT NULL` por default)
   quando `bank_account_id` é omitido, ou exatamente uma quando informado.
   O serviço **não escreve nada** (nenhum INSERT/UPDATE/DELETE, nenhum checkpoint criado, nenhum
   status recalculado), no mesmo padrão dos serviços do `financial_intelligence`.
   → design §5.1; epic §3.2

2. **`saldo_banco` vem do checkpoint, e só dele.** Para cada conta:
   `bank.service.latest_checkpoint(db, bank_account_id=…, on_or_before=end)` (Story 8.4) e **depois**
   o filtro de janela desta story: se o checkpoint devolvido tem `reference_date < start`, ele **não
   serve** para o bloco 1 (cai no AC3). Quando serve: `saldo_banco_cents = checkpoint.balance_cents`,
   `saldo_banco_data = checkpoint.reference_date`, **`saldo_banco_origem = ORIGEM_BANCO`**
   (`app.core.money_planes`, criado pela Story 8.1) e **`saldo_banco_fonte = checkpoint.origin`**
   (o valor cru — `manual` nesta onda, `ofx` a partir da Onda 3 —, **sem tradução nenhuma**).
   ⚠️ **[ATUALIZADO por @po, 2026-07-29 — ratificação da @architect, D-3]** o AC dizia
   `saldo_banco_origem = ORIGEM_DECLARADO`; **`declarado` foi REVOGADO** como valor de `*_origem`.
   São **dois eixos**: `*_origem` responde *"de qual plano de dinheiro §1.1 vem este número"* (aqui:
   `banco`, porque o checkpoint é saldo do plano 3) e `*_fonte` responde *"por qual porta este saldo
   **externo** entrou no e1p"* (`{manual, ofx}` = `ORIGIN_MANUAL`/`ORIGIN_OFX` de `bank/models.py`,
   Story 8.4). A tradução `manual` → `declarado` que a 8.4 prescrevia **deixou de existir**.
   → design §2.4, §5.1, **§1.3.1** (novo); Story 8.4 AC6; ratificação D-3

3. **Sem checkpoint na janela, o relatório diz que não sabe — nunca mostra número falso.**
   Quando `latest_checkpoint` devolve `None` **ou** devolve um checkpoint com
   `reference_date < start`: `saldo_banco_cents = None`, `saldo_banco_data = None`,
   `saldo_banco_origem = ORIGEM_INDISPONIVEL`, **`saldo_banco_fonte = None`** (não houve porta de
   entrada — **[@po 2026-07-29, D-3]**), `saldo_sistema_cents = None`,
   `divergencia_cents = None`, `dentro_da_tolerancia = None`, **`tolerancia_cents = 0`**
   (**[@po 2026-07-29]** o campo é `int` não-opcional e o AC não dizia qual valor usar aqui; fixado em
   `0` porque não há saldo sobre o qual calcular banda — o consumidor **nunca** deve ler tolerância
   quando `divergencia_cents is None`, e um `0` explícito é mais honesto que um número derivado de um
   saldo que o relatório acabou de declarar que não conhece), **`saldo_sistema_origem = ORIGEM_BANCO`**,
   e uma `note` explícita na conta.
   **É proibido** cair para "compara com o saldo de hoje", "compara com o último checkpoint de
   qualquer data" ou "compara contra zero" como consolo — este último seria o pior bug possível
   aqui: uma divergência inteira inventada.
   → design §5.1 ("o bloco 1 diz isso em vez de mostrar um número falso"); Story 8.4 Testing

4. **O serviço se recusa a comparar datas diferentes.** `saldo_sistema_cents` é **sempre**
   `bank.service.derived_balance(db, bank_account_id=…, until=saldo_banco_data)` — a **mesma** data do
   checkpoint, nunca `end`, nunca `today`. O `saldo_sistema` declara origem `ORIGEM_BANCO`. A soma
   `opening_balance_cents + SUM(amount_cents)` **não** é reimplementada aqui.
   → design §3.1, §5.1; Story 8.2 AC5 / Story 8.3 AC5

4b. **⚠️ `derived_balances_as_of` (uma data comum) é PROIBIDA no bloco 1 — e isto agora é CONTRATO, não divergência.**
   Cada conta tem a sua própria data de referência (o `reference_date` do seu checkpoint mais
   recente); uma data comum reintroduziria exatamente o erro que o AC4 proíbe. Portanto: **laço de
   `derived_balance` por conta, com o `until` de cada uma**.
   ✅ **[ATUALIZADO por @po, 2026-07-29 — ratificação da @architect, D-4: "a 8.5 ESTÁ CERTA".]** A
   recomendação em contrário da Story 8.2 foi **revogada**, e a função em lote foi **renomeada** para
   `derived_balances_as_of(db, *, as_of=…)` justamente porque `derived_balances` diferia de
   `derived_balance` por **um `s`** e era a função errada para este trabalho — com sintoma silencioso
   e plausível (o relatório não quebra; ele mente um número). As duas assinaturas canônicas estão
   gravadas no **design §3.1.1**, e a docstring da função em lote declara a proibição. Isto **deixou
   de ser "divergência consciente"**: é o contrato ratificado.

5. **Divergência com sinal definido e documentado.**
   `divergencia_cents = saldo_banco_cents − saldo_sistema_cents`.
   - `> 0` → o banco tem dinheiro que o sistema não conhece (provável **entrada** não lançada);
   - `< 0` → o banco está **abaixo** do que o sistema calculou (provável **saída** não lançada — é o
     achado de maior valor, REQ-14, o foco declarado em contas a pagar).
   O sinal e a leitura de cada direção ficam na docstring do módulo (mesma disciplina de
   `payables/models.py` para caixa×competência).
   → design §5.1, §5.2; REQ-14

6. **Banda de tolerância `max(R$ 50,00, 0,5% do saldo)`, parametrizada, e silêncio dentro dela.**
   Função **pura** `tolerance_cents(saldo_cents: int, *, floor_cents: int = TOLERANCE_FLOOR_CENTS, pct: float = TOLERANCE_PCT) -> int`
   com `TOLERANCE_FLOOR_CENTS = 5_000` e `TOLERANCE_PCT = 0.005`, aplicada sobre
   `abs(saldo_banco_cents)`. `dentro_da_tolerancia = abs(divergencia_cents) <= tolerancia_cents`.
   Quando está dentro da banda, o relatório **não acrescenta nenhuma nota de alerta** — silêncio é o
   comportamento correto, não omissão.
   → design §5.1 `[SUPOSIÇÃO do design — D1]`; REQ-16; epic §9 D1

7. **Consolidado sem decomposição é proibido.** `ConferenciaReport` **sempre** carrega `contas`.
   `total_divergencia_cents` soma **apenas** as contas com divergência avaliável e vem acompanhado,
   na mesma resposta, de `contas_avaliadas: int`, `contas_sem_checkpoint: int` e
   `contas_fora_da_banda: list[ContaForaDaBanda]` (com `bank_account_id`, `bank_account_name`,
   `divergencia_cents`, `tolerancia_cents`) — para que a 8.6 possa apontar **qual** conta está fora
   da banda. Não existe rota, schema nem caminho de código que devolva o consolidado sozinho.
   → epic §3.2 (restrição de produto, decisão do fundador F3)

8. **Bloco 4 (transparência) na medida em que a Onda 1 permite.** Por conta:
   `dias_desde_ultima_conferencia = max(0, (min(end, today) − reference_date do checkpoint devolvido por latest_checkpoint(on_or_before=end)).days)`
   — ou seja, **usa o checkpoint mesmo quando ele está fora da janela** (é justamente o que permite a
   frase honesta *"saldo não confirmado há 47 dias"*), e `None` quando a conta nunca teve checkpoint;
   `movimentos_ignorados` = contagem de `bank_transactions` da conta com `status == STATUS_IGNORED`
   (`bank.models`) e `posted_at` em `[start, end]`.
   `today` é injetável (default `datetime.now(UTC).date()`), como em `projection.cash_projection`.
   → design §5.1 bloco 4

9. **Rota read-only.** `GET /bank/reconciliation-report?bank_account_id=&start=&end=` →
   `ConferenciaReportOut` no router do módulo `bank` (criado pela 8.2), protegida por
   `require_module("bank")` + `get_tenant_db`, com `end < start` → 422 (mesmo padrão do
   `financial_intelligence/router.py`) e `bank_account_id` inexistente **ou de outro tenant** → 404
   fail-closed via `bank.service.get_account` / `BankError` (a RLS esconde a linha).
   → design §5.1; Story 8.2 (router + `BankError`), Story 8.4 AC8

10. **Fora de escopo, explicitamente:** blocos 2 e 3 do relatório (`movimentos_sem_contrapartida`,
    `lancamentos_sem_extrato`) dependem de `bank_reconciliations`, que é da Onda 3/4 — os campos
    **não** são criados nesta story nem preenchidos com zero enganoso. Reforço vindo da Story 8.3: na
    Onda 1 **todo** movimento manual é `unmatched` por definição (não existe conciliação), então o
    bloco 2 devolveria "todos os movimentos" — o que não é informação, é ruído. Nenhuma IA (sugestão
    de match e classificação são Onda 4). Nenhuma importação de arquivo. **Nenhuma migration.**
    → epic §5 (Ondas 3 e 4 não liberadas); epic §4.5; Story 8.3 Dev Notes

### Integration Verification

- **IV1 — read-only de verdade.** Teste fotografa `bank_accounts`, `bank_transactions`,
  `bank_balance_checkpoints`, `charges`, `payables` e `transactions` antes e depois de **duas**
  chamadas do relatório e exige igualdade (mesmo padrão de `test_projection_is_read_only`, 5.7).
- **IV2 — o eixo já em produção segue intacto.** DRE (`dre.py`), Lucratividade
  (`profitability.py`), Cockpit (`cockpit/service.py`), Fila de Pagamentos (`FilaPagamentosPage` /
  `payables`) e Carteira (`wallet`) **não são alterados por esta story**. Teste de snapshot:
  `dre_report(start, end)` idêntico antes e depois de existirem contas, movimentos e checkpoints
  bancários (é o `test_movimento_bancario_nao_altera_dre` prometido pelo design §6.4).
- **IV3 — Regra dos Planos.** `bank/reconciliation.py` **não lê `transactions`** nem chama
  `wallet_service` (§1.3a); o teste estrutural `test_wallet_nao_importa_bank` (criado pela 8.2)
  continua passando (§1.3b); todo campo de saldo trafega com o irmão `*_origem` (§1.3c —
  `saldo_banco_origem` e `saldo_sistema_origem`, ambos constantes `"banco"` no caminho avaliável, mais
  o `saldo_banco_fonte` do eixo B).
- **IV4 — isolamento.** Nenhuma query filtra `tenant_id` à mão (Regra de Ouro nº 1). Teste
  `rls_e2e` no Postgres real: o relatório do tenant A nunca enxerga conta, movimento ou checkpoint
  do tenant B; sem GUC, fail-closed.
- **IV5 — zero IA, zero rede.** Nenhuma chamada a `core/ai`, nenhum `anonymizer` necessário (esta
  story não expõe `raw_description` nem contraparte), nenhuma chamada de rede.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false` por instrução explícita do orquestrador desta missão (mesma decisão das Stories 5.7/5.8/6.1/6.2/7.5).
> Quality validation will use manual review process only (ver `quality_gate_tools` acima e `scripts/check.sh`).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [ ] **Task 1 — Confirmar o contrato da metade 8.2/8.3/8.4 antes de escrever uma linha (AC: 1, 2, 4, 8)**
  - [ ] Ler `apps/api/app/modules/bank/models.py`, `service.py` e `router.py` (criados pelas Stories
        8.2/8.3/8.4, escritas **em paralelo** a esta) e conferir os nomes contra a tabela de contrato
        das Dev Notes. As assinaturas **declaradas** por aquelas stories são:
        `derived_balance(db, *, bank_account_id: str, until: date | None = None) -> int` (8.2 Task 4);
        `latest_checkpoint(db, *, bank_account_id: str, on_or_before: date, origins: tuple[str, ...] | None = None) -> BankBalanceCheckpoint | None` (8.4 Task 4);
        `list_accounts(db, *, include_archived: bool = False) -> list[BankAccount]` (8.2 Task 4);
        `get_account` + `BankError` (8.2); `STATUS_IGNORED` (8.3 Task 3);
        `ORIGEM_BANCO` / `ORIGEM_INDISPONIVEL` / `ORIGEM_MISTO` / `ORIGEM_PLATAFORMA` em
        `apps/api/app/core/money_planes.py` (8.1 Task 1 — **4 valores; `ORIGEM_DECLARADO` NÃO
        existe**) e `ORIGIN_MANUAL`/`ORIGIN_OFX` em `bank/models.py` (8.4 Task 3, eixo B do
        `saldo_banco_fonte`).
  - [ ] **Se algum nome divergir, adapte ESTA story ao que existe — nunca o contrário.** Não crie
        função duplicada de saldo derivado: a Regra dos Planos §1.3a só é auditável se existir **uma**
        implementação de saldo bancário no repositório (o `dedup-checker` deve reprovar uma segunda).
  - [ ] **[ATUALIZADO por @po — D-3] Dois vocabulários, cada um num arquivo só.** Eixo A (`*_origem`,
        4 valores) em `app/core/money_planes.py`; eixo B (`*_fonte`, `{manual, ofx}`) em
        `bank/models.py`. Nunca string literal solta, e **nunca traduza um eixo no outro**. O valor
        `"extrato"` do design §5.1 está **REVOGADO** — o conceito é `ofx` e já vive no eixo B desde a
        §2.4; a instrução antiga de "acrescentar `extrato` a `ORIGENS`" **não vale mais**. Na Onda 3,
        zero mudança de vocabulário: só se passa a **escrever** `ofx`.
  - [ ] O filtro de **janela** (`reference_date >= start`) é aplicado **aqui**, em `reconciliation.py`
        — `latest_checkpoint` só conhece `on_or_before`. É filtro de relatório, não regra de domínio
        da 8.4; não peça mudança de assinatura a ela.

- [ ] **Task 2 — Dataclasses e a função pura da banda de tolerância (AC: 1, 5, 6, 7)**
  - [ ] `ConferenciaConta` (frozen): `bank_account_id`, `bank_account_name`, `bank_account_kind`,
        `saldo_banco_cents: int | None`, `saldo_banco_origem: str`, **`saldo_banco_fonte: str | None`**,
        `saldo_banco_data: date | None`,
        `saldo_sistema_cents: int | None`, **`saldo_sistema_origem: str`**, `divergencia_cents: int | None`,
        `dentro_da_tolerancia: bool | None`, `tolerancia_cents: int`,
        `dias_desde_ultima_conferencia: int | None`, `movimentos_ignorados: int`, `notes: list[str]`.
  - [ ] ⚠️ **[@po 2026-07-29] `saldo_sistema_origem` estava exigido pelo AC4 e pelo IV3 mas faltava nesta
        lista** — sem ele a Regra dos Planos §1.3c é violada (campo de saldo sem o irmão `*_origem`) e o
        `ConferenciaContaOut` da Task 5 nasceria incompleto. Valor: **sempre** `ORIGEM_BANCO`, inclusive
        no caminho `indisponivel` (a procedência do saldo derivado não muda por não haver checkpoint;
        o que fica `None` é o **valor**, não a origem).
  - [ ] `ContaForaDaBanda` (frozen): `bank_account_id`, `bank_account_name`, `divergencia_cents`,
        `tolerancia_cents`.
  - [ ] `ConferenciaReport` (frozen): `start`, `end`, `contas: list[ConferenciaConta]`,
        `total_divergencia_cents: int | None`, `contas_avaliadas: int`, `contas_sem_checkpoint: int`,
        `contas_fora_da_banda: list[ContaForaDaBanda]`, `notes: list[str]`.
  - [ ] `tolerance_cents(...)` **pura** + constantes `TOLERANCE_FLOOR_CENTS = 5_000` /
        `TOLERANCE_PCT = 0.005`. Arredondamento: `round(abs(saldo) * pct)` (inteiro de centavos —
        dinheiro nunca é float no resultado).
  - [ ] Docstring do módulo com: a convenção de **sinal** da divergência (AC5), a razão de a
        comparação ser **na mesma data** (AC4) e a razão de a decomposição por conta ser
        **obrigatória** (AC7, decisão do fundador F3).

- [ ] **Task 3 — O cálculo por conta (AC: 2, 3, 4, 5, 6, 8)**
  - [ ] Para cada conta: buscar o checkpoint da janela → derivar `saldo_sistema` **na
        `reference_date` dele** → divergência → tolerância → `dentro_da_tolerancia`.
  - [ ] Caminho `indisponivel` (AC3) com a nota explícita — e **teste que prova** que nenhum número
        de divergência é produzido nesse caminho.
  - [ ] `dias_desde_ultima_conferencia` e `movimentos_ignorados` (AC8). Atenção: o checkpoint usado
        aqui é o mais recente com `reference_date <= end` (**pode estar fora da janela** — é
        justamente o que permite a frase honesta "saldo não confirmado há 47 dias"), diferente do
        checkpoint do AC2 (que exige estar **dentro** da janela). São duas buscas com critérios
        diferentes de propósito; documentar para não "simplificar" em uma só depois.

- [ ] **Task 4 — Agregação do relatório sem esconder a decomposição (AC: 7)**
  - [ ] `total_divergencia_cents` = soma das divergências avaliáveis; `None` quando nenhuma conta é
        avaliável. `contas_sem_checkpoint > 0` acrescenta uma `note` no nível do relatório dizendo
        que o total **não cobre todas as contas**.
  - [ ] `contas_fora_da_banda` preenchido a partir das contas com `dentro_da_tolerancia is False`.
  - [ ] Teste `test_consolidado_nunca_vem_sem_decomposicao`: para qualquer combinação de entrada,
        `len(report.contas) >= 1` sempre que existir conta ativa, e o cenário do epic §3.2
        (+R$ 1.200 / −R$ 900 / +R$ 40 em três contas) produz `total = +R$ 340` **e** três entradas em
        `contas`, com **duas** em `contas_fora_da_banda`.

- [ ] **Task 5 — Schemas + rota (AC: 9)**
  - [ ] `apps/api/app/modules/bank/schemas.py` (arquivo criado pela 8.2): `ConferenciaContaOut`,
        `ContaForaDaBandaOut`, `ConferenciaReportOut` — conversão dataclass→schema no router, mesmo
        padrão de `_projection_out` em `financial_intelligence/router.py`.
  - [ ] `GET /bank/reconciliation-report` no router do módulo `bank` (criado pela 8.2), com
        `require_module("bank")`, `end < start` → 422 e `bank_account_id` desconhecido → 404
        fail-closed (via `get_account`/`BankError` da 8.2).
  - [ ] Docstring da rota deixando explícito: SOMENTE LEITURA; a comparação é na data do checkpoint;
        `indisponivel` é resposta legítima, não erro.

- [ ] **Task 6 — Testes (AC: todas; IV1–IV5)**
  - [ ] `apps/api/tests/test_bank_reconciliation_report.py`: conta batendo exato (divergência 0,
        dentro da banda, nenhuma nota de alerta); divergência **negativa** acima da banda (saída não
        lançada — o caso REQ-14); divergência positiva acima da banda; divergência de R$ 3,50 em
        saldo de R$ 25.000 → **dentro** da banda (silêncio — o teste que protege a confiança no
        sinal); banda dominada pelo piso (R$ 50 em saldo pequeno) e banda dominada pelo percentual
        (0,5% em saldo grande); sem checkpoint na janela → `indisponivel` + `divergencia is None`;
        checkpoint **fora** da janela existindo → segue `indisponivel` no AC2 **mas**
        `dias_desde_ultima_conferencia` preenchido; comparação **na data do checkpoint** provada por
        cenário divergente (movimento lançado DEPOIS da `reference_date` não pode alterar a
        divergência — se o código comparasse em `end`, o teste falharia); três contas com sinais
        opostos (cenário do epic §3.2); conta arquivada fora do relatório; `movimentos_ignorados`.
  - [ ] `test_reconciliation_report_is_read_only` (IV1) e
        `test_movimento_bancario_nao_altera_dre` (IV2).
  - [ ] `apps/api/tests/test_bank_reconciliation_report_rls.py` marcado `rls_e2e` (IV4).
  - [ ] Rodar `bash scripts/check.sh` e os 3 agentes de QA (CLAUDE.md §5). Confirmar zero regressão
        na suíte existente.

## Dev Notes

### Contexto de escopo — de onde vem cada obrigação desta story
- Item de escopo da Onda 1: *"**Conferência bloco 1**, **por conta** (§3.2): saldo do banco vs. saldo
  do sistema **na mesma data de referência** — se não há checkpoint na janela,
  `saldo_banco_origem='indisponivel'` e o relatório **diz isso** em vez de mostrar número falso"*.
  [Source: docs/prd/epic-8-controle-bancario.md#5 — Onda 1]
- Delimitação da story: *"Serviço **read-only** que compara `saldo_banco` (checkpoint) ×
  `saldo_sistema` (derivado) **na mesma data de referência**, aplica a banda de tolerância
  `max(R$ 50, 0,5%)` configurável e devolve a divergência **por conta**"*; *"**Não inclui:** blocos 2
  e 3 (…); consolidado sem decomposição por conta é **proibido**"*.
  [Source: docs/prd/epic-8-controle-bancario.md#6 — Story 8.5]
- Estrutura do relatório e semântica dos blocos: [Source: docs/architecture/controle-bancario-design.md#5.1]

### ⚠️ Decisão do fundador (2026-07-29): VÁRIAS CONTAS PJ — e por que isso é requisito, não UI
A topologia real do tenant do fundador é **várias contas PJ**: corrente + poupança + aplicação,
**possivelmente em bancos diferentes**. Não é conta PF misturada com PJ, então a divergência é sinal
relativamente limpo. A consequência é dura e é o coração desta story:

> **Divergência agregada entre várias contas perde poder de diagnóstico.** Se três contas divergem
> +R$ 1.200, −R$ 900 e +R$ 40, o consolidado (+R$ 340) parece saudável e **esconde dois problemas**.

Por isso a conferência é **calculada e apresentada por conta**; um total consolidado pode existir,
mas **sempre acompanhado da decomposição**, nunca sozinho — a mesma disciplina que a Regra dos Planos
§1.3c impõe a saldos de origem diferente. Isto é **requisito de produto (F3)**, não preferência de
tela. [Source: docs/prd/epic-8-controle-bancario.md#3.2 e #9 (F3); docs/architecture/controle-bancario-design.md#10 (D2)]

### ⚠️ Objetivo declarado do fundador (verbatim) e o foco em contas a PAGAR
> *"de saldo batendo é uma conferência para achar possível furos"* (R1)
[Source: docs/architecture/controle-bancario-design.md#0 — falas do fundador]

O critério de sucesso é **"quantos lançamentos faltantes foram encontrados"**, nunca *"fechou em
zero"* (REQ-13). E o foco é **contas a PAGAR** (REQ-14): *"movimentos de saída no extrato sem
`Payable` correspondente são o achado de maior valor do sistema"* — receber já tem três testemunhas
independentes (gateway, webhook, split na Carteira) e não é o problema. É por isso que o AC5 obriga a
documentar o **sinal** da divergência: `divergencia < 0` (banco abaixo do sistema) é o caso que
importa, e a leitura precisa estar escrita, não inferida por quem lê o código depois.
[Source: docs/research/2026-07-29-controle-bancario-requisitos-e-viabilidade.md — REQ-13, REQ-14; docs/prd/epic-8-controle-bancario.md#1]

### ⚠️ A banda de tolerância existe para proteger a confiança no sinal
`max(R$ 50,00, 0,5% do saldo)` é o **default do design (D1)**, marcado lá como
`[SUPOSIÇÃO do design]` e ratificado pelo epic como "adotar o default e torná-lo configurável por
tenant". Dentro da banda: **verde e silêncio**.

> *"O e1p não é ferramenta contábil; não precisa fechar em zero, e alertar sobre R$ 3,50 num mês de
> R$ 25.000 treina o usuário a ignorar o alerta."*
[Source: docs/architecture/controle-bancario-design.md#5.1]

Uma tela que grita por R$ 3 de diferença destrói a única coisa que o produto está vendendo: que o
número é confiável. O teste de R$ 3,50 em R$ 25.000 (Task 6) é a defesa disso.

**[AUTO-DECISION] Configurabilidade por tenant fica parametrizada, não persistida, nesta story** →
a banda vira uma função pura com `floor_cents`/`pct` como parâmetros (a "costura"), com os defaults
como constantes do módulo.
⚠️ **[JUSTIFICATIVA TROCADA por @po, 2026-07-29 — ratificação da @architect, D-7.]** A razão original
era de **custo** (persistir exige coluna → migration → fora do escopo). Ela é verdadeira e é a **mais
fraca** das disponíveis. **A razão forte é que a banda fixa é uma propriedade do instrumento de
medição:** a Onda 1 existe, segundo o epic §3.1, como **experimento** — o número dela é o gate que
libera ou mata as Ondas 3 e 4 (~4,5 ondas de trabalho). **Se cada tenant pode mover a banda, a régua
muda junto com o que ela mede e a leitura do gate perde sentido.** Uma banda **fixa e conhecida**
durante a janela de observação é **rigor, não limitação**.
Sobre a letra do REQ-16: o que é *load-bearing* nele é **a existência da banda e o silêncio dentro
dela** — é isso que protege a confiança no sinal. "Configurável" é propriedade de segunda ordem, e a
função pura com `floor_cents`/`pct` já a satisfaz no sentido de "não está cravada no meio de uma
expressão".
✅ **[DECIDIDO pelo @po, 2026-07-29 — e a @architect recomenda o mesmo (D-7).]** A parametrização
**satisfaz a Onda 1**. A persistência **não** entra aqui e, se um dia entrar, **NÃO** pertence à
migration do `bank_accounts`: `tenant_profiles` não é tabela do módulo `bank`, e enfiar
`ALTER TABLE tenant_profiles` no revision que cria `bank_accounts` mistura dois domínios num arquivo e
torna o `downgrade` mais arriscado do que precisa.
**Lugar já resolvido, se persistir (design §5.1.1, novo):** `tenant_profiles`
(`apps/api/app/modules/settings/models.py` — já carrega configuração operacional como `timezone` e
credenciais, com o padrão default-on-demand testado em produção), **duas colunas inteiras**:
`bank_tolerance_floor_cents BIGINT NOT NULL DEFAULT 5000` e
`bank_tolerance_bps INTEGER NOT NULL DEFAULT 50` (basis points; 50 bps = 0,5%) — **sem float no
banco**, cálculo `round(abs(saldo) * bps / 10_000)`. **Story própria, revision própria** (~0,25 onda);
e, graças ao D-6, não há "vaga de 0058" a disputar. Banda **por conta** foi avaliada e rejeitada para
agora: o componente percentual já adapta a banda ao tamanho de cada conta; por conta só se justifica
com evidência de uma conta com regime de ruído próprio, e então entra como coluna **nullable de
override** em `bank_accounts`. **Consequência para esta story: nada muda** — mantenha a função pura e
**não** prometa ao usuário, em texto de UI ou de API, que a banda é configurável.

### ⚠️ Esta story NÃO tem migration — e isso é um sinal, não uma lacuna
Nenhuma tabela nova, nenhuma coluna nova. O relatório **lê** `bank_accounts`, `bank_transactions` e
`bank_balance_checkpoints`. ⚠️ **[CORRIGIDO por @po, 2026-07-29]** essas três tabelas **não** vêm de uma
migration só: a Onda 1 consome **três** revisions — uma por story — porque cada uma cria uma tabela:
`bank_accounts` (8.2), `bank_transactions` (8.3), `bank_balance_checkpoints` (8.4), encadeadas
linearmente após o head real (confirmado nesta sessão: **`0057_device_tokens.py`**), previsivelmente
0058 → 0059 → 0060. O *"uma migration por onda"* do design §2/§8 e o mapa `Onda 2 → 0059` / `Onda 3 → 0060`
do epic §5 são **expectativa de planejamento, não contrato** — o que é lei é encadear após o head real
(ver 8.2 Dev Notes "Numeração de migration — confirmar, não fixar"). Se durante a implementação parecer
necessário criar uma migration **aqui**, **pare**: é sinal de invasão do escopo da outra metade —
registre em Completion Notes e escale ao @po.
[Source: docs/prd/epic-8-controle-bancario.md#5; docs/stories/8.2.story.md Task 1]

### Contrato consumido da metade 8.1/8.2/8.3/8.4 (conferido contra as stories em 2026-07-29)

| Consumido | Declarado em | Uso aqui |
|---|---|---|
| `bank.models.BankAccount` (`name`, `kind`, `opening_balance_cents`, `opening_date`, `archived_at`) | Story 8.2 AC1 / Task 3 | iteração por conta |
| `bank.service.list_accounts(db, *, include_archived=False)` | Story 8.2 Task 4 | contas do relatório (já esconde arquivadas) |
| `bank.service.derived_balance(db, *, bank_account_id, until=None) -> int` | Story 8.2 Task 4; corpo real preenchido pela 8.3 Task 5 | `saldo_sistema_cents` (**não reimplementar**) |
| `bank.service.get_account` + `BankError` | Story 8.2 Task 4 | 404 fail-closed da rota |
| `bank.models.STATUS_IGNORED` (+ `STATUS_UNMATCHED/PARTIAL/MATCHED`) | Story 8.3 Task 3 | `movimentos_ignorados` |
| `bank.service.latest_checkpoint(db, *, bank_account_id, on_or_before, origins=None)` | Story 8.4 AC6 / Task 4 | `saldo_banco_cents` — **`None` é caminho normal** |
| ~~`bank.service.days_since_last_declared_balance(...)`~~ | Story 8.4 Task 4 | ⚠️ **[@po 2026-07-29] deliberadamente NÃO consumida.** A 8.4 a declarava como consumida por esta story; não é. Ela faz `MAX(reference_date)` **sem teto em `end`**, e o AC8 daqui precisa do checkpoint **da janela do relatório** (`on_or_before=end`) para que a frase *"saldo não confirmado há N dias"* seja coerente com o período que o usuário está olhando. O contrato da 8.4 foi corrigido nesse ponto. **Não** ligue as duas fontes ao mesmo campo |
| `app.core.money_planes.ORIGEM_BANCO / ORIGEM_INDISPONIVEL / ORIGENS` (**4 valores; sem `ORIGEM_DECLARADO`**) | Story 8.1 Task 1 | eixo A — todo campo `*_origem` |
| `bank.models.ORIGIN_MANUAL / ORIGIN_OFX / ORIGINS` | Story 8.4 Task 3 | eixo B — `saldo_banco_fonte` (valor cru do checkpoint, **sem tradução**) |
| Router do módulo `bank` + `require_module("bank")` + `get_tenant_db` | Story 8.2 Task 5; Story 8.4 AC8 | a rota do AC9 |

**Nada de API nova para a camada deles.** Se falta algo, ou é leitura local deste relatório, ou é
escalação — nunca uma função nova enfiada no `service.py` da 8.2/8.3/8.4.

✅ **Contrato ratificado (ver AC4b):** a recomendação da Story 8.2 de usar a função em lote foi
**revogada pela @architect** (ratificação D-4, design §3.1.1). A conferência usa **laço de
`derived_balance` com o `until` de cada conta**; a função em lote virou
`derived_balances_as_of(as_of=…)` e é **proibida aqui**, com a proibição escrita na própria docstring
dela. A @architect também rejeitou a alternativa de dar a ela um mapa `{conta: data}`: degeneraria em
N queries (ou num `CASE` menos legível que o laço) e convidaria o chamador a construi-lo a partir de
uma data única — o bug de volta por outro caminho. Escala em jogo: uma empresa de 1 pessoa com um
punhado de contas — não é problema de performance.

### Regra dos Planos — o que ela proíbe **dentro** deste arquivo
> **(a)** Nenhum cálculo de saldo bancário lê `transactions`, e nenhum cálculo de saldo de carteira
> lê `bank_transactions`. **(b)** `bank` pode importar `wallet`; `wallet` **nunca** importa `bank`.
> **(c)** Todo campo de API que carrega saldo declara a procedência num campo irmão `*_origem`.
[Source: docs/architecture/controle-bancario-design.md#1.3; docs/prd/epic-8-controle-bancario.md#4]

Aplicado aqui: `reconciliation.py` **não importa `wallet`** (não precisa — o saldo comparado é o
bancário, plano 3, e o `available_cents` da Carteira é plano 1). `saldo_banco_origem` é o campo
`*_origem` do AC2. Sem o teste estrutural da 8.2, esta disciplina degrada por acidente: basta uma
story futura importar o módulo errado para recriar o bug original numa forma nova.

### Por que a comparação na MESMA data não é detalhe
Comparar `saldo_banco` de ontem com `saldo_sistema` de hoje é **o erro clássico** desta classe de
relatório: qualquer movimento lançado no intervalo aparece como "furo" inexistente. Por isso o AC4
obriga `saldo_sistema` a ser derivado **exatamente** em `saldo_banco_data`, e a Task 6 exige um teste
que **falharia** se o código comparasse em `end` (movimento posterior à `reference_date` não pode
mexer na divergência). É o mesmo tipo de teste divergente-por-construção que provou o regime de caixa
na 5.7 (`test_uses_due_date_not_competence_date`).

### Datas: `DATE`, comparação por data de calendário, sem fuso
`posted_at` e `reference_date` são `DATE` **por decisão de design** — precisamente para evitar na
origem o bug de fuso que mordeu a Agenda (*"toda data de negócio que vira evento all-day deve ser
comparada por data de calendário, nunca por horário local"*). Não converta para `datetime`, não
normalize para UTC, não use `datetime.now()` sem `.date()`.
[Source: docs/architecture/controle-bancario-design.md#3.3; CLAUDE.md#6.0]

### PII — nada nesta story, mas o vizinho tem
O bloco 1 devolve apenas números, datas e o **nome da conta** (dado do próprio tenant). Ele **não**
expõe `raw_description`, `counterparty_name` nem `counterparty_document`. Esses campos carregam
**PII de terceiro que nunca contratou com a e1p** (CPF/CNPJ) e, quando forem exibidos (Story 8.7) ou
enviados a qualquer modelo (Onda 4), o **anonimizador é obrigatório** antes da chamada a `core/ai`
(Regra de Ouro nº 2 / REQ-18). Registrado aqui para que uma story futura não ligue extração
agressiva "porque dá".
[Source: docs/architecture/controle-bancario-design.md#7.4; docs/research/…#REQ-18; CLAUDE.md#3]

### File Locations (novos/alterados)
- **NOVO:** `apps/api/app/modules/bank/reconciliation.py` — dataclasses + `tolerance_cents` +
  `reconciliation_report`.
- **ALTERAR:** `apps/api/app/modules/bank/schemas.py` (criado pela 8.2) — `ConferenciaReportOut` e
  filhos.
- **ALTERAR:** `apps/api/app/modules/bank/router.py` (criado pela 8.2) — rota
  `GET /bank/reconciliation-report`.
- **NOVO:** `apps/api/tests/test_bank_reconciliation_report.py`
- **NOVO:** `apps/api/tests/test_bank_reconciliation_report_rls.py` (`rls_e2e`)
- **Sem migration.** **Sem alteração no frontend** (a tela é da Story 8.7).

### Technical Constraints
- Dinheiro em **centavos** `int`/`BigInteger`, nunca float — inclusive no cálculo da banda
  (`round(abs(saldo) * pct)`).
- **Somente leitura** estrita (IV1). Nenhum recálculo de `status` de movimento (isso é da Onda 3/4).
- **Nenhum filtro manual de `tenant_id`** — RLS é a única garantia (Regra de Ouro nº 1).
- Agregação preferencialmente no banco quando houver mais de ~uma dezena de contas; para a escala
  real (empresa de 1 pessoa, poucas contas) uma query por conta é aceitável e mais legível — mesma
  ordem de grandeza do argumento de saldo derivado do design §3.1.
- Nenhuma IA, nenhum `core/ai`, nenhum `core/anonymizer` nesta story (AC10 / IV5).

### Testing
- `pytest` (`apps/api/tests/`), SQLite na suíte rápida; `rls_e2e` (testcontainers + Postgres real)
  para o cross-tenant, mesmo tratamento das Stories 5.3–5.9.
- Os dois testes mais valiosos desta story: **(1)** o divergente-por-construção da data de
  comparação (AC4) e **(2)** o de silêncio dentro da banda (AC6).
- `bash scripts/check.sh` + os 3 agentes de QA (CLAUDE.md §5). O `dedup-checker` tem uma tarefa
  específica aqui: garantir que **não** existe uma segunda implementação de saldo derivado.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-29 | 0.1 | Story criada a partir do Epic 8 (Controle Bancário e Conferência), Onda 1 — River (@sm) | River (@sm) |
| 2026-07-29 | 0.1.1 | Validated GO (8/10) — Status: Draft → Ready. **AC4b confirmado pelo @po: a recusa de usar `derived_balances` está CORRETA** e a Dev Note da 8.2 que induzia ao erro foi corrigida na própria 8.2 — nada a mudar aqui. Correções aplicadas: (a) `saldo_sistema_origem: str` **acrescentado** ao `ConferenciaConta` (era exigido pelo AC4 e pelo IV3, faltava na Task 2 → violaria a Regra dos Planos §1.3c e o `Out` nasceria incompleto); (b) AC3 passa a fixar `tolerancia_cents = 0` no caminho `indisponivel`, que estava indefinido num campo `int` não-opcional; (c) corrigida a afirmação factual de que as tabelas vêm "da migration 0058" — são **três** migrations (0058/0059/0060 previstos, head real confirmado `0057`). **REQ-16 decidido pelo @po:** a banda parametrizada em função pura **satisfaz a Onda 1**; persistência por tenant vira follow-up, **não** entra na migration da 8.2. | @po |
| 2026-07-29 | 0.1.2 | Reconciliada com a **ratificação da @architect** (D-3, D-4, D-7). Segue **GO (8/10), Ready**, e a story **sai fortalecida**: (a) **D-4 — "a 8.5 ESTÁ CERTA"**: o AC4b deixa de ser *"divergência consciente"* e passa a ser **contrato ratificado** (design §3.1.1); a função proibida foi renomeada para `derived_balances_as_of`, e a alternativa de mapa `{conta: data}` foi avaliada e **rejeitada** pela @architect. (b) **D-3**: `saldo_banco_origem = ORIGEM_BANCO` (não `ORIGEM_DECLARADO`, que **deixou de existir**) + novo **`saldo_banco_fonte = checkpoint.origin`** (valor cru, sem tradução; `None` no caminho indisponível); `saldo_sistema_origem` confirmado. (c) **D-7**: a justificativa da banda parametrizada foi **trocada da fraca para a forte** — não é "não tem migration", é que **a banda fixa é propriedade do instrumento de medição** (se cada tenant move a régua, a leitura do gate do epic §3.1 perde sentido); e o lugar da persistência, se um dia ocorrer, está resolvido: `tenant_profiles`, 2 colunas inteiras (`..._floor_cents` + `..._bps`), **story própria com revision própria — nunca a migration do `bank_accounts`**. | @po |

## Dev Agent Record

**Agent Model Used:** —

### Completion Notes

### [AUTO-DECISIONS]

### File List

## QA Results

---

## Story Draft Checklist — Resultado (River, modo autônomo)

| Categoria | Status | Observações |
|---|---|---|
| 1. Goal & Context Clarity | PASS | Objetivo (divergência por conta), valor (achar furos, não fechar em zero) e dependências (8.2/8.3/8.4 em paralelo, migration 0058 de posse delas) explícitos. |
| 2. Technical Implementation Guidance | PASS | Arquivo novo nomeado, dataclasses completas, contrato assumido da outra metade tabelado com instrução de adaptar-se a ele (Task 1). |
| 3. Reference Effectiveness | PASS | Citações por seção (`design#5.1`, `epic#3.2`, REQ-13/14/16) com a informação crítica **resumida** na própria story, não só linkada. |
| 4. Self-Containment Assessment | PASS | Regra dos Planos, banda de tolerância, convenção de sinal e o caminho `indisponivel` estão escritos aqui. Edge cases cobertos: sem checkpoint, checkpoint fora da janela, conta arquivada, borda da banda. |
| 5. Testing Guidance | PASS | Dois testes divergentes-por-construção nomeados (data de comparação; silêncio na banda) + `rls_e2e` + tarefa específica para o `dedup-checker` (não duplicar saldo derivado). |
| 6. CodeRabbit Integration (conditional) | N/A | Disabled — skip notice presente. |

**Final Assessment: READY** (clareza 9/10).
**Risco residual conhecido (não bloqueante):** os **nomes de função** da metade 8.2/8.3/8.4 são
inferência (o design é o contrato comum, mas não nomeia funções). A Task 1 existe exatamente para
resolver isso no início da implementação, com a instrução explícita de adaptar **esta** story ao que
existir — nunca criar API nova na camada da outra metade.
**Ponto para o @po:** a persistência **por tenant** da banda de tolerância exige coluna nova
(migration) e ficou **fora** desta story de propósito; se for exigida na Onda 1, pertence à migration
0058 (Story 8.2) ou a um follow-up.

# Story 4.5: Endurecimentos e refinamentos diversos

## Status
Done

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["ruff (apps/api)", "pytest (apps/api/tests)", "pnpm --filter @e1p/web typecheck", "revisão manual do fluxo de enumeração de e-mail (auth/service.py)", "revisão manual da normalização de fuso (Agenda/Cockpit)", "scripts/check.sh"]
```
> [AUTO-DECISION] A story mistura 3 refinamentos heterogêneos (hardening de auth, tooling de geração de tipos, correção de fuso horário) sem introduzir padrão arquitetural novo — reaproveita módulos e convenções já existentes (`auth/service.py`, `core/email.py`, `settings` module, `zoneinfo` da stdlib). O item mais sensível (enumeração de e-mail) é da categoria "Security" da tabela de Story Type Analysis → executor=@dev, quality_gate=@architect (mesmo par usado pela tabela para stories de Security/Architecture). Reason: nenhuma mudança de esquema complexa o suficiente para justificar @data-engineer como executor (a única migration desta story é um `add_column` simples, mesmo padrão trivial já usado em `0029_user_invite_fields.py`).

## Story
**As a** mantenedor do sistema,
**I want** fechar as dívidas menores de segurança/manutenção não bloqueantes,
**so that** o sistema fique mais robusto e coeso após o lançamento.

## Acceptance Criteria
1. Enumeração de e-mail no `/register` reavaliada (fluxo de confirmação de e-mail).
2. Geração de tipos TS a partir do OpenAPI do FastAPI (elimina a manutenção manual do `shared-types`).
3. Rastro da IA propagado na Agenda (`is_ai` real quando a camada de ações de IA existir) e normalização de fuso por tenant (janela do dia/`all_day`).

### Integration Verification
- IV1: Cadastro/login e o contrato `shared-types` continuam funcionando (sem quebra do contrato).
- IV2: As correções de fuso já feitas (all-day por data de calendário) não regridem.
- IV3: Nenhuma regressão nos testes existentes.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false` por instrução explícita do orquestrador desta missão (mesmo padrão já usado nas Stories 1.1-1.4 e 4.1).
> Quality validation will use manual review process only (ver `quality_gate_tools` acima e `scripts/check.sh`).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [x] **Task 1 — Mitigar enumeração de e-mail no `/register` (AC: 1, IV1, IV3)**
  - [x] `apps/api/app/modules/auth/service.py::register_tenant` — mensagens de pré-checagem unificadas na mesma mensagem genérica (`CONFLICT_MESSAGE = "Subdomínio ou e-mail já cadastrado"`), idêntica à do caminho de corrida (`IntegrityError`). `status_code=409` preservado.
  - [x] **[AUTO-DECISION]** Não implementar fluxo completo de confirmação por link/token (quebraria IV1). Entregue: (a) mensagens unificadas e (b) e-mail de boas-vindas best-effort. Gate de ativação fica como dívida.
  - [x] E-mail de boas-vindas best-effort via `core/email.py::send_email`, envolto em `try/except` com log — nunca derruba o registro.
  - [x] `test_duplicate_slug_rejected`/`test_duplicate_email_rejected` continuam válidos (mensagem contém "subdomínio" E "e-mail"); novo `test_register_conflict_message_does_not_leak_which_field` (mensagens idênticas).
  - [x] Testes do e-mail: dispatch com `to=email` (monkeypatch/spy) + exceção simulada NÃO impede `201`.

- [x] **Task 2 — Geração de tipos TS a partir do OpenAPI do FastAPI (AC: 2, IV1)**
  - [x] NOVO `apps/api/scripts/export_openapi.py` — standalone, importa `app.main:app`, `app.openapi()` → `packages/shared-types/openapi.json`. `parents[3]` chega na raiz do monorepo (confirmado no runtime).
  - [x] `packages/shared-types/package.json` — `devDependencies.openapi-typescript: "^7.13.0"` (versão estável mais recente no npm no momento da implementação).
  - [x] Script `"generate": "openapi-typescript ./openapi.json -o ./src/generated.ts"`.
  - [x] Raiz `package.json` — `"generate:types": "python apps/api/scripts/export_openapi.py && pnpm --filter @e1p/shared-types generate"`.
  - [x] `packages/shared-types/src/index.ts` — cabeçalho documenta o novo mecanismo (generated.ts + passo manual, sem CI).
  - [x] **[AUTO-DECISION]** Não migrar as ~806 linhas de `index.ts` para `generated.ts` (risco/escopo). Entregue a INFRAESTRUTURA de geração; migração incremental fica como dívida (Story 4.6).
  - [x] `openapi.json` (111 paths / 132 schemas) e `generated.ts` (9432 linhas) gerados e commitados. Regenerar é passo MANUAL (drift documentado).

- [x] **Task 3 — Fuso por tenant: coluna `timezone` + helper de normalização (AC: 3, IV2)**
  - [x] `settings/models.py::TenantProfile` — `timezone: Mapped[str] = mapped_column(String(64), default="America/Sao_Paulo", nullable=False)` (const `DEFAULT_TIMEZONE`).
  - [x] Migration `0044_tenant_timezone.py` — número RESERVADO. `down_revision="0031"` (head confirmado). `add_column` com `server_default="America/Sao_Paulo"`; downgrade `drop_column`.
  - [x] **Nota de coordenação:** `0044` exclusivo desta story; religar `down_revision` no merge se o head mudar.
  - [x] NOVO `apps/api/app/core/tz.py` — `DEFAULT_TENANT_TIMEZONE`, `tenant_zone` (fail-safe p/ nome inválido), `day_window_utc(day, tz_name)`.
  - [x] `requirements.txt` — `tzdata==2024.2` (base IANA pura-Python, obrigatória em `python:3.13-slim`/Windows).

- [x] **Task 4 — Normalizar `all_day` na Agenda usando o fuso do tenant (AC: 3, IV2)**
  - [x] `agenda/service.py::create_event` — quando `all_day`, busca `get_profile` (import lazy) e recalcula `starts_at`/`ends_at` via `day_window_utc(data.starts_at.date(), profile.timezone)` antes de montar o evento.
  - [x] Compatibilidade confirmada: p/ `America/Sao_Paulo` (UTC-3) meia-noite local do dia D = `03:00Z` do MESMO dia D → `starts_at[:10]` = D (IV2 preservada). Fuso positivo documentado como dívida.
  - [x] **[AUTO-DECISION]** Escopo limitado a `create_event`; `reschedule_event` de all-day não re-normaliza (dívida).
  - [x] `settings/service.py::get_profile` — sem mudança (default herdado da coluna).

- [x] **Task 5 — Corrigir a janela do dia do Cockpit usando o fuso do tenant (AC: 3, IV2)**
  - [x] `cockpit/router.py::summary` — substituído o cálculo cru por `day_window_utc(base, profile.timezone)` (`get_profile` via import lazy). Comentário de dívida de fuso removido/atualizado.
  - [x] Descrição do parâmetro `day` atualizada ("interpretado no fuso do tenant").
  - [x] Fix real do item catalogado em `CLAUDE.md` §"Cockpit — pendência" — sinalizado no Dev Agent Record para atualização posterior do CLAUDE.md.

- [x] **Task 6 — Propagar `is_ai` em update/cancel/reschedule da Agenda (AC: 3)**
  - [x] `update_event`/`cancel_event`/`reschedule_event` ganham `by_ai: bool = False`; passam `is_ai=by_ai` ao `audit.record`.
  - [x] `agenda/router.py` — os 3 endpoints threadam `by_ai=user.is_ai` (mesmo padrão do `create_event`).
  - [x] Default `False` mantém retrocompatibilidade com chamadores/testes existentes.

- [x] **Task 7 — Contrato compartilhado (`shared-types`) (AC: 3, IV1)**
  - [x] `shared-types/src/index.ts::TenantProfile` — `timezone: string;`.
  - [x] `settings/schemas.py::ProfileOut` — `timezone: str`; `ProfileUpdate` — `timezone: str | None = Field(default=None, max_length=64)`.
  - [x] `settings/service.py::_FIELDS` — inclui `"timezone"`. **Adicional necessário:** `settings/router.py::_out` (que monta o `ProfileOut` à mão) também recebeu `timezone=p.timezone` — sem isso o `GET/PATCH /settings/profile` quebrava (a story não previu este arquivo; corrigido).
  - [x] **[AUTO-DECISION]** Sem seletor de fuso na UI (dívida; Story 4.6). Campo disponível via `PATCH /settings/profile`.

- [x] **Task 8 — Testes (AC: 1, 2, 3; IV1, IV2, IV3)**
  - [x] `test_auth.py` — mensagem unificada idêntica + e-mail best-effort (dispatch e falha não derruba `201`).
  - [x] `test_settings.py` — default `timezone == "America/Sao_Paulo"` + `PATCH` com `America/Manaus` persiste.
  - [x] `test_agenda.py` — all-day ancorado (`2026-07-01T00:00Z` → persistido `2026-07-01T03:00Z`.../`2026-07-02T03:00Z`, `starts_at[:10] == "2026-07-01"`) + `by_ai=True` grava `AuditEntry.is_ai is True` em update/cancel/reschedule.
  - [x] `test_cockpit.py` — evento às 23:30 local (`2026-07-02T02:30Z`) aparece no dia local `2026-07-01`.
  - [x] Suíte completa verde (293 testes; baseline do branch era 286).
  - [x] `pnpm --filter @e1p/web typecheck` verde após `timezone` na interface + `generated.ts` (arquivo separado, sem colisão).

- [x] **Task 9 — Checklist de qualidade (AC: todas)**
  - [x] `ruff check .` (apps/api) — zero erros.
  - [x] `python -m pytest` (apps/api) — 293 passed.
  - [x] `pnpm --filter @e1p/web typecheck` — sem erros.
  - [x] `bash scripts/check.sh` — equivalente executado manualmente (ruff + pytest + typecheck + vitest) neste ambiente (ver Debug Log).
  - [x] `export_openapi.py` roda e produz `openapi.json` válido; `pnpm --filter @e1p/shared-types generate` produz `generated.ts` sem erro.

## Dev Notes

### Previous Story Insights
- Nenhuma story do Epic 4 foi implementada ainda (todas em Draft/paralelas, mesma situação documentada pela Story 4.1); esta story é autocontida e não depende de nenhuma outra do backlog. Não assumir que Stories 4.1-4.4/4.6 já rodaram.
- Padrão de "não derrubar a request por falha de integração externa" (best-effort, `try/except` + log) já é uma convenção estabelecida no projeto (`core/whatsapp.py`, `core/email.py`) e citado como requisito explícito na Story 4.3/IV2 do mesmo epic — reaproveitado aqui na Task 1 (e-mail de boas-vindas).
- Padrão de "import lazy dentro da função para evitar acoplar módulos" já documentado no CLAUDE.md (Fase 3: `quotes.approve_quote` → `contracts.build_contract_from_quote`) e reaproveitado pela Story 4.1 — usado aqui nas Tasks 4 e 5 (Agenda/Cockpit → `settings.get_profile`).
- Padrão de migration "reservar um número exclusivo entre 13 stories paralelas + religar `down_revision` no merge" já estabelecido pela Story 4.1 (migration `0040`) e por um fix real já mergeado neste repo (`16648b3`).

### Contexto do sistema existente (fonte: epic)
- "Refinamentos: enumeração de e-mail no `/register`; `shared-types` mantido à mão (vs OpenAPI); `is_ai` placeholder na Agenda; janela do dia/`all_day` ancorada em meia-noite UTC (dívida de fuso por tenant)." [Source: docs/prd/epic-4-backlog-pos-lancamento.md — "Contexto do sistema existente"]
- "Nenhuma destas histórias bloqueia produção" — Epic 4 é backlog pós-lançamento, não bloqueante.
- Demais fontes: CLAUDE.md §6.1 (enumeração de e-mail, geração de tipos, rastro da IA, semântica de all_day) e §Cockpit (janela do dia em UTC).

### File Locations (novos/alterados) — ver File List abaixo

### Technical Constraints
- **IV1 é a restrição mais importante:** nenhuma AC quebra o contrato de `/register`/`/login` nem os tipos de `index.ts`. Mudanças ADITIVAS (mensagem unificada mas ainda 409; tipos gerados em arquivo novo; coluna nova com default seguro).
- **RLS não é tocada:** `0044` é `add_column` simples em `tenant_profiles` (RLS desde `0022`).
- **`tzdata` (pip) é obrigatório** — sem ele `zoneinfo` falha em `python:3.13-slim`/Windows.
- **Import lazy obrigatório** de `settings.service.get_profile` em `agenda/service.py` e `cockpit/router.py`.

### Testing
- Backend: `pytest` (SQLite em memória; RLS Postgres-only não exercida). Migration `0044` (add_column simples) — validar com Postgres real antes de produção (ver Debug Log / limitações).
- Frontend: `tsc --noEmit` (web) verde; sem novo teste de componente (nenhuma UI nova).

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-10 | 0.1 | Story criada a partir do Epic 4 (Backlog Pós-Lançamento) — River (@sm) | River (@sm) |
| 2026-07-10 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-07-10 | 0.2 | Implementação completa (Tasks 1-9), 293 testes verdes, typecheck OK — Status: Ready → InReview | Dex (@dev) |
| 2026-07-11 | 1.0 | QA Gate PASS (7 checks; contrato aditivo + fuso all-day não regride, confirmados em código; suíte 523 passed/9 skipped) — Status: InReview → Done | Quinn (@qa) |

## Dev Agent Record

### Agent Model Used
Opus 4.8 (1M) — claude-opus-4-8[1m]

### Debug Log References
- Ambiente sem Python nativo no host Windows; usada a distro WSL (Python 3.14) com venv bootstrapped via `get-pip.py`. As versões PINADAS de `requirements.txt` (para `python:3.13-slim`) não têm wheels em Python 3.14 (`psycopg-binary==3.2.3`, `pydantic-core`, `pillow==11.0.0`, `sqlalchemy==2.0.36` incompatível com typing do 3.14). Para RODAR os testes localmente, o venv efêmero usou: SQLAlchemy 2.0.51, pydantic 2.13.x, pillow 12.x e SEM psycopg (testes usam SQLite via `DATABASE_URL=sqlite://`). **Os pins do `requirements.txt` NÃO foram alterados** — permanecem os canônicos do container (só `tzdata==2024.2` foi adicionado). Esta é a mesma classe de "drift venv↔produção" já registrada no CLAUDE.md §6.1; validação canônica deve rodar na imagem Docker (Python 3.13).
- `ruff check .` (apps/api): All checks passed. `pytest`: 293 passed (baseline do branch: 286; +7 novos). `pnpm --filter @e1p/web typecheck`: sem erros. `pnpm --filter @e1p/web test` (vitest): 4 passed. `export_openapi.py` + `pnpm --filter @e1p/shared-types generate`: OK (openapi.json 111 paths/132 schemas; generated.ts 9432 linhas).

### Completion Notes List
- **AC1 (enumeração):** mensagens de conflito unificadas em `CONFLICT_MESSAGE` (as duas mensagens agora idênticas — provado por `test_register_conflict_message_does_not_leak_which_field`). E-mail de boas-vindas best-effort adicionado (falha nunca derruba o registro). Fluxo de confirmação por link/token NÃO implementado (dívida; precisa provedor de e-mail real — SMTP/SES — que este ambiente não tem; hoje `core/email.py` só loga em dev).
- **AC2 (tipos):** infraestrutura de geração entregue (script + comandos + `openapi.json` + `generated.ts` versionados). Migração das ~806 linhas de `index.ts` NÃO feita (dívida, Story 4.6). Sem CI, regeneração é MANUAL (risco de drift documentado no cabeçalho de `index.ts`).
- **AC3 (fuso + is_ai):** coluna `timezone` + helper `core/tz.py` + normalização de `all_day` no `create_event` + janela do dia do Cockpit no fuso do tenant. `is_ai` propagado em update/cancel/reschedule (default `False`; a "camada de ator IA" ainda não existe — `CurrentUser.is_ai` permanece placeholder `False`, fora de escopo).
- **Ajuste não previsto na story (feito):** `settings/router.py::_out` monta `ProfileOut` campo a campo (não via `from_attributes`), então precisou receber `timezone=p.timezone` — sem isso `GET/PATCH /settings/profile` quebrava (pego por teste). Arquivo dentro do escopo (parte do contrato do timezone).
- **Precisa de credencial/infra real para validação end-to-end (NÃO testável neste ambiente):**
  - Provedor de e-mail real (SMTP/SES) para o e-mail de boas-vindas efetivamente ENVIAR (hoje `send_email` retorna `"logged"` sem `smtp_host`). Testado apenas o caminho best-effort (dispatch + tolerância a falha) com mock.
  - `alembic upgrade head` da migration `0044` contra Postgres real (RLS é Postgres-only; SQLite não exerce). Baixo risco (add_column simples, sem política nova), mas não validado em Postgres neste ambiente.
- **Dívida a atualizar no CLAUDE.md (fora do escopo de código):** remover/atualizar as notas §6.1 (enumeração de e-mail — mitigada; geração de tipos — infra entregue; rastro da IA na Agenda — propagação fechada; semântica de all_day — normalizada) e §"Cockpit — pendência" (janela do dia agora no fuso do tenant). Sinalizado aqui para quem mantiver o CLAUDE.md.

### File List
- ALTERADO: `apps/api/app/modules/auth/service.py` (mensagens unificadas + e-mail de boas-vindas best-effort)
- ALTERADO: `apps/api/tests/test_auth.py` (novos casos)
- NOVO: `apps/api/scripts/export_openapi.py`
- ALTERADO: `packages/shared-types/package.json` (devDep `openapi-typescript` + script `generate`)
- NOVO: `packages/shared-types/openapi.json` (gerado, commitado)
- NOVO: `packages/shared-types/src/generated.ts` (gerado, commitado)
- ALTERADO: `packages/shared-types/src/index.ts` (cabeçalho + `TenantProfile.timezone`)
- ALTERADO: `package.json` (script `generate:types`)
- ALTERADO: `pnpm-lock.yaml` (resolução do `openapi-typescript`)
- ALTERADO: `apps/api/app/modules/settings/models.py` (`DEFAULT_TIMEZONE` + `TenantProfile.timezone`)
- ALTERADO: `apps/api/app/modules/settings/schemas.py` (`ProfileOut.timezone`, `ProfileUpdate.timezone`)
- ALTERADO: `apps/api/app/modules/settings/service.py` (`_FIELDS` inclui `"timezone"`)
- ALTERADO: `apps/api/app/modules/settings/router.py` (`_out` inclui `timezone`)
- NOVO: `apps/api/migrations/versions/0044_tenant_timezone.py`
- NOVO: `apps/api/app/core/tz.py` (`tenant_zone`, `day_window_utc`)
- ALTERADO: `apps/api/requirements.txt` (`tzdata`)
- ALTERADO: `apps/api/app/modules/agenda/service.py` (`create_event` normaliza `all_day`; `update/cancel/reschedule` ganham `by_ai`)
- ALTERADO: `apps/api/app/modules/agenda/router.py` (thread `user.is_ai` nos 3 endpoints)
- ALTERADO: `apps/api/tests/test_agenda.py` (novos casos)
- ALTERADO: `apps/api/app/modules/cockpit/router.py` (`summary` usa `day_window_utc` + fuso do tenant)
- ALTERADO: `apps/api/tests/test_cockpit.py` (novo caso)
- ALTERADO: `apps/api/tests/test_settings.py` (novos casos `timezone`)

## QA Results

### Gate Decision: **PASS** — Quinn (@qa), 2026-07-11 (SDC Phase 4, QA Gate)

Suíte canônica rodada UMA vez na imagem `e1p-api-test:local`: `ruff check .` **All checks passed**; `python -m pytest -q` **523 passed, 9 skipped** (inclui `test_auth.py`, `test_settings.py`, `test_agenda.py`, `test_cockpit.py`).

**7 Quality Checks**
| # | Check | Resultado |
|---|-------|-----------|
| 1 | AC coverage (AC1-3, IV1-3) | PASS — enumeração de e-mail mitigada (mensagem unificada + e-mail best-effort); infra de geração de tipos OpenAPI→TS entregue; fuso por tenant + `is_ai` propagado |
| 2 | Code quality | PASS — mudanças ADITIVAS; import lazy de `get_profile` em Agenda/Cockpit; helper `core/tz.py` fail-safe reutilizável |
| 3 | Testes | PASS — casos de mensagem idêntica, timezone default/PATCH, all-day ancorado, `by_ai` gravando `AuditEntry.is_ai`, Cockpit 23:30 local → dia correto; suíte verde |
| 4 | Segurança | PASS — `/register` continua 409 com `CONFLICT_MESSAGE` unificada (não revela qual campo colidiu); e-mail best-effort envolto em try/except (não derruba o registro) |
| 5 | Performance | PASS — `day_window_utc` é cálculo puro; migration 0044 `add_column` simples com `server_default` seguro |
| 6 | Confiabilidade (IV2, foco) | PASS — verificado em código: `core/tz.py::day_window_utc` ancora meia-noite REAL do fuso do tenant → UTC; para `America/Sao_Paulo` (UTC-3), dia D = `[D 03:00Z, D+1 03:00Z)`, então `starts_at[:10]` continua sendo D (correção all-day-por-data-de-calendário NÃO regride) |
| 7 | Documentação | PASS — dívidas: confirmação de e-mail por link (precisa SMTP/SES); migração das 806 linhas `index.ts`→`generated.ts` deferida (4.6); fuso positivo e reschedule all-day não re-normalizado |

**Verificação de código real:** confirmado por leitura direta — `core/tz.py::day_window_utc` usa `datetime.combine(day, time.min, tzinfo=zone).astimezone(UTC)` (ancoragem correta, IV2 preservada); `tenant_zone` fail-safe; migration 0044 `add_column timezone server_default="America/Sao_Paulo"`, `down_revision="0042"` (cadeia linear, 0043 livre). Mudanças de contrato `shared-types` são todas aditivas. Concordo com o @architect.

**Decisão: PASS → Status InReview → Done.**

## Architect Review (Aria)

**Veredito: PASS** — 2026-07-11 (quality_gate=@architect). Status NÃO alterado (permanece InReview).

### Gates de qualidade
- `ruff check .`: **All checks passed**. `pytest`: **523 passed, 9 skipped** (inclui `test_auth.py`, `test_settings.py`, `test_agenda.py`, `test_cockpit.py`). `pnpm typecheck` verde conforme Dev Agent Record (contrato `shared-types` propagado).

### Foco da missão — nenhuma mudança regride o contrato `shared-types` nem a correção de fuso (all-day por data de calendário)
Confirmado por leitura direta:
- **Contrato `shared-types` não regride (IV1):** todas as mudanças são ADITIVAS — `TenantProfile.timezone: string` (index.ts:645), `AgendaEvent.google_event_id`/`GoogleCalendarStatus`/`PublicImage` coexistem; `generated.ts` foi gerado em arquivo SEPARADO (as ~806 linhas de `index.ts` NÃO foram migradas — dívida deliberada, sem colisão). `/register` continua 409 (`CONFLICT_MESSAGE` unificada, ainda contém "subdomínio" E "e-mail" — asserts existentes válidos). Sem quebra de contrato.
- **Correção de fuso NÃO regride (IV2):** `core/tz.py::day_window_utc` ancora o dia-calendário na meia-noite REAL do fuso do tenant convertida p/ UTC. Para `America/Sao_Paulo` (UTC-3), meia-noite local do dia D = `03:00Z` do MESMO dia D → `starts_at[:10]` continua sendo D. A lição registrada no CLAUDE.md §6.0 ("all-day comparado por data de calendário, nunca horário local") é preservada e agora reforçada no backend. `agenda/service.py::create_event` (linhas 69-77) e `cockpit/router.py` (linhas 40-42) usam `day_window_utc` com o fuso do tenant via import lazy de `get_profile` — sem acoplar os módulos-núcleo a settings. Correto.
- `is_ai` propagado em update/cancel/reschedule (default `False` = retrocompat). Rastro da IA (Regra de Ouro nº 3) endereçado.
- Migration `0044` (`add_column timezone` com `server_default` seguro em `tenant_profiles`, RLS desde 0022) — aditiva, sem tocar política. `tzdata` adicionado ao requirements (obrigatório para `zoneinfo` em `python:3.13-slim`/Windows). Correto.
- **Correção não prevista, aprovada:** `settings/router.py::_out` monta `ProfileOut` campo a campo e precisou receber `timezone=p.timezone` — sem isso `GET/PATCH /settings/profile` quebraria (pego por teste). Dentro do escopo do contrato do timezone.

### Dívidas aceitas (corretamente escopadas)
- Fluxo completo de confirmação de e-mail por link/token NÃO implementado (só mitigação de enumeração + e-mail best-effort) — precisa provedor SMTP/SES real. OK.
- Migração das ~806 linhas de `index.ts` → `generated.ts` deferida (Story 4.6); sem CI, regeneração é MANUAL → risco de drift `openapi.json`/`generated.ts` documentado no cabeçalho. Aceitável como dívida.
- Normalização de `all_day` cobre fusos NEGATIVOS (Brasil-first); fuso positivo documentado como dívida. `reschedule_event` de all-day não re-normaliza. OK.
- Validação da migration `0044` em Postgres real é manual (RLS Postgres-only, SQLite não exerce) — baixo risco (add_column simples). Limitação de ambiente.

---

## Story Draft Checklist — Resultado (River, modo autônomo)

| Categoria | Status | Observações |
|---|---|---|
| 1. Goal & Context Clarity | PASS | Objetivo claro (3 refinamentos de hardening/manutenção não bloqueantes). |
| 2. Technical Implementation Guidance | PASS | Arquivos-alvo com paths/linhas reais; tecnologia especificada; migration única (`0044`). |
| 3. Reference Effectiveness | PASS | Referências a seções/linhas específicas com `[Source: ...]`. |
| 4. Self-Containment Assessment | PASS | Decisões de escopo via `[AUTO-DECISION]`. |
| 5. Testing Guidance | PASS | Casos de teste específicos por Task. |
| 6. CodeRabbit Integration (conditional) | N/A | Disabled por instrução do orquestrador. |

**Final Assessment: READY** (clareza 9/10).

### Riscos / lacunas técnicas sinalizadas
1. Fluxo de confirmação de e-mail completo (link clicável) NÃO implementado — só mitigação de enumeração + e-mail best-effort. Dívida (precisa provedor de e-mail).
2. Migração completa de `shared-types/src/index.ts` (806 linhas) NÃO feita — só a infra de geração. Dívida (Story 4.6).
3. Drift entre `openapi.json`/`generated.ts` commitados e o schema real — regeneração manual sem CI.
4. Normalização de `all_day` só cobre fusos NEGATIVOS (Brasil-first). Fuso positivo documentado.
5. `reschedule_event` de all-day não re-normaliza. Dívida.
6. Migration `0044` reservada; `down_revision` pode precisar de religamento manual no merge.
7. Versão de `openapi-typescript` escolhida na implementação: `^7.13.0` (estável mais recente).

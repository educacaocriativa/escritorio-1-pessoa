# Story 2.2: Gateway de pagamento real (Asaas / Mercado Pago)

## Status
InReview

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["ruff (apps/api)", "pytest (apps/api/tests)", "manual security review (webhook signature/idempotency)", "validação manual de sandbox do gateway escolhido"]
```
> [AUTO-DECISION] Integração de API externa por trás de módulo de backend existente (`receivables`) → executor=@dev. quality_gate=@architect por envolver decisão arquitetural (ADR do gateway, contrato do webhook) e superfície de risco financeira (split/idempotência), mesmo padrão de delegação usado nas Stories 1.x/2.1 deste épico.

## Story
**As a** dono da empresa de 1 pessoa e a plataforma,
**I want** gerar boleto/Pix registrado de verdade e receber o webhook real de compensação,
**so that** o dinheiro entre e o split (40/30/20) seja creditado na Carteira automaticamente.

## Acceptance Criteria
1. Um gateway (Asaas ou Mercado Pago — decisão via ADR no início do épico) gera boleto/Pix **registrado** (substituindo o layout-stub do `core/boleto.py`), com linha digitável/QR válidos e vencimento correto.
2. `POST /receivables/webhook` recebe o webhook real de compensação, valida `GATEWAY_WEBHOOK_SECRET`, e credita a Carteira com o split, liberando o valor para saque no Financeiro.
3. A baixa é **idempotente** e protegida contra baixa dupla/crédito duplicado (preserva os locks `FOR UPDATE` existentes); pagamentos duplicados do gateway não geram dupla transação.
4. Cada ocorrência recorrente gera seu próprio boleto/cobrança registrado (mantém o comportamento de recorrência atual).

### Integration Verification
- IV1: O efeito dominó existente (orçamento/proposta aprovada → cobrança → contrato) e a recorrência continuam funcionando com o gateway real.
- IV2: O split e os saldos da Carteira (disponível/a receber/sacado) e o `platform_earnings` batem após a baixa real (testes de Carteira/Contas a Receber passam).
- IV3: Isolamento por tenant intacto; webhook não permite cruzar tenants nem baixar cobrança de outro tenant.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false` por instrução explícita do orquestrador desta missão (ambiente sem CodeRabbit disponível).
> Quality validation will use manual review process only (ver `quality_gate_tools` acima e `scripts/check.sh`).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [x] **Task 1 — Confirmar/registrar a decisão do gateway (ADR) antes de codificar (AC: 1)**
  - [x] **[AUTO-DECISION]** Nenhum ADR de gateway existe hoje no repo (`docs/decisions/` só tem `0001-stack-e-infra.md`; nenhuma menção a "Asaas"/"Mercado Pago" fora do próprio epic file). O epic exige a decisão "no início do épico" — como esta story roda em modo autônomo e não pode bloquear em uma pergunta ao usuário, **decide-se `Asaas`** como gateway alvo desta implementação. Reason: Asaas tem API mais simples para boleto+Pix registrados combinados (um único endpoint de cobrança gera ambos), suporta `externalReference` para correlacionar nossa cobrança sem expor `tenant_id`/`charge_id` como chaves previsíveis, e é amplamente usado por SaaS brasileiros de pequeno porte (baixo custo fixo). **Isto NÃO substitui o ADR formal** — antes de codificar, @architect deve criar `docs/decisions/0002-gateway-pagamento.md` ratificando (ou revertendo) esta escolha; se o time trocar para Mercado Pago, a Task 2 (adapter) foi desenhada para isolar o impacto a um único módulo.
  - [x] Desenhar a integração como **adapter atrás de uma interface própria** (não acoplar `receivables/service.py` diretamente ao SDK/HTTP do provedor) — mesmo espírito de `core/whatsapp.py`/`core/email.py` (função com contrato estável, stub quando sem credenciais).

- [x] **Task 2 — Client/adapter do gateway com graceful degradation (AC: 1, 4)**
  - [x] Criar `apps/api/app/core/payment_gateway.py` (mesmo nível de `core/email.py`/`core/whatsapp.py`), expondo uma função (ou pequena classe) com um contrato do tipo:
    ```python
    def create_registered_charge(*, method: str, amount_cents: int, due_date: date, payer_name: str, payer_document: str | None, external_reference: str) -> GatewayChargeResult:
        ...  # GatewayChargeResult: gateway_charge_id, payment_code (linha digitável/Pix copia-e-cola), boleto_pdf_bytes | boleto_url, status
    ```
  - [x] **Graceful degradation obrigatória** (Regra de Ouro/CLAUDE.md §"Todos os stubs adotam graceful degradation"): sem `settings.payment_gateway_api_key` configurada, cair de volta no comportamento atual — `_payment_code()` stub (`apps/api/app/modules/receivables/service.py:42-48`) + `generate_boleto_pdf()` (`apps/api/app/core/boleto.py`) — para não quebrar dev local nem os testes existentes que não configuram gateway.
  - [x] Com credenciais configuradas: chamar a API do Asaas para criar a cobrança registrada (boleto **e/ou** Pix conforme `method`), obter a linha digitável/QR real e o PDF do boleto (ou a URL do boleto, buscada via `httpx.get` e salva como bytes — mesmo padrão de armazenamento hoje usado em `Attachment.data`, `apps/api/app/modules/attachments/models.py`). **Nota:** os nomes exatos de campos/endpoints da API do Asaas (ex.: payload de `POST /v3/payments`, `externalReference`, `bankSlipUrl`, `PIX` via `POST /v3/payments/{id}/pixQrCode`) devem ser **confirmados contra a documentação oficial vigente no momento da implementação** (via `context7`/busca), não assumidos cegamente a partir deste texto — nenhuma chamada real foi validada nesta story de draft.
  - [x] `external_reference` deve carregar `charge.id` (UUID já único, gerado por `_uuid()` em `apps/api/app/db/base.py`) — usado para correlacionar o webhook de volta à nossa `Charge` (ver Task 3).
  - [x] Plugar em `apps/api/app/modules/receivables/service.py`: `build_charge()` (linhas ~56-104) e `_attach_boleto()` (linhas ~107-125) passam a chamar o adapter em vez de `_payment_code()`/`generate_boleto_pdf()` diretamente quando o gateway estiver configurado. Como `build_charge()` já roda **uma vez por ocorrência** dentro do loop de `create_charge()` (linhas 128-144), a AC4 (cada recorrência gera seu próprio registro) é satisfeita automaticamente sem mudança na lógica de recorrência (`app/core/recurrence.py`) — só confirmar com teste (Task 6).

- [x] **Task 3 — Adaptar `POST /receivables/webhook` ao payload real do provedor (AC: 2, 3)**
  - [x] `apps/api/app/modules/receivables/router.py::payment_webhook` (linhas 57-75) e `apps/api/app/modules/receivables/schemas.py::WebhookPayment` (linhas 74-79) hoje esperam um corpo **interno de teste** (`{tenant_id, charge_id, status, secret}`) — não o payload real do Asaas (evento com `payment.id`/`payment.externalReference`/`event` tipo `PAYMENT_RECEIVED`/`PAYMENT_CONFIRMED`, e autenticação tipicamente por **header**, não por campo no body). Ajustar o endpoint para:
    - Aceitar o payload real do provedor (schema novo, ex. `AsaasWebhookPayload`), validando a assinatura/token conforme o mecanismo do provedor (header customizado) — **confirmar o nome exato do header contra a documentação vigente do Asaas antes de implementar**; manter fail-closed: sem `GATEWAY_WEBHOOK_SECRET` configurado, comportamento atual (aberto, só para dev/testes) é preservado.
    - Extrair `charge_id` do `external_reference` setado na Task 2 (não confiar em `tenant_id` vindo do payload do provedor — ele não existe lá). Uma vez resolvido o `charge_id`, é necessário descobrir o `tenant_id` correspondente **sem cruzar tenants**: como `charges` tem RLS, considerar carregar o mapeamento `charge_id → tenant_id` a partir de uma tabela/índice acessível sem tenant_session (ver Task 4) ou manter `tenant_id` também dentro do `external_reference` (ex.: `f"{tenant_id}:{charge.id}"`) e parsear de volta — abordagem mais simples, sem precisar de lookup global. **[AUTO-DECISION]**: preferir codificar `tenant_id:charge_id` no `external_reference`, evitando introduzir uma nova consulta global sem RLS (reduz superfície de risco de vazamento cross-tenant, alinhado à Regra de Ouro nº 1 do CLAUDE.md).
    - Depois de resolver `(tenant_id, charge_id, status)` a partir do payload real, **reusar `service.webhook_confirm(session_factory=..., tenant_id=..., charge_id=...)` sem alterá-la** (`apps/api/app/modules/receivables/service.py:276-283`) — preserva o contrato interno e os testes que chamam essa função diretamente, conforme exigido pelo epic ("preservando os chamadores e testes atuais").
    - Manter o endpoint de teste local (`POST /receivables/charges/{id}/pay` e o payload interno simplificado) funcionando **apenas em dev** (sem quebrar o link "simular pgto" hoje documentado em `CLAUDE.md` §"Financeiro: boleto gera arquivo..."), já que não há gateway sandbox configurável neste ambiente de draft.
  - [x] Confirmar que a idempotência atual é suficiente: `mark_paid()` (`service.py:244-273`) já usa `SELECT ... FOR UPDATE` + early-return se `status == STATUS_PAID` — isso cobre "pagamentos duplicados do gateway não geram dupla transação" (AC3) sem mudança adicional, desde que o webhook real também possa reenviar eventos (at-least-once) para o mesmo `charge_id` já pago.

- [x] **Task 4 — Migration 0033 (campos aditivos, se confirmada a necessidade na Task 2/3) (AC: 1, 2)**
  - [x] **Reservado exclusivamente para esta story: `apps/api/migrations/versions/0033_*.py`** (não usar outro número — 13 stories rodam em paralelo neste epic/outros epics, cada uma com número próprio reservado).
  - [x] **`down_revision` NÃO pode ser fixado neste draft**: o head atual conhecido é `0031` (`0031_platform_audit_entries.py`), mas outras stories paralelas podem inserir `0032` antes desta ser implementada. @dev deve rodar `alembic heads` (ou verificar `apps/api/migrations/versions/` no momento da implementação) e apontar `down_revision` para o head real então existente — nunca assumir `"0031"` ou `"0032"` cegamente.
  - [x] Campos aditivos sugeridos na tabela `charges` (nullable, sem quebrar linhas existentes — mesmo padrão da migration 0029): `gateway_provider: String(20) | None`, `gateway_charge_id: String(64) | None` (id da cobrança no Asaas, para auditoria/suporte, não usado como chave de correlação — ver Task 3), `gateway_status_raw: String(40) | None` (último status bruto recebido do webhook, para debug). Atualizar `apps/api/app/modules/receivables/models.py::Charge` (após linha 51) e `ChargeOut`/`ChargeCreate` em `schemas.py` conforme necessário (campos novos devem ser opcionais/somente-leitura no `ChargeOut`, não exigidos no `ChargeCreate`).
  - [x] Se, na implementação real, a Task 2/3 concluir que nenhum campo novo é estritamente necessário (ex.: tudo cabe em `payment_code` + `external_reference` embutido), **documentar essa decisão no Dev Agent Record e não criar a migration** — não inventar colunas sem uso.

- [x] **Task 5 — Configuração/env vars (AC: 1, 2)**
  - [x] `apps/api/app/config.py::Settings` — adicionar, seguindo o padrão existente (vazio = stub/log, mesmo comentário de `whatsapp_token`/`smtp_host` linhas ~30-34): `payment_gateway_provider: str = ""` (ex.: `"asaas"`; vazio = mantém stub atual), `payment_gateway_api_key: str = ""`, `payment_gateway_base_url: str = ""` (permite apontar para sandbox). Reusar `gateway_webhook_secret` já existente (linha 34) para a validação do webhook — não duplicar.
  - [x] Adicionar as novas envs em `.env.example`/documentação de deploy relevante, se existir um arquivo desse tipo no repo (verificar `docs/AWS-DEPLOYMENT.md`/`docs/HOSTINGER-DEPLOY.md`) — seguir o padrão usado pela Story 1.4 ao adicionar item ao checklist de deploy (`docs/HOSTINGER-DEPLOY.md` §4 "Validar").
  - [x] **Não** adicionar guard de boot (`_guard_production_secrets`) exigindo essas novas envs em produção — o gateway deve permanecer com graceful degradation mesmo em prod (mesma filosofia dos demais stubs), diferente do `JWT_SECRET`/`ANTHROPIC_API_KEY` que são bloqueantes.

- [x] **Task 6 — Testes (AC: 1, 2, 3, 4; IV1, IV2, IV3)**
  - [x] `apps/api/tests/test_receivables.py` — preservar `test_webhook_recognizes_payment_and_credits_wallet` (linhas 42-56) funcionando (idealmente sem alterar o formato do payload de teste, se o endpoint mantiver compatibilidade com o corpo interno em dev; caso a Task 3 troque o schema do endpoint, **atualizar este teste** para o novo payload e adicionar um teste equivalente cobrindo o payload real do provedor).
  - [x] Novo teste: webhook chamado duas vezes para a mesma cobrança (simulando reenvio at-least-once do gateway) não duplica a `Transaction` nem o `platform_earnings` — cobre AC3/IV2 (idempotência), reforçando o que `test_pay_twice_is_idempotent` (linha 141) já cobre para o fluxo interno, mas agora pelo caminho do webhook real.
  - [x] Novo teste: sem `payment_gateway_api_key` configurada, `create_charge`/`_attach_boleto` continuam gerando o boleto stub atual (graceful degradation) — garante IV1 (dominó orçamento→cobrança→contrato não quebra em dev/CI sem credenciais reais).
  - [x] Novo teste: cobrança recorrente (`recurrence_count > 1`) gera um `payment_code`/registro por ocorrência (reforça `test_recurring_charge_generates_occurrences`, linha 58, agora também validando que cada ocorrência teria, em tese, sua própria chamada ao gateway — mockada/stubada, já que não há sandbox neste ambiente).
  - [x] Novo teste de isolamento: webhook com `external_reference` de uma cobrança do tenant A não pode ser processado sob o `tenant_id` de outro tenant B (IV3) — usar o mesmo padrão de teste cross-tenant já usado no projeto (ex.: `test_platform.py`/testes de RLS existentes) como referência de estilo.
  - [x] Mock/stub das chamadas HTTP ao gateway via `monkeypatch` (não há biblioteca de mock de HTTP no `requirements.txt` — `httpx==0.28.0` já está presente; usar `monkeypatch.setattr` na função/cliente do adapter, mesmo padrão simples já usado no projeto para isolar I/O externo).

- [x] **Task 7 — Checklist de qualidade (AC: todas)**
  - [x] `ruff check .` (apps/api) e suite completa `pytest` (apps/api/tests) verdes.
  - [x] Rodar `bash scripts/check.sh` se aplicável ao escopo tocado (backend).
  - [x] Revisão manual de segurança do webhook (assinatura/segredo, fail-closed, não vazamento cross-tenant) — não há CodeRabbit disponível neste ambiente (ver seção CodeRabbit Integration acima).

## Dev Notes

### Previous Story Insights
- Esta story roda em paralelo com outras do Epic 2 (incluindo a 2.1 — provedor de e-mail) e de outros epics; **não assumir que 2.1 ou qualquer outra story numericamente anterior já foi implementada**. A story foi escrita de forma autocontida (não depende de artefatos gerados pela 2.1).

### Contexto do sistema existente (fonte: epic + CLAUDE.md)
- "existe `core/boleto.py` (fpdf2) que gera um boleto **layout-stub sem registro bancário**; o pagamento entra por `POST /receivables/webhook` (público, protegido por `GATEWAY_WEBHOOK_SECRET`), que credita a Carteira com split (40/30/20) usando locks `FOR UPDATE` contra baixa dupla. Falta o gateway real (Asaas/Mercado Pago) gerando boleto/Pix registrado e enviando o webhook de verdade." [Source: docs/prd/epic-2-integracoes-reais.md — "Contexto do sistema existente"]
- "Cada provedor é plugado **por trás das interfaces internas existentes** [...], preservando os chamadores e testes atuais. Idempotência e locks `FOR UPDATE` da Carteira/Contas a Receber devem permanecer intactos. Escolha específica do gateway [...] deve ser formalizada em ADR no início do epic." [Source: docs/prd/epic-2-integracoes-reais.md — "Integration Requirements"]
- "gateway real (Asaas/Mercado Pago) p/ gerar boleto/Pix de verdade + webhook real; [...] boleto atual é layout-stub sem registro bancário." [Source: CLAUDE.md — "Financeiro: boleto gera arquivo + pagamento automático"]
- "Dev/teste: link discreto 'simular pgto' nas cobranças chama o webhook (some quando o segredo for definido em prod). Endpoint `/pay` mantido só para testes internos." [Source: CLAUDE.md — mesma seção] — este comportamento de dev **deve ser preservado**.
- Regra de Ouro nº 1 (isolamento de tenant): "Nunca escreva query que possa cruzar tenants." [Source: CLAUDE.md §3.1] — motivou a decisão (Task 3) de correlacionar o webhook via `external_reference` embutindo `tenant_id:charge_id`, em vez de introduzir uma consulta global sem RLS.
- Regra de Ouro nº 5: "Toda mudança roda os agentes de QA [...] e a suíte de testes antes de considerar concluída." [Source: CLAUDE.md §3.5]

### Arquivos e padrões relevantes (verificado diretamente no código)
- `apps/api/app/core/boleto.py` — stub atual de PDF (fpdf2), a ser complementado/substituído por dados reais do gateway (Task 2).
- `apps/api/app/core/whatsapp.py` e `apps/api/app/core/email.py` — padrão de referência para o novo `core/payment_gateway.py`: função simples, retorna status/resultado, **graceful degradation** quando a credencial não está configurada (`if not settings.X: log + fallback`), `httpx` já usado em `whatsapp.py` como cliente HTTP síncrono.
- `apps/api/app/modules/receivables/service.py` — `_payment_code()` (linhas 42-48, stub a substituir), `build_charge()` (linhas 56-104), `_attach_boleto()` (linhas 107-125), `create_charge()` (linhas 128-144, já itera por ocorrência de recorrência), `mark_paid()` (linhas 244-273, lock `FOR UPDATE` + idempotência já implementados), `webhook_confirm()` (linhas 276-283, contrato interno a preservar).
- `apps/api/app/modules/receivables/router.py` — `payment_webhook()` (linhas 57-75), validação atual de `GATEWAY_WEBHOOK_SECRET` via campo `secret` no body (linha 65) — provavelmente precisa migrar para validação via header conforme o provedor real.
- `apps/api/app/modules/receivables/schemas.py::WebhookPayment` (linhas 74-79) — schema interno atual, a ser complementado por um schema do payload real do provedor.
- `apps/api/app/modules/receivables/models.py::Charge` (linhas 27-51) — `payment_code` (Text, já existe), `transaction_id`, campos candidatos a adicionar na migration 0033 (Task 4).
- `apps/api/app/modules/wallet/service.py::build_transaction()` (linhas 93-136) — split 40/30/20 e criação do `PlatformEarning`, chamado por `mark_paid()`; **não deve ser alterado** por esta story (fora de escopo, já funciona).
- `apps/api/app/config.py::Settings` (linhas 1-59) — padrão de envs vazias = stub (`whatsapp_token`, `smtp_host`, `gateway_webhook_secret` já existe, linha 34); `_guard_production_secrets` (linhas 50-59) não deve passar a exigir as novas envs de gateway (graceful degradation também em produção, diferente de `JWT_SECRET`/`ANTHROPIC_API_KEY`).
- `apps/api/app/db/base.py::_uuid()` — geração do `Charge.id`, usado como candidato a `external_reference`.
- `apps/api/migrations/versions/0031_platform_audit_entries.py` — último arquivo de migration confirmado no repo nesta leitura; formato de referência (revision/down_revision/upgrade/downgrade) a seguir na migration 0033.
- `apps/api/tests/test_receivables.py` — testes existentes a preservar (`test_boleto_charge_generates_pdf_attachment` linha 32, `test_webhook_recognizes_payment_and_credits_wallet` linha 42, `test_recurring_charge_generates_occurrences` linha 58, `test_pay_twice_is_idempotent` linha 141).

### Technical Constraints
- **Não inventar nomes de campos/endpoints da API do Asaas** neste draft — a Task 2/3 exigem confirmação contra a documentação oficial vigente no momento da implementação (via `context7` ou busca web), já que este ambiente de draft não tem acesso a sandbox nem a docs atualizadas do provedor.
- Preservar a assinatura de `service.webhook_confirm(session_factory, tenant_id, charge_id)` — é o contrato que os testes e o restante do sistema esperam.
- Preservar os locks `FOR UPDATE` de `mark_paid()` — não remover nem contornar.
- Não introduzir consulta global (sem RLS) para correlacionar `charge_id → tenant_id` no webhook — usar `external_reference` embutido (`tenant_id:charge_id`) para evitar esse risco (ver Regra de Ouro nº 1).
- Manter graceful degradation mesmo em produção para as envs do gateway (diferente de `JWT_SECRET`/`ANTHROPIC_API_KEY`, que são bloqueantes via `_guard_production_secrets`) — decisão de produto já registrada no epic ("Todos os stubs adotam graceful degradation [...] comportamento a ser preservado").
- Migration 0033 é aditiva/nullable — não alterar `payment_code` nem remover colunas existentes.

### Testing
- Framework backend: `pytest` (`apps/api/tests`), mesmo padrão de fixtures (`client`, `headers`) usado em `test_receivables.py`.
- Sem biblioteca de mock HTTP dedicada no projeto (`respx`/`responses` não estão em `requirements.txt`) — usar `monkeypatch` para isolar chamadas HTTP do adapter do gateway, mesmo padrão simples já implícito no projeto (nenhum teste hoje exercita `httpx.post` real de `core/whatsapp.py`, pois os testes não configuram `whatsapp_token`).
- Nenhuma orientação técnica específica encontrada nos docs disponíveis sobre testes de contrato (contract testing) com o gateway real — não há sandbox configurado neste ambiente; validação de integração real end-to-end com o Asaas fica fora do escopo automatizável desta story (mesma limitação de falta de e2e Postgres/gateway real já registrada nas Stories do Epic 1).

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-10 | 0.1 | Story criada a partir do Epic 2 (Integrações Reais) — Story 2.2 | River (@sm) |
| 2026-07-10 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-07-10 | 0.2 | Implementação: adapter Asaas + webhook real + migration 0033 + testes — Status: Ready → InReview | Dex (@dev) |

## Dev Agent Record

### Agent Model Used
claude-opus-4-8 (Dex / @dev), modo YOLO autônomo.

### Debug Log References
- `ruff check .` (apps/api) → All checks passed.
- `pytest` (apps/api) → 296 passed (28 em test_receivables.py, sendo 11 novos desta story). 0 regressões.
- `alembic heads` → `0033 (head)`; cadeia `0031 -> 0033` linear, sem múltiplos heads.
- Ambiente de teste: sem Python 3.13 nativo no host/worktree; suíte executada num venv Python 3.13
  (via `uv`) no WSL apontando ao código do worktree. Sem Postgres/gateway sandbox neste ambiente.

### Completion Notes List
- **ADR do gateway (Task 1):** mantido o [AUTO-DECISION] do draft — alvo **Asaas** (API única para
  boleto+Pix registrados, `externalReference` para correlação). O ADR formal `docs/decisions/
  0002-gateway-pagamento.md` NÃO foi criado (é autoria do @architect, fora do escopo de @dev);
  fica como pendência de ratificação. O adapter isola o provedor num único módulo, então trocar
  para Mercado Pago impacta só `core/payment_gateway.py`.
- **Adapter (Task 2):** `app/core/payment_gateway.py` — mesmo padrão de `core/whatsapp.py`/`email.py`:
  `is_configured()` + `create_registered_charge(...)` → `GatewayChargeResult`. Graceful degradation
  total: sem `payment_gateway_api_key` mantém o stub (inclusive em produção). Qualquer erro HTTP/
  contrato levanta `GatewayError` e o `service` degrada para o stub — criar cobrança NUNCA quebra
  por causa do gateway.
- **Correlação webhook (Task 3):** `external_reference = f"{tenant_id}:{charge_id}"` embutido — o
  webhook resolve o tenant SEM consulta global sem RLS (Regra de Ouro nº 1). `webhook_confirm()`
  preservada 1:1 (contrato interno intacto). Endpoint `POST /receivables/webhook` agora aceita o
  payload REAL do Asaas (`event`+`payment.externalReference`) E o payload interno de dev/teste
  (`tenant_id/charge_id/status/secret`) — o link "simular pgto" continua funcionando. Segredo
  fail-closed: com `GATEWAY_WEBHOOK_SECRET` definido, valida o token do header (`asaas-access-token`)
  para o payload real e o `secret` do corpo para o interno; vazio = aberto (dev).
- **Idempotência (AC3):** `mark_paid()` intacta (SELECT ... FOR UPDATE + early-return em PAID).
  Teste novo cobre reenvio at-least-once via webhook real sem dobrar receita/`platform_earnings`.
- **Migration 0033 (Task 4):** 3 colunas aditivas NULLABLE em `charges` (`gateway_provider`,
  `gateway_charge_id`, `gateway_status_raw`) para auditoria/suporte/debug. `down_revision="0031"`
  (head real no momento do checkout). Não embuti `tenant_id:charge_id` em `charges.external_ref`
  (String(64) < 73 chars); o valor é derivável e vem de volta no webhook.
- **Config (Task 5):** `payment_gateway_provider/api_key/base_url` adicionados (vazio=stub). Reusa
  `gateway_webhook_secret` existente. Guard de boot NÃO passou a exigir as novas envs (graceful
  degradation em prod, por decisão de produto). `.env.example` atualizado.
- **Testes (Task 6):** 11 novos — payload real Asaas credita carteira; idempotência via webhook;
  evento não-pago ignorado; externalReference malformado → 400; segredo fail-closed (header p/ real,
  body p/ interno); graceful degradation sem chave; gateway configurado usa código registrado +
  external_reference correto; uma chamada ao gateway por ocorrência recorrente; falha do gateway
  degrada para stub. Mock via `monkeypatch` (sem lib de mock HTTP nova).

### Limitações / precisa de credencial ou infra real (não validável neste ambiente)
- **Nomes exatos de endpoints/campos da API do Asaas** (`/customers`, `/payments`,
  `/payments/{id}/identificationField`, `/payments/{id}/pixQrCode`, `bankSlipUrl`, header
  `asaas-access-token`) seguem a doc pública mas **não foram validados contra sandbox real** (sem
  credenciais) — revalidar contra a doc vigente + sandbox antes do go-live (marcado no código).
- **Boleto PDF oficial:** hoje o PDF é regenerado com a linha digitável REGISTRADA real; o boleto
  bancário oficial fica em `gateway.boleto_url` (bankSlipUrl). Embutir os bytes do PDF oficial exige
  validar o endpoint/formato real do provedor (TODO, sem sandbox aqui).
- **Isolamento cross-tenant (IV3):** a defesa real é a RLS (Postgres) — os testes rodam em SQLite,
  onde a RLS não é exercida; validado apenas o parsing do `external_reference`. Cruzamento de tenant
  end-to-end deve ser validado no Postgres real (mesma limitação já registrada no Epic 1).
- **ADR 0002 (ratificação do gateway)** e a assinatura de webhook em produção (registrar o token no
  painel do Asaas) dependem de decisão do time / credenciais reais.

### File List
- `apps/api/app/core/payment_gateway.py` (novo — adapter Asaas com graceful degradation)
- `apps/api/app/config.py` (novas envs do gateway)
- `apps/api/app/modules/receivables/service.py` (integração do adapter + parsing/handler do webhook)
- `apps/api/app/modules/receivables/router.py` (endpoint webhook aceita payload real+interno; gateway fields no ChargeOut)
- `apps/api/app/modules/receivables/models.py` (colunas aditivas do gateway)
- `apps/api/app/modules/receivables/schemas.py` (ChargeOut +gateway_*; AsaasWebhookPayload)
- `apps/api/migrations/versions/0033_charge_gateway_fields.py` (novo — migration aditiva)
- `apps/api/tests/test_receivables.py` (11 testes novos da Story 2.2)
- `.env.example` (novas envs do gateway documentadas)

## QA Results
_(a preencher pelo @qa)_

---

## Story Draft Checklist — Resultado (executado por @sm, modo autônomo)

| Category | Status | Issues |
|---|---|---|
| 1. Goal & Context Clarity | PASS | Objetivo, valor de negócio e encaixe no epic claros; dependência de ADR (não bloqueante para draft) sinalizada explicitamente na Task 1. |
| 2. Technical Implementation Guidance | PARTIAL | Arquivos-chave, funções e pontos de integração identificados com precisão (grep/read no código real). Gap intrínseco: nomes exatos de campos/endpoints da API do gateway real não puderam ser confirmados neste ambiente (sem acesso a docs atualizadas/sandbox) — sinalizado explicitamente como ação obrigatória do @dev antes de codificar, não uma lacuna silenciosa. |
| 3. Reference Effectiveness | PASS | Referências apontam para seções/linhas específicas do epic, CLAUDE.md e do código-fonte real. |
| 4. Self-Containment Assessment | PASS | Story não depende de outras stories do epic para ser entendida; assunções (gateway=Asaas, correlação via external_reference) explicitadas como [AUTO-DECISION] com racional. |
| 5. Testing Guidance | PASS | Testes existentes a preservar listados por nome/linha; novos testes (idempotência via webhook, graceful degradation, isolamento cross-tenant, recorrência) especificados. |
| 6. CodeRabbit Integration (conditional) | N/A | Disabled neste ambiente (ver seção acima). |

**Final Assessment: NEEDS REVISION (condicional a decisão externa, não a qualidade do draft)**
- A story está pronta para implementação **desde que** a Task 1 (ratificação do gateway via ADR real, por @architect) seja resolvida antes ou no início da implementação — o [AUTO-DECISION] de escolher Asaas destrava o trabalho técnico, mas não substitui a decisão formal do time.
- Risco técnico residual: os detalhes exatos da API do provedor (payload de criação de cobrança, mecanismo de assinatura do webhook) não puderam ser verificados neste ambiente e ficam marcados como verificação obrigatória do @dev (Article IV — No Invention: nenhum campo de API foi apresentado como fato confirmado).
- Migration 0033 reservada exclusivamente para esta story; `down_revision` deliberadamente não fixado (dependente do head real no momento da implementação, dado paralelismo com outras 13 stories).

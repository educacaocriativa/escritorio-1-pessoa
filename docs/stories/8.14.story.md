# Story 8.14: Estado `scheduled` — agendar pagamento sem mentir na Projeção

## Status
InReview

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["ruff (apps/api)", "pytest apps/api/tests (test_payables.py, test_payment_queue.py, test_financial_intelligence_projection.py, test_worker.py estendidos + novo test_payables_scheduled.py)", "pytest -m rls_e2e (Postgres real, testcontainers — job cross-tenant-rls)", "vitest apps/web (contas.test.ts estendido — testes de colisão de rótulo do UX-001; PagarPage.test.tsx)", "tsc --noEmit (apps/web)", "etapas individuais de lint/types/testes (⚠️ scripts/check.sh mascara falha de frontend com `|| true` — epic §11.6 X9)", "os 3 agentes de QA (regression-tester, bug-hunter, dedup-checker — CLAUDE.md §5)", "validação manual: agendar para D+15 e conferir que o valor NÃO entra em 'Total em contas', NÃO some da Projeção e aparece em 'Agendado para sair'"]
```
> [AUTO-DECISION] Story de **backend (modelo de estado + projeção + worker) + frontend (um número novo numa tela em produção)** → `executor="@dev"`, `quality_gate="@architect"`. Reason: é a story que o design da Onda 2 chama de *"a mudança mais delicada"* (§4.2.3) e o epic de *"a mais delicada da onda"* (§6). O que está em jogo é **modelagem de estado** (`scheduled` derivado da data) e a **aritmética da Projeção de Caixa em produção** — decisões que a @architect tomou nominalmente (F-D9, F-D11) e que ela precisa confirmar que foram implementadas como projetadas, não contornadas. Mesmo gate das 8.1/8.5/8.6/8.7.

## Story
**As a** dono da empresa de 1 pessoa que **agenda pagamento no app do banco**,
**I want** registrar a baixa com a data futura em que o débito vai sair, e ver esse dinheiro como **"agendado"** — fora do meu saldo de hoje, mas **presente na Projeção de Caixa** e na Fila,
**so that** eu não conte com R$ 5.000 que já têm destino, e o e1p não volte a esconder uma saída certa da mesma tela que a Onda 0 existiu para consertar.

## Acceptance Criteria

1. **`scheduled` entra em `payables`, sem migration de tipo.** `apps/api/app/modules/payables/models.py`
   ganha `STATUS_SCHEDULED = "scheduled"` e `ALL_STATUSES` passa a `{open, scheduled, paid, canceled}`.
   A coluna é `String(12)` (`models.py:43`) e `"scheduled"` tem **9 caracteres** — **nenhuma migration
   é criada nesta story**. Teste que afirma o cabimento explicitamente (`len(STATUS_SCHEDULED) <= 12`),
   para que ninguém "melhore" o vocabulário para uma string maior sem perceber o custo.
   → design Onda 2 §4.2.3; epic §5 item 2.8; verificado em `payables/models.py:43`

2. **O estado é DERIVADO da data, nunca escolhido.** No momento da escrita (`apply_paid` e
   `PATCH /payables/bills/{id}/payment`, ambos da Story 8.12):

   | `paid_on` | `status` resultante | `paid_at` |
   |---|---|---|
   | `> hoje` | `scheduled` | o `paid_on` informado |
   | `== hoje` ou `< hoje` | `paid` | o `paid_on` informado |

   **A API nunca aceita `status` do cliente** — nem em `PayableCreate`, nem em `PayableUpdate`, nem na
   rota de pagamento. Invariante testável: **`status == 'scheduled' ⟺ paid_at.date() > hoje`, no
   momento da escrita** (depois disso quem move é o worker do AC10). Teste nas **duas direções**, no
   estilo da Invariante da Origem da 8.9.
   → design Onda 2 §4.2.4 ("Guardas finais"); epic §5 item 2.8

3. **O teto de `paid_on` que a Story 8.12 pôs é REMOVIDO — e só ele.** A 8.12 limita `paid_on <= hoje`
   `[CORTE DO @PM]` exatamente para que nunca exista `paid` com data futura no banco antes deste
   estado existir. Esta story remove esse teto. **O piso continua**: `paid_on > opening_date` da conta
   escolhida, com o 422 que nomeia as duas saídas (mover a abertura da conta ou escolher outra conta).
   → design Onda 2 §4.2, §4.3; epic §5 item 2.7; epic §6 — Story 8.12 (*"o teto sai na 8.14"*)

4. **A guarda de data futura de `bank_transactions` passa a distinguir origem de sistema de origem
   externa.** `bank/service.py::_validate_posted_at:605-628` hoje recusa `posted_at > hoje` com 422,
   com a justificativa escrita *"extrato bancário é fato passado; data futura é erro de digitação"*.
   Essa justificativa vale para o que o usuário **transcreve** (`SOURCES_EXTERNA`), **não** para o que
   o e1p **originou** (`SOURCES_SISTEMA`): um débito agendado para 15/08 é verdade registrada, não
   digitação errada. Portanto:
   - `source ∈ SOURCES_EXTERNA` (`manual`, `ofx`, `csv`) → **continua recusando** data futura (422,
     mensagem inalterada);
   - `source ∈ SOURCES_SISTEMA` (o caminho de `sync_origin_movement`) → **aceita** data futura;
   - o **piso** (`posted_at > opening_date`) vale para os dois, sem exceção.

   ⚠️ **[AUTO-DECISION]** O design enuncia a mudança de forma da guarda (*"De: recuse `posted_at`
   futuro. **Para:** nenhuma superfície de saldo corrente inclui o futuro"* — §4.2) mas **não diz onde
   o corte por origem é feito**. Reason: manter a recusa para o lançamento manual preserva a proteção
   real que ela dá hoje (erro de ano na digitação) sem custo nenhum para o caminho novo; liberar para
   os dois transformaria um erro de digitação num movimento futuro que só apareceria semanas depois.
   Alternativa considerada e rejeitada: remover a guarda inteira, porque a segurança do saldo corrente
   agora vem da 8.10 (`until=None` = hoje) — rejeitada porque as duas guardas protegem coisas
   diferentes (uma protege o saldo, a outra protege o dado).
   → design Onda 2 §4.2; `bank/service.py:605-628` (docstring citada verbatim)

5. **`_window_sums` da Projeção passa a incluir as agendadas, pela DATA DO DÉBITO — e é aqui que a
   story se justifica.** `financial_intelligence/projection.py:337-380` hoje filtra
   `model.status == open_status` (= `'open'`) e agrupa por `due_date`. Passa a, **para `Payable`**:
   - somar `status IN ('open', 'scheduled')`;
   - para as `open`, continuar usando `due_date`;
   - para as `scheduled`, usar a **data do débito** (`paid_at::date`), não o `due_date` — o dinheiro
     sai no dia agendado, não no dia do vencimento;
   - manter a soma **cumulativa** por horizonte e a devolução da parcela vencida, sem mudar a
     assinatura pública nem o formato de retorno.

   ⚠️ **Sem este AC o dinheiro agendado SOME da Projeção**: a conta não é `open` (sai dos fluxos de
   saída) e o movimento é futuro (não entra no `saldo_inicial`, que usa
   `active_balance_total(until=today)` — `projection.py:329`). *"O saldo diz que você os tem, e nada
   diz que vão sair"* — a máquina de falso negativo da Onda 0 ressuscitada **na mesma superfície que a
   Onda 0 consertou**.
   → design Onda 2 §4.2.2; epic §7.1 (risco "Agendamento sumindo da Projeção"); epic §9.1 F11

6. **A agendada de HOJE não pode ser contada duas vezes — e sem este recorte ela seria.**
   `[SUPOSIÇÃO DO @SM — achado desta story, não está em nenhum documento]` No dia agendado, entre
   `00:00` e a varredura do worker (AC10), o `Payable` ainda está `scheduled` **e** o
   `bank_transaction` já tem `posted_at <= hoje`, logo **já entra** em
   `active_balance_total(db, until=today)` (`_movements_sums` usa `posted_at <= until`, inclusivo —
   `bank/service.py:255-306`). Somar a mesma conta também em `_window_sums` a subtrairia **duas
   vezes** da projeção.

   **Regra:** o recorte das agendadas em `_window_sums` é `status == 'scheduled' **AND**
   paid_at::date > today` — a data manda, não o status materializado, exatamente como
   `payment_queue` calcula seus baldes **na leitura** (*"nunca gravados … sem precisar de job/cron"*,
   `payables/service.py:361-363`). Teste dedicado: agendada com `paid_at = hoje`, worker **ainda não
   executado** → o valor aparece **uma única vez** na projeção (via `saldo_inicial`), e o
   `saldo_projetado` de cada janela é idêntico ao de um cenário em que a mesma conta já foi promovida.
   ⚠️ **Este AC é a diferença entre a story consertar o falso negativo e trocá-lo por um falso
   positivo do mesmo tamanho.**

7. **`payment_queue` ganha o balde "Agendadas" — a conta NÃO some da Fila.** `PaymentQueueOut` ganha
   `agendadas: list[PayableOut]` e `PaymentQueueSummary` ganha `agendadas_count` + `agendadas_cents`.
   As agendadas saem dos baldes de **vencimento** (atrasados/hoje/próximos 7/próximos 30), porque a
   pergunta da Fila é *"o que eu preciso pagar"* e uma agendada **já foi resolvida** — mas continuam
   visíveis num balde próprio, ordenadas pela **data do débito**. **Esconder é erro; misturar
   também.** O balde continua calculado **na leitura**.
   → design Onda 2 §4.2.3; epic §5 item 2.8

8. **`summary()` ganha `scheduled_cents`, e ele não se mistura com nada.** `PayablesSummary` ganha
   `scheduled_cents` = Σ das contas em `scheduled`. Ele fica **fora de `open_cents`** (agendada não é
   "a pagar") e **fora de `paid_month_cents`** (não saiu). `overdue_cents`, `week_cents` e
   `month_cents` **não mudam de definição**; `month_cents` já filtra `status != canceled` e portanto já
   inclui a agendada por `due_date` — comportamento **preservado de propósito** (é o total do mês por
   vencimento, não por caixa). Teste de snapshot dos 5 campos antigos antes/depois num cenário sem
   agendamento: idênticos.
   → design Onda 2 §4.2.3

9. **`reverse_payable` aceita `scheduled` — cancelar um agendamento é estornar.** `service.py:291`
   hoje exige `status == STATUS_PAID`. Passa a aceitar `paid` **e** `scheduled`: a conta volta para
   `open`, `paid_at = None`, o `bank_account_id`/`bank_transaction_id` são limpos, o movimento é
   **apagado** (mecânica da 8.12, com a guarda de linha puramente sintética) e o evento da Agenda
   volta para `scheduled` (pendente). **Nenhuma rota nova, nenhum verbo novo** — o significado de
   `reverse` já é *"esta saída não vai acontecer"*, e serve igualmente para uma saída que ainda não
   aconteceu. A conta reaparece na Fila, que é o comportamento certo: agendamento cancelado = a conta
   voltou a ser problema.
   → design Onda 2 §4.5

10. **A promoção `scheduled → paid` roda no worker que JÁ existe.** `app/worker.py::run_sweep` ganha
    **uma quarta etapa**, no mesmo formato das três atuais (sessão própria por tenant sob
    `tenant_session_factory`, `try/except` que loga e **não** trava o sweep dos demais, contador no
    dicionário de resultado — `scheduled_promoted`). A varredura promove `Payable` com
    `status == 'scheduled'` **e** `paid_at::date <= hoje` para `paid`, sem tocar em mais nada
    (`paid_at` **não** é re-datado, `bank_account_id`/`bank_transaction_id` intactos, evento da Agenda
    já está `done` desde a baixa) e grava `audit.record(action="payable.scheduled_promoted")`.
    **Idempotente:** rodar duas vezes seguidas promove zero na segunda.

    ⚠️ **O saldo derivado NÃO depende deste worker** e a story precisa provar isso: o movimento nasce
    com `posted_at` = data agendada e o saldo é **função da data** — ele entra sozinho quando o dia
    chega. O worker só move o `status`, para a Fila e o resumo pararem de mostrar como agendado.
    Teste: com o worker **desligado**, o saldo derivado do dia seguinte já contempla o movimento.
    → design Onda 2 §4.2.3 e F-D11; `app/worker.py` (estrutura das 3 etapas atuais)

11. **`receipts.list_candidates` inclui as agendadas.** `payables/receipts.py:117` hoje lista abertas
    (por `due_date`) + pagas recentes (por `paid_at` desc, janela de 30 dias). Passa a incluir as
    `scheduled` **junto das pagas recentes**, pela mesma janela e ordenação — *"o comprovante do
    agendamento existe e é o que o dono tem na mão"*. Canceladas continuam nunca aparecendo.
    → design Onda 2 §4.2.3 (recomendação explícita)

12. **A tela "Contas & Saldos" ganha o terceiro número: "Agendado para sair".**
    `apps/web/src/features/financeiro/contas.ts` + `ContasSaldosPage.tsx`:
    - um terceiro `ResumoSaldo` com `rotulo = AGENDADO_SAIDA_LABEL`, **omitido quando o valor é zero**
      (mesma disciplina do "Disponível como caixa", que some quando não há aplicação — ruído);
    - "Agendado para entrar" existe como par simétrico e nasce **igualmente omitido**, porque só passa
      a ter valor com a Story 8.15 (recebimento fora do trilho com `received_on` futuro);
    - a explicação acompanha o número (*"débitos já agendados, com data futura — não estão no Total em
      contas"*), no mesmo formato dos dois totais existentes.

    ⚠️ **O rótulo novo passa pelos MESMOS testes de colisão que o UX-001 instituiu**
    (`contas.test.ts:157-168`): não pode **ser** nem **conter** `ROTULO_BANCO` (`"no banco"`),
    `TOTAL_EM_CONTAS_LABEL` nem `DISPONIVEL_CAIXA_LABEL`. Estender o teste existente, não criar um
    paralelo.
    → design Onda 2 §4.2.1; `CLAUDE.md` §UX-001; `docs/stories/8.7.story.md`

13. **De onde vem o número do AC12 — e ele declara a procedência.** O backend expõe, por conta, a soma
    dos movimentos com `posted_at > hoje`:
    - `BankAccountOut` ganha `agendado_saida_cents` (Σ dos negativos futuros, **em módulo**),
      `agendado_entrada_cents` (Σ dos positivos futuros) e o irmão de procedência
      `agendado_origem: str = ORIGEM_BANCO`;
    - a soma reusa `_movements_sums` (a **única** implementação da soma de movimentos do repositório —
      `bank/service.py:255`), com o recorte de data invertido; **proibido** escrever uma segunda
      fórmula de soma;
    - `status != 'ignored'` continua aplicado **dentro** da soma, e quem consome **não refiltra**.

    ⚠️ **[AUTO-DECISION]** O design pede o número na tela e **não** especifica o transporte. Reason:
    pendurar no `BankAccountOut` mantém a tela com **uma** chamada (`GET /bank/accounts`, que a 8.7 já
    faz) e mantém o número **por conta**, que é a disciplina do épico inteiro (§3.2: consolidado nunca
    sem decomposição). Alternativa rejeitada: derivar na tela por diferença entre
    `balance?until=date.max` e o saldo de hoje — seria uma segunda fórmula de saldo na camada mais
    frágil, e a Regra 4 do `CLAUDE.md` manda o saldo ser derivado num lugar só.
    O irmão `*_origem` é a **Regra 3 do `CLAUDE.md`** e evita engordar a dívida G-1 do gate das Ondas
    0–1 (6 campos de saldo sem irmão, hoje).
    → `CLAUDE.md` regras 3 e 4; gate das Ondas 0–1, G-1

14. **Zero migration, zero coluna, zero impacto na DRE — e os três são verificados, não presumidos.**
    Nenhum arquivo em `apps/api/migrations/versions/` é criado ou editado por esta story.
    `dre.py` filtra `status != canceled` nas **4** agregações (`dre.py:124,290,437,634`), então o
    status novo passa direto: teste de **snapshot campo a campo** da DRE e da Lucratividade, com e sem
    conta agendada no período. `is_overdue` (`payables/service.py:44`) já exige `STATUS_OPEN` —
    agendada **não** é atrasada; teste explícito.
    → design Onda 2 §4.2.3; epic §10 (rastreabilidade)

### Integration Verification

- **IV1 — a Projeção não mente em nenhuma das três leituras.** Cenário único (saldo de abertura +
  1 conta agendada para D+15 + 1 conta aberta vencendo em D+20), verificado nas três superfícies ao
  mesmo tempo: (a) `saldo_inicial` **não** inclui o agendado; (b) `_window_sums` **inclui** o agendado
  na janela de 30 dias, pela data do débito; (c) `saldo_projetado_cents` de D+30 = saldo inicial −
  agendado − aberta. Mais o cenário do AC6 (agendada de hoje, worker não rodado) provando que o valor
  aparece **uma vez só**.

  ⚠️ **ASSERÇÃO OBRIGATÓRIA — `_saldo_inicial` chama `active_balance_total` com `until=today`.**
  **[ACRESCENTADO PELO @PO, 2026-07-30 — ratificação §C-7.3 / achado A-4]** O recorte do AC6 tira a
  agendada de `_window_sums` quando a data já chegou **confiando** que o movimento está no
  `saldo_inicial`. Isso só é verdade porque `projection.py:327-331` passa `until=today`. Trocado por
  `None` ou por `SEM_CORTE` (8.10 AC3), a agendada futura passa a contar **nos dois lugares** e a
  dupla contagem volta — agora pelo lado oposto, **em silêncio**, num arquivo que esta story declara
  não tocar (Task 3). O comentário que já existe ali (*"a MESMA âncora do resto da projeção"*) diz
  **por que** o argumento existe, mas **não diz o que quebra** — e o que quebra agora é outra coisa.
  **Teste explícito:** asserir que a chamada de `_saldo_inicial` a `active_balance_total` recebe
  `until` igual ao `today` da projeção (varredura de texto no estilo dos gates da Regra dos Planos,
  **ou** espião sobre `active_balance_total` capturando o kwarg — a segunda forma é mais forte).
  ⚠️ Esta asserção é o que torna o AC6 **auditável**, e não só correto hoje.
- **IV2 — as 9 superfícies em produção seguem intactas.** Snapshot antes/depois, campo a campo, sem
  conta agendada no cenário: **DRE**, **Lucratividade**, **Cockpit** (`monthly_costs`), **Fila de
  Pagamentos** (os 4 baldes antigos), **Carteira** (`wallet_summary` — esta story não toca `wallet`),
  **Projeção**, **Diagnóstico**, **Conferência** (`reconciliation_report`) e **Contas & Saldos**
  (`TOTAL_EM_CONTAS_LABEL` / `DISPONIVEL_CAIXA_LABEL` inalterados). ⚠️ **Isto mexe em código que está
  em produção** — a Fila, o resumo, a Projeção e a tela de contas são telas que o fundador usa hoje.
- **IV3 — a Regra dos Planos continua valendo.** `test_money_planes.py` passa **sem edição**:
  `wallet` não importa `bank`; `bank` não importa `payables`/`receivables` (asserção nova da 8.9 —
  esta story **não** a enfraquece: quem chama `sync_origin_movement` é `payables`, nunca o contrário);
  saldo bancário não lê `transactions` e vice-versa.
- **IV4 — o worker não regride.** `test_worker.py` passa sem edição; as três etapas atuais (tick de
  funil, fila de notificações, mídia do WhatsApp) mantêm contadores e isolamento de falha por tenant;
  um erro na etapa nova **não** impede as outras três nem os demais tenants.
- **IV5 — RLS e2e cross-tenant no Postgres real.** `pytest -m rls_e2e` verde, incluindo um caso novo:
  o sweep do worker promove **só** as agendadas do tenant correspondente (o tenant B não é tocado ao
  processar o tenant A), sob `tenant_session`, sem filtro manual de `tenant_id` (Regra de Ouro nº 1).
- **IV6 — `competence_date` não se move.** Alterar `paid_on` (inclusive cruzando a fronteira
  `paid ⇄ scheduled`) **não** altera `competence_date`, e portanto não altera DRE nem Lucratividade:
  `test_alterar_data_de_baixa_nao_altera_dre` (criado pela 8.12) é **estendido** para cobrir as duas
  transições, não duplicado.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false`, mesma decisão das Stories 5.7/5.8/6.1/6.2/7.5/8.1–8.8.
> Quality validation will use manual review process only (ver `quality_gate_tools` acima).
> ⚠️ `CLAUDE.md` registra que o CLI via WSL reporta 500+ arquivos alterados por descasamento CRLF/LF e que não há GitHub App no repo — não usar como evidência.

## Tasks / Subtasks

- [x] **Task 1 — O estado e a invariante (AC: 1, 2, 3)**
  - [x] `payables/models.py`: `STATUS_SCHEDULED = "scheduled"`, `ALL_STATUSES` +1, comentário dizendo
        que o valor cabe em `String(12)` e **por que isso importa** (evitar migration de tipo).
  - [x] `payables/service.py::apply_paid`: derivar o status a partir de `paid_on` (futuro ⇒
        `scheduled`; hoje/passado ⇒ `paid`), num helper **único**, usado **também** pela rota
        `PATCH .../payment` da 8.12 — duas cópias da regra derivada divergiriam.
  - [x] ⚠️ **[CORREÇÃO DO @PO, 2026-07-30] O helper é PÚBLICO e mora em local NEUTRO, não privado
        dentro de `payables/`.** Forma: `app/core/scheduling.py::status_por_data(data, today, *,
        status_agendado, status_pago) -> str`. Razão: a **Story 8.15 (outro @sm) o consome** para
        `Charge` (Task 1 de lá: *"Reusar o helper de derivação da 8.14, não reimplementar"*), e a v0.1
        desta story o declarava `_status_por_data`, **privado**, dentro de `payables/service.py` —
        e **nem o listava na tabela de Contrato público**. Um símbolo com `_` importado por outro
        módulo é exatamente a costura frouxa que produz duas cópias no primeiro ajuste; e fazer
        `receivables` importar `payables` só por causa dele é acoplamento gratuito entre dois módulos
        de negócio. **Registrar na tabela de Contrato público desta story.**
        ⚠️ Se o @dev preferir outro caminho de módulo, tudo bem — o que **não** pode é ficar privado
        nem ficar dentro de `payables/`.
  - [x] Remover o teto `paid_on <= hoje` que a 8.12 instalou. **Manter** o piso contra `opening_date`
        e a mensagem de 422 que nomeia as duas saídas.
  - [x] Confirmar que nenhum schema de entrada (`PayableCreate`, `PayableUpdate`, `PayablePayment`)
        aceita `status` — teste que tenta enviar `status="scheduled"` e verifica que é ignorado/422.
  - [x] Transição `scheduled → paid` em `apply_paid` (além da idempotência atual `if status == PAID:
        return`): dar baixa numa conta agendada com data de hoje muda o status e **não** duplica o
        movimento (o `sync_origin_movement` é upsert).

- [x] **Task 2 — A guarda de data por origem (AC: 4)**
  - [x] `bank/service.py::_validate_posted_at`: parametrizar **por `source`, e por `source` apenas**.
        O piso contra `opening_date` **não** muda.
  - [x] ⚠️ **[CORREÇÃO DO @PO, 2026-07-30] O booleano `permite_futuro` decidido pelo chamador está
        REJEITADO** pela @architect (ratificação §C-6.2). Razão: *"é o parâmetro que alguém passa
        `True` no caminho manual, um dia, por conveniência — e nenhum gate de AST o pega, porque não
        há import envolvido"*. O eixo **já existe** e é `source`; **toda regra desta onda é escrita
        contra `SOURCES_SISTEMA`/`SOURCES_EXTERNA`, nunca contra valor solto nem contra um eixo
        paralelo** (design §3.1, normativa). **Um eixo, uma pergunta.**
        **Membro** de `SOURCES_SISTEMA`: `transfer` → aceita futuro por esta guarda (mas a 8.18 o
        recusa em `create_transfer`, por outro motivo — ver a A-3 lá).
        **Não-membro:** `manual` → 422 com a mensagem inalterada.
  - [x] Atualizar a docstring dizendo **por que** as duas origens são tratadas diferente — a
        justificativa atual (*"extrato bancário é fato passado"*) descreve transcrição, não origem de
        sistema.
  - [x] Testes: manual com data futura → **422** (comportamento preservado); origem de sistema com
        data futura → **aceita**; qualquer origem com `posted_at <= opening_date` → 422.

- [x] **Task 3 — A Projeção (AC: 5, 6) — a mudança mais delicada da onda**
  - [x] `projection.py::_window_sums`: incluir `scheduled` **apenas para `Payable`**, com a data do
        débito. `Charge` continua só `open` nesta story (o lado de recebimento é da 8.15).
        **[AUTO-DECISION]** parametrizar com um argumento nomeado explícito (ex.:
        `scheduled_status: str | None = None` + a coluna de data correspondente) em vez de ramificar
        por `isinstance(model, Payable)` dentro da função genérica. Reason: a função é compartilhada
        entre entradas e saídas; um `if` por tipo dentro dela é o começo de duas funções fingindo ser
        uma, e a 8.15 precisa do mesmo mecanismo para `Charge`.
  - [x] Aplicar o recorte do AC6 (`paid_at::date > today`) **dentro** da query, não em Python — o
        padrão do arquivo é agregação-no-banco (`SUM(CASE ...)`), e carregar linha para filtrar
        depois quebraria a propriedade que a docstring declara.
  - [x] Atualizar a docstring de `_window_sums` com a nova população e **com o motivo do recorte de
        hoje** (a dupla contagem do AC6), para que ninguém "simplifique" o predicado depois.
  - [x] **Não tocar `_saldo_inicial`** (ver Dev Notes — conflito com a Story 8.19 em voo).

- [x] **Task 4 — Fila, resumo, estorno, bandeja (AC: 7, 8, 9, 11)**
  - [x] `payment_queue`: 5º balde + 2 campos no `PaymentQueueSummary`; agendadas fora dos baldes de
        vencimento; ordenação pela data do débito; cálculo **na leitura**.
  - [x] `summary()`: `scheduled_cents`; teste de snapshot dos 5 campos antigos.
  - [x] `reverse_payable`: aceitar `scheduled`; limpar `paid_at`, `bank_account_id`,
        `bank_transaction_id`; apagar o movimento (mecânica da 8.12); evento da Agenda → `scheduled`.
  - [x] `receipts.list_candidates`: incluir agendadas no grupo das "pagas recentes".
  - [x] `PayableOut` já expõe `status` — confirmar que o valor novo trafega sem tradução e que
        `is_overdue` continua `False` para agendadas.

- [x] **Task 5 — Worker (AC: 10)**
  - [x] `app/worker.py::run_sweep`: etapa 4, sessão própria, `try/except` isolando a falha por tenant,
        `result["scheduled_promoted"]`, log na linha de resumo.
  - [x] A varredura vive em `payables/service.py` (`promote_scheduled(db, *, tenant_id, actor,
        today=None) -> int`), não no worker — o worker orquestra, o módulo conhece a regra. `today`
        injetável, pelo mesmo motivo da 8.5 (contador preso ao relógio da máquina não é testável).
  - [x] Testes: promove só o que venceu; idempotente; **não** re-data `paid_at`; não toca
        `bank_transaction`; saldo derivado correto **com o worker desligado** (a prova do F-D11).

- [x] **Task 6 — Backend do número novo (AC: 13)**
  - [x] `bank/service.py`: soma dos movimentos futuros por conta, reusando `_movements_sums` com o
        recorte invertido (`posted_at > hoje`), separando entradas e saídas.
  - [x] `BankAccountOut`: `agendado_saida_cents`, `agendado_entrada_cents`, `agendado_origem`.
  - [x] Confirmar que `GET /bank/accounts` continua fazendo **uma** query de soma (não N+1) — é a
        razão de `derived_balances_as_of` existir.

- [x] **Task 7 — Frontend (AC: 12)**
  - [x] `contas.ts`: `AGENDADO_SAIDA_LABEL` / `AGENDADO_ENTRADA_LABEL`, entrada(s) extra(s) em
        `resumoSaldos`, omitidas quando zero.
  - [x] `contas.test.ts`: **estender** o teste de colisão do UX-001 (`:157-168`) para varrer os
        rótulos novos; testes de omissão quando zero e de exibição quando há valor.
  - [x] `ContasSaldosPage.tsx`: renderizar o(s) número(s) ao lado dos dois totais, com a explicação.
  - [x] `PagarPage.tsx`: exibir o status `scheduled` com rótulo próprio (*"Agendada"*) e a data do
        débito na linha; o botão **Estornar** passa a aparecer também em `scheduled` (rotulado
        *"Cancelar agendamento"*). ⚠️ A tabela já usa `overflow-x-auto` (corrigido no PR #58) — **não
        reintroduzir `overflow-hidden`**.
  - [x] Verificar em **~360px** que a coluna de status e o botão de cancelar agendamento ficam
        alcançáveis (a lição dos PRs #56/#58; G-4 do gate **bloqueia release**, não merge).

- [x] **Task 8 — Testes (AC: todas; IV1–IV6)**
  - [x] `apps/api/tests/test_payables_scheduled.py` (novo): a invariante do AC2 nas duas direções, o
        teto removido, o piso mantido, a transição `scheduled → paid`, o estorno de agendada.
  - [x] `test_financial_intelligence_projection.py` (estender): IV1 completo + o cenário de dupla
        contagem do AC6.
  - [x] `test_payment_queue.py` (estender): o 5º balde e a ausência das agendadas nos 4 antigos.
  - [x] `test_payables.py` (estender): `summary` com `scheduled_cents`; snapshot dos 5 campos antigos.
  - [x] `test_worker.py` (estender): etapa 4, idempotência, isolamento de falha.
  - [x] `test_bank_transactions.py` (estender): a guarda de data por origem (AC4).
  - [x] `test_financial_intelligence_dre.py` / `_profitability.py` (estender): snapshot campo a campo
        com e sem agendada (AC14).
  - [x] `rls_e2e`: promoção por tenant no Postgres real (IV5).
  - [x] `apps/web/.../contas.test.ts` e `PagarPage.test.tsx`.
  - [x] Rodar **as etapas separadas** (`ruff`, `pytest`, `tsc --noEmit`, `eslint --max-warnings 0`,
        `vitest run`) — **não** confiar no `scripts/check.sh` (mascara falha de frontend, epic §11.6
        X9). Baseline a bater: backend **1051 passed, 1 skipped**; `rls_e2e` **22**; frontend **304
        testes / 50 arquivos**.

## Dev Notes

### Contexto de escopo
- Item de escopo da Onda 2: *"**Estado `scheduled` em `payables`**, **derivado da data, nunca
  escolhido** … `payment_queue` (+balde "Agendadas"), `summary` (+`scheduled_cents`),
  `reverse_payable` (aceita `scheduled`), `apply_paid` (transição `scheduled→paid`),
  `receipts.list_candidates`, e **`projection._window_sums`: `status IN ('open','scheduled')` com a
  data do débito**"*. [Source: docs/prd/epic-8-controle-bancario.md#5 — Onda 2, itens 2.7 (teto), 2.8,
  2.9, 2.25]
- Delimitação da story e o alerta: *"É a story mais delicada da onda: sem `_window_sums`, o dinheiro
  agendado **some da Projeção**. *Não inclui:* agendamento de recebimento fora do trilho (herda a
  regra na 8.15); DRE (impacto zero, verificado)."*
  [Source: docs/prd/epic-8-controle-bancario.md#6 — Story 8.14]

### ⚠️ Por que `scheduled` e não `paid` com data futura — o argumento que decide, e ele é um bug
O fundador pediu data futura porque **agenda pagamento no app do banco** (F10, fala verbatim:
*"no futuro também permitir, pq posso estar agendando"*). A alternativa — `paid` com `paid_at`
futuro — foi **rejeitada por um bug verificado no código**, não por gosto:

| Superfície | Código | Com `paid`+futuro |
|---|---|---|
| Fluxos de saída da Projeção | `projection.py:370-373` filtra `model.status == 'open'` | a conta **não é `open`** → **sai dos fluxos de saída** |
| Saldo inicial da Projeção | `projection.py:329` usa `active_balance_total(until=today)` | o movimento é **futuro** → **não entra no saldo** |

> **Resultado: os R$ 5.000 agendados somem por completo da Projeção.** O saldo diz que você os tem, e
> nada diz que vão sair. É a **máquina de falso negativo da Onda 0 ressuscitada, na mesma tela que a
> Onda 0 consertou.**

E o argumento de modelagem que sobrevive ao bug: *"esta conta ainda vai sair da minha conta?"* com
`scheduled` é `status == 'scheduled'`; com `paid` é `status == 'paid' AND paid_at > today` — um
predicado que se lê **"pago no futuro"**, contradição em termos, que teria de ser replicado em
**cinco** lugares (`payment_queue`, `summary`, `_window_sums`, `list_candidates`, a regra do
Diagnóstico da 8.16). *"Cinco cópias de um predicado autocontraditório é a definição de o modelo estar
errado."*
[Source: docs/architecture/controle-bancario-onda2-design.md#4.2.2; epic §9.1 F11]

### ⚠️ O custo já foi MEDIDO pela @architect — verifique, não re-estime
| Item | Custo verificado | Onde conferir |
|---|---|---|
| `ALL_STATUSES` | +1 valor; coluna `String(12)`, `"scheduled"` tem 9 → **sem migration de tipo** | `payables/models.py:43` |
| DRE / Lucratividade | **Zero** — filtram `status != canceled` nas 4 agregações | `dre.py:124,290,437,634` |
| `is_overdue` | **Zero** — já exige `STATUS_OPEN` | `payables/service.py:44` |
| `payment_queue` | +1 balde, +2 campos; **não some da Fila** | `payables/service.py:357-414` |
| `summary()` | +1 campo, fora de `open_cents` e de `paid_month_cents` | `payables/service.py:310-340` |
| `_window_sums` | ~15 linhas; é a mudança que conserta o bug | `projection.py:337-380` |
| `reverse_payable` | passa a aceitar `scheduled` | `payables/service.py:285-302` |
| Promoção | worker **já existe**, +1 varredura de ~20 linhas | `app/worker.py::run_sweep` |
| **Saldo derivado** | **não precisa do worker** — `posted_at` é a data agendada e saldo é função da data | `bank/service.py:255-306`; precedente: `payables/service.py:361-363` |

Se algum desses números não bater com o repo no momento da implementação, **o repo vence** e a story
é corrigida com o motivo escrito (mesma disciplina que o `CLAUDE.md` §6.1 registra sobre a validação
de CPF/CNPJ: *"quando uma instrução se apoiar em premissa que você verificar ser falsa, siga o repo e
documente"*).
[Source: docs/architecture/controle-bancario-onda2-design.md#4.2.3]

### ⚠️ O achado desta story: a dupla contagem no dia D (AC6)
Nenhum documento da onda registra isto — é achado da redação da story e precisa de ratificação no
gate. `[SUPOSIÇÃO DO @SM]`

O design diz, corretamente, que *"o saldo derivado NÃO precisa do worker"* porque o movimento entra
sozinho quando o dia chega. E diz, também corretamente, que `_window_sums` deve somar
`status IN ('open','scheduled')`. **As duas afirmações juntas produzem, no dia agendado e antes da
varredura, uma subtração dupla:** o movimento já conta no `saldo_inicial` (`posted_at <= today`,
inclusivo) **e** o `Payable` ainda está `scheduled`, logo contaria de novo nos fluxos de saída.

A correção é barata e coerente com o próprio projeto: **a data manda, não o status materializado** —
o mesmo princípio que faz `payment_queue` calcular baldes na leitura *"sem precisar de job/cron"*. Por
isso o predicado do AC6 é `status == 'scheduled' AND paid_at::date > today`.

Consequência boa: com esse recorte, **a corretude da Projeção deixa de depender da frequência do
worker**. O worker vira o que o F-D11 diz que ele é — cosmética de status para a Fila e o resumo —, e
não um componente do qual a aritmética depende.

### ⚠️ `projection.py` — quem toca o quê, e a linha que não pode ser tocada por ninguém

> **[CORREÇÃO DO @PO, 2026-07-30] Esta seção afirmava que a Story 8.19 corrige
> `projection._saldo_inicial` e *"entra antes de todos"*. Não corrige, e não entra.** A v0.2 da 8.19
> teve a premissa refutada pelo fundador (*"o zero é consciente"* + *"é o saldo real hoje"*) e
> **removeu `projection.py` do escopo**: o AC6 dela diz *"A Projeção de Caixa NÃO é tocada. Nada."* e
> `projection.py` está na lista **NÃO TOCAR**. Consequência: **`ORIGEM_MISTO` pela mera existência de
> conta permanece — e está CORRETO**, porque a Projeção está certa. Não há conserto alheio a esperar,
> e a 8.19 **não é pré-requisito** de nada desta onda.

**Esta story é a única dona de `projection.py` na Onda 2 até a 8.15** (que liga o lado `Charge` do
mesmo `_window_sums`, `:337-380`). Regras que ficam:

- **Não tocar `_saldo_inicial`** (`:315-335`) — nem a decisão de origem, nem, **sobretudo**, o
  `until=today`. Ver a asserção obrigatória da **IV1** (ratificação §C-7.3): é esse argumento que
  impede a dupla contagem do dia D pela porta oposta, e o comentário que já existe no código não diz
  o que quebra.
- Fazer o commit **arquivo a arquivo**, nunca com `-a` (o precedente está nas Completion Notes da 8.6,
  quando 8.6 e 8.8 dividiram o mesmo working tree).
- A **8.15** entra depois e mexe na **mesma função** `_window_sums`: deixe a parametrização do AC6
  pronta para o segundo tipo (Task 3), senão ela vai ramificar por `isinstance`.

### Contrato consumido das Stories 8.9–8.13 (outro @sm, em voo) — confirmar antes de usar
| Consumido | De quem | O que esta story assume |
|---|---|---|
| `bank/origin.py::sync_origin_movement(db, *, tenant_id, actor, source, origin_id, bank_account_id, posted_at, amount_cents, description, …)` — idempotente, **não commita** | 8.9 | que ela aceita `posted_at` futuro quando `source ∈ SOURCES_SISTEMA` (ver AC4: **quem libera a guarda é esta story**, mas o ponto de chamada é dela) |
| `SOURCES_SISTEMA` / `SOURCES_EXTERNA` em `bank/models.py` | 8.9 | que existem como **tuplas** e que toda regra de origem é escrita contra elas, nunca contra valor solto |
| `payables.bank_account_id` (autoritativa) e `payables.bank_transaction_id` (cache de leitura) | 8.9 | que existem e que **quem manda é o `origin_id`** em caso de divergência |
| `until=None` significa **HOJE** em `derived_balance`/`derived_balances_as_of` | 8.10 | **pré-requisito duro**: sem ele o agendado entra em "Total em contas" e o AC12 fica sem sentido |
| `apply_paid(db, *, payable_id, tenant_id, actor, bank_account_id, paid_on=None)` com default `due_date` e **teto em hoje** | 8.12 | que o teto existe e que **esta story o remove** |
| `POST /payables/bills/{id}/reverse` **apagando** o movimento, com a guarda de linha puramente sintética (`fitid IS NULL AND import_batch_id IS NULL`) | 8.12 | que a mecânica existe e que esta story só **amplia a população aceita** |
| `PATCH /payables/bills/{id}/payment {bank_account_id?, paid_on?}` | 8.12 | que ela existe e que passa a poder **mover o estado** `paid ⇄ scheduled` |
| Seletor de conta + campo de data na `PagarPage`, dentro da barra fixa | 8.13 | que a superfície existe; esta story só acrescenta o rótulo de status e o "Cancelar agendamento" |

**Se qualquer assinatura divergir, o design é o contrato comum** (`controle-bancario-onda2-design.md`
§3.5, §4.1, §4.2, §4.4) — **não invente API para a camada da 8.9–8.13** e não a edite; ajuste **esta**
story e registre a divergência.

### Contrato público criado por esta story — o que 8.15 e 8.16 consomem
> **[ACRESCENTADO PELO @PO, 2026-07-30]** A v0.1 desta story tinha tabela de contrato **consumido**
> e nenhuma de contrato **criado** — e a 8.15 (outro @sm) declara consumir quatro símbolos daqui.
> Sem esta tabela, quem escreve a 8.15 adivinha nomes.

| Símbolo | Arquivo | Consumidores |
|---|---|---|
| `status_por_data(data, today, *, status_agendado, status_pago) -> str` — **público**, local **neutro** (`app/core/scheduling.py`), Task 1 | `app/core/` | **8.15** (para `Charge`, com `received_on`) — **importar, nunca copiar** |
| `payables.STATUS_SCHEDULED` + `ALL_STATUSES` (AC1) | `payables/models.py` | 8.15 (o espelho em `receivables`), **8.16** (a população do débito suspeito) |
| `payables/service.py::promote_scheduled(db, *, tenant_id, actor, today=None) -> int` (AC10) | `payables/service.py` | `app/worker.py` (etapa 4). ⚠️ A **8.15** precisa do **irmão** `receivables/service.py::promote_scheduled(...)` com a **mesma assinatura**, e a etapa 4 chama **os dois** — ver a correção do @po na 8.15 AC5 |
| Etapa 4 de `run_sweep` + contador `scheduled_promoted` (AC10) | `app/worker.py` | **8.15** — que **acrescenta a chamada de `Charge` à mesma etapa**, sem criar uma quinta |
| `_window_sums` parametrizado por `scheduled_status` + a coluna de data do agendado (Task 3) | `financial_intelligence/projection.py` | **8.15 AC6** — liga o **mesmo** parâmetro para `Charge`; **não** ramificar por `isinstance` |
| Recorte `status == 'scheduled' AND paid_at::date > today` (AC6) | idem | **8.15 AC6** — espelhado para o lado das entradas |
| `BankAccountOut.agendado_saida_cents` / `.agendado_entrada_cents` / `.agendado_origem` (AC13) | `bank/schemas.py` | **8.15** — que passa a **produzir valor** para "Agendado para entrar" |
| `AGENDADO_SAIDA_LABEL` / `AGENDADO_ENTRADA_LABEL` (AC12) | `apps/web/.../contas.ts` | 8.15, **8.18** (os dois estendem o teste de colisão do UX-001, não criam um paralelo) |
| `reverse_payable` aceitando `scheduled` (AC9) | `payables/service.py` | 8.16 (a população nunca vê agendamento cancelado) |

### Posição na ordem de merge (epic §6.1)
```
8.9 ──► 8.12 ──► 8.13 ──► [ 8.14 ] ──► 8.15 ──► 8.16
 │                          ▲
8.10 ─────────────────────┘
```
**Serial obrigatório: `8.10 + 8.12 → 8.14 → 8.16`.** A 8.14 não pode entrar antes da 8.10 (o
agendamento entraria no saldo corrente) nem antes da 8.12 (não haveria data de baixa a agendar).
⚠️ **[CORREÇÃO DO @PO]** A frase *"a Story 8.19 entra antes de todas"* está **revogada**: a v0.2 da
8.19 reduziu o escopo, tirou `projection.py` dele e a própria story declara *"a urgência caiu… não
bloqueia a Onda 2 e pode entrar em paralelo"*. **A 8.19 é independente desta cadeia.**
[Source: docs/prd/epic-8-controle-bancario.md#6.1; `docs/stories/8.19.story.md` v0.2]

### Restrições normativas que valem para esta story
- **Regra da Origem** (epic §4.8): o movimento nasce conciliado (`status='matched'`), carrega
  `origin_id`, é espelho do lançamento (corrigir **move**, estornar **apaga**) e **não é editável nem
  ignorável** pela tela de movimentos — só `user_description`. O ponto único de escrita é
  `sync_origin_movement`; esta story **não** abre um segundo caminho.
- **Invariante do Trilho** (epic §4.9): não é tocada aqui (é da 8.15), mas o `Payable` **nunca** cria
  `Transaction` nem `PlatformEarning` — `payables` não toca a Carteira por design
  (`payables/models.py:4`), e isso continua verdade com `scheduled`.
- **Caixa vs. competência nunca se invertem** (epic §4.3): `paid_on` move caixa, projeção e o
  `bank_transaction`; **não** move `competence_date`, DRE nem Lucratividade (IV6).
- **A Onda 2 não chama IA em lugar nenhum** (epic §4.4): nada nesta story fala com o Claude.
- **Nenhum filtro manual de `tenant_id`** (Regra de Ouro nº 1) — isolamento só por RLS, inclusive na
  varredura do worker, que roda dentro de `tenant_session`.

### Migrations — esta story não cria nenhuma, e isso é verificável
Head em produção: **0060** (`0060_bank_balance_checkpoints.py`, `down_revision="0059"`). As Stories
**8.9** e **8.18** acrescentam revisions; esta **não**. Antes de qualquer coisa, **confirme o head
programaticamente** (`alembic heads` deve devolver **um** head) — `down_revision` errado gera
`multiple heads` e derruba o job `rls_e2e` inteiro. Se esta story precisar de migration, isso é sinal
de que o AC1 foi violado (algum valor não coube em `String(12)`), e a divergência tem de ser escrita
antes de a migration ser escrita.

### File Locations (novos/alterados)
- **ALTERAR:** `apps/api/app/modules/payables/models.py` — `STATUS_SCHEDULED`, `ALL_STATUSES`.
- **NOVO:** `apps/api/app/core/scheduling.py` — `status_por_data` (**público**, neutro; consumido pela
  8.15 — correção do @po na Task 1).
- **ALTERAR:** `apps/api/app/modules/payables/service.py` — `apply_paid`, `reverse_payable`,
  `summary`, `payment_queue`, `promote_scheduled`.
- **ALTERAR:** `apps/api/app/modules/payables/schemas.py` — `PayablesSummary.scheduled_cents`,
  `PaymentQueueOut.agendadas`, `PaymentQueueSummary` +2 campos.
- **ALTERAR:** `apps/api/app/modules/payables/receipts.py` — `list_candidates`.
- **ALTERAR:** `apps/api/app/modules/bank/service.py` — `_validate_posted_at` por origem; soma dos
  futuros por conta.
- **ALTERAR:** `apps/api/app/modules/bank/schemas.py` — `BankAccountOut` +3 campos.
- **ALTERAR:** `apps/api/app/modules/financial_intelligence/projection.py` — **só** `_window_sums`.
- **ALTERAR:** `apps/api/app/worker.py` — etapa 4.
- **ALTERAR:** `apps/web/src/features/financeiro/contas.ts` + `contas.test.ts` +
  `ContasSaldosPage.tsx`.
- **ALTERAR:** `apps/web/src/features/pagar/PagarPage.tsx` + `PagarPage.test.tsx`.
- **NOVO:** `apps/api/tests/test_payables_scheduled.py`.
- **Sem migration.** ⚠️ `packages/shared-types/src/generated.ts` está defasado desde o PR #45 e sem
  check de drift no CI (epic §11.6 X8) — esta story **aumenta** a dívida; registrar, não consertar.

### Technical Constraints
- Dinheiro em centavos `BigInteger`; nada de `float` em soma de dinheiro.
- `posted_at` é `DATE`, nunca `TIMESTAMP` — comparações por **data de calendário**, jamais por
  horário local (a lição de fuso registrada no `CLAUDE.md` §6.0).
- `paid_at` é `DateTime(timezone=True)`; a derivação do estado usa `paid_at.date()` contra `hoje` em
  **UTC**, a mesma âncora de `payables.summary` (`datetime.now(UTC).date()`) e de
  `projection.cash_projection` — **não** introduzir um segundo conceito de "hoje" nesta story.
- `today` injetável em toda função nova que dependa da data (padrão das 8.5/8.6).
- Agregação **no banco** (`SUM(CASE ...)`) na projeção; nada de carregar linhas para filtrar em
  Python.
- Nenhuma escrita nova fora de `payables` e do movimento gerado por `sync_origin_movement`.

### Testing
- Os testes mais valiosos, nesta ordem: **(1)** o cenário de dupla contagem do AC6 (é o que separa
  consertar o falso negativo de criar um falso positivo); **(2)** IV1 (o agendado presente na
  projeção e ausente do saldo inicial, no mesmo cenário); **(3)** a invariante do AC2 nas duas
  direções; **(4)** o saldo derivado correto **com o worker desligado** (a prova do F-D11).
- Bordas a fixar por teste: `paid_on == hoje` → `paid` (não `scheduled`); `paid_on == hoje + 1` →
  `scheduled`; `paid_on == opening_date` → 422; `paid_on == opening_date + 1` → aceito.
- `rls_e2e` com testcontainers + Postgres real (o SQLite dos testes unitários **não** exerce RLS).
- Rodar as etapas **separadamente**; `scripts/check.sh` não serve como evidência (epic §11.6 X9).

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-30 | 0.1 | Story criada a partir do Epic 8 §5 (itens 2.7/2.8/2.9/2.25) e do design da Onda 2 §4.2–§4.2.4. Achado próprio registrado no AC6 (dupla contagem no dia agendado antes da varredura do worker), marcado `[SUPOSIÇÃO DO @SM]` para ratificação no gate. | River (@sm) |
| 2026-07-30 | 0.2 | **Validação do @po — nenhum bloqueio; 5 correções.** **AC6 e AC4 estão RATIFICADOS** pela @architect (ratificação §C-7 e §C-6) — deixam de ser `[SUPOSIÇÃO DO @SM]`; o AC13 idem, sem reserva. (1) **Task 2: o booleano `permite_futuro` está REJEITADO** (§C-6.2) — o corte é por `source`, e só. (2) **IV1 ganha a asserção obrigatória de que `_saldo_inicial` usa `until=today`** (§C-7.3 / A-4): sem ela o AC6 é correto hoje e não é auditável amanhã. (3) **Task 1: o helper de derivação vira PÚBLICO e neutro** (`app/core/scheduling.py::status_por_data`) — a 8.15 o consome e a v0.1 o declarava privado dentro de `payables/`, sem registrá-lo em contrato nenhum. (4) **Tabela de Contrato público criada** — a story tinha só a de contrato consumido, e a 8.15 declara consumir 4 símbolos daqui; inclui o irmão `receivables::promote_scheduled` que a etapa 4 precisa chamar. (5) A coordenação com a 8.19 estava sobre premissa **revogada**: a v0.2 dela não toca `projection.py` e não entra antes de ninguém. | Pax (@po) |
| 2026-08-04 | 1.0 | **Implementada (@dev / Dex).** Backend 1195 → **1275**, `rls_e2e` 30 → **32**, frontend 391 → **404**; **zero migrations**. Gate de mutação: **7 mutantes aplicados, 7 mortos** — o M6 (`_movements_sums` com `>=` no corte `since`) **sobreviveu a 58 testes verdes** e revelou uma **terceira porta** para a dupla contagem, entre `saldo_derivado_cents` e `agendado_saida_cents`, que nenhum documento da onda registra (ver Completion Notes). Seis guard tests plantados pelas 8.10/8.12/8.13 foram **invertidos** (expectativa reescrita, teste preservado). Um teste próprio do AC6 passou **pelo motivo errado** na 1ª versão (agendar "para hoje" não produz agendada — a borda do AC2 é estrita) e foi reescrito antes do commit. | Dex (@dev) |

## Dev Agent Record

**Agent Model Used:** Opus 5 (claude-opus-5[1m]) — @dev (Dex), modo autônomo (YOLO)

### Completion Notes

**Baseline medida no início da sessão (não herdada da story).** A story declarava `backend 1051 / rls_e2e 22 / frontend 304 em 50 arquivos`; o repo real em 2026-08-04, no HEAD `a4924b4`, estava em **backend 1195 passed + 1 skipped**, **rls_e2e 30**, **frontend 391 em 52 arquivos**. Os números da story envelheceram com as stories 8.9–8.13/8.17 mergeadas depois da redação — o repo vence, como o próprio §"O custo já foi MEDIDO" manda.

| | Antes | Depois | Δ |
|---|---|---|---|
| backend (`pytest -m "not rls_e2e"`) | 1195 passed, 1 skipped | **1275 passed, 1 skipped** | +80 |
| `rls_e2e` (Postgres real, testcontainers) | 30 | **32** | +2 |
| frontend (`vitest run`) | 391 / 52 arquivos | **404 / 52 arquivos** | +13 |
| migrations | head 0061 | head 0061 | **0** |

#### O que foi entregue, por AC

- **AC1** `STATUS_SCHEDULED` em `payables/models.py`, `ALL_STATUSES` de 3 → 4 valores. **Sem migration:** o teste `test_scheduled_cabe_na_coluna_e_por_isso_nao_ha_migration` afere `len(valor) <= Payable.__table__.c.status.type.length` contra a **coluna real**, não contra o literal `12` — quem "melhorar" o vocabulário para uma string maior é reprovado antes de gravar truncado.
- **AC2** Estado derivado da data em `app/core/scheduling.py::status_por_data` (público, neutro). Usado por `apply_paid` **e** `update_payment`. Invariante testada nas duas direções, e por varredura sobre **todas** as linhas do cenário (estilo Invariante da Origem da 8.9), não sobre uma. Guarda estrutural (AST) prova que `payables` **importa** o helper em vez de reimplementar — e que o usa em ≥ 2 pontos.
- **AC3** Teto de `paid_on` removido de `_valida_data_de_baixa`; piso mantido, com a mensagem que nomeia as duas saídas (asserida por texto, não por status — a lição de mutação da 8.12).
- **AC4** `_validate_posted_at(…, *, source)`. Corte por `source` e só por `source`; **sem** booleano `permite_futuro`.
- **AC5/AC6** `_window_sums` parametrizado por `scheduled_status` + `scheduled_at`, agregação no banco, com o recorte `paid_at::date > today`.
- **AC7/AC8** 5º balde + `agendadas_count`/`agendadas_cents`; `scheduled_cents` no summary.
- **AC9** `reverse_payable` aceita `scheduled`, sem rota nova.
- **AC10** `promote_scheduled` + etapa 4 de `run_sweep`, com isolamento de falha por tenant.
- **AC11** `receipts.list_candidates` inclui agendadas.
- **AC12/AC13** `AGENDADO_SAIDA_LABEL`/`AGENDADO_ENTRADA_LABEL`, omitidos quando zero; `BankAccountOut` +3 campos.
- **AC14** DRE e Lucratividade com snapshot campo a campo, com e sem agendada. `is_overdue` inalterado.

#### ⚠️ O achado desta implementação: uma TERCEIRA porta para a dupla contagem, encontrada por mutação

O AC6 fecha a dobra entre `saldo_inicial` e `_window_sums` (Projeção). A ratificação §C-7.3 fecha a porta oposta (`active_balance_total(until=today)`). **Existe uma terceira**, que nenhum documento da onda registra: entre `saldo_derivado_cents` e `agendado_saida_cents`, na tela "Contas & Saldos".

O mutante **M6** (`_movements_sums`: `posted_at > since` → `posted_at >= since`) **sobreviveu a 58 testes verdes**. Com ele, o débito de **hoje** conta nos dois recortes ao mesmo tempo: dentro do saldo corrente (`until` é inclusivo) **e** dentro do "Agendado para sair". O dono veria o mesmo dinheiro descontado do saldo *e* anunciado como "ainda vai sair".

Nenhum teste pegava porque todos os cenários usavam data futura — **a borda era o único lugar onde os dois recortes podem se sobrepor, e era exatamente o lugar sem caso**. Corrigido com `test_A_BORDA_do_agendado_o_movimento_de_HOJE_conta_no_SALDO_e_NAO_no_agendado`, que fixa a invariante como **partição**: `(…, hoje]` e `(hoje, …)` cobrem o histórico inteiro, sem sobreposição e sem buraco.

**Regra que fica:** todo par de recortes complementares sobre a mesma coluna precisa de um caso **na borda**, não só de um caso em cada lado. Um teste em cada lado passa com os dois operadores.

#### Gate de mutação (Epic 8 — "pelo menos 3 mutantes aplicados e mortos"): 7 aplicados, **7 mortos**

| Mutante | Veredito | Quem matou |
|---|---|---|
| M1 `_window_sums` perde o recorte `paid_at > hoje` | MORTO | `test_AC6_a_agendada_no_DIA_DO_DEBITO_nao_conta_duas_vezes` |
| M2 `status_por_data` troca `>` por `>=` | MORTO | `test_status_por_data_nas_duas_direcoes[0-paid]` |
| M3 `overdue_col` perde o filtro de status | MORTO | `test_agendada_nao_entra_em_overdue_outflow` |
| M4 guarda de data vira fail-**open** p/ `source` desconhecido | MORTO | `test_AC4_source_desconhecido_e_422_e_nao_ganha_a_isencao` |
| M5 `promote_scheduled` não promove o débito de hoje | MORTO | `test_promote_scheduled_promove_so_o_que_venceu` |
| M6 `_movements_sums` usa `>=` no corte `since` | **SOBREVIVEU → MORTO** | teste novo (ver o achado acima) |
| M7 `apply_paid` volta a cravar `paid` | MORTO | `test_payables_IMPORTA_o_helper_em_vez_de_reimplementar` (AST) |
| M8 `payment_queue` mistura agendada nos baldes de vencimento | MORTO | `test_a_agendada_SAI_dos_quatro_baldes_de_vencimento` |

⚠️ O mutante M7 foi morto pelo teste **estrutural**, não por um de comportamento: reimplementar a derivação inline passaria em todos os testes de comportamento, e a 8.15 nasceria com a segunda cópia da regra. É o caso "um mutante que teste comportamental nenhum mata é um teste do **tipo errado**, não um teste faltando".

#### Guard tests invertidos (o padrão da casa: reescrever a expectativa, nunca apagar o teste)

Seis testes plantados por stories anteriores **nomeavam a 8.14** e mudaram de expectativa. Todos ganharam um bloco `⚠️ [8.14] MUDOU DE EXPECTATIVA, e a mudança é a CORREÇÃO` explicando que o teto era **faseamento**, não regra de produto:

1. `test_payables_bank_origin.py::test_data_futura_e_422_e_nao_truncada` → `..._agora_grava_scheduled_e_nao_e_mais_422`. **O que sobrevive da asserção original**: a data **não** é truncada em silêncio para hoje.
2. `test_payables_bank_origin.py::test_vencimento_futuro_sem_data_informada_tambem_bate_no_teto` → `..._agora_agenda_em_vez_de_recusar`.
3. `baixa.test.ts` — o teto virou `toBeUndefined()`; a frase de `avisoDeDataFutura` mudou porque a antiga (*"pagamento agendado ainda não é acompanhado pelo e1p"*) **virou mentira**.
4. `PagarPage.test.tsx` — `max` do campo de data agora `toBeNull()`.
5. `ComprovantePage.test.tsx` — idem. **Herdou a mudança sem a tela ser editada** (as três telas compartilham `baixa.ts`) — o retorno prometido por ter uma implementação só.
6. `ContasSaldosPage.test.tsx::AC9` — a asserção de ausência **continua valendo**, agora por outro motivo: o terceiro número existe e é **omitido quando zero**. A afirmação ficou mais forte que a original.

Dois guards **não** foram invertidos, de propósito: `test_estados_candidatos_existem_no_vocabulario` (8.17) passou a valer com `scheduled` dentro de `_ESTADOS_CANDIDATOS`, que é a dívida que aquela story previu pagar "com uma entrada numa tupla"; e `test_corrigir_pagamento_de_conta_aberta_e_409` continua 409 (a ampliação foi só para `scheduled`).

### [AUTO-DECISIONS]

1. **`AGENDA_STATUS_PENDENTE` / `AGENDA_STATUS_DONE` — alias obrigatório em `payables/service.py`.** `agenda.models.STATUS_SCHEDULED` já existia no arquivo e colide com o novo `payables.models.STATUS_SCHEDULED`. **As duas palavras apontam para lados opostos no mesmo instante**: o `scheduled` da Agenda é *"evento ainda pendente"*; o de `payables` é *"débito já tem dia marcado"* — que deixa o evento da Agenda **`done`**. Reason: um import nu faria a colisão passar como sombreamento silencioso, e o sintoma seria o evento voltando a "pendente" numa baixa bem-sucedida. O símbolo público de `payables` mantém o nome que o contrato da 8.15 exige; quem ganhou prefixo foi o import da Agenda.
2. **`agendado_sums` devolve `AgendadoDaConta` (dataclass), não `tuple[int,int]`.** Reason: `(500_00, 0)` posicional é ilegível no ponto de uso e não diz qual é a saída. Mesmo precedente de `_SaldoInicial` em `projection.py`.
3. **Duas queries constantes em `agendado_sums`** (saídas e entradas), em vez de um `SUM(CASE)` único. Reason: o `CASE` único exigiria duplicar o `WHERE` de `_movements_sums` — exatamente a segunda fórmula que o AC13 proíbe. Duas agregações para a lista inteira continuam O(1) em número de contas; **não é N+1**.
4. **`tetoDaDataDeBaixa` foi mantida, devolvendo `undefined`,** em vez de a função e a chamada sumirem. Reason: é o lugar onde a decisão está escrita e é a ela que se volta se o teto precisar retornar. Um `max` apagado do JSX não deixa rastro nenhum; `undefined` também é o que faz o React **omitir** o atributo (com `""` ele seria renderizado vazio).
5. **`_validate_posted_at` recebe `source=tx.source` em `update_transaction`,** não `SOURCE_MANUAL` fixo. Reason: se a porta de edição um dia se abrir para origem de sistema, as duas guardas continuam coerentes em vez de um `manual` hard-coded decidindo sobre uma linha que não é manual.
6. **A partição de `source` é escrita nas DUAS metades** (`in SOURCES_SISTEMA` … `not in SOURCES_EXTERNA` → 422). Reason: `not in SOURCES_SISTEMA` sozinho faria valor novo herdar o teto sem ninguém decidir; `in SOURCES_EXTERNA` sozinho o faria herdar a **isenção** — o lado caro. O mutante M4 exercita exatamente isso.
7. **`FilaPagamentosPage` ganhou o 5º balde,** apesar de a Task 7 não a listar. Reason: o AC7 diz *"a conta NÃO some da Fila"*, e sem renderizar `agendadas` ela sumiria **da Fila que o fundador usa**, não só do payload. `onPay` virou opcional — o balde não oferece baixa (a conta já foi resolvida), e a ausência do botão é a informação.
8. **`packages/shared-types` foi editado** (`PayableStatus` +`scheduled`, `PayablesSummary.scheduled_cents?`, `PaymentQueue.agendadas?`), apesar de não estar nas File Locations. Reason: `PayableStatus` é uma união fechada — sem o valor novo, `p.status === "scheduled"` **não compila**. Os campos novos entraram **opcionais** para que a tela tolere um backend anterior à 8.14 (há teste para isso).

### Desvios declarados

- **`test_payables.py` não foi estendido.** A Task 8 pedia `summary` + snapshot dos 5 campos antigos ali; eles vivem em `test_payables_scheduled.py` (`test_summary_ganha_scheduled_cents_sem_contaminar_os_cinco_antigos` e `test_snapshot_dos_cinco_campos_antigos_sem_agendamento_nenhum`), junto do resto do estado novo. Reason: coesão — o cenário exige conta bancária + baixa com data futura, que é o fixture daquele arquivo e não do `test_payables.py`. O teste existente `test_summary_open_and_overdue` continua passando **sem edição**, que é a prova de não-regressão que a Task queria.
- **Os casos `rls_e2e` foram acrescentados a `test_bank_rls.py`,** não a arquivo novo — seguindo a instrução escrita naquele arquivo pela 8.3 (*"acrescente casos AQUI: cada boot de testcontainer custa minutos de CI"*), mesmo precedente da 8.5.
- **`_seed_account` (test_bank_rls.py) ganhou o parâmetro `opening_date`,** com o **mesmo default de antes** (`date(2026, 7, 1)`). Reason: o estado `scheduled` é derivado do relógio **real**, então os casos novos precisam de datas ancoradas em "hoje"; uma abertura fixa em julho/2026 barraria a baixa pelo piso assim que o calendário passasse dela. Nenhum chamador existente muda de comportamento.
- **⚠️ Um teste meu passou pelo motivo errado e foi corrigido antes do commit.** A 1ª versão de `test_AC6_a_agendada_DE_HOJE_nao_conta_duas_vezes` agendava "para hoje" — o que **não produz uma agendada** (a borda do AC2 é estrita: `paid_on == hoje ⇒ paid`). Não havia `scheduled` nenhum no banco e nada poderia ser contado duas vezes: o teste ficava verde sem exercitar o AC. Reescrito para agendar **para amanhã** e pedir a projeção **de amanhã** (`today` injetável), com a pré-condição (`status == "scheduled"`) asserida antes de o número ser medido. Renomeado para `..._no_DIA_DO_DEBITO_...`.

### Achados registrados e NÃO consertados (fora de escopo)

1. **`packages/shared-types/src/generated.ts` continua defasado** (zero menções a `bank`, sem check de drift no CI — epic §11.6 X8). Esta story **aumenta** a dívida: os campos novos entraram no `index.ts` mantido à mão. Registrado, não consertado, como a story manda.
2. **`chart_of_accounts.create_account` com `audit.record(target='')`** (MNT-001, 17 call sites). Não tocado.
3. **`scripts/check.sh` mascara falha de frontend com `|| true`** — não foi usado como evidência; todas as etapas rodaram separadas (epic §11.6 X9).
4. **Recomendação para a 8.15 (não implementada aqui):** o `promote_scheduled` de `receivables` deve entrar **na etapa 4 existente**, nunca numa quinta — é a mesma pergunta ("já chegou o dia?") sobre os dois lados do dinheiro. E o `_window_sums` já está parametrizado: basta ligar `scheduled_status` + a coluna de caixa de `Charge`; **não ramifique por `isinstance`**.
5. **Recomendação para quem endurecer a tela de movimentos:** o movimento de origem de sistema com `posted_at` futuro agora existe em produção. A Regra da Origem (d) diz que ele **não é editável nem ignorável** pela tela de movimentos, e hoje isso é garantido só pelo contrato — não há guarda no `update_transaction`/`ignore_transaction` que recuse `source ∈ SOURCES_SISTEMA`. Não é desta story (o design não a pede), mas passou a ter consequência maior: ignorar um débito agendado tira do saldo um dinheiro que **vai sair mesmo**.

### Validação manual pendente (bloqueia release, não merge)

- **~360px** na tela "Contas & Saldos" com o terceiro número presente (o `TotaisCard` usa `flex-wrap`, mas o aceite visual em aparelho real não foi feito — mesma pendência G-4 do gate das Ondas 0–1).
- **Fluxo ponta a ponta:** agendar para D+15 e conferir que o valor **não** entra em "Total em contas", **não** some da Projeção e aparece em "Agendado para sair".

### File List

**NOVO**
- `apps/api/app/core/scheduling.py` — `status_por_data` (público, neutro; consumido pela 8.15)
- `apps/api/tests/test_payables_scheduled.py`

**ALTERADO — backend**
- `apps/api/app/modules/payables/models.py` — `STATUS_SCHEDULED`, `ALL_STATUSES`
- `apps/api/app/modules/payables/service.py` — `apply_paid`, `update_payment`, `reverse_payable`, `summary`, `payment_queue`, `promote_scheduled`, `_valida_data_de_baixa`, `_ESTADOS_CANDIDATOS`, alias dos status da Agenda
- `apps/api/app/modules/payables/schemas.py` — `PayablesSummary.scheduled_cents`, `PaymentQueueOut.agendadas`, `PaymentQueueSummary` +2
- `apps/api/app/modules/payables/receipts.py` — `list_candidates`, `_ESTADOS_LIQUIDADOS`
- `apps/api/app/modules/bank/service.py` — `_validate_posted_at` por `source`, `_movements_sums` (+`since`/`sign`), `agendado_sums`, `AgendadoDaConta`
- `apps/api/app/modules/bank/schemas.py` — `BankAccountOut` +3 campos
- `apps/api/app/modules/bank/router.py` — `_out` com agendado, `_agendado_de`, 5 rotas de conta
- `apps/api/app/modules/financial_intelligence/projection.py` — **só** `_window_sums` (+ o helper `_meia_noite`) e a chamada dela para `Payable`
- `apps/api/app/worker.py` — etapa 4 + `scheduled_promoted`

**ALTERADO — testes backend**
- `apps/api/tests/test_payables_bank_origin.py` (2 guards invertidos)
- `apps/api/tests/test_financial_intelligence_projection.py` (IV1, AC6, espião do `until=today`)
- `apps/api/tests/test_payment_queue.py`, `test_worker.py`, `test_bank_transactions.py`
- `apps/api/tests/test_financial_intelligence_dre.py`, `test_financial_intelligence_profitability.py`
- `apps/api/tests/test_bank_rls.py` (IV5 + `_seed_account` com `opening_date`)

**ALTERADO — frontend**
- `apps/web/src/features/financeiro/contas.ts` + `contas.test.ts`
- `apps/web/src/features/financeiro/ContasSaldosPage.test.tsx`
- `apps/web/src/features/financeiro/FilaPagamentosPage.tsx` + `.test.tsx`
- `apps/web/src/features/pagar/baixa.ts` + `baixa.test.ts`
- `apps/web/src/features/pagar/EscolhaDaBaixa.tsx`
- `apps/web/src/features/pagar/PagarPage.tsx` + `PagarPage.test.tsx`
- `apps/web/src/features/pagar/ComprovantePage.test.tsx`
- `packages/shared-types/src/index.ts`

**SEM MIGRATION** — `alembic heads` permanece em **0061**, um head só. Nenhum arquivo em `apps/api/migrations/versions/` criado ou editado (verificado por teste: `test_a_story_nao_cria_migration`).

## QA Results
_(vazio — a preencher pelo @qa/@architect no gate)_

---

## Story Draft Checklist — Resultado (River, modo autônomo)

| Categoria | Status | Observações |
|---|---|---|
| 1. Goal & Context Clarity | PASS | O "por quê" é um bug verificado em duas linhas de código (`projection.py:370-373` e `:329`), citadas com número; a fala do fundador que originou o requisito está verbatim. |
| 2. Technical Implementation Guidance | PASS | Cada ponto de alteração tem arquivo e faixa de linha lidos no repo, não presumidos; a parametrização de `_window_sums` está decidida com alternativa rejeitada nomeada. |
| 3. Reference Effectiveness | PASS | design Onda 2 §4.2–§4.2.4, epic §5/§6.1/§7.1/§9.1, `CLAUDE.md` regras 3/4 e UX-001, mais o código real. |
| 4. Self-Containment Assessment | PASS | O contrato consumido das 8.9–8.13 está tabelado com instrução explícita de não inventar API; a coordenação com a 8.19 no mesmo arquivo está escrita. |
| 5. Testing Guidance | PASS | Quatro testes nomeados como os mais valiosos, bordas de data fixadas, baseline numérica da suíte declarada. |
| 6. CodeRabbit Integration (conditional) | N/A | Disabled — skip notice presente. |

**Final Assessment: READY** (clareza 9/10).

**Pontos para o @architect no gate:**
1. **Ratificar o AC6** (recorte `paid_at::date > today` nas agendadas de `_window_sums`). É achado do
   @sm e **não está em nenhum documento da onda**; sem ele, o dia agendado subtrai duas vezes.
2. **Ratificar o AC4** (a guarda de data futura passa a distinguir `SOURCES_SISTEMA` de
   `SOURCES_EXTERNA`). O design muda a *forma* da guarda mas não diz onde fica o corte.
3. **Ratificar o AC13** (o transporte do "Agendado para sair" via `BankAccountOut`, com irmão de
   procedência) — o design pede o número na tela e não especifica a API.

**Risco residual (não bloqueante):** o balde "Agendadas" e o campo `scheduled_cents` mudam payloads
que a tela de Fila de Pagamentos e o resumo de Contas a Pagar já consomem **em produção**; o
frontend precisa tolerar o campo novo antes do deploy do backend, ou os dois vão juntos.

# Story 8.13: As telas da baixa — seletor de conta e data, do desktop ao 360px

## Status
InReview — **AC9 (aceite manual em ~360px) NÃO verificado, bloqueante confirmado pelo founder (F-D8). Ver Dev Agent Record.**

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["pnpm --filter @e1p/web test (PagarPage.test.tsx, ComprovantePage.test.tsx, FilaPagamentosPage — novo)", "tsc --noEmit", "eslint --max-warnings 0", "ruff + pytest apps/api/tests (receipts_router/receipts_schemas)", "pytest -m rls_e2e (test_receipts_rls.py)", "⚠️ ACEITE MANUAL EM ~360px — BLOQUEIA A STORY (G-4, F-D8)", "os 3 agentes de QA (regression-tester, bug-hunter, dedup-checker — CLAUDE.md §5)"]
```
> [AUTO-DECISION] Story majoritariamente de **frontend de superfície crítica**, com uma fatia fina de backend (os dois schemas da bandeja) → `executor="@dev"`. `quality_gate="@architect"` mantendo o padrão da onda **e** porque o critério que reprova esta story não é de código: é **físico** — se o seletor de conta puder ficar fora da área visível em ~360px, a story falha, independentemente de a suíte estar verde. Dois PRs de fix de campo já foram pagos exatamente por isso (#56 e #58).

## Story
**As a** dono que dá baixa numa conta a pagar — no computador, ou pelo celular com o comprovante recém-compartilhado do app do banco,
**I want** escolher **de qual conta o dinheiro saiu** e **em que dia**, com a conta principal já pré-selecionada e **visível junto do botão que comete a ação**,
**so that** eu não confirme uma baixa sem ver o que estou confirmando — como já aconteceu **duas vezes** neste produto, uma delas com uma conta real marcada paga sem eu conseguir ver o checkbox.

> ⚠️ **O aceite manual em ~360px BLOQUEIA esta story.** Não é recomendação: é o veredito F-D8 do
> fundador sobre o achado G-4, e esta é a story que o epic marca como bloqueante.

## Acceptance Criteria

1. **`PagarPage`: a baixa passa a pedir conta e data, e o 409 tem saída.**
   O botão *"Marcar paga"* (`PagarPage.tsx:221`) deixa de chamar `POST /payables/bills/{id}/pay` sem
   corpo (`PagarPage.tsx:118`) e passa por um passo de confirmação com **dois campos**:
   - **conta bancária** — `<select>` com as contas **ativas**, **conta primária pré-selecionada**;
   - **data do pagamento** — default `due_date` da conta (**não** hoje, **não** `now()`), sempre
     visível e editável.
   Ambos vão no corpo `PayablePayIn {bank_account_id, paid_on}` (Story 8.12).
   → design Onda 2 §4.1, §4.2; epic item 2.14, *"superfície de 2.6/2.7"*

2. **O 409 acionável vira cadastro embutido, e volta para o pagamento.**
   Recebido `409 {"acao": "cadastrar_conta", ...}` (contrato da 8.12), a UI **não** mostra um erro
   cru: abre o cadastro de conta **ali mesmo** e, ao salvar, **retoma a baixa** com a conta recém
   criada já selecionada. *"Um passo a mais, uma vez na vida do tenant."*
   Reusar o `AccountModal` de `ContasSaldosPage` **ou** um formulário mínimo equivalente é decisão de
   implementação **[SUPOSIÇÃO DO @SM]**; o que é contrato é o **fluxo**: erro → cadastro → volta ao
   pagamento, sem o usuário perder de vista qual conta ele estava pagando.
   ⚠️ Se a Story **8.11** já estiver em produção (e ela merge antes de tudo), o cadastro embutido
   herda o **aviso pró-ativo** e a exigência de saldo no recuo de data — **não** reimplemente nem
   contorne nenhum dos dois.
   → design Onda 2 §4.1; epic item 2.6; F-D2

3. **A bandeja de comprovantes ganha `bank_account_id` e `paid_on` — e é o afluente principal.**
   `ReceiptLinkIn` e `ReceiptNewBillIn` ganham:
   - `bank_account_id: str | None = None` — **obrigatório quando `mark_paid=True`** (422 se ausente
     nesse caso; ignorado quando `mark_paid=False`);
   - `paid_on: date | None = None`.
   `link_receipt` e `new_bill_from_receipt` repassam para `_attach_and_commit` → `apply_paid`.
   **Esta story remove o `TODO(8.13)` e a `[SUPOSIÇÃO DO @SM]` de "usa a conta primária" que a 8.12
   deixou no código** — a conta passa a vir do payload, sempre.
   → design Onda 2 §4.6; epic item 2.14; REQ-12

4. **⚠️ O seletor fica DENTRO da mesma barra fixa do botão. Esta é a linha que reprova a story.**
   Em `ComprovantePage`, o seletor de conta e o campo de data vão para **dentro** do
   `<div className="fixed inset-x-0 bottom-0 ...">` (`ComprovantePage.tsx:266`) — o mesmo bloco onde o
   PR #58 já moveu o checkbox *"Marcar como paga"* e o resumo da conta escolhida, com o comentário
   que está no código:
   > *"Fica aqui dentro, colado no botão que comete a ação: fisicamente não dá pra tocar em Anexar sem
   > ver o que está sendo confirmado."*
   **É proibido** pôr o seletor em qualquer bloco que exija rolagem para ser visto. **Teste
   automatizado** verificando que o seletor está no mesmo container do botão (query por ancestral
   comum), além do aceite manual do AC9.
   → design Onda 2 §4.6; `CLAUDE.md` (PRs #56, #58); epic §7.1

5. **A conta primária vem pré-selecionada e visível NO PRÓPRIO BOTÃO, com troca a um toque.**
   O rótulo do botão de ação da bandeja passa a dizer para onde o dinheiro está indo — por exemplo
   *"Anexar e dar baixa · sai do Itaú PJ"*. O texto exato é decisão de implementação; o que é
   contrato é que **o nome da conta escolhida seja legível sem interação adicional**.
   **Isto não é o "opcional com default" que o fundador recusou:** o campo é **obrigatório** e é
   **sempre gravado**; o que o pré-preenchimento evita é **construir**, não **confirmar** (teto de
   simplicidade, design-mãe §0).
   ⚠️ **Sem conta primária definida** (estado válido — é onde o tenant fica ao arquivar a primária),
   nada é pré-selecionado e o botão fica **desabilitado** até haver escolha. Silêncio, nunca um
   palpite.
   → design Onda 2 §4.6; epic item 2.14

6. **`FilaPagamentosPage` também paga — e também tem de mudar.**
   `FilaPagamentosPage.tsx:60` chama `POST /payables/bills/{id}/pay` **sem corpo**, com o comentário
   *"Reusa o mesmo endpoint de Contas a Pagar (mark_paid, lock FOR UPDATE) — sem fluxo novo."* A
   partir da 8.12 esse endpoint **exige corpo**, e a tela quebra.
   ⚠️ **[ACHADO DO @SM, 2026-07-30]** — esta superfície **não é nomeada em lugar nenhum** do epic
   (§5 item 2.6, §6 story 8.13) nem do design (§4.1, §4.6), que listam apenas `PagarPage` e a
   bandeja. Ela entra nesta story porque a **Integration Verification obrigatória da onda** exige que
   a **Fila de Pagamentos** siga intacta — e "intacta" não é compatível com "quebrada".
   **O fluxo é o mesmo do AC1** (seletor + data + 409 acionável); a implementação **reusa o
   componente**, não o duplica (`dedup-checker`).
   → **[ACHADO DO @SM]**; epic §6.1 IV; `FilaPagamentosPage.tsx:60`

7. **Data de pagamento: default `due_date`, teto em hoje, piso na `opening_date`.**
   A UI espelha as guardas da 8.12 e **não** as reimplementa:
   - default = `due_date` da conta (decisão do fundador F10);
   - **teto em hoje nesta story** — o campo `type="date"` recebe `max={hoje}`. ⚠️ **O teto sai na
     Story 8.14**, junto com `scheduled`; **deixe o comentário** dizendo isso, senão a 8.14 gasta
     tempo entendendo por que o `max` está ali;
   - o **422 do piso** (`paid_on <= opening_date` da conta) é exibido **com a mensagem do backend**,
     que já nomeia as duas saídas (*"mova a abertura desta conta para antes de DD/MM e informe o saldo
     daquele dia, ou escolha outra conta"*). **Não reescrever a mensagem no frontend.**
   → design Onda 2 §4.2, §4.3; epic itens 2.7, 2.12

8. **Nenhum backend novo, e nenhum rótulo de saldo novo.**
   O comportamento veio todo na 8.12. Esta story toca **apenas** `receipts_schemas.py`,
   `receipts_router.py` e `receipts.py` (repasse dos dois campos) — **zero** mudança em
   `payables/service.py`, **zero** migration.
   E **não** cria *"Agendado para sair"* (é a **8.14**), portanto **não** introduz string sujeita aos
   testes de colisão de rótulo do UX-001 em Contas & Saldos. Verificar explicitamente que
   `contas.test.ts:157-168` segue verde sem edição.
   → epic §6, delimitação da 8.13

9. **⚠️ ACEITE MANUAL EM ~360px — BLOQUEIA A STORY.**
   Em aparelho real (ou emulação fiel) a ~360px de largura, verificar e **registrar com evidência** no
   Dev Agent Record:
   - **(a)** o seletor de conta, o campo de data e o botão de ação estão **simultaneamente visíveis**,
     sem rolagem, na barra fixa da `ComprovantePage`;
   - **(b)** o nome da conta escolhida é legível (truncagem não pode comer o nome inteiro);
   - **(c)** a confirmação de baixa da `PagarPage` cabe na tela e seus botões são alcançáveis;
   - **(d)** a tabela de `PagarPage` continua com `overflow-x-auto` (**não** `overflow-hidden`) —
     a regressão do PR #58, que escondia a coluna Status e os botões Editar/Marcar paga/**Estornar**;
   - **(e)** nada é coberto pela sidebar: abaixo de `md` ela nasce fechada e abre sobreposta (fix do
     PR #56), e `/comprovante/:id` roda em `ProtectedBareLayout`, **sem** sidebar/topbar.
   G-4 está **aberto desde a Onda 1** e o fundador decidiu que ele **bloqueia release** (F-D8); o epic
   marca **esta** story como aquela em que o aceite **bloqueia o merge**.
   → epic §9.2 F-D8/G-4, §7.1; design Onda 2 §4.6; `CLAUDE.md` (PRs #56, #58)

10. **Isolamento da bandeja preservado, e a dívida herdada não piora.**
    `get_staged` continua exigindo `owner_id == user_id`, e o isolamento cross-tenant do `link`
    continua coberto por `test_receipts_rls.py` (`pytest.mark.rls_e2e`, testcontainers, Postgres
    real). ⚠️ A dívida conhecida — as rotas **genéricas** de `/attachments` não conhecem o convênio da
    bandeja, então um tenant-mate alcança o comprovante em staging de um colega — **é herdada e não é
    resolvida aqui**; esta story **não pode agravá-la** (nenhum campo novo é exposto pelas rotas
    genéricas).
    → `CLAUDE.md` §"Anexos: comprovante pelo share sheet"; epic §7

### Integration Verification

- **IV1 — A bandeja de comprovantes continua funcionando ponta a ponta, e é a verificação mais cara
  desta story.** O share sheet do Android e o Atalho do iOS **estão em produção e uma conta real já
  passou por eles**. Verificar: upload → bandeja → escolher candidata → escolher conta → *Anexar* →
  anexo vinculado **e** baixa dada **num commit só** **e** movimento bancário gerado.
  `test_receipts.py` e `test_receipts_rls.py` verdes (adaptados, **não** apagados) — incluindo o teste
  que a suíte chama de *"guarda de regressão do refactor `apply_paid`/`mark_paid`"*
  (`test_receipts.py:253`).
  ⚠️ **O Atalho do iOS é publicado manualmente, uma vez só** (limitação de plataforma). Ele só
  deposita arquivo na bandeja (`POST /payables/receipts`) e **não** dá baixa — logo **não** é afetado
  pelo campo novo. Confirmar isso por leitura antes de assumir.
- **IV2 — Fila de Pagamentos INTACTA.** É a superfície do AC6. Backend não é tocado (os baldes seguem
  calculados **na leitura**, por `due_date`, sem job/cron). A tela passa a mandar conta e data;
  os números da fila e do `PaymentQueueSummary` são **idênticos** antes e depois. Teste de tela novo
  (hoje `FilaPagamentosPage` não tem arquivo de teste próprio).
- **IV3 — Contas & Saldos intacta.** `ContasSaldosPage` **não muda de comportamento** (o `AccountModal`
  pode ser reusado pelo AC2, mas sem alteração de contrato). Os dois recortes rotulados
  (`TOTAL_EM_CONTAS_LABEL`, `DISPONIVEL_CAIXA_LABEL`) e os testes de **colisão de rótulo** do UX-001
  (`contas.test.ts:157-168`) **verdes sem edição** — nenhum rótulo é criado (AC8).
- **IV4 — Conferência e Diagnóstico intactos.** `reconciliation.py` e `engine.py` **não são tocados**.
  A divergência muda porque o razão ficou mais completo — efeito **da 8.12**, não desta story.
- **IV5 — DRE, Lucratividade e Projeção intactas.** Esta story não altera nenhum caminho de escrita de
  `payables`; `competence_date` continua intocada pela data de baixa (o snapshot está na 8.12).
  ⚠️ **[CORREÇÃO DO @PO, 2026-07-30] A dependência da 8.19 escrita aqui está DESATUALIZADA.** A v0.2
  da 8.19 removeu `projection.py` do escopo (AC6 + lista **NÃO TOCAR**): **ela não corrige
  `_saldo_inicial` e não é pré-requisito desta story.** Esta story continua **não tocando
  `projection.py`**.
- **IV6 — Carteira intacta.** `payables` nunca toca a Carteira e continua não tocando. Nenhum arquivo
  de `app/modules/wallet/`.
- **IV7 — Agenda intacta.** `apply_paid` já move o evento para `done` e `reverse_payable` o devolve;
  comportamento inalterado.
- **IV8 — Rotas públicas e deslogado.** `ProtectedLayout` guarda a rota de origem
  (`state={{ from: location }}`) e `LoginRoute` a retoma — sem isso a chave do comprovante, que **só
  existe na URL**, é destruída e o arquivo fica perdido no IndexedDB. **Não regredir.** Teste do
  fluxo deslogado → login → volta para `/comprovante/:id` com a escolha preservada.
- **IV9 — Service worker e cache.** `public/sw.js` **não faz cache de nada** de propósito (só
  intercepta o POST do share target) e o `nginx.conf` tem `location = /sw.js` com `no-cache`. **Não
  introduzir cache** — é a classe de bug "deploy novo, app velho em cache", e esta story muda o corpo
  de uma requisição.
- **IV10 — Zero regressão.** Baseline: frontend **304 / 50**; backend **1051 passed, 1 skipped**;
  `rls_e2e` **22**. Ao final: os mesmos + os novos. **Sem migration.**

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Este projeto (e1p, brownfield sem `.aiox-core` próprio) não tem `coderabbit_integration.enabled` configurado localmente. Tratado como `false`, mesma decisão já documentada nas Stories 5.7, 6.1, 6.2, 7.5 e 8.2–8.8.
> Quality validation will use manual review process only (ver `quality_gate_tools` acima e `scripts/check.sh`).
> To enable, set `coderabbit_integration.enabled: true` em um `core-config.yaml` próprio do projeto.

## Tasks / Subtasks

- [x] **Task 1 — Confirmar o contrato da 8.12 antes de escrever uma linha (AC: 1, 2, 3, 7)**
  - [x] Ler `payables/schemas.py`: `PayablePayIn`, `PayablePaymentUpdate`, `PayableOut`
  - [x] Confirmar o **formato exato** do 409 (`{"acao": "cadastrar_conta", ...}`) — o AC2 depende dele
  - [x] Confirmar as mensagens de 422 (piso `opening_date`, teto hoje) para **exibi-las**, não
        reescrevê-las
  - [x] Localizar o `TODO(8.13)` que a 8.12 deixou em `receipts.py` — esta story o remove
  - [x] **Se a 8.12 não estiver mergeada, PARE.** Serial obrigatório: `8.9 → 8.12 → 8.13`

- [x] **Task 2 — Backend fino: os dois schemas da bandeja (AC: 3)**
  - [x] `ReceiptLinkIn` e `ReceiptNewBillIn` ganham `bank_account_id` e `paid_on`
  - [x] Validação **condicional**: obrigatório quando `mark_paid=True`; **ignorado** quando `False`
        (anexar sem dar baixa continua sendo um caso legítimo e não pode exigir conta)
  - [x] `receipts_router.py` repassa; `receipts.py` remove o default de conta primária e o `TODO`
  - [x] Testes de backend dos 4 casos (`mark_paid` × conta presente/ausente)

- [x] **Task 3 — Um componente de escolha, reusado por três telas (AC: 1, 5, 6)**
  - [x] Criar **um** componente de seleção de conta + data (ex.:
        `apps/web/src/features/pagar/EscolhaDaBaixa.tsx` **[SUPOSIÇÃO DO @SM: o nome]**), consumido
        por `PagarPage`, `ComprovantePage` e `FilaPagamentosPage`
  - [x] Buscar contas ativas via `GET /bank/accounts`; pré-selecionar `is_primary`
  - [x] Sem primária → nada pré-selecionado, ação **desabilitada**
  - [x] ⚠️ **Não duplicar** a lógica em três lugares — o `dedup-checker` reprova, e três cópias
        divergem no primeiro ajuste

- [x] **Task 4 — `PagarPage` (AC: 1, 2, 7)**
  - [x] Substituir a chamada direta de `pay(id)` (`:118`) pelo passo de confirmação
  - [x] Corpo `PayablePayIn`; default `paid_on = due_date`; `max = hoje` no input
  - [x] Tratar o 409 → cadastro embutido → **retomar a baixa** com a conta nova selecionada
  - [x] Exibir a mensagem do 422 **como veio do backend**
  - [x] ⚠️ Conferir que a tabela segue com `overflow-x-auto` (regressão do PR #58)

- [x] **Task 5 — `ComprovantePage`: o seletor DENTRO da barra fixa (AC: 4, 5)**
  - [x] Mover/inserir o seletor e a data **dentro** do `fixed inset-x-0 bottom-0` (`:266`), ao lado do
        checkbox e do resumo que o PR #58 já pôs ali
  - [x] Rótulo do botão exibindo a conta escolhida
  - [x] Preservar o comportamento existente: conta **já paga** não mostra o checkbox de baixa — e,
        nesse caso, o seletor de conta **também não é exigido**
  - [x] Teste automatizado de **co-localização** (seletor e botão no mesmo container)

- [x] **Task 6 — `FilaPagamentosPage` (AC: 6)**
  - [x] Reusar o componente da Task 3 no `pay()` (`:60`)
  - [x] Criar `FilaPagamentosPage.test.tsx` (não existe hoje): baixa com conta e data; 409 acionável;
        os números da fila inalterados
  - [x] Atualizar o comentário *"sem fluxo novo"*, que deixou de ser verdade

- [x] **Task 7 — Testes de frontend (AC: 1–8)**
  - [x] `PagarPage.test.tsx`: corpo enviado; default `due_date`; `max` hoje; 409 → cadastro → retoma
  - [x] `ComprovantePage.test.tsx`: seletor na barra fixa; primária pré-selecionada; sem primária →
        desabilitado; `mark_paid=false` **não** exige conta
  - [x] `FilaPagamentosPage.test.tsx`: **novo**
  - [x] `contas.test.ts` verde **sem edição** (colisão de rótulo)

- [ ] **Task 8 — ⚠️ Aceite manual em ~360px (AC: 9) — BLOQUEANTE**
  - [ ] Os 5 itens (a)–(e) do AC9, em aparelho real ou emulação fiel
  - [ ] **Registrar evidência** no Dev Agent Record (captura ou descrição verificável)
  - [ ] ⚠️ **Se qualquer item falhar, a story não fecha.** Não é "dívida para depois": o fundador já
        decidiu (F-D8), e a terceira rodada de fix de campo seria imperdoável

- [x] **Task 9 — Fechamento (AC: todos)**
  - [x] `pnpm --filter @e1p/web test`, `tsc --noEmit`, `eslint --max-warnings 0` — **individualmente**
        (X9: `scripts/check.sh` mascara falha de frontend com `|| true`)
  - [x] `pytest apps/api/tests` + `pytest -m rls_e2e`
  - [x] Os 3 agentes de QA
  - [x] File List e Dev Agent Record (com a evidência do 360px)

## Dev Notes

### Por que esta story é separada da 8.12

O epic é explícito quanto ao critério de corte (§6, (iv)):

> **frontend de superfície crítica em ~360px vira story própria, porque dois PRs de fix de campo já
> foram pagos** por elemento fora da área visível (PRs #56 e #58).

E na ordem de merge (§6.1, ordem 5): *"Superfície da 8.12. Separada porque o aceite em 360px é
bloqueante e merece atenção isolada."*

### A história que esta story existe para não repetir

Está escrita no `CLAUDE.md`, em duas entradas consecutivas, e as duas foram **achados de campo em
aparelho real**, não de revisão de código:

**PR #56** — *"`AppShell` nunca teve breakpoint responsivo nenhum"*: sidebar de 256px fixos espremia
**qualquer** tela num aparelho de ~360px. Nesta feature isso deixou o checkbox *"marcar como paga"*
fora da área visível **e uma conta real foi marcada paga sem o usuário conseguir ver/desmarcar**.
E a nota que ficou: *"Só o shell + as 2 telas do comprovante foram auditados — **nenhuma outra tela do
app foi verificada** quanto ao mesmo padrão."*

**PR #58** — segunda rodada, mesmo incidente: mesmo com o shell responsivo, o checkbox vivia num bloco
**separado** do botão Anexar; quem selecionava a conta e tocava Anexar sem rolar **nunca via o
checkbox**, e a baixa saía com o padrão (marcado) sem confirmação visível. No mesmo incidente: a
tabela de `PagarPage` usava `overflow-hidden` em vez de `overflow-x-auto`, e em tela estreita a coluna
Status e os botões Editar/Marcar paga/**Estornar** ficavam invisíveis — *"sem jeito de conferir ou
desfazer uma baixa incorreta"*.

O design da Onda 2 fecha o argumento (§4.6):

> *"O `CLAUDE.md` registra duas rodadas de fix de campo por isso; **a terceira seria imperdoável**."*

**Esta story acrescenta um seletor de conta na barra fixa do celular.** É exatamente a classe de
elemento que já falhou duas vezes.

### A bandeja de comprovantes vira o afluente principal — de graça

`link_receipt(mark_paid=True)` **já chama** `apply_paid`. Com a 8.12, `apply_paid` gera o movimento —
então o **share sheet do Android** e o **Atalho do iOS** (*"a captura mais barata do produto inteiro,
fisicamente no instante do pagamento"*) passam a alimentar o razão bancário **sem uma linha de tela
nova**.

Duas consequências registradas no epic e no design:
1. é o argumento mais forte para a conta ser obrigatória **em `apply_paid`** e não numa tela nova:
   *"é o único lugar por onde todos os caminhos de baixa já passam"*;
2. **fecha o conflito C3 em favor do REQ-12** e **revoga `POST /bank/accounts/{id}/imports` como porta
   primária**. Se a importação (Onda 4) for liberada, o arquivo entra pela infraestrutura de
   anexo/bandeja já existente (`owner_type='bank_import'`), com a conta escolhida **depois** do
   upload. A rota dedicada sobrevive como caminho de desktop, **não como o caminho**.

### Pré-selecionar não é tornar opcional — a distinção que o fundador cobrou

**F7:** *"opcional significa que alguém pula, e a conferência volta a medir o que você esqueceu de
preencher."*

O design responde à objeção óbvia antes que ela apareça (§4.6):

> *"Isso não é o 'opcional com default' que o fundador recusou: o campo é **obrigatório** e é **sempre
> gravado**; o que o pré-preenchimento evita é **construir**, não **confirmar**."*

A consequência prática de projeto: **a conta pré-selecionada tem de estar visível**. Um default
invisível seria, na prática, um campo opcional pulado — e cairia exatamente na crítica do fundador.
É por isso que o AC5 exige a conta **no próprio botão**, não só dentro do `<select>`.

### A terceira superfície, que nenhum documento da onda nomeou

**[ACHADO DO @SM, 2026-07-30]** — o design §4.1 lista os chamadores de `apply_paid`; o epic item 2.6 e
a delimitação da 8.13 (§6) nomeiam `PagarPage` e a bandeja. Nenhum dos três menciona:

```
apps/web/src/features/financeiro/FilaPagamentosPage.tsx:60
  // Reusa o mesmo endpoint de Contas a Pagar (mark_paid, lock FOR UPDATE) — sem fluxo novo.
  await api.post(`/payables/bills/${id}/pay`);
```

A partir da 8.12, esse endpoint **exige corpo**. A tela quebra com 422. E a **Fila de Pagamentos** é
uma das superfícies que a Integration Verification obrigatória desta onda manda manter **intactas**.

**Por isso ela entra aqui, no AC6** — não por ampliação de escopo, mas porque a alternativa é entregar
a onda com uma tela do financeiro quebrada. O epic §6 permite reagrupar *"desde que nenhum item da §5
caia fora"*; nenhum item cai fora, e um consumidor esquecido passa a ser coberto.

### O teto de hoje que sai na 8.14

O campo de data recebe `max={hoje}` **nesta story**, espelhando o teto que a 8.12 pôs no backend. É
**`[CORTE DO @PM]`**, não o design: o design §4.2 diz *"sem teto superior"*, e o teto existe para
garantir que **nunca exista `paid` com data futura no banco** enquanto `scheduled` não existir — porque
separar os dois depois seria **migration com backfill sob `FORCE RLS`**, a armadilha da 0046.

**Deixe o comentário no código.** A 8.14 remove o `max` no mesmo commit em que `scheduled` passa a
existir, e o estado passa a ser **derivado da data**: futuro ⇒ `scheduled`; hoje ou passado ⇒ `paid`.

### O que esta story NÃO faz

| Não faz | Onde está |
|---|---|
| Backend de baixa (conta obrigatória, data, estorno, `PATCH .../payment`) | **8.12** |
| *"Agendado para sair"* como terceiro número em Contas & Saldos | **8.14** |
| Estado `scheduled` e a remoção do teto | **8.14** |
| Curadoria do formulário manual (`operation_nature`) e a guarda de contagem dupla | **8.17** |
| UI de recebimento fora do trilho | **8.15** |
| Endurecer `/attachments` (a dívida do isolamento por usuário da bandeja) | fora do épico — **herdada, não agravada** |

### Posição na ordem de merge (epic §6.1)

```
8.11 ──► 8.9 ──► 8.10 ──► 8.12 ──► 8.13 ──► 8.14
```

**5ª a mergear, e a última das minhas cinco.** **Serial obrigatório: `8.9 → 8.12 → 8.13`.**
⚠️ **8.12 e 8.13 formam um par de release**: soltar a 8.12 sem esta deixa `PagarPage` e
`FilaPagamentosPage` quebradas. A 8.14 vem depois e **remove o teto** que esta story aplica no `max`
do input.

### Contrato público criado/alterado — o que 8.14–8.18 consomem

| Símbolo | Arquivo | Consumidores |
|---|---|---|
| `ReceiptLinkIn {bill_id, mark_paid, bank_account_id?, paid_on?}` | `payables/receipts_schemas.py` | **8.14** (`receipts.list_candidates` passa a incluir agendadas; o `paid_on` futuro vira `scheduled`) |
| `ReceiptNewBillIn {…, bank_account_id?, paid_on?}` | idem | 8.14 |
| `link_receipt(..., bank_account_id, paid_on)` / `new_bill_from_receipt(...)` | `payables/receipts.py` | 8.14 |
| Componente de escolha de conta + data (Task 3) | `apps/web/src/features/pagar/` | **8.14** (remove o `max`), **8.15** (a UI de recebimento fora do trilho pede conta + data com a **mesma** disciplina de barra fixa) |
| Fluxo "409 → cadastro embutido → retoma" | `PagarPage`, `FilaPagamentosPage`, `ComprovantePage` | **8.15** — o recebimento fora do trilho tem o mesmo problema (tenant sem conta) e deve reusar o fluxo, não inventar outro |

### File Locations (novos/alterados)

| Arquivo | O quê |
|---|---|
| `apps/api/app/modules/payables/receipts_schemas.py` | +2 campos nos 2 schemas |
| `apps/api/app/modules/payables/receipts_router.py` | repasse |
| `apps/api/app/modules/payables/receipts.py` | remove o default de primária e o `TODO(8.13)` |
| `apps/web/src/features/pagar/EscolhaDaBaixa.tsx` | **novo** — o componente único |
| `apps/web/src/features/pagar/PagarPage.tsx` | confirmação da baixa + 409 acionável |
| `apps/web/src/features/pagar/ComprovantePage.tsx` | seletor **dentro** da barra fixa + rótulo do botão |
| `apps/web/src/features/financeiro/FilaPagamentosPage.tsx` | reusa o componente |
| `apps/web/src/features/pagar/PagarPage.test.tsx`, `ComprovantePage.test.tsx` | casos novos |
| `apps/web/src/features/financeiro/FilaPagamentosPage.test.tsx` | **novo** |
| `apps/api/tests/test_receipts.py` | adaptação (**não** apagar o teste `:253`) |

**Sem migration.** `payables/service.py`, `bank/`, `reconciliation.py`, `projection.py`, `engine.py` e
`wallet/` **NÃO são tocados**.

### Technical Constraints

- **Um componente, três telas.** Duplicar a escolha de conta reprova no `dedup-checker` e diverge no
  primeiro ajuste.
- **O seletor mora no mesmo container do botão** — não é estilo, é o AC4.
- Data em `type="date"`, valor `YYYY-MM-DD`, **comparação por data de calendário**, sem fuso
  (`CLAUDE.md` §6.0).
- Mensagens de erro do backend são **exibidas**, não reescritas — elas nomeiam as saídas reais.
- `public/sw.js` **não faz cache**; `nginx.conf` tem `location = /sw.js` com `no-cache`. **Não
  introduzir cache.**
- `ProtectedBareLayout` (sem sidebar/topbar) para `/comprovante/:id`; sidebar sobreposta abaixo de
  `md`. **Não regredir.**
- `eslint --max-warnings 0` e `tsc --noEmit` limpos.

### Testing

- **Frontend (vitest)**: corpo enviado nas três telas; default `due_date`; `max` hoje; primária
  pré-selecionada; sem primária → desabilitado; 409 → cadastro → retoma; `mark_paid=false` não exige
  conta; **co-localização** do seletor com o botão (query por ancestral comum); fluxo deslogado →
  login → volta para `/comprovante/:id`.
- **Backend**: os 4 casos de validação condicional dos schemas da bandeja; `test_receipts.py`
  adaptado; `test_receipts_rls.py` (`rls_e2e`) verde.
- **Regressão de rótulo**: `contas.test.ts:157-168` verde **sem edição**.
- **Manual, BLOQUEANTE**: os 5 itens do AC9 em ~360px, com evidência no Dev Agent Record.
- **Mutação**: pelo menos 2 mutantes mortos — mover o seletor para fora da barra fixa (o teste de
  co-localização tem de cair); remover a pré-seleção da primária.
- Rodar `pnpm test`, `tsc` e `eslint` **individualmente** (X9).

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-30 | 0.1 | Story criada a partir do Epic 8 §5 (item 2.14 + superfície de 2.6/2.7) e §6, Onda 2 — River (@sm). Inclui o **[ACHADO DO @SM]** do AC6: `FilaPagamentosPage.tsx:60` é uma **terceira** superfície que chama `POST /bills/{id}/pay` e não é nomeada em nenhum documento da onda — sem ela, a Fila quebra e a Integration Verification obrigatória da onda falha | River (@sm) |
| 2026-07-30 | 0.2 | **Validação do @po — nenhum bloqueio; 1 correção de premissa.** A dependência da Story 8.19 na IV5 estava desatualizada (a v0.2 dela removeu `projection.py` do escopo). O AC6 (a terceira superfície) é **ratificado pelo @po**: o epic §6 permite reagrupar *"desde que nenhum item da §5 caia fora"*, nenhum cai, e a alternativa era entregar a onda com uma tela do financeiro quebrada. O AC9 (aceite em ~360px) permanece **bloqueante de merge** — é o veredito F-D8 do fundador, não recomendação. | Pax (@po) |

## Dev Agent Record

### Agent Model Used
Claude Opus 5 (implementação original), Claude Sonnet 5 (recuperação e fechamento da documentação após interrupção de sessão).

### Debug Log References

O @dev original (Opus 5) caiu no meio da Task 8 (mutação de teste) por limite semanal de uso da conta — não por falha da story. Ao revertê-la, um `git checkout` involuntário sobre um arquivo com trabalho não commitado (`ComprovantePage.tsx`) apagou as mudanças em voo. A sessão de recuperação (Sonnet 5) confirmou por `git diff`/leitura que o arquivo foi restaurado de forma coerente (import, hook, JSX fechando corretamente) e então validou o estado real do zero, sem confiar na aparência:

```
$ cd apps/web && npx tsc --noEmit
(sem saída = OK)

$ npx vitest run
Test Files  52 passed (52)
     Tests  374 passed (374)

$ cd apps/api && .venv/Scripts/python.exe -m pytest -q -m "not rls_e2e"
1162 passed, 1 skipped, 29 deselected, 2526 warnings in 243.39s

$ .venv/Scripts/python.exe -m ruff check app tests
All checks passed!

$ cd apps/web && npx eslint . --max-warnings 0
(sem saída = OK)
```

Baseline antes desta story: backend 1154/1/29 (pós-8.12); frontend 330/50. Delta real: **backend +8** (os 4 casos de validação condicional dos schemas da bandeja, mais testes de `test_receipts.py` adaptados), **frontend +44** (374−330: `PagarPage.test.tsx`, `ComprovantePage.test.tsx`, `FilaPagamentosPage.test.tsx` novo, `baixa.test.ts` novo, `contas.test.ts`).

`rls_e2e` **não foi re-executado** nesta sessão de recuperação — nenhum arquivo tocado por esta story tem marcador `rls_e2e` fora de `test_receipts.py`/`test_receipts_rls.py`, que a Task 1/IV1 exigem verdes mas que não fazem parte do diff funcional desta story (não foram alterados). Ficará coberto pelo QA gate da onda.

### Completion Notes List

- **Interrupção e recuperação.** A implementação (Tasks 1–7, 9 exceto QA agents) foi concluída pelo @dev original. A sessão caiu por limite de uso da conta durante um teste de mutação da Task 8/9, com perda e recuperação de um arquivo em voo (ver acima). A conclusão da documentação (checkboxes, este registro, File List) foi feita por uma sessão de recuperação que **verificou o estado real por execução de suíte completa antes de declarar qualquer coisa pronta** — não presumiu com base no relato do agente interrompido.
- **AC1–AC8 e AC10: satisfeitos**, com teste. Confirmado por leitura direta (não apenas pela suíte passar):
  - `EscolhaDaBaixa`/`DialogDeBaixa` (`apps/web/src/features/pagar/EscolhaDaBaixa.tsx`, novo) é o componente único, importado por `PagarPage.tsx`, `ComprovantePage.tsx` e `FilaPagamentosPage.tsx` — sem duplicação (AC: 1, 5, 6, Task 3).
  - `ComprovantePage.tsx`: `<EscolhaDaBaixa estado={escolha} compact />` está **dentro** de `<div className="fixed inset-x-0 bottom-0 ...">` (linha 301 em diante) — confirmado por leitura direta do arquivo, não só pelo teste de co-localização. Satisfaz o AC4 estruturalmente.
  - `PagarPage.tsx`: tabela mantém `overflow-x-auto` (não `overflow-hidden`), com o comentário do achado de campo preservado — não houve regressão do PR #58.
  - `FilaPagamentosPage.tsx`: importa `DialogDeBaixa` de `EscolhaDaBaixa` e envia corpo (`corpo`) no `POST /bills/{id}/pay` — reusa, não duplica.
  - `receipts.py`: o texto `TODO(8.13)` só aparece agora dentro de um comentário histórico explicando que o default de conta primária **foi removido** — não há mais TODO vivo no código.
- **AC9 — NÃO satisfeito. Este é o item que bloqueia o fechamento da story.** A validação em aparelho real (ou emulação fiel a ~360px) dos 5 itens (a)–(e) não foi executada em nenhuma das duas sessões: nem a original (interrompida antes de chegar lá) nem a de recuperação (sem acesso a stack de dev viva com autenticação neste ambiente — o mesmo limite que já havia sido registrado na Story 8.7). A auditoria estrutural que **pôde** ser feita (acima) dá confiança de que o código está desenhado corretamente para 360px, mas **não substitui** o veredito F-D8 do fundador, que exige verificação física.
- **Os 3 agentes de QA do `CLAUDE.md` §5 não foram executados** — mesma limitação estrutural já registrada em todas as stories anteriores da onda (sem ferramenta de spawn a partir do executor).

### Evidência do aceite em ~360px (AC9 — bloqueante)

**NÃO CAPTURADA.** Nenhum dos itens (a)–(e) foi verificado em aparelho real ou emulador nesta rodada. Auditoria estrutural (proxy, não substituto):
- (a) confirmado por leitura: `EscolhaDaBaixa compact` está dentro do container fixo, ao lado do botão — mas altura total do bloco fixo em 360px não foi medida contra o viewport.
- (d) confirmado por leitura: `overflow-x-auto` presente, `overflow-hidden` ausente em `PagarPage.tsx`.
- (b), (c), (e): não verificados.

**Este item precisa ser fechado pelo founder (dispositivo real) ou por uma sessão futura com acesso a ambiente de dev + Playwright antes do merge para `main`**, conforme o veredito F-D8 já registrado no epic.

### File List

**Backend (produção):**
- `apps/api/app/modules/payables/receipts.py`
- `apps/api/app/modules/payables/receipts_router.py`
- `apps/api/app/modules/payables/receipts_schemas.py`

**Backend (testes):**
- `apps/api/tests/test_payables_bank_origin.py`
- `apps/api/tests/test_receipts.py`

**Frontend (produção, novos):**
- `apps/web/src/features/pagar/EscolhaDaBaixa.tsx`
- `apps/web/src/features/pagar/baixa.ts`
- `apps/web/src/features/financeiro/AccountModal.tsx` (extraído de `ContasSaldosPage.tsx` para reuso pelo AC2)

**Frontend (produção, alterados):**
- `apps/web/src/features/pagar/PagarPage.tsx`
- `apps/web/src/features/pagar/ComprovantePage.tsx`
- `apps/web/src/features/financeiro/FilaPagamentosPage.tsx`
- `apps/web/src/features/financeiro/ContasSaldosPage.tsx` (refatorado para consumir `AccountModal.tsx` extraído)
- `apps/web/src/lib/api.ts`
- `packages/shared-types/src/index.ts`

**Frontend (testes, novos):**
- `apps/web/src/features/pagar/baixa.test.ts`
- `apps/web/src/features/financeiro/FilaPagamentosPage.test.tsx`

**Frontend (testes, alterados):**
- `apps/web/src/features/pagar/PagarPage.test.tsx`
- `apps/web/src/features/pagar/ComprovantePage.test.tsx`
- `apps/web/src/features/financeiro/ContasSaldosPage.test.tsx`

**Sem migration.** `payables/service.py`, `bank/`, `reconciliation.py`, `projection.py`, `engine.py`, `wallet/` não foram tocados — confirmado por `git status`.

## QA Results
_(a preencher pelo @architect no quality gate)_

# Dívida de validação em ~360px — Plano de Implementação

> **Para quem executa:** SUB-SKILL OBRIGATÓRIA — use `superpowers:subagent-driven-development`
> (recomendado) ou `superpowers:executing-plans` para implementar tarefa a tarefa. Os passos usam
> checkbox (`- [ ]`) para acompanhamento.

**Objetivo:** Fechar a dívida "validação manual em ~360px NÃO foi feita" das seis telas mergeadas
sem medição, e trocar a disciplina de sessão que falhou seis vezes por uma régua executável.

**Arquitetura:** Playwright entra no repositório como **gate de layout**, não como suíte de
integração: sobe só o Vite, intercepta `**/api/**` com `page.route` e afere geometria
(`scrollWidth`, `getBoundingClientRect`, texto efetivamente visível, altura do alvo de toque).
Sem backend, sem Docker, sem banco — a lição do PR #102 é que a medição só custa caro quando se
insiste em subir a pilha inteira para obtê-la. Cada um dos 4 PRs abre com um teste que **falha
contra o código de hoje** e fecha com o conserto que o faz passar.

**Stack:** `@playwright/test` (chromium), Vite 6, React 18, Tailwind 3, vitest 2.

## Restrições globais

- **Viewport de referência:** 360×740, `deviceScaleFactor` 2, `locale` `pt-BR`, `timeZone`
  `America/Sao_Paulo`.
- **Asserção de classe CSS não é prova.** Nenhum teste deste plano pode usar
  `toContain("flex-wrap")` ou equivalente. Só valem números medidos no navegador.
  (`toContain("flex-wrap")` passou com a `FilaPagamentosPage` quebrada em produção por duas
  sessões.)
- **Porta do e2e: 5273**, não 5173 — a 5173 colide com outro projeto nesta máquina. Sempre
  `127.0.0.1`, nunca `localhost`.
- **Alvo de toque mínimo: 44×44 CSS px** para qualquer controle que o dono precise acertar com o
  polegar.
- **Em 360px a saída não é fazer a rolagem lateral funcionar melhor — é não precisar dela.**
  `overflow-x-auto` em vez de `overflow-hidden` é a lição do PR #58 e continua valendo; a lição da
  Onda 2b-ii é mais funda: informação que exige rolagem lateral para existir é informação que o
  dono não lê.
- **Um PR por assunto.** O conserto do `AppShell` não entra no mesmo diff que o das telas — misturar
  tira do gate a capacidade de julgar qual mudança quebrou o quê.
- **`main` é protegida.** Todo trabalho vai por PR. `git push` e `gh pr create` são **exclusivos do
  @devops** — quem executa este plano faz `git commit` e para aí.
- **Os TRÊS comandos antes de dizer que terminou:** `ruff check .` (em `apps/api`),
  `pytest -m "not rls_e2e"` (em `apps/api`), `pnpm --filter @e1p/web test`. O ruff é gate de CI e é
  o mais fácil de esquecer porque não é teste.
- **A entrada no `CLAUDE.md` é AC obrigatório de cada PR**, não passo final opcional — escrita a
  partir do código que subiu, com a regra que fica e a dívida que sobrou. Dívida que o PR FECHOU
  sai de lá.

## O que a medição de 2026-08-09 encontrou (a base deste plano)

| Tela | Medido em 360×740 | Veredito |
|---|---|---|
| Painel de Conversas | painel da conversa em x=360→501, 12 elementos fora, `main` com `overflow-x-hidden` | 🔴 quebrada |
| Modal de conta bancária | conteúdo 1010px em caixa de 629px; botão a 942px do topo (y=1043,5), 467px abaixo da escolha | 🔴 quebrada |
| Topbar do AppShell | linha mínima 375px em `/financeiro/investimentos`; botão de menu espremido a 16px | 🔴 quebrada |
| Contas & Saldos | largura OK (`scrollWidth` 360); 34 de 35 controles com <40px de altura; 7 ações de 16px | 🟡 alvo de toque |
| Config → "A sua empresa" | 16.495px = 22,3 telas; pergunta nova do #103 a 14,6 telas de distância | 🟡 alcance |
| Vínculo da aplicação (modal) | 596px em 740; botão visível sem rolar | 🟢 passou |
| Briefing | `scrollWidth` 360, nada fora, nada cortado | 🟢 passou |
| Núcleo do DNA | `scrollWidth` 360, um alvo de 54px por linha | 🟢 passou |

**Correção de premissa registrada:** o culpado do estouro do shell **não é o `ChevronDown`** de
`AppShell.tsx:209` — é o comprimento do rótulo da ação primária. Medido: linha mínima 216px sem
ação, 326px com "Nova conta", **375px** com "Nova conta de investimento". O chevron é apenas o
último elemento da fila, então é sempre ele que sobra para fora. E o defeito **não vale para todas
as telas**: `/vima` e `/dna/nucleo` vivem em `ProtectedBareLayout` (sem shell) e estão imunes.

---

## Estrutura de arquivos

**A régua (PR 1):**

| Arquivo | Responsabilidade |
|---|---|
| `apps/web/playwright.config.ts` | viewport de referência, `webServer` na 5273, só chromium |
| `apps/web/e2e/support/sessao.ts` | semeia o `localStorage` de sessão autenticada |
| `apps/web/e2e/support/medidas.ts` | **a régua**: as funções de aferição reutilizadas por todo spec |
| `apps/web/e2e/support/api.ts` | `page.route("**/api/**")` com as respostas por rota |
| `apps/web/e2e/fixtures/*.json` | payloads de pior caso plausível, nas formas dos schemas reais |
| `.github/workflows/ci.yml` | job `frontend` — typecheck + vitest + playwright |

**Os consertos (um arquivo de spec por PR):**

| PR | Spec | Código |
|---|---|---|
| 1 | `e2e/conversas-360.spec.ts` | `features/conversas/ConversasPage.tsx` |
| 2 | `e2e/shell-360.spec.ts` | `app/AppShell.tsx` |
| 3 | `e2e/modal-conta-360.spec.ts` | `components/Modal.tsx`, `features/financeiro/AccountModal.tsx` |
| 4 | `e2e/toque-360.spec.ts` | `features/financeiro/ContasSaldosPage.tsx`, `features/config/EmpresaDnaTab.tsx` |

---

# PR 1 — A régua, provada na tela que está pior

**Branch:** `fix/360px-conversas-painel-unico`

**Por que a régua e a Conversas no mesmo PR:** infraestrutura de teste sem consumidor é
infraestrutura não testada. O spec da Conversas falha contra o `main` de hoje e passa depois do
conserto — é isso que prova que a régua mede.

### Task 1: A régua (Playwright + fixtures + CI)

**Arquivos:**
- Criar: `apps/web/playwright.config.ts`
- Criar: `apps/web/e2e/support/sessao.ts`
- Criar: `apps/web/e2e/support/medidas.ts`
- Criar: `apps/web/e2e/support/api.ts`
- Criar: `apps/web/e2e/fixtures/conversas.json`
- Modificar: `apps/web/package.json` (devDependencies + scripts)
- Modificar: `apps/web/tsconfig.json` (`include` passa a cobrir `e2e`)
- Modificar: `.gitignore` (saídas do Playwright)
- Modificar: `.github/workflows/ci.yml` (job `frontend`)

**Interfaces:**
- Produz: `semearSessao(page)`, `mockarApi(page, respostas)`, e de `medidas.ts` →
  `medirPagina(page): Promise<Medidas>`, `alvosPequenos(page, min?): Promise<Alvo[]>`,
  `textoForaDaTela(page): Promise<Corte[]>`.

- [ ] **Passo 1: instalar o Playwright no workspace do front**

```bash
cd apps/web
pnpm add -D @playwright/test @types/node
pnpm exec playwright install chromium
```

`@types/node` não é enfeite: `playwright.config.ts` lê `process.env.CI`, e sem os tipos o
`pnpm typecheck` quebra assim que o `e2e/` entrar no `include` (Passo 2b).

- [ ] **Passo 2b: `tsconfig.json` passa a cobrir o `e2e`**

Em `apps/web/tsconfig.json`, a última linha:

```json
  "include": ["src", "e2e", "playwright.config.ts"]
```

Sem isto o `pnpm typecheck` ignora a régua inteira, e uma régua que não é verificada é a mesma
disciplina de sessão que este plano existe para substituir.

- [ ] **Passo 2: criar `apps/web/playwright.config.ts`**

```ts
import { defineConfig, devices } from "@playwright/test";

/**
 * Gate de LAYOUT, não suíte de integração.
 *
 * Sobe só o Vite; a API é interceptada por `page.route` (ver `e2e/support/api.ts`). Sem backend,
 * sem Docker, sem banco — foi por achar que medir custava caro que seis telas subiram sem medição
 * e três PRs de correção em campo foram pagos (#56, #58, #89).
 *
 * Porta 5273, não 5173: a 5173 colide com outro projeto nas máquinas de desenvolvimento. Sempre
 * `127.0.0.1` — `localhost` resolve para ::1 em algumas delas e o Vite não atende ali.
 */
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: true,
  forbidOnly: Boolean(process.env.CI),
  retries: 0,
  reporter: process.env.CI ? [["github"], ["list"]] : [["list"]],
  use: {
    baseURL: "http://127.0.0.1:5273",
    locale: "pt-BR",
    timezoneId: "America/Sao_Paulo",
    screenshot: "only-on-failure",
    trace: "retain-on-failure",
  },
  projects: [
    {
      // O aparelho de referência da dívida: 360px de largura é o piso que o produto declara
      // atender, e é onde o dono lê o WhatsApp de manhã.
      name: "360px",
      use: { ...devices["Desktop Chrome"], viewport: { width: 360, height: 740 },
             deviceScaleFactor: 2, isMobile: false },
    },
  ],
  webServer: {
    command: "pnpm vite --port 5273 --host 127.0.0.1",
    url: "http://127.0.0.1:5273",
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
});
```

- [ ] **Passo 3: criar `apps/web/e2e/support/sessao.ts`**

```ts
import type { Page } from "@playwright/test";

/**
 * Sessão autenticada sem passar pelo login: o app guarda tudo em `localStorage`
 * (`store/auth.tsx`) e não chama `/auth/me` no boot. Um teste de LAYOUT não deve gastar dois
 * requests provando de novo que o login funciona — isso é assunto de outro teste.
 */
export async function semearSessao(page: Page): Promise<void> {
  await page.addInitScript(() => {
    localStorage.setItem("e1p_token", "e2e.layout.token");
    localStorage.setItem("e1p_user", JSON.stringify({
      id: "00000000-0000-4000-8000-000000000001",
      tenant_id: "00000000-0000-4000-8000-000000000002",
      email: "dono@exemplo.com.br", name: "Flávio Kato", role: "owner",
      allowed_modules: [], is_active: true, is_platform_admin: false,
      document: null, address: null, phone: null, must_reset_password: false,
    }));
    localStorage.setItem("e1p_tenant", JSON.stringify({
      id: "00000000-0000-4000-8000-000000000002",
      legal_name: "Flávio Kato Consultoria Empresarial ME",
      slug: "flaviokato", timezone: "America/Sao_Paulo",
    }));
    // As duas marcas de "já decidi isto neste aparelho" — sem elas a raiz sequestra a navegação.
    localStorage.setItem("e1p_dna_nucleo", "1");
    localStorage.setItem("e1p_entrada_do_dia", "1");
  });
}
```

- [ ] **Passo 4: criar `apps/web/e2e/support/api.ts`**

```ts
import type { Page } from "@playwright/test";

/**
 * Intercepta `**\/api\/**` e responde do mapa recebido. A chave é um trecho do caminho; a primeira
 * que casar vence. Rota não mapeada devolve `[]` — a tela não quebra por causa de um endpoint que
 * o teste não estava medindo.
 *
 * ⚠️ Os payloads são de **pior caso plausível** (nomes longos de banco, valores de 6 dígitos,
 * título de grupo comprido) e nas formas dos schemas reais. Dado curto sempre cabe: medir com ele
 * é medir uma tela que não existe.
 */
export async function mockarApi(
  page: Page,
  respostas: Record<string, unknown>,
): Promise<void> {
  await page.route("**/api/**", async (route) => {
    const caminho = new URL(route.request().url()).pathname.replace(/^\/api/, "");
    const chave = Object.keys(respostas).find((k) => caminho.startsWith(k));
    await route.fulfill({
      status: 200,
      contentType: "application/json; charset=utf-8",
      body: JSON.stringify(chave ? respostas[chave] : []),
    });
  });
}
```

- [ ] **Passo 5: criar `apps/web/e2e/support/medidas.ts` — a régua**

```ts
import type { Page } from "@playwright/test";

export interface Medidas {
  /** `document.documentElement.scrollWidth`. Maior que a viewport = a página rola de lado. */
  larguraDaPagina: number;
  alturaDaPagina: number;
}

export interface Alvo { descricao: string; largura: number; altura: number; }
export interface Corte { texto: string; descricao: string; forcaFora: number; }

export async function medirPagina(page: Page): Promise<Medidas> {
  return page.evaluate(() => ({
    larguraDaPagina: document.documentElement.scrollWidth,
    alturaDaPagina: document.documentElement.scrollHeight,
  }));
}

/**
 * Controles menores que o mínimo tocável. `min` é ALTURA e LARGURA em CSS px.
 *
 * 44px não é preciosismo: foi um checkbox pequeno demais, separado da ação que o tornava efetivo,
 * que fez uma conta real ser marcada como paga sem o dono conseguir ver (PR #56).
 */
export async function alvosPequenos(page: Page, min = 44): Promise<Alvo[]> {
  return page.evaluate((limite) => {
    // As duas funções auxiliares são declaradas AQUI DENTRO de propósito: o corpo de um
    // `page.evaluate` é serializado e executado no navegador, e nada do escopo do Node viaja
    // junto. Passá-las como string para dar `eval` do outro lado funcionaria e seria pior.
    const descrever = (el: Element): string => {
      const cls = typeof el.className === "string"
        ? el.className.split(/\s+/).filter(Boolean).slice(0, 5).join(".") : "";
      const txt = (el.textContent || "").trim().replace(/\s+/g, " ").slice(0, 50);
      return el.tagName.toLowerCase() + (cls ? "." + cls : "") + (txt ? ` «${txt}»` : "");
    };
    const visivel = (el: Element): boolean => {
      const s = getComputedStyle(el);
      if (s.display === "none" || s.visibility === "hidden" || s.opacity === "0") return false;
      const r = el.getBoundingClientRect();
      return r.width > 0 && r.height > 0;
    };
    const SEL = 'button,a[href],input:not([type="hidden"]),select,textarea,'
      + '[role="button"],[role="checkbox"],summary';
    return [...document.querySelectorAll(SEL)]
      .filter(visivel)
      .map((el) => {
        const r = el.getBoundingClientRect();
        return { descricao: descrever(el), largura: +r.width.toFixed(1), altura: +r.height.toFixed(1) };
      })
      .filter((a) => a.altura < limite || a.largura < limite);
  }, min);
}

/**
 * Texto que só EXISTE se o dono rolar de lado — o defeito que a Onda 2b-ii achou na primeira
 * medição (`R$ 3.` no lugar de `R$ 3.000,00`). Para cada folha com texto, acha o ancestral que
 * recorta e mede quanto sobra fora dele.
 */
export async function textoForaDaTela(page: Page): Promise<Corte[]> {
  return page.evaluate(() => {
    const descrever = (el: Element): string => {
      const cls = typeof el.className === "string"
        ? el.className.split(/\s+/).filter(Boolean).slice(0, 5).join(".") : "";
      return el.tagName.toLowerCase() + (cls ? "." + cls : "");
    };
    const visivel = (el: Element): boolean => {
      const s = getComputedStyle(el);
      if (s.display === "none" || s.visibility === "hidden" || s.opacity === "0") return false;
      const r = el.getBoundingClientRect();
      return r.width > 0 && r.height > 0;
    };
    // Quem de fato recorta este elemento — o ancestral mais próximo com `overflow-x` que não seja
    // `visible`. É contra a borda DELE que o texto sobra, não contra a da página: era exatamente
    // isso que `main.overflow-x-hidden` escondia enquanto o `scrollWidth` da página dizia 360.
    const recorte = (el: Element): Element => {
      for (let p = el.parentElement; p && p !== document.body; p = p.parentElement) {
        const ox = getComputedStyle(p).overflowX;
        if (ox === "hidden" || ox === "auto" || ox === "scroll" || ox === "clip") return p;
      }
      return document.documentElement;
    };
    const vw = document.documentElement.clientWidth;
    const fora: { texto: string; descricao: string; forcaFora: number }[] = [];
    for (const el of [...document.querySelectorAll("body *")].filter(visivel)) {
      if (el.children.length > 0 || !(el.textContent || "").trim()) continue;
      const r = el.getBoundingClientRect();
      const limite = Math.min(recorte(el).getBoundingClientRect().right, vw);
      const sobra = +(r.right - limite).toFixed(1);
      if (sobra > 0.5) {
        fora.push({
          texto: (el.textContent || "").trim().replace(/\s+/g, " ").slice(0, 60),
          descricao: descrever(el),
          forcaFora: sobra,
        });
      }
    }
    return fora;
  });
}
```

- [ ] **Passo 6: criar `apps/web/e2e/fixtures/conversas.json`**

```json
{
  "/whatsapp-conversations": [
    { "chat_id": "00000000-0000-4000-8000-000000000021", "kind": "direct",
      "title": "Maria Aparecida Gonçalves de Souza", "phone": "+55 11 98765-4321",
      "client_id": "00000000-0000-4000-8000-000000000031",
      "last_message_at": "2026-08-09T13:42:00Z",
      "last_message_preview": "Bom dia! Consegui olhar a proposta ontem à noite, ficou muito bom mesmo. Só queria confirmar se o prazo continua o mesmo",
      "unread": true },
    { "chat_id": "00000000-0000-4000-8000-000000000022", "kind": "group",
      "title": "Obra Residencial Alphaville — Fornecedores", "phone": null, "client_id": null,
      "last_message_at": "2026-08-09T11:05:00Z",
      "last_message_preview": "Pedro: o caminhão do material chega amanhã cedo", "unread": false }
  ],
  "/whatsapp-templates": []
}
```

> **Nota para quem executa:** a timeline da conversa vem de
> `/whatsapp-conversations/{id}/timeline`, que casa com o prefixo acima. Acrescente essa chave ao
> JSON no Passo 3 da Task 2, junto com o spec que a consome — fixture sem consumidor apodrece.

- [ ] **Passo 7: scripts e `.gitignore`**

Em `apps/web/package.json`, dentro de `"scripts"`:

```json
"e2e": "playwright test",
"e2e:ui": "playwright test --ui"
```

Em `.gitignore` (raiz), acrescente:

```gitignore
# Playwright (gate de layout — apps/web/e2e)
apps/web/test-results/
apps/web/playwright-report/
apps/web/blob-report/
```

- [ ] **Passo 8: o job de CI**

Acrescente ao fim de `.github/workflows/ci.yml`:

```yaml
  # Gate de LAYOUT do frontend (dívida de 360px, 2026-08-10). Este repositório NÃO tinha nenhum
  # job de frontend: `vitest` nunca rodou no CI e nenhuma medição de tela era exigida em PR — foi
  # assim que seis telas mergearam com "validação em ~360px não foi feita" e três PRs de correção
  # em campo foram pagos (#56, #58, #89).
  #
  # Observável primeiro, bloqueante depois — MESMO modelo de `secret-scan`/`sast-semgrep` deste
  # arquivo: o job aparece no PR e fica vermelho quando acha algo, mas só IMPEDE merge quando
  # @devops o acrescentar em "Require status checks to pass" (GitHub → Settings → Branches).
  # Sem esse passo a régua é conselho, não régua. NÃO usamos `continue-on-error` (deixaria o check
  # sempre verde e tornaria a promoção um no-op).
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm

      - name: Instalar dependências
        run: pnpm install --frozen-lockfile
        working-directory: .

      - name: Typecheck
        run: pnpm typecheck

      - name: Testes de unidade (vitest)
        run: pnpm test

      # Só chromium: o gate mede geometria de layout, não compatibilidade entre motores.
      # `--with-deps` instala as bibliotecas de sistema que o runner não traz.
      - name: Instalar chromium do Playwright
        run: pnpm exec playwright install --with-deps chromium

      - name: Gate de layout em 360px
        run: pnpm e2e

      - if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: apps/web/playwright-report/
          retention-days: 7
```

- [ ] **Passo 9: provar que a régua roda (ainda sem spec de tela)**

Rode: `cd apps/web && pnpm e2e`
Esperado: `No tests found` — o Playwright sobe, acha o `testDir` e sai limpo. Se ele reclamar da
porta 5273, algum Vite ficou de pé: derrube-o antes.

- [ ] **Passo 10: commit**

```bash
git add apps/web/playwright.config.ts apps/web/e2e apps/web/package.json \
        pnpm-lock.yaml .gitignore .github/workflows/ci.yml
git commit -m "test: Playwright entra como régua de layout em 360px [dívida 360px]"
```

### Task 2: Conversas — painel único no celular

**Arquivos:**
- Criar: `apps/web/e2e/conversas-360.spec.ts`
- Modificar: `apps/web/e2e/fixtures/conversas.json` (acrescentar a timeline)
- Modificar: `apps/web/src/features/conversas/ConversasPage.tsx:66-120` e a assinatura de
  `ConversationThread` (linha 180)

**Interfaces:**
- Consome: `semearSessao`, `mockarApi`, `medirPagina`, `textoForaDaTela` da Task 1.
- Produz: `ConversationThread` passa a aceitar `onVoltar?: () => void`.

- [ ] **Passo 1: acrescentar a timeline à fixture**

Em `apps/web/e2e/fixtures/conversas.json`, acrescente a chave (o prefixo casa com
`/whatsapp-conversations/{id}/timeline` **antes** do prefixo mais curto, então ela vem primeiro):

```json
"/whatsapp-conversations/00000000-0000-4000-8000-000000000021/timeline": [
  { "source": "conversation", "direction": "in", "kind": "text",
    "text_body": "Bom dia! Consegui olhar a proposta ontem à noite, ficou muito bom mesmo.",
    "media_attachment_id": null, "purpose_label": null, "sender_name": null,
    "created_at": "2026-08-09T13:40:00Z" },
  { "source": "automated", "direction": "out", "kind": "text",
    "text_body": "Olá Maria! Sua cobrança de R$ 3.000,00 vence em 3 dias.",
    "media_attachment_id": null, "purpose_label": "Lembrete de cobrança", "sender_name": null,
    "created_at": "2026-08-09T13:41:00Z" }
]
```

⚠️ Em `support/api.ts` a primeira chave que casar vence, e `Object.keys` preserva a ordem de
inserção — a chave da timeline **precisa vir antes** de `"/whatsapp-conversations"` no JSON.

- [ ] **Passo 2: escrever o teste que falha**

Crie `apps/web/e2e/conversas-360.spec.ts`:

```ts
import { expect, test } from "@playwright/test";
// O atributo `with { type: "json" }` NÃO é opcional: o Playwright carrega os specs como ESM
// nativo do Node, que recusa importar JSON sem ele ("needs an import attribute").
import fixtures from "./fixtures/conversas.json" with { type: "json" };
import { mockarApi } from "./support/api";
import { medirPagina, textoForaDaTela } from "./support/medidas";
import { semearSessao } from "./support/sessao";

/**
 * O Painel de Conversas em 360px.
 *
 * Antes deste spec, `w-80 shrink-0` (a lista, 320px) e `flex-1` (a conversa) dividiam a tela SEM
 * breakpoint: com uma conversa selecionada, o painel dela nascia em x=360 — inteiro fora da
 * viewport — e `main` tem `overflow-x-hidden`, então não havia barra nem pan por toque. O dono
 * tocava numa conversa e nada acontecia.
 */
test.beforeEach(async ({ page }) => {
  await semearSessao(page);
  await mockarApi(page, fixtures);
});

test("a lista cabe na tela e não força rolagem lateral", async ({ page }) => {
  await page.goto("/conversas");
  await expect(page.getByText("Maria Aparecida Gonçalves de Souza")).toBeVisible();

  const { larguraDaPagina } = await medirPagina(page);
  expect(larguraDaPagina).toBe(360);
});

test("tocar numa conversa mostra a conversa — inteira, dentro da tela", async ({ page }) => {
  await page.goto("/conversas");
  await page.getByText("Maria Aparecida Gonçalves de Souza").click();

  // A mensagem tem de estar VISÍVEL, não apenas presente no DOM: era exatamente isso que
  // `overflow-x-hidden` escondia enquanto o teste de classe CSS ficava verde.
  const mensagem = page.getByText("Consegui olhar a proposta ontem à noite");
  await expect(mensagem).toBeInViewport();

  const caixa = await mensagem.boundingBox();
  expect(caixa).not.toBeNull();
  expect(caixa!.x).toBeGreaterThanOrEqual(0);
  expect(caixa!.x + caixa!.width).toBeLessThanOrEqual(360);

  // E nada mais pode ter ficado para trás da borda.
  expect(await textoForaDaTela(page)).toEqual([]);
});

test("dá para voltar da conversa para a lista", async ({ page }) => {
  await page.goto("/conversas");
  await page.getByText("Maria Aparecida Gonçalves de Souza").click();
  await expect(page.getByText("Consegui olhar a proposta")).toBeInViewport();

  await page.getByRole("button", { name: "Voltar para as conversas" }).click();
  await expect(page.getByText("Obra Residencial Alphaville — Fornecedores")).toBeInViewport();
});
```

- [ ] **Passo 3: rodar e confirmar que falha**

Rode: `cd apps/web && pnpm e2e conversas-360`
Esperado: os testes 2 e 3 FALHAM — o 2 porque a mensagem não está na viewport (`toBeInViewport`
falha) e o 3 porque não existe botão "Voltar para as conversas". O teste 1 passa (a lista sozinha
já cabia).

- [ ] **Passo 4: implementar — a lista e a conversa deixam de dividir a tela abaixo de `lg`**

Em `apps/web/src/features/conversas/ConversasPage.tsx`, no `return` da página (linha ~66):

```tsx
  return (
    <div className="flex h-[calc(100vh-8rem)] gap-4">
      {/* Abaixo de `lg` é UMA coisa por vez: a lista OU a conversa. Duas colunas de 320px num
          aparelho de 360px não é layout apertado, é layout ausente — a segunda nasce fora da
          tela e `main` (`overflow-x-hidden`) a corta sem deixar nem barra de rolagem. Era o
          "corte" que o PR #58 proibiu, escondendo a jornada inteira do contato. A gaveta do
          histórico (abaixo) já tratava o celular assim; a divisão principal é que não. */}
      <div
        className={`w-full shrink-0 overflow-y-auto rounded-2xl bg-white shadow-sm lg:w-80 ${
          selected ? "hidden lg:block" : "block"
        }`}
      >
```

E no painel da conversa (linha ~107):

```tsx
      <div className={`flex-1 rounded-2xl bg-white shadow-sm ${selected ? "block" : "hidden lg:block"}`}>
        {selected ? (
          <ConversationThread
            key={selected}
            chatId={selected}
            chat={conversations.find((c) => c.chat_id === selected) ?? null}
            onSent={loadConversations}
            onVoltar={() => setSelected(null)}
          />
        ) : (
          <div className="flex h-full items-center justify-center text-sm text-neutral-400">
            Selecione uma conversa
          </div>
        )}
      </div>
```

- [ ] **Passo 5: implementar — o botão de voltar no cabeçalho da conversa**

Em `ConversationThread`, mude a assinatura (linha ~180):

```tsx
function ConversationThread({
  chatId, chat, onSent, onVoltar,
}: {
  chatId: string;
  chat: ConversationSummary | null;
  onSent: () => void;
  /** Só existe abaixo de `lg`, onde a conversa OCUPA a tela e a lista some. */
  onVoltar?: () => void;
}) {
```

Acrescente `ChevronLeft` ao import de `lucide-react` no topo do arquivo e troque o cabeçalho
(linha ~281):

```tsx
      <div className="flex items-center gap-2 border-b border-neutral-100 px-4 py-3">
        {onVoltar && (
          <button
            type="button"
            onClick={onVoltar}
            aria-label="Voltar para as conversas"
            className="-ml-2 flex h-11 w-11 shrink-0 items-center justify-center rounded-pill text-neutral-500 hover:bg-neutral-50 lg:hidden"
          >
            <ChevronLeft size={20} />
          </button>
        )}
        <div className="min-w-0">
          <p className="flex items-center gap-1.5 truncate text-sm font-semibold text-neutral-800">
            {chat?.kind === "group" && <Users size={14} className="shrink-0 text-neutral-400" />}
            {chat?.title ?? "Conversa"}
          </p>
          <p className="truncate text-xs text-neutral-400">
            {chat?.kind === "group" ? "Grupo" : chat?.phone}
          </p>
        </div>
      </div>
```

- [ ] **Passo 6: rodar e confirmar que passa**

Rode: `cd apps/web && pnpm e2e conversas-360`
Esperado: 3 testes PASSAM.

- [ ] **Passo 7: rodar os três de sempre**

```bash
cd apps/api && ruff check . && python -m pytest -q -m "not rls_e2e"
cd ../web && pnpm typecheck && pnpm test
```

Esperado: tudo verde. `ConversasPage.test.tsx` existe — se ele afirmar a presença simultânea dos
dois painéis, atualize-o; se ele afirmar classes CSS, **troque a asserção**, não o layout.

- [ ] **Passo 8: a entrada no `CLAUDE.md`**

Acrescente na seção do módulo de Conversas/CRM. Escreva a partir do código que subiu:

```markdown
- **360px: a Conversa é uma tela, não uma coluna.** Abaixo de `lg` a lista e a conversa NÃO
  dividem a largura — mostra-se uma por vez, com "Voltar para as conversas" no cabeçalho da
  conversa. Duas colunas de 320px em 360px faziam o painel nascer em x=360, fora da viewport, e
  `main` (`overflow-x-hidden`) o cortava sem deixar barra: o dono tocava e nada acontecia.
  Medido e travado por `apps/web/e2e/conversas-360.spec.ts`.
- **Régua de layout:** `apps/web/e2e/` roda em 360×740 com o Vite sozinho (API por `page.route`).
  Asserção de classe CSS não vale como prova de layout — `toContain("flex-wrap")` já passou com a
  `FilaPagamentosPage` quebrada em produção. **Dívida:** o job `frontend` do CI é observável, não
  bloqueante, até @devops o promover em "Require status checks to pass".
```

- [ ] **Passo 9: commit**

```bash
git add apps/web/src/features/conversas/ConversasPage.tsx apps/web/e2e CLAUDE.md
git commit -m "fix: a conversa deixa de nascer fora da tela em 360px [dívida 360px]"
```

---

# PR 2 — A barra superior do AppShell

**Branch:** `fix/360px-topbar-do-shell`

**Base:** ramifique de PR 1 (precisa da régua). Não misture com PR 1 — foi separar SIG-001 da 8.16
e a 8.19 da 8.20 pelo mesmo motivo: misturar tira do gate a capacidade de julgar o quê quebrou o
quê.

### Task 3: a linha da topbar para de espremer e de estourar

**Arquivos:**
- Criar: `apps/web/e2e/shell-360.spec.ts`
- Modificar: `apps/web/src/app/AppShell.tsx:163-212` (`Topbar`)

- [ ] **Passo 1: escrever o teste que falha**

```ts
import { expect, test } from "@playwright/test";
import { mockarApi } from "./support/api";
import { medirPagina } from "./support/medidas";
import { semearSessao } from "./support/sessao";

/**
 * A barra superior em 360px.
 *
 * A atribuição original ("o culpado é o ChevronDown de AppShell.tsx:209") era geométrica, não
 * causal: o chevron é o ÚLTIMO da fila, então é sempre ele que sobra para fora. Medido, o que
 * decide se a linha cabe é o COMPRIMENTO DO RÓTULO da ação primária — linha mínima de 216px sem
 * ação, 326px com "Nova conta", 375px com "Nova conta de investimento". Tirar o chevron compraria
 * 24px e mascararia a classe até o próximo rótulo longo.
 *
 * O dano que os 15px de estouro escondiam era pior: todos os filhos têm `flex-shrink: 1` sem
 * `min-width`, então o botão "Abrir menu" — o ÚNICO acesso à navegação no celular — era espremido
 * de 36px para 16px de largura.
 */
test.beforeEach(async ({ page }) => {
  await semearSessao(page);
  await mockarApi(page, { "/investments": [], "/bank/accounts": [] });
});

test("a rota de rótulo mais longo não faz a página rolar de lado", async ({ page }) => {
  await page.goto("/financeiro/investimentos");
  await expect(page.getByRole("button", { name: /Nova conta de investimento/ })).toBeVisible();

  const { larguraDaPagina } = await medirPagina(page);
  expect(larguraDaPagina).toBe(360);
});

test("o botão de abrir o menu continua tocável quando há ação primária", async ({ page }) => {
  await page.goto("/financeiro/investimentos");
  const menu = page.getByRole("button", { name: "Abrir menu" });
  const caixa = await menu.boundingBox();
  expect(caixa!.width).toBeGreaterThanOrEqual(44);
  expect(caixa!.height).toBeGreaterThanOrEqual(44);
});

test("nenhum botão da barra fica abaixo do mínimo tocável", async ({ page }) => {
  for (const rota of ["/financeiro/investimentos", "/financeiro/contas"]) {
    await page.goto(rota);
    // Escopo pelo `header`, não por classe: `alvosPequenos` corta a lista de classes em 5 para o
    // relatório caber na tela, então filtrar por `.includes("rounded-pill")` acertaria por acaso
    // hoje e erraria em silêncio no dia em que alguém reordenar as classes.
    const botoes = page.locator("header button");
    const total = await botoes.count();
    expect(total, `rota ${rota}`).toBeGreaterThan(0);
    for (let i = 0; i < total; i++) {
      const caixa = await botoes.nth(i).boundingBox();
      const texto = (await botoes.nth(i).innerText()).trim().slice(0, 30);
      expect(caixa!.height, `${rota} → botão «${texto || "(ícone)"}»`).toBeGreaterThanOrEqual(44);
    }
  }
});
```

- [ ] **Passo 2: rodar e confirmar que falha**

Rode: `cd apps/web && pnpm e2e shell-360`
Esperado: teste 1 falha (`larguraDaPagina` = 375), teste 2 falha (largura 16px).

- [ ] **Passo 3: implementar a `Topbar`**

Substitua o corpo do `return` de `Topbar` em `apps/web/src/app/AppShell.tsx`:

```tsx
  return (
    // `flex-wrap` + `order-last` abaixo de `sm`: a ação primária desce para uma linha própria, de
    // largura inteira, em vez de espremer os vizinhos até a linha estourar. Reflui, não corta —
    // a mesma escolha do PR #58, aplicada ao eixo em que a barra falhava.
    <header className="flex flex-wrap items-center gap-3 border-b border-neutral-100 bg-white px-4 py-3 sm:px-6">
      {!sidebarOpen && (
        <button
          onClick={onOpen}
          aria-label="Abrir menu"
          // `shrink-0` é o que impede a espremida: sem ele este botão ia a 16px de largura e a
          // navegação inteira do celular ficava atrás de um alvo que o polegar não acerta.
          className="flex h-11 w-11 shrink-0 items-center justify-center rounded-pill bg-primary-50 text-primary-600"
        >
          <Search size={16} />
        </button>
      )}

      {/* A busca é decoração até alguém ligá-la num handler: abaixo de `md` ela sai da frente e
          devolve 152px à linha, em vez de virar um bolo cinza de 52px sem placeholder legível. */}
      <div className="relative hidden min-w-0 max-w-md flex-1 md:block">
        <Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-neutral-400" />
        <input
          placeholder="Buscar cliente, projeto ou processo"
          className="w-full rounded-pill bg-neutral-50 py-2 pl-9 pr-4 text-sm outline-none focus:ring-2 focus:ring-primary-300"
        />
      </div>

      {/* `min-h-[44px]`: com `py-2.5` a altura dava 40px e o teste desta task pegaria. */}
      {action && (
        <button
          onClick={action.onClick}
          className="order-last flex min-h-[44px] w-full shrink-0 items-center justify-center gap-2 rounded-pill bg-accent-400 px-4 py-2 text-sm font-semibold text-white transition hover:bg-accent-500 sm:order-none sm:w-auto"
        >
          <Plus size={16} className="shrink-0" />
          {action.label}
        </button>
      )}

      <div className="ml-auto flex shrink-0 items-center gap-1">
        <button
          className="flex h-11 w-11 items-center justify-center rounded-pill text-neutral-500 hover:text-neutral-800"
          aria-label="Notificações"
        >
          <Bell size={20} />
        </button>

        <button
          onClick={logout}
          className="flex h-11 items-center gap-1 rounded-pill px-1"
          title={tenant ? `${tenant.legal_name} — sair` : "Sair"}
        >
          <div className="flex h-8 w-8 items-center justify-center rounded-full bg-primary-200 text-xs font-bold text-primary-700">
            {(user?.name ?? "?").slice(0, 1).toUpperCase()}
          </div>
          <ChevronDown size={16} className="shrink-0 text-neutral-500" />
        </button>
      </div>
    </header>
  );
```

- [ ] **Passo 4: rodar e confirmar que passa**

Rode: `cd apps/web && pnpm e2e shell-360`
Esperado: 3 testes PASSAM. Se o teste 3 ainda listar algo, é achado NOVO — meça antes de mudar o
teste, e nunca afrouxe o limite para o número que apareceu.

- [ ] **Passo 5: os três de sempre + `AppShell.test.tsx`**

```bash
cd apps/api && ruff check . && python -m pytest -q -m "not rls_e2e"
cd ../web && pnpm typecheck && pnpm test
```

`apps/web/src/app/AppShell.test.tsx` existe e provavelmente afirma a presença do campo de busca.
Ele agora está `hidden md:block` — presente no DOM, invisível em 360px. Se o teste usar
`getByPlaceholder(...)` ele continua passando (jsdom não aplica media query); **não mexa nele por
mexer**, e se precisar mexer, mova a afirmação para o spec de 360px, onde ela é aferível.

- [ ] **Passo 6: a entrada no `CLAUDE.md`**

```markdown
- **360px: a barra superior reflui, não espreme.** Abaixo de `sm` a ação primária desce para uma
  linha própria de largura inteira; a busca (que ainda não tem handler) some abaixo de `md`; o
  botão "Abrir menu" tem `shrink-0` e 44px. O que fazia a página rolar 15px em
  `/financeiro/investimentos` NÃO era o `ChevronDown` — era o comprimento do rótulo da ação
  (linha mínima: 216px sem ação, 326px com "Nova conta", 375px com "Nova conta de investimento").
  Rótulo novo e comprido não quebra mais a linha; se quebrar, `apps/web/e2e/shell-360.spec.ts`
  pega. `/vima` e `/dna/nucleo` vivem em `ProtectedBareLayout` e nunca tiveram este problema.
```

- [ ] **Passo 7: commit**

```bash
git add apps/web/src/app/AppShell.tsx apps/web/e2e/shell-360.spec.ts CLAUDE.md
git commit -m "fix: a barra superior reflui em 360px em vez de espremer o menu [dívida 360px]"
```

---

# PR 3 — O modal de conta bancária

**Branch:** `fix/360px-modal-de-conta`

### Task 4: o botão que efetiva a escolha para de sair da tela

**Arquivos:**
- Criar: `apps/web/e2e/modal-conta-360.spec.ts`
- Modificar: `apps/web/src/components/Modal.tsx` (prop `footer`)
- Modificar: `apps/web/src/features/financeiro/AccountModal.tsx` (rádios e botão)

**Interfaces:**
- Produz: `Modal` aceita `footer?: ReactNode`, renderizado numa barra `sticky bottom-0` dentro da
  caixa que rola. Prop **opcional** — os outros modais do sistema não mudam.

- [ ] **Passo 1: escrever o teste que falha**

```ts
import { expect, test } from "@playwright/test";
import { mockarApi } from "./support/api";
import { alvosPequenos } from "./support/medidas";
import { semearSessao } from "./support/sessao";

/**
 * O modal de conta bancária em 360×740.
 *
 * Medido antes: conteúdo de 1010px numa caixa de 629px (`max-h-[85vh]`), com "Cadastrar conta" a
 * 942px do topo do modal — y=1043,5, ou seja 303px ABAIXO da borda inferior da tela, e 467px
 * abaixo da escolha "Não sei o saldo agora" que ele efetiva. É a forma exata do PR #56: o
 * controle e a ação que o torna efetivo em lugares separados, e uma conta real foi baixada sem
 * confirmação visível.
 */
test.beforeEach(async ({ page }) => {
  await semearSessao(page);
  await mockarApi(page, {
    "/bank/accounts": [],
    "/payables/bills/paid-before": { count: 0, total_cents: 0, oldest_paid_on: null, newest_paid_on: null },
  });
  await page.goto("/financeiro/contas");
  await page.getByRole("button", { name: "Nova conta" }).click();
  await expect(page.getByRole("heading", { name: "Nova conta" })).toBeVisible();
});

test("o botão de cadastrar fica na tela enquanto o dono preenche", async ({ page }) => {
  await expect(page.getByRole("button", { name: "Cadastrar conta" })).toBeInViewport();
});

test("o botão continua na tela depois de escolher 'Não sei o saldo agora'", async ({ page }) => {
  await page.getByText("Não sei o saldo agora").click();
  await expect(page.getByRole("button", { name: "Cadastrar conta" })).toBeInViewport();
});

test("o botão continua na tela com o modal rolado até o fim", async ({ page }) => {
  await page.evaluate(() => {
    const caixa = document.querySelector(".fixed.inset-0.z-50 > div");
    if (caixa) caixa.scrollTop = caixa.scrollHeight;
  });
  await expect(page.getByRole("button", { name: "Cadastrar conta" })).toBeInViewport();
});

test("a escolha do saldo é tocável com o polegar", async ({ page }) => {
  for (const rotulo of ["Sei o saldo", "Não sei o saldo agora", "Conta principal"]) {
    const caixa = await page.getByText(rotulo, { exact: true }).boundingBox();
    expect(caixa!.height, rotulo).toBeGreaterThanOrEqual(44);
  }
});

test("nenhum controle do modal fica abaixo do mínimo tocável", async ({ page }) => {
  const pequenos = (await alvosPequenos(page)).filter((a) => !a.descricao.startsWith("header"));
  expect(pequenos).toEqual([]);
});
```

- [ ] **Passo 2: rodar e confirmar que falha**

Rode: `cd apps/web && pnpm e2e modal-conta-360`
Esperado: os 3 primeiros falham (`toBeInViewport` — o botão está em y=1043,5) e os 2 últimos
falham (rádios de 13×13px).

- [ ] **Passo 3: `Modal` ganha rodapé fixo**

Em `apps/web/src/components/Modal.tsx`:

```tsx
export default function Modal({
  title,
  open,
  onClose,
  children,
  footer,
}: {
  title: string;
  open: boolean;
  onClose: () => void;
  children: ReactNode;
  /**
   * Ação primária do modal. Vive numa barra `sticky bottom-0` DENTRO da caixa que rola, para que
   * ela nunca saia da tela enquanto o dono preenche o formulário. Opcional: modal que não passa
   * `footer` continua exatamente como era.
   *
   * ⚠️ Não é enfeite. Em 360×740 o formulário de conta tem 1010px numa caixa de 629px — sem esta
   * barra, o botão que efetiva a escolha "não sei o saldo" fica 467px abaixo dela, que é a forma
   * do PR #56 (uma conta real foi marcada como paga sem o dono ver o controle).
   */
  footer?: ReactNode;
}) {
  if (!open) return null;
  return (
    <div
      className="fixed inset-0 z-50 flex items-center justify-center bg-black/30 p-4"
      onClick={onClose}
    >
      <div
        className="max-h-[85vh] w-full max-w-md overflow-y-auto rounded-2xl bg-white p-6 shadow-xl"
        onClick={(e) => e.stopPropagation()}
      >
        <div className="mb-4 flex items-center justify-between">
          <h2 className="text-lg font-bold text-neutral-800">{title}</h2>
          <button
            onClick={onClose}
            aria-label="Fechar"
            className="-mr-2 flex h-11 w-11 items-center justify-center rounded-pill text-neutral-400 hover:text-neutral-700"
          >
            <X size={20} />
          </button>
        </div>
        {children}
        {footer && (
          // `-mx-6 -mb-6` desfaz o `p-6` da caixa para a barra encostar nas bordas; o fundo
          // opaco é o que impede o conteúdo de aparecer por baixo enquanto rola.
          <div className="sticky bottom-0 -mx-6 -mb-6 mt-4 border-t border-neutral-100 bg-white px-6 py-4">
            {footer}
          </div>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Passo 4: `AccountModal` usa o rodapé e engorda os alvos**

Em `apps/web/src/features/financeiro/AccountModal.tsx`, troque as duas `<label>` dos rádios
(linha ~262):

```tsx
          <div className="flex flex-col gap-1 sm:flex-row sm:gap-4">
            {/* 44px de altura na LINHA INTEIRA: o alvo é o rótulo, não o círculo de 13px. */}
            <label className="flex min-h-[44px] flex-1 items-center gap-3 text-sm text-neutral-700">
              <input
                type="radio"
                name="saldo-conhecido"
                className="h-5 w-5 shrink-0"
                checked={saldoConhecido === true}
                onChange={() => setSaldoConhecido(true)}
              />
              Sei o saldo
            </label>
            <label className="flex min-h-[44px] flex-1 items-center gap-3 text-sm text-neutral-700">
              <input
                type="radio"
                name="saldo-conhecido"
                className="h-5 w-5 shrink-0"
                checked={saldoConhecido === false}
                onChange={() => setSaldoConhecido(false)}
              />
              Não sei o saldo agora
            </label>
          </div>
```

O checkbox "Conta principal" (linha ~348):

```tsx
        <label className="flex min-h-[44px] items-center gap-3 text-sm text-neutral-600">
          <input
            type="checkbox"
            className="h-5 w-5 shrink-0"
            checked={isPrimary}
            onChange={(e) => setIsPrimary(e.target.checked)}
          />
          Conta principal
        </label>
```

Depois **remova** o `<button>` de salvar de dentro do corpo (linhas ~357-364) e passe-o como
`footer`. A linha 227 —

```tsx
    <Modal title={editing ? "Editar conta" : "Nova conta"} open={open} onClose={onClose}>
```

— vira:

```tsx
    <Modal
      title={editing ? "Editar conta" : "Nova conta"}
      open={open}
      onClose={onClose}
      footer={
        <button
          type="button"
          onClick={save}
          disabled={saving || !name.trim() || (recuou && !saldoRedeclarado) || faltaDecidirSaldo}
          className="w-full rounded-pill bg-accent-400 py-3 font-semibold text-white transition hover:bg-accent-500 disabled:opacity-60"
        >
          {saving ? "Salvando…" : editing ? "Salvar" : "Cadastrar conta"}
        </button>
      }
    >
```

O `{error && <p className="rounded-lg bg-red-50 p-2 text-sm text-danger">{error}</p>}` **continua
no corpo**, onde já está: erro é contexto do formulário, não da barra de ação — e na barra ele
empurraria o botão para fora justamente quando o dono mais precisa dele.

- [ ] **Passo 5: rodar e confirmar que passa**

Rode: `cd apps/web && pnpm e2e modal-conta-360`
Esperado: 6 testes PASSAM.

- [ ] **Passo 6: os três de sempre**

```bash
cd apps/api && ruff check . && python -m pytest -q -m "not rls_e2e"
cd ../web && pnpm typecheck && pnpm test
```

⚠️ `Modal` é compartilhado por muitas telas. Um `pnpm test` verde não prova que nenhuma delas
regrediu visualmente — mas como `footer` é opcional e nada mais mudou na caixa, a superfície de
risco é o `aria-label="Fechar"` novo no botão de fechar. Se algum teste procurava esse botão por
outra via, atualize-o.

- [ ] **Passo 7: a entrada no `CLAUDE.md`**

```markdown
- **`Modal` aceita `footer`** (`components/Modal.tsx`): a ação primária vai numa barra
  `sticky bottom-0` dentro da caixa que rola, e não sai da tela enquanto o dono preenche.
  Formulário longo em modal — o de conta bancária tem 1010px numa caixa de 629px em 360×740 —
  DEVE usar `footer`; sem ele o botão nasce 303px abaixo da borda e 467px abaixo da escolha que
  ele efetiva, que é a forma do PR #56. Alvos da escolha de saldo (Story 8.21) e de "Conta
  principal" agora têm 44px na linha inteira. Travado por
  `apps/web/e2e/modal-conta-360.spec.ts`.
```

- [ ] **Passo 8: commit**

```bash
git add apps/web/src/components/Modal.tsx apps/web/src/features/financeiro/AccountModal.tsx \
        apps/web/e2e/modal-conta-360.spec.ts CLAUDE.md
git commit -m "fix: a ação do modal de conta para de sair da tela em 360px [dívida 360px]"
```

---

# PR 4 — Alvos de toque de Contas & Saldos + acordeão do DNA

**Branch:** `fix/360px-alvos-e-acordeao`

**Por que juntos:** os dois são "a tela cabe, o dedo não alcança" — a mesma classe de defeito em
dois formatos (alvo pequeno demais e conteúdo longe demais). Decisão do dono do produto em
2026-08-10.

### Task 5: as ações da conta viram alvos de verdade

**Arquivos:**
- Criar: `apps/web/e2e/toque-360.spec.ts`
- Modificar: `apps/web/src/features/financeiro/ContasSaldosPage.tsx:178-188` e `:393-452`

- [ ] **Passo 1: escrever o teste que falha**

```ts
import { expect, test } from "@playwright/test";
import { mockarApi } from "./support/api";
import { medirPagina } from "./support/medidas";
import { semearSessao } from "./support/sessao";

const CONTA = {
  id: "00000000-0000-4000-8000-000000000001", name: "Conta Corrente Principal",
  kind: "checking", institution: "Banco Cooperativo Sicredi S.A.", institution_code: "748",
  branch: "1234-5", number: "12345-6", holder_document: "123.456.789-00", pix_key: "",
  opening_balance_cents: 125000, opening_balance_is_known: true, opening_date: "2026-01-01",
  is_primary: true, archived_at: null, saldo_derivado_cents: 12845079,
  saldo_derivado_origem: "banco", agendado_saida_cents: 1238000, agendado_entrada_cents: 0,
  agendado_origem: "banco", created_at: "2026-01-01T10:00:00Z",
};

test("as ações da conta são tocáveis com o polegar", async ({ page }) => {
  await semearSessao(page);
  await mockarApi(page, { "/bank/accounts": [CONTA], "/bank/transfers": [] });
  await page.goto("/financeiro/contas");
  await expect(page.getByText("Conta Corrente Principal")).toBeVisible();

  // As sete ações por conta eram links de texto de 16px de altura, e "Arquivar" — destrutiva —
  // ficava a 4px de "Editar". É a classe de defeito do PR #56.
  for (const rotulo of ["Declarar saldo", "Lançar movimento", "Transferir entre contas",
                        "Conferir", "Ver movimentos", "Editar", "Arquivar"]) {
    const caixa = await page.getByRole(rotulo === "Conferir" ? "link" : "button",
                                       { name: rotulo }).first().boundingBox();
    expect(caixa!.height, rotulo).toBeGreaterThanOrEqual(44);
  }

  const caixaCheckbox = await page.getByText("Mostrar arquivadas").boundingBox();
  expect(caixaCheckbox!.height).toBeGreaterThanOrEqual(44);

  // O que já estava certo continua certo: a largura nunca precisou de rolagem lateral, e os
  // valores aparecem inteiros (R$ 128.450,79, não "R$ 128.").
  const { larguraDaPagina } = await medirPagina(page);
  expect(larguraDaPagina).toBe(360);
  await expect(page.getByText("R$ 128.450,79")).toBeVisible();
});
```

- [ ] **Passo 2: rodar e confirmar que falha**

Rode: `cd apps/web && pnpm e2e toque-360`
Esperado: falha na primeira ação (altura 16px).

- [ ] **Passo 3: implementar**

Em `ContasSaldosPage.tsx`, acrescente perto do topo do arquivo (depois dos imports):

```tsx
/**
 * A classe das ações de uma conta. Uma constante, sete consumidores.
 *
 * `min-h-[44px]` não é estética: eram sete links de 16px de altura, com "Arquivar" (destrutiva) a
 * 4px de "Editar". O padding cresce, a fonte não — o cartão fica mais alto e a rolagem vertical é
 * nativa e gratuita; errar o alvo de "Arquivar" não é.
 */
const ACAO_DA_CONTA =
  "inline-flex min-h-[44px] items-center gap-1 px-1 text-neutral-600 hover:text-primary-600";
```

Troque as seis ocorrências de
`className="inline-flex items-center gap-1 text-neutral-600 hover:text-primary-600"`
por `className={ACAO_DA_CONTA}` (linhas ~400, ~407, ~415, ~424, ~431, ~440) e a de "Arquivar"
(linha ~447) por:

```tsx
              className={`${ACAO_DA_CONTA} hover:text-danger`}
```

Na `<div>` que envolve as ações (linha ~393), o `gap-y-2` já não é necessário — o `min-h`
resolve o espaçamento vertical:

```tsx
      <div className="mt-4 flex flex-wrap items-center gap-x-4 border-t border-neutral-50 pt-3 text-xs font-medium">
```

E o checkbox "Mostrar arquivadas" (linha ~181):

```tsx
          <label className="flex min-h-[44px] items-center gap-3 text-sm text-neutral-600">
            <input
              type="checkbox"
              className="h-5 w-5 shrink-0"
              checked={includeArchived}
              onChange={(e) => setIncludeArchived(e.target.checked)}
            />
            Mostrar arquivadas
          </label>
```

- [ ] **Passo 4: rodar e confirmar que passa**

Rode: `cd apps/web && pnpm e2e toque-360`
Esperado: PASSA.

- [ ] **Passo 5: commit**

```bash
git add apps/web/src/features/financeiro/ContasSaldosPage.tsx apps/web/e2e/toque-360.spec.ts
git commit -m "fix: as ações de conta viram alvos de 44px em 360px [dívida 360px]"
```

### Task 6: a aba "A sua empresa" abre em 5 cabeçalhos, não em 46 perguntas

**Arquivos:**
- Modificar: `apps/web/e2e/toque-360.spec.ts` (acrescentar o teste)
- Modificar: `apps/web/src/features/config/EmpresaDnaTab.tsx:53-90`

- [ ] **Passo 1: acrescentar o teste que falha**

Ao fim de `apps/web/e2e/toque-360.spec.ts`:

```ts
test("a aba 'A sua empresa' abre recolhida, não com 22 telas de rolagem", async ({ page }) => {
  await semearSessao(page);
  await mockarApi(page, {
    "/dna/catalogo": CATALOGO_DNA,
    "/dna/respostas": {},
    "/settings/profile": { display_name: "Consultoria ME", timezone: "America/Sao_Paulo" },
  });
  await page.goto("/config");
  await page.getByRole("button", { name: "A sua empresa" }).click();
  await expect(page.getByText("Oferta")).toBeVisible();

  // Medido antes: 16.495px = 22,3 telas de 740px, com a pergunta nova do PR #103 a 14,6 telas de
  // distância. O cabeçalho da própria aba diz que "a Vima pergunta aos poucos" — despejar as 46
  // de uma vez contradiz a promessa que a tela faz.
  const { alturaDaPagina } = await medirPagina(page);
  expect(alturaDaPagina).toBeLessThan(1500);

  // E o eixo abre quando o dono pede.
  await page.getByRole("group", { name: /Dinheiro/ }).getByRole("button").first().click();
  await expect(page.getByText("E de uma cobrança que você tem a receber?")).toBeInViewport();
});
```

E no topo do arquivo, a fixture do catálogo (recorte real de
`apps/api/app/modules/dna/catalog.py` — dois eixos bastam para medir):

```ts
const CATALOGO_DNA = [
  { key: "oferta.o_que_vende", classe: "retrato", eixo: "oferta", texto: "O que você vende?",
    formato: "escolha", opcoes: [
      { rotulo: "Serviço recorrente", valor: "servico_recorrente" },
      { rotulo: "Serviço por projeto", valor: "servico_projeto" },
      { rotulo: "Um pouco de cada", valor: "misto" }] },
  { key: "oferta.em_uma_frase", classe: "retrato", eixo: "oferta",
    texto: "Se um cliente perguntar o que você faz, o que você responde?",
    formato: "texto", opcoes: [] },
  { key: "dinheiro.antecedencia_dias", classe: "calibracao", eixo: "dinheiro",
    texto: "E de uma conta a pagar?", formato: "escolha", opcoes: [
      { rotulo: "No próprio dia", valor: 0 }, { rotulo: "1 dia antes", valor: 1 },
      { rotulo: "3 dias antes", valor: 3 }, { rotulo: "1 semana antes", valor: 7 }] },
  { key: "dinheiro.cobranca_antecedencia_dias", classe: "calibracao", eixo: "dinheiro",
    texto: "E de uma cobrança que você tem a receber?", formato: "escolha", opcoes: [
      { rotulo: "No próprio dia", valor: 0 }, { rotulo: "1 dia antes", valor: 1 },
      { rotulo: "3 dias antes", valor: 3 }, { rotulo: "1 semana antes", valor: 7 }] },
];
```

- [ ] **Passo 2: rodar e confirmar que falha**

Rode: `cd apps/web && pnpm e2e toque-360`
Esperado: o teste novo falha — a altura passa de 1500px e não há botão de expandir.

- [ ] **Passo 3: implementar o acordeão**

Em `apps/web/src/features/config/EmpresaDnaTab.tsx`, troque o `<section>` do `map` (linha ~57)
por `<details>` nativo — sem estado em React, com semântica de expandir/recolher de graça:

```tsx
        return (
          /* Recolhido por padrão. A aba inteira tinha 16.495px em 360px (22,3 telas), e a
             pergunta que o PR #103 acrescentou ao eixo `dinheiro` ficava a 14,6 telas do topo.
             O texto de abertura da aba promete que "a Vima pergunta aos poucos"; despejar as 46
             de uma vez é a tela contradizendo a própria promessa. */
          <details key={eixo.key} className="group rounded-xl border border-neutral-200">
            <summary className="flex min-h-[44px] cursor-pointer items-center justify-between gap-3 px-4 py-3 [&::-webkit-details-marker]:hidden">
              <span>
                <span className="block text-lg font-semibold text-neutral-800">{eixo.titulo}</span>
                <span className="block text-xs text-neutral-500">{eixo.descricao}</span>
              </span>
              <span className="shrink-0 text-xs text-neutral-400">
                {doEixo.filter((p) => p.key in respostas).length}/{doEixo.length}
              </span>
            </summary>
            <div className="space-y-3 border-t border-neutral-100 p-4">
              {doEixo.map((p) => (
                <div key={p.key}>
                  {p.key in respostas && !reabertas.includes(p.key) ? (
                    <div className="rounded-xl border border-neutral-200 bg-neutral-50 p-4">
                      <p className="text-sm text-neutral-600">{p.texto}</p>
                      <p className="mt-1 text-sm font-medium text-neutral-800">
                        {rotulo(p, respostas[p.key])}
                      </p>
                      <button
                        type="button"
                        onClick={() => setReabertas([...reabertas, p.key])}
                        className="mt-2 inline-flex min-h-[44px] items-center text-xs text-neutral-400 underline"
                      >
                        Mudar
                      </button>
                    </div>
                  ) : (
                    <PerguntaDaVima
                      pergunta={p}
                      source="config"
                      onPronto={carregar}
                      onPular={carregar}
                    />
                  )}
                </div>
              ))}
            </div>
          </details>
        );
```

⚠️ `<details>` não expõe `role="group"` em todos os motores. Se o seletor
`getByRole("group", ...)` do teste não achar o elemento, troque-o por
`page.locator("details", { hasText: "Dinheiro" }).locator("summary").click()` — **ajuste o
seletor, nunca o limite de altura**.

- [ ] **Passo 4: rodar e confirmar que passa**

Rode: `cd apps/web && pnpm e2e toque-360`
Esperado: os 2 testes PASSAM.

- [ ] **Passo 5: os três de sempre**

```bash
cd apps/api && ruff check . && python -m pytest -q -m "not rls_e2e"
cd ../web && pnpm typecheck && pnpm test
```

`EmpresaDnaTab.test.tsx` existe. Ele provavelmente busca o texto das perguntas — dentro de
`<details>` fechado o texto **continua no DOM** e o Testing Library o acha, então tende a passar.
Se ele afirmar visibilidade (`toBeVisible`), ajuste-o.

- [ ] **Passo 6: a entrada no `CLAUDE.md`**

```markdown
- **360px: alvo de toque é 44px, e conteúdo longe é conteúdo que não existe.** As sete ações por
  conta em Contas & Saldos (`ACAO_DA_CONTA`) e o "Mostrar arquivadas" têm 44px de altura — eram
  links de 16px, com "Arquivar" (destrutiva) a 4px de "Editar". A aba "A sua empresa"
  (`EmpresaDnaTab`) abre com os 5 eixos RECOLHIDOS (`<details>` nativo, com contador
  respondidas/total): aberta, ela tinha 16.495px = 22,3 telas, e a pergunta que o PR #103
  acrescentou ao eixo `dinheiro` ficava a 14,6 telas do topo. Travado por
  `apps/web/e2e/toque-360.spec.ts`.
```

- [ ] **Passo 7: commit**

```bash
git add apps/web/src/features/config/EmpresaDnaTab.tsx apps/web/e2e/toque-360.spec.ts CLAUDE.md
git commit -m "fix: a aba do DNA abre recolhida em 360px [dívida 360px]"
```

---

## Fecho da dívida

Depois dos 4 PRs mergeados, **remova das seis stories** a linha "validação manual em ~360px NÃO
foi feita; bloqueia release, não bloqueia merge". Dívida resolvida e ainda escrita manda o próximo
leitor resolver de novo o que já está resolvido (`CLAUDE.md` §5, passo 4).

**Fica pendente, e é do @devops:** promover o job `frontend` a *required* em GitHub → Settings →
Branches → "Require status checks to pass". Enquanto isso não acontece, a régua **mede mas não
barra** — e foi a ausência de barreira, não a ausência de conhecimento, que deixou seis telas
passarem.

## Achados medidos que este plano NÃO conserta

Registrados para não serem redescobertos:

- **Régua de abas do `/config`:** 641px de conteúdo em 312px visíveis (329px escondidos). Rola
  (`overflow-x-auto`), então obedece o PR #58, mas as abas 3–5 não têm nenhuma pista de que
  existem além de um ícone espiando na borda. Não é quebra; é descoberta.
- **Campo de busca da topbar:** não tem handler nenhum — é decoração ocupando 152px. Este plano o
  esconde abaixo de `md`; ligá-lo ou removê-lo é decisão de produto.
- **`vitest` nunca rodou no CI** antes deste plano. O job `frontend` passa a rodá-lo, mas a suíte
  de unidade nunca foi gate e pode ter regressões dormentes — se ela vier vermelha no primeiro PR,
  isso é achado pré-existente, não regressão sua.
